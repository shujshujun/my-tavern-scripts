# 特殊剧情线依次开启

日期：2026-09-14。基于本地 `release/rq0924` 的已发布版本后续维护，不更新版本号或正式卡。

## 需求来源与行为

用户明确要求：可以买剧情道具，但当前有其他角色的剧情线进行中时，须完成后才能使用新道具开启剧情。

- `剧情线使用门.ts` 从现有线路阶段、完成事实及旧版录像带状态推导占线情况，不新增存档锁。
- 等待日、筹备与微信交接持续占线；仅购买、从未使用的道具不占线。
- 首次使用入口和背包提示共用此判断；拦截不扣钱、不消耗道具、不改变进度。
- 家庭计划、第二机位、分居拆成购买入包与主动使用两步；购买后不再作为可重复领取的商品。
- 旧档已并行的线路允许收尾，禁止再开第三条；回档按快照重新推导，避免遗留锁。
- 已有的前置资格、地点、时间和现场事务检查继续生效。

## 验证

- 新增 28 项独占使用回归，覆盖等待期、完成释放、已购未用、真实商店与使用入口、重复操作、旧档和序列化。
- 商店原组件、实际父层货架与新回归合计 107 项通过。
- TypeScript、Vue 类型检查与本次修改文件 ESLint 通过。
- 最终隔离生产构建位于 `.codex-tmp/story-exclusive/isolated-final/`：两入口均无错误，源码、声明及正式 `dist` 哈希保持不变。5 项警告均为现有体积阈值类警告。
- 完整回归 6734/6734 通过，无失败或跳过；输出：`.codex-tmp/story-exclusive/all-fixed.log`。

此前测试输出内存异常定位为：分居购买后仍在货架，旧断言试图输出循环引用的 Vue 界面节点。已修正上架资格，并以布尔断言保留该界面行为检查，避免错误输出放大。

本次验证的是线路开启顺序和状态事务；不以此断言玩家截图中的正文错角色已被复现或完全消除。
