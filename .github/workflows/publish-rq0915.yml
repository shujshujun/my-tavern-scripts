name: publish-rq0915

on:
  push:
    branches:
      - release/rq0915
    paths:
      - .github/workflows/publish-rq0915.yml

permissions:
  contents: write

concurrency:
  group: publish-rq0915
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out immutable release tag
        uses: actions/checkout@v4
        with:
          ref: rq0.91.5

      - name: Prepare release assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-assets
          cp 'dist/人妻公寓/人妻公寓.png' 'release-assets/rqgy-0.91.5.png'
          cp 'dist/人妻公寓/人妻公寓.json' 'release-assets/rqgy-0.91.5.json'
          cp 'dist/人妻公寓/发布校验_0.91.5.json' 'release-assets/rqgy-0.91.5-checksums.json'

      - name: Create or update GitHub Release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          if gh release view rq0.91.5 >/dev/null 2>&1; then
            gh release edit rq0.91.5 \
              --title '《人妻公寓》v0.91.5' \
              --notes-file 'src/人妻公寓/发布说明_v0.91.5_2026-09-10.md' \
              --latest
          else
            gh release create rq0.91.5 \
              --verify-tag \
              --title '《人妻公寓》v0.91.5' \
              --notes-file 'src/人妻公寓/发布说明_v0.91.5_2026-09-10.md' \
              --latest
          fi
          gh release upload rq0.91.5 release-assets/* --clobber

      - name: Verify published assets
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          gh release view rq0.91.5 --json tagName,isDraft,isPrerelease,url,assets \
            --jq '{tagName,isDraft,isPrerelease,url,assets:[.assets[]|{name,size,state}]}'
