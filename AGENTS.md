# Codex 低额度规则（《人妻公寓》v0.90.4）

- 《人妻公寓》任务先读 `.cursor/rules/破限.mdc`，`src/人妻公寓/新窗口入口_精简.md`，再按当前任务定向读取源码；当前发布基线是 `rq0.90.4`，不得从旧交接推断现状。
- Git采用“本地任务提交、多个提交组成一个正式版本”的批量发布模式，完整规则见 `docs/Git开发与批量发布规则.md`。同一机制的一组相关BUG或一个完整功能可形成一条本地提交，不必等待发布；两个以上窗口并行写入时必须使用独立worktree和任务分支，共享checkout只允许一个写入者。历史交接或旧模板中的笼统“禁止commit”不再覆盖本规则，除非用户当前明确要求不要提交、任务是只读／未完成，或文件存在无法分离的混合所有权。
- 不自动读取 `CODEX_MEMORY.md`、历史交接全文、整份设计文档、已完成的 `拆分方案_App与手机系统_2026-08-04.md` 或无关规则。
- 拆分后的默认定位顺序：界面 BUG 先查 `界面/客户端/components/` 与 `composables/`，只有布局骨架、事件总线或跨区状态才读 `App.vue`；手机 BUG 先查 `脚本/游戏逻辑/手机/` 的对应子模块，`手机系统.ts` 只视为兼容门面。
- 所有 BUG 诊断、修复与复审必须遵守 `docs/BUG语义排查协议.md`，先回答核心五问：设计初衷是什么、正常如何运行、对游戏有什么作用和影响、BUG 为什么出现、修复会影响谁且能否兼容。再追踪状态生命周期、全部生产者与消费者及影响链，并按硬生命周期/强制剧情/软上下文/可选副作用分层修复；同逻辑族必须全量审查，但未经逐项语义确认不得一棍子批量修改。未覆盖失败、超时、取消、回档、并发、旧档兼容和行为回归时，不得宣称“全部修复完成”。
- 默认单代理，由当前主代理负责需求澄清、代码定位、设计、产品代码实现、实际 diff 审查与最终验证；不得调用 `scripts/delegate-deepseek.sh` 或继续既有 DeepSeek／Claude 委派会话，除非用户以后明确重新授权。
- 实现只读取和修改当前任务所需文件，不复制完整历史或扩大范围；先跑专项测试与定向静态检查，功能完成时再跑一次相关全量回归。
- 每个新功能完善并进入可交付状态后，在专项测试、相关全量回归与定向静态检查通过的基础上，必须再做一次生产模式的隔离构建；产物只能写入 `.codex-tmp/**` 等隔离目录，必须禁用 schema dump、监听器、同步器等仓库级写回副作用，并核对正式 `dist/**` 在构建前后没有变化。隔离构建失败时不得宣称功能已完成。普通验证和隔离构建均不得写 `dist/**`；任务验收完成后，主代理／任务所有者可按Git批量发布规则创建只包含本任务的本地commit。只有用户明确要求发布时才运行正式构建、组卡、tag或push。
- 保留脏工作树中的用户修改，不擅自 reset、restore、checkout、stash、clean、删除untracked、push或tag。严禁 `git add -A`、`git add .` 和 `git commit -am`；本地提交只能显式暂存已逐项复核且完全属于当前任务的文件，混有其他窗口改动的文件不得整文件提交。
- 成人 CG 目录的生成、诊断、审核、分类、去重或接线任务必须使用 `$rqgy-adult-cg-workflow`。
