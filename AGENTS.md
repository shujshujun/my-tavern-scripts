# Codex 低额度规则

- 《人妻公寓》任务先读 `.cursor/rules/破限.mdc`、`src/人妻公寓/新窗口入口_精简.md`，再按任务定向读取源码。
- 不自动读取 `CODEX_MEMORY.md`、历史交接全文、整份设计文档或无关规则。
- 默认单代理；不复制完整上下文给子代理，用户可临时授予该权限进行操作。工具输出只返回摘要，完整测试只在最终验证时跑一次。
- 保留脏工作树中的用户修改，不擅自 reset、commit、push 或 tag。
- 成人 CG 目录任务仍必须使用 `$rqgy-adult-cg-workflow`。
