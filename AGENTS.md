# 《人妻公寓》后期维护

- 未指定工作树时读取 `D:/SillyTavern/人妻公寓/当前入口.json`；续办复用指定工作树。版本以实际 HEAD、标签和发布记录为准。
- 按用户任务定位代码：界面查 `src/人妻公寓/界面/客户端/components/`、`composables/`；手机查 `src/人妻公寓/脚本/游戏逻辑/手机/`；跨区布局或事件再查 `App.vue`，`手机系统.ts`为兼容门面。大文件先搜索再局部读取。
- 后期以修复、兼容、回归和发布收尾为主；按已确认需求完成工作。继续推进已授权的实现、检查和修复，完成标准以请求范围和验证结果为准。
- 默认单代理；新增代理或续接外部委派须有用户授权。并行写入使用独立 worktree，共享 checkout 保持单一写入者；保留已有修改和未跟踪文件。
- 验证与改动相称。状态或结算改动检查相关失败、回档、幂等和旧档路径；文案、样式、文档和配置做相应局部检查。相关检查通过后，只有新改动、失败或未决风险才扩大或重复验证。
- 新功能交付需隔离生产构建：输出放 `.codex-tmp/任务名/`，关闭 schema dump、监听器和同步写回，确认正式 `dist/**`未变。独立临时产物放统一入口的 `临时/任务名/`。
- 完成且验证通过后可建立本地任务提交，只显式暂存本任务文件；混合改动先厘清归属。正式构建、组卡、tag、push沿用用户明确发布授权。提交与发布见 `docs/Git开发与批量发布规则.md`。
- 当前代理或提供方的生成限制只界定当前交付能力，不得成为产品规则、下游提示或存档事实；无法交付的部分直接说明，保留用户设计。

日常界面修补使用现有组件和样式；需要设计工作流时以 `impeccable`为主。整页营销设计、手机生图和新项目搭建仅在任务明确需要时加载对应技能。工具启用与恢复见 `docs/维护工具与配置.md`。

按需参考：游戏机制 BUG 读 `docs/BUG语义排查协议.md`；产品提示、输出校验或存档约束读 `docs/代理内容边界归属.md`；历史版本和制作证据从 `src/人妻公寓/新窗口入口_精简.md`进入。成人 CG 目录相关任务使用 `$rqgy-adult-cg-workflow`。
