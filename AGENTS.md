# Codex 低额度规则（《人妻公寓》v0.90.3）

- 《人妻公寓》任务先读 `.cursor/rules/破限.mdc`、`src/人妻公寓/新窗口入口_精简.md`，再按当前任务定向读取源码；当前发布基线是 `rq0.90.3`，不得从旧交接推断现状。
- 不自动读取 `CODEX_MEMORY.md`、历史交接全文、整份设计文档、已完成的 `拆分方案_App与手机系统_2026-08-04.md` 或无关规则。
- 拆分后的默认定位顺序：界面 BUG 先查 `界面/客户端/components/` 与 `composables/`，只有布局骨架、事件总线或跨区状态才读 `App.vue`；手机 BUG 先查 `脚本/游戏逻辑/手机/` 的对应子模块，`手机系统.ts` 只视为兼容门面。
- 所有 BUG 诊断、修复与复审必须遵守 `docs/BUG语义排查协议.md`，先回答核心五问：设计初衷是什么、正常如何运行、对游戏有什么作用和影响、BUG 为什么出现、修复会影响谁且能否兼容。再追踪状态生命周期、全部生产者与消费者及影响链，并按硬生命周期/强制剧情/软上下文/可选副作用分层修复；同逻辑族必须全量审查，但未经逐项语义确认不得一棍子批量修改。未覆盖失败、超时、取消、回档、并发、旧档兼容和行为回归时，不得宣称“全部修复完成”。
- 默认单代理；《人妻公寓》代码实现任务例外，已获用户长期授权使用 `scripts/delegate-deepseek.sh` 委派 DeepSeek-V4F 落地。主代理负责需求澄清、代码定位、设计、编写 `.delegate/*.md` 规格、审查实际 diff 和最终验证；除非用户另行指定，不由主代理直接编写产品代码。
- 委派只提供必要文件、定位、规则和验收标准，不复制完整历史。实现后先跑专项测试与定向静态检查，功能完成时再跑一次相关全量回归。
- DeepSeek 会话必须遵守 `docs/DeepSeek委派会话控制.md`：`-c` 只用于同一目标、同一文件范围内的即时小修；语义审查项或实现阶段改变、一次长实现后仍需继续、响应明显变慢或外层超时后，先确认没有仍在运行的 `claude` 进程，再写新的精简 spec（新文件名）并且不带 `-c` 启动新会话。不得把完整历史复制进新 spec，也不得在旧进程状态不明时重启造成双进程计费。
- 普通验证不得写 `dist/**`；需要生产编译证据时使用隔离输出。只有用户明确要求发布时才运行正式构建、组卡、commit、tag 或 push。
- 保留脏工作树中的用户修改，不擅自 reset、checkout、stash、commit、push 或 tag。
- 成人 CG 目录的生成、诊断、审核、分类、去重或接线任务必须使用 `$rqgy-adult-cg-workflow`。
