# 酒馆助手项目维护

@AGENTS.md

按任务参考 `.cursor/rules/`：宿主接口与目录结构看 `项目基本概念.mdc`；持久变量看 `酒馆变量.mdc`；界面或脚本分别看 `前端界面.mdc`、`脚本.mdc`；MVU 状态更新看 `mvu变量框架.mdc`，角色卡结构看 `mvu角色卡.mdc`；真实浏览器验证看 `mcp.mdc`。

先搜索再局部阅读大文件。构建产物和历史交接只在任务确实需要时查看相关部分。具体工具按当前可用能力选择。
