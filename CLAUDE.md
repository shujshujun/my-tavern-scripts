# 酒馆助手前端界面或脚本编写

@.cursor/rules/项目基本概念.mdc
@.cursor/rules/mcp.mdc
@.cursor/rules/酒馆变量.mdc
@.cursor/rules/前端界面.mdc
@.cursor/rules/脚本.mdc

## 按需读取的规则（体积大，不默认加载，节省上下文）

涉及以下主题时，必须先用 Read 工具读完对应规则文件再动手：

- MVU 变量框架（变量更新、`_.set`、`stat_data` 等）：`.cursor/rules/mvu变量框架.mdc`
- MVU 角色卡（角色卡内变量结构设计）：`.cursor/rules/mvu角色卡.mdc`
- 破限/预设编写（预设词条、越狱段、输出协议）：`.cursor/rules/破限.mdc`

## 上下文节约规则（每次会话必须遵守）

本仓库存在超大源文件，整读一个就会耗尽大半上下文窗口。任何修复或开发任务：

1. **禁止整读大文件**：先用 Grep 按关键词定位行号，再用 Read 的 offset/limit 只读相关行段（单次 ≤ 400 行）。已知大文件（> 80 KB，整读约 3 万~15 万 token）：
   - `src/人妻公寓/界面/客户端/App.vue`（~450 KB）
   - `src/人妻公寓/脚本/游戏逻辑/手机系统.ts`、`index.ts`、`回合引擎.ts`、`私聊图库清单.ts`、`src/人妻公寓/stageConfig.ts`
   - `src/云霜凝/脚本/游戏逻辑/promptInjection.ts`、`src/赵霞/脚本/游戏逻辑/promptInjection.ts`
   - 通用判断：不确定大小时先 `wc -c`，超过 80 KB 一律局部读取。
2. **非必要不读取 `dist/**` 与 `*.map`**：均为构建产物，体积巨大；仅在任务明确需要核对构建产物（如排查打包问题、验证发布内容）时才读取，且同样遵守局部读取规则。
3. **跨文件搜索用 Grep/Glob 工具或 Explore 子代理**，不要为找代码而逐个整读文件。
4. **《人妻公寓》任务入口**：先读 `src/人妻公寓/新窗口入口_精简.md`；`src/人妻公寓/新窗口交接_*.md`、`*审计*.md` 等历史文档除非任务明确涉及，否则不读。
5. **长命令输出**（构建、测试）：只关注报错摘要；输出很长时重定向到临时文件后用 Grep 提取关键行。
