# Codex 项目工作记忆

## 2026-08-03（二）新手指引幻觉修复 + 微信回车发送

- 玩家实测：待办写"去 101 修水管"但到场即打勾，AI 又在回管理员室后把没动工的报修演成
  "已修好"（账上任务仍挂着，逾期照扣胜任分）。用户拍板最小改动方案。
- 快照注入（snapshotSystem.ts 组公寓快照）：活跃楼务非空时逐轮注入【楼务·尚未处理】清单
  （复用管理任务摘要+期限口吻），并明令正文不得演成已完成；账上清空后该块消失。
- 待办文字对齐（App.vue 待办定义）：101 条改为"去 101 看看水管报修"，与到场即打勾一致。
- 微信回车发送（手机系统.ts）：妻子/群聊输入框与父亲通话输入框都加 Enter=发送、
  Shift+Enter=换行；isComposing/组合标记双保护防输入法确认误发；红灯（停止）态回车不代点。
- 回归：AI状态快照矩阵.test.mjs 新增未完成楼务注入用例；tests/人妻公寓 619/619 通过，
  ESLint、build:dev、diff --check 通过，dist 已恢复干净。仍未 commit、push、tag。

## 2026-08-03 玩家三项 BUG 修复完成（Claude 接手 Codex 未完成任务）

- 翻垃圾阻塞修复：地图房卡下架远程翻垃圾（App.vue），只保留人到垃圾房后的房内直显按钮；
  脚本端翻垃圾/催租翻出必演事件时改走`即时开演`当场演出，空手/零钱仍只`落地`弹提示。
  系统强制剧情（角色入驻登场等）的排队机制未动——用户确认那是防打断亲热的刻意设计。
- 时间/楼务被锁时的提示改为报出具体待演事件【标签】（新增`描述待发送事件`，时间推进系统.ts），
  解决"找不到要处理的事情"。
- 亲密判定修复（玩家资源系统.ts）：计费楼以玩家输入的性爱意图词为准；AI 自报高尺度只在
  玩家确有意图时作佐证；免计费系统正戏/特殊场景照旧凭 AI 尺度或身体开发增量建账不扣资源。
- 地图远程操作全面收紧：对饮、丈夫礼物、催租、打听、捡零钱都加`当前房间===id`门；
  看监控自动回302、送礼普通排队为既定设计保留。
- 测试适配：玩家资源系统.test.mjs 意外正戏用例改为断言扣精力（即用户反馈的 BUG 场景）、
  两个多人开场行动加意图词"做爱"（避免命中101偏好多加满意度）；组合动作地点门.test.mjs
  到达门数量 6→5 并新增翻垃圾不上地图断言。
- 验证：tests/人妻公寓 618/618 通过；ESLint（改动文件）、`git diff --check`、`pnpm run build:dev`
  均通过；dist 已恢复干净。仍未 commit、push、tag。

## 2026-08-01 首次登场公共场景门完成，等待独立复核与风闻胜任讨论

- 新窗口权威交接：
  `src/人妻公寓/新窗口交接_2026-08-01_首次登场复核与风闻胜任讨论.md`。
- 用户拍板新住户与母亲首次登场只在公共场景触发；当前实现包含排队前门控、实际注入前
  二次门控、暂缓事件不消费，以及在写入事件角色粘滞前冻结本楼事件，避免事件自拦截。
- 当前门按 `房间表.类型 === 公共` 判断，并阻挡妻子粘滞、特殊场景和荣耀洞；管理员室的
  公共分类与丈夫单独公共对话是否也应阻挡，留给新窗口独立复核和用户确认。
- 当前 `tests/人妻公寓` 145/145 通过；目标 ESLint、`git diff --check`、`build:dev` 通过，
  dist 已恢复干净。仍未 commit、push、tag。
- 下一窗口先报告首次登场修复的独立复核结论，再讨论风闻与胜任度；未经用户拍板不得先改数值。

## 2026-07-31 独立复核后缺陷修复与 36 楼冷却拍板

- 用户已拍板：外地项目每户复用冷却由 72 楼改为 36 楼，丈夫回楼时即可再次使用；
  35/36 楼边界已有回归。
- 独立复核发现的数据库栅栏目标覆盖已修：同聊天 pending 期间，后续删除回调只能保持或
  收窄冻结目标，不能用较大的中间末楼放宽。
- 已修首次要钱 `-1` 假冷却、荣耀洞旧档负哨兵（脚本与 UI 共用规范函数）、AI 快照漏内衣、
  录像带连续失败刷新归零。
- 录像带失败次数复用 `系统._特殊场景.交互` MVU 快照：完整 5 秒尝试失败才记一次；刷新保留，
  回档/重掷随目标楼快照回退，单击进度不持久化。
- 换装余波统一为 3 楼发酵、差值达到 18 楼即过期；朋友圈晒装和丈夫起疑不再硬编码 2 楼。
  钓鱼券仍保持 12 楼效果，仅把错误的“一天”文案改为“大半天”。
- 当前回归 `tests/人妻公寓` 83/83 通过；目标 ESLint、`git diff --check`、`pnpm run build:dev`
  均通过；构建生成的 dist 已恢复，仍未 commit、push、tag。
- 仍待后续玩法/数值拍板：无冷却数值清洗道具、性癖即时卸载重装频控、公共金币刷新，以及
  堕落降低和金钱/胜任/风闻整体重做。本轮没有擅自修改这些设计项。

## 2026-07-31 《人妻公寓》全变量修复完成，等待新窗口独立复核

- 当前工作区：D:\SillyTavern\tavern_helper_template-consolidated；分支
  consolidate/rq062-local-optimizations；HEAD 2b1006ba。
- 权威复核交接：
  src/人妻公寓/新窗口交接_2026-07-31_全变量修复复核与冷却审查.md。
- 用户下一步只要求独立核查本轮修复；复核完成前不要继续设计或修改。
- 已实施：MVU/聊天存档版本迁移、情报与阶段线路守护、同房间场景子状态、父亲来电持久化、
  微信私聊图片回档去重、手机异步时间线租约、数据库四表回档栅栏、六户四阶段线路真实接线。
- 回归状态：tests/人妻公寓 共 69 项全过；阶段线路专项 36 项全过；目标 ESLint、diff check、
  build:dev 均通过。未 commit、push、tag，修复只在脏工作树中。
- 用户明确要求保留四项部位开发与阶段标题；四项部位将用于未来道具穿戴门槛。
- 冷却只完成审查，未改 72 楼。外地项目首次 -1 假冷却已修；72 楼复用、首次要钱负哨兵、
  旧档荣耀洞、零冷却数值清洗及性癖即时重装是否需要频控均待复核和用户拍板。
- 堕落降低玩法及金钱、胜任、风闻整体重做属于复核之后的下一阶段，不得提前开展。

## 2026-07-31 《人妻公寓》全变量审查

- 当前审查工作区：`D:\SillyTavern\tavern_helper_template-consolidated`
- 当前分支：`consolidate/rq062-local-optimizations`
- 审查时 HEAD：`2b1006ba`
- 完整交接：`src/人妻公寓/新窗口交接_2026-07-31_全变量审查与联动优化.md`
- 本轮只完成只读审查，没有实施变量删除、存档迁移或数值调整。
- 新窗口应先重新核对源码和工作树，再决定实施范围；源码与 Git 状态始终高于交接结论。
