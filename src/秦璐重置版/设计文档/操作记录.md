# 秦璐重置版 — 操作与修复记录

> 记录每次发布 tag 的改动、修复根因与解决方案。
> 从 v0.04 之后按时间顺序累积。

---

## v0.05 — 念头过期 + 习惯腾位补转 + AI 相关度加速 + 设计文档对齐

**背景**：主目录停留在旧 detached HEAD，与 worktree v0.03 不同步；世界书中的规则丢失了脚本管理说明、时间/日期归属、依存度 delta 规则。

**改动**：
- 主目录 `git checkout 8a61f21 -- src/秦璐重置版/` 同步到 v0.03
- 世界书补回：习惯列表/货币"脚本管理"说明；时间/日期"AI 每轮维护"；依存度 delta（+1~3 / -1~2）规则
- 补实现缺口 #1 念头过期：`RETENTION_FLOORS=30`，`isThoughtExpired()`，超保留楼数未成熟 → 已过期
- 补实现缺口 #3 习惯满 5 腾位补转：抽取 `transferThoughtToHabit()`；`sellHabit` 变卖后按植入楼层 FIFO 补转入习惯 + 数值结算
- 补实现缺口 #2 AI 相关度加速：schema 加 `系统.本轮相关念头`；AI 写 `{念头ID: 2|1}`；`tickThoughtProgress` 读后给培育中念头 +2/+1 楼进度
- 设计文档 7 份逐份勾掉已实现待办、标注缺口、修矛盾

---

## v0.06 — 修正 AI/脚本职责边界 + 恢复状态栏美化 + 补 options 条目

**背景**：v0.05 让 AI 写依存度 delta 和货币"+100"提示是错的——依存度应由脚本在念头成熟时结算，货币结算是脚本内部事情不该告诉 AI。同时状态栏美化版（`th-stage`/`bg-glow`/`st-mark`/脉冲特效）在 worktree reset 中丢失。

**改动**：
- 依存度从 AI 白名单移除，改为脚本在念头成熟时结算
- 货币不再暴露"+100"细节给 AI，只标脚本管理
- 从 Kilo session diff 反向 patch 恢复美化版 App.vue 的 template + style，保留 v0.05 script
- 补 `<options>` 世界书条目（`系统设定/行动选项.yaml`）
- `initvar`/`schema` 恢复默认心理想法（秦璐/苏梦各一句，用「儿子」而非固定名）
- 设计文档同步更新

---

## v0.07 — 6 点修正（世界字段 + 10 字 + 3 楼动态判定）

**背景**：用户提出 6 点期望需求。

**改动**：
- 日期/时间：AI 剧情自由更新（世界书规则明确）
- 地点：schema 改为固定 `Location` 枚举
- 环境氛围：schema/initvar/世界书/前端 CSS 全删
- 念头植入 `MAX_LEN` 20 → 10 字
- 待判定念头判定规则改为植入后 ≤3 楼动态注入，并说明越级由脚本处理为未达标

---

## v0.08 — 动态念头/习惯 baseline + 判定状态时序修复

**背景**：AI 判定后前端仍显示"判定中"；用户想要类似云霜凝的动态"角色当下受这些念头/习惯影响"注入。

**改动**：
- 修 `_isInAiCycle` 时序 bug：`MESSAGE_RECEIVED` 不再提前关闭 AI 周期，改在 `VARIABLE_UPDATE_ENDED` 写回后关闭
- 快照新增"当下受这些念头/习惯的影响"块（培育中念头 → 想法层；成熟习惯 → 行为层）
- 拆分判定任务 A（新念头 → 只判类型）/ 任务 B（培育中念头 → 只判相关度），避免 AI 混淆
- 类型判定改为明确 JSONPatch 路径
- 变量输出格式/念头习惯表现同步动态 baseline 说明
- 前端变量更新后延迟 50ms 刷新

---

## v0.09 — 对齐云霜凝：注册 Schema + defineMvuDataStore

**背景**：v0.08 仍无法把状态快照注入 prompt，旧楼层还显示新状态。

**根因**：
1. 脚本没等 Mvu 就绪、没 `registerMvuSchema(Schema)`
2. 前端硬编码 `message_id: -1`，所有楼层 iframe 都读最新楼

**改动**：
- 脚本入口改 `async IIFE` + `waitGlobalInitialized('Mvu')` + `registerMvuSchema(Schema)`
- 加 `dryRun` 守卫（后面又出问题，见 v0.11/0.13）
- 快照插入策略对齐云霜凝：末尾为 assistant prefill 则插到之前，否则 push 到末尾
- 新增 `store.ts` 使用 `defineMvuDataStore(Schema, () => ({ message_id: getCurrentMessageId() }))`
- App.vue 从 `Mvu.getMvuData` 硬编码切到 store.data

---

## v0.10 — 修前端框架空白 (Pinia 挂载)

**背景**：v0.09 状态栏 iframe 空白。

**根因**：`defineMvuDataStore` 基于 pinia `defineStore`，但 `index.ts` 没 `app.use(createPinia())`。

**改动**：
- `createApp(App).use(createPinia()).mount('#app')`
- `waitUntil` 改为命名导入

---

## v0.11 — 移除 dryRun 守卫（错误决策，v0.13 恢复）

**当时判断**：用户报告 `dryRun=true，跳过注入`，误以为该酒馆版本 dryRun 永远为 true。

**改动**：去掉 `if (dryRun) return` 允许 dryRun=true 时也注入。

**后果**：见 v0.13——实际是有两次调用，dryRun=true 那次应该跳过。

---

## v0.12 — 修 message_id 越界

**背景**：v0.11 之后报错 `提供的消息楼层号 '5' 超出了范围 [-5, 5)`。

**根因**：`getCurrentFloor()` 返回 `chat.length = 5`，但 Mvu 的 `message_id` 是数组下标 `[-5, 5)`，5 越界。

**改动**：`Mvu.getMvuData` 用 `message_id: -1`（最新楼）；`getCurrentFloor()` 仍返回 `chat.length` 用于楼层判定逻辑。

---

## v0.13 — 修 isInVulnerableWindow 导入错误 + 恢复 dryRun 守卫

**背景**：v0.12 之后报错 `isInVulnerableWindow is not a function`。

**根因 1**：`isInVulnerableWindow` 定义在 `thoughtEngine.ts`，但 `index.ts` 从 `suwenRoutine` 导入，运行时为 undefined。

**根因 2**：v0.11 误移除 dryRun 守卫。实际酒馆每次生成会调用 CHAT_COMPLETION_PROMPT_READY 两次，第一次 dryRun=true 是预热应跳过，第二次 dryRun=false 才是真实生成。

**改动**：
- 修正 `isInVulnerableWindow` 从 `thoughtEngine` 导入
- 恢复 dryRun 守卫，与云霜凝一致

---

## v0.14 — 测试期跳过越级闸门

**背景**：用户想暂停越级检测测试整体流程完成度，不希望念头因越级被退回。

**改动**：
- `resolveThoughtType` 拿到 AI 判定类型后直接转"培育中"，不再区分合格/未达标
- 保留越级/非越级日志区分，便于观察
- 待整体流程验证完毕后（数值 / AI 相关度加速 / 念头成熟结算）再恢复越级检查

---

## v0.15 — 植入上限 + 快照瘦身 + 在场角色机制 + 界面重设计

### 1. 每楼植入上限 3 条

**背景**：植入无频率限制，一轮内可无限灌念头，会淹没 AI 判定通道。

**改动**：
- `thoughtEngine` 新增 `MAX_IMPLANTS_PER_FLOOR = 3` + `countImplantsAtFloor()`（按 `植入楼层 === 当前楼层` 计数，秦璐+苏梦合计）
- `implantThought()` 超限拒绝（返回 null）；状态栏植入前拦截 + 额度点显示
- 规则：AI 回复后楼层推进额度自动刷新；ROLL 不推进楼层、不刷新额度

### 2. 状态快照瘦身（去三重重复）

**背景**：用户实测发现快照大量冗余：baseline 样板每条念头重复一遍、判定任务 B 把念头再列一遍、判定规则与常驻世界书整段重复、玩家侧信息（货币/习惯栏/待腾位）混入。

**改动**（原则：只注入脚本管理且 AI 无法从上下文推出的信息）：
- 删：堕落度数字/依存度/情绪/位置/内心/货币/习惯栏容量/已成熟待腾位/⚡越级植入提示
- 保留并瘦身：阶段行（+心防松动⚡后缀，脚本覆写 AI 上下文里没有）、苏文位置（脚本黑盒作息，快照是唯一通道）
- 念头影响块：语义样板交给世界书「念头习惯表现」（已手动转蓝灯常驻，index.yaml 同步 `constant: true`），快照只列清单，念头附 ID
- 判定任务 A/B：只列条目+一行路径提示，完整规则依赖常驻「变量输出格式」；任务 B 不再重复列念头（引用影响块 ID）

### 3. 在场角色机制（对标云霜凝 _当前场景角色）

**背景**：`系统.当前角色` 初始化后无人写入（UI 只改本地状态、AI 无权、脚本不写），快照永远只注入秦璐——苏梦的念头永远进不了判定任务 A，会错过 3 楼窗口卡死在判定中。

**改动**：
- schema 加 `系统.在场角色 {秦璐:bool, 苏梦:bool}`，`当前角色` 标记 @deprecated
- AI 每轮维护（进场/离场/复核），世界书「变量更新规则」「变量输出格式」补白名单和维护规则；initvar 补初始值
- 快照按在场过滤：状态行/影响块/判定任务 B 只给在场角色，同时在场则合并；**判定任务 A 不过滤**（类型判定纯语义，过滤会让提前埋的念头错过 3 楼窗口），改为两角色都列、路径逐条附
- 心防松动覆写改为对所有在场角色生效；脚本兜底：全 false 按秦璐在场

### 4. 状态栏界面重设计

**背景**：旧版满屏发光无层次、三层动画背景、✦ 装饰陈旧、桌面优先两栏在移动端总是塌单列。

**改动**（App.vue template+style 重写，script 逻辑不变）：
- 保留阶段驱动主题色机制（5 阶段暗金→电紫），变量精简为 acc/acc2/bg/panel/line
- 移动端优先单列流：顶栏(世界+货币) → 角色 tab(含在场绿点) → 阶段/情绪/三维 hero → 内心独白(衬线引文) → 植入(含 3 点额度计量) → 念头/习惯/苏文/仪容卡片；≥640px 双列
- 光效只留心防松动（唯一高调元素）；UI 数据无衬线，念头/习惯内容衬线"她的笔迹"

---

## v0.16 — 网店与装备系统 v1（18 件首批目录）

**背景**：设计讨论定案（见 念头系统生命周期与仪容装备.md / 装备与商店目录.md），
装备挂进念头培育主循环。隐藏配方池整体搁置另场设计。

**改动**：
- 新增 `shopSystem.ts`：单一 SHOP_ITEMS 目录（12 装备 + 3 消耗品 + 3 特权），
  槽位即互斥组、每角色对称装备表、消耗品即买即用——四点云霜凝代码优化
- schema：`角色状态.装备状态` Record + `_越级加成/_越级加成至楼层` + `系统._已首穿`
- thoughtEngine：培育进度接装备加速（类型倾向匹配）；`getEffectiveStage` 接药效越级；
  植入上限/字数上限改为动态（特权可扩容 3→4 条、10→20 字）
- 快照：在场角色注入 `【角色·穿着】`；首穿事件走 `_待发送道具事件` 一次性注入后写阶段清空
- 状态栏：新增可折叠网店面板（购买/装备/卸下/使用/特权，按当前角色 tab 选购）
- 世界书：`装备状态` 列入只读名单（需手动同步酒馆）
- v1 未启用（数据就绪逻辑后接）：越级钥匙（闸门测试期关闭）、疑心风险（累积规则未设计）、
  瞬发植入特权（未上架）

---

## v0.17 — 培育槽 + 阶段禁区 + 界面 tab 分页重构（frost 皮肤）

**背景**：①每楼3条的流量限制过松，连续几楼植入可积压大量培育中念头；
②低阶段角色被念头影响到几乎改性格——影响本身不设门槛（这是核心爽点），
但行为/话题的出口需要阶段限幅；③用户不满上版极简样式，要求层次照云霜凝
（毛玻璃/渐变/底纹），商店独立成页。

**改动**：
- **培育槽制取代每楼限制**：判定中+培育中 计槽，每角色各 3 槽（植入扩容特权 → 4）；
  删除 `MAX_IMPLANTS_PER_FLOOR/countImplantsAtFloor`，新增 `countActiveThoughts/getCultivationSlots`；
  退回放宽：判定中/培育中/未达标/已过期 均可退回腾槽（已成熟不可）
- **阶段禁区**：`STAGE_RESTRAINTS` 5 档"允许+禁止"清单，按在场角色动态注入影响块末尾；
  原则：念头决定她"想什么"，阶段决定她"敢什么"——影响恒在，出口限幅；
  世界书「念头习惯表现」重写（删"脱口聊到"，接入阶段约束语义，**需手动同步酒馆**）
- **界面重构**（对标云霜凝架构，代码省一半）：
  - App.vue = frost 三明治（多段渐变底+斜纹底纹+径向光晕+毛玻璃顶栏）+ tab 分页
    （秦璐/苏梦/苏文/网店），阶段换色暖调保留（主题随最近选中的角色 tab）
  - CharPanel 单组件双角色共用（云霜凝是 YunPanel/LuoPanel 两份重复代码）
  - SuwenPanel 独立页（双疑心条 + 冻结徽章）；ShopPanel 独立页（"为谁选购"切换 +
    类别分页 + 固定详情栏 + 秦/梦拥有者徽章）
  - 老楼层内容区默认折叠（对标云霜凝 v2.0.31 性能优化）

---

## v0.18 — 界面布局三处修正（用户 0.17 试用反馈）

- 苏文取消独立 tab → 顶栏下常驻一行速览条（状态/位置/加速·安全期/双疑心/冻结❄），SuwenPanel 删除
- 商店列表改双列网格（宽屏三列）+ `min-height` 固定——切装备/消耗品类目不再高度跳变；
  卡片改纵向紧凑（名称+价格 / 徽章+按钮）
- 角色面板宽屏（≥560px）双列并排：左=状态+植入，右=念头/习惯/仪容，压缩整体长度

---

## v0.19 — 苏文卡入角色面板 + 苏文心理活动

- 苏文卡放进角色面板左列（仪容左侧空位），卡片规格对齐：状态chip/位置/情绪/
  **心理活动**（衬线引文）/对本页角色的疑心条/加速·安全期提示/疑心冻结提示。顶栏速览条保留
- schema 新增 `苏文状态.当前心理想法`（AI 每轮更新，不论在不在场——被瞒在鼓里的
  丈夫视角，戏剧反讽窗口，对标云霜凝苗广心理活动先例）；`当前情绪` 一并入 AI 白名单
- 世界书「变量更新规则」「变量输出格式」补苏文两字段（**需手动同步酒馆**）；initvar 补初始值

---

## v0.20 — 商店扩容：装备+10 + 体改类 7 件

**背景**：装备种类偏少（对比云霜凝），且缺纹身/穿刺/隆胸等肉体改造类道具。

**改动**：
- 装备 12→22：内着+3（无痕丁字裤/情趣三点式/开裆连裤袜）、外装+3（紧身瑜伽套装/
  露脐短打/真空围裙）、饰品+2（情侣项链/肚脐链）、妆容+2（鲜红美甲/催情香水），
  全部带阶段门槛与首穿事件
- **新增体改类 7 件**（肚脐环/舌环/乳头环/后腰蝶纹/下腹淫纹·阶4；阴蒂环/隆胸手术·阶5）：
  一次性购买即改造，写入 schema `身体改造` 字段（穿刺/纹身/体态变化），永久被动加速
  +0.5/楼、不占槽不可卸、各买各的、带改造仪式事件
- **堕落度第二通道**：体改每件 +4~6 堕落度（原唯一来源是念头成熟结算 简单+6/困难+8），
  改造后即时阶段校正——货币→堕落的转换渠道
- 商店加"体改"类目页；仪容卡加"改造"行；世界书只读名单补 `身体改造`（需手动同步）

---

## v0.21 — 阶段约束暂停 + 换装写回仪容 + 回滚保护补缺 + 卖习惯300

**背景**：①用户测试期要求暂停阶段约束注入；②装备后仪容栏的上装/下装/内衣等仍显示旧衣
（用户预期换装应改这些位置——云霜凝 syncClothing 即此做法）；③用户快照截图出现
"第2阶段「抵抗」"标题错位 → 排查发现 `rollbackProtectedFields` 只捕获未回滚
堕落度/阶段/依存度（v0.05 实现缺口），AI 私改这些字段无人拦；④收入端偏窄。

**改动**：
- `ENABLE_STAGE_RESTRAINTS = false` 暂停阶段约束注入（表保留，**正式版连同越级闸门一起恢复**）
- 装备↔仪容写回：`CLOTHING_WRITE` 表（20 件映射到 上装/下装/内衣上下/袜裤/配饰/
  特殊装饰/唇妆/眼妆/特殊妆容），装备写入、卸下（含同槽替换）恢复**当前阶段默认外观**
  （stageConfig 默认外观首次派上用场）；香水类不映射（仅快照穿着行）
- 回滚保护补缺：堕落度/当前阶段/阶段标题/对主角依存度/对苏文依存度 强制回滚——
  "第2阶段抵抗"类错位从根堵死
- 卖习惯 100→300（`HABIT_SELL_PRICE` 常量，引擎+前端+按钮统一）

---

## v0.22 — 0.21 试用反馈五连修（已发布）

**反馈与根源**：
1. AI 对换装困惑（历史正文=毛衣棉裤 vs 变量=连衣裙）——三重根源：换装只改变量没有叙事桥；
   `变量输出格式` 白名单没列 服装细节/妆容细节（AI 判断"不得修改"是对的）；穿着信息注入了三处互相冗余
2. UI 仪容卡"装备中"标签与下方服装字段重复
3. 装备可购买但阶段不足无法装备（buyEquipment 没吃阶段门槛）
4. 服装道具正文零表现——同 1 根源，AI 收到快照但选择跟随历史正文
5. 苏文疑心值从未上涨——**非 bug**：疑心累积规则本来就未实现（待设计），与测试暂停项无关

**改动**：
- 换装叙事桥：每次装备/卸下注入一次性换装事件（首穿用专属文案，复穿/卸下用通用文案），
  `queueItemEvent` 统一事件队列
- 穿着单一事实源 = 服装/妆容细节变量：删快照【穿着】行、删 UI"装备中"标签行；
  妆容细节新增 `香氛` 字段，雾致香水/催情香水补进 CLOTHING_WRITE（22 件全映射）；
  仪容卡增列 配饰/装饰/唇妆/眼妆/妆饰/香氛（"无"不占行）
- 世界书：`变量更新规则` 增「换装机制」条目 + 服装/妆容细节入 AI 可更新字段；
  `变量输出格式` 白名单补 服装细节/妆容细节（**两条都需手动同步酒馆**）
- 能买即能穿：buyEquipment + ShopPanel 购买按钮同吃阶段门槛（未达显示"需阶段N"）

**装备接入主循环（"道具鸡肋"讨论产物）**——被否决的方向：疑心挂苏文作息时机
（逼玩家跳换装舞、烧楼层篇幅、违背 AIRP 自由度）；采纳 C+D：
- C 装扮意识动态提示：脚本检测在场角色装备中**风险≥1**的物品（正常衣物如修身连衣裙/
  红绳手链不触发），快照注入一行表现指令（异物感/遮掩/走神），满星追加"从内到外都是
  他置办的"意识；哪件该提示靠调整该物品风险值控制
- D 仪容星标·全套加成：5 星 = 4 装备槽各有网店装备 + 任意体改（`getOutfitStars`）。
  满星 = 所有培育中念头 +1/楼（无视类型匹配）**+ 苏文对她疑心 +1/楼**（高收益高风险，
  用户提议的代价耦合）；不满星疑心每楼回落 0.5；借口短信冻结期不涨不落、到期自动解冻
  （消耗品从此有实际用途）。疑心值纳入回滚保护（AI 不可改，脚本结算）。
  UI：仪容卡 5 星行，满星点亮 + 脉冲动画 + "全套共鸣"注记。数值全部待平衡期统一调。

**疑心系统完善（阶梯 + 坏结局）**：
- 阶梯提示（快照动态注入，AI 只演态度不改数值）：30 起疑（多看两眼/状似无意盘问）、
  60 警觉（盘问变多/留意行踪手机/气氛发紧）、90 临界（阴沉冷淡/暗中翻查/一触即发）；<30 不注入
- 触顶 100 → `系统._坏结局 = '疑心爆表·角色名'` 锁定：快照只注入终局指引块（培育/判定停止，
  演绎摊牌后果）；引擎写阶段全停（回滚保护仍生效）；植入/商店/出售全部拒绝（"结局已锁定"）；
  状态栏顶部红色脉冲横幅"☠ 坏结局已锁定"
- 满星装扮意识句改**动态生成**（列实际装备+体改清单），并追加知情隔离标注：
  "此信息仅属于她的私密认知，苏文并不知情（除非正文已揭穿）"——不做来历/路线的叙事断言，
  防止写苏文心理的 AI 把信息泄进苏文视角（用户指正 ×2）
- 疑心值可能出现 0.5 步进，速览条/苏文卡显示取整

**培育速度平衡（原待办，本版定稿）**：
- 基础需要楼数 3/6 → **简单 5 / 困难 10**
- 新增**满配下限** `MIN_MATURE_FLOORS`：距植入不足 简单3/困难4 楼不成熟（进度先攒着，
  楼数到了才结算）——满星+装备+相关度全堆也无法瞬发，"冲刺"保留过程感
- 满配节奏：简单 3 楼 / 困难 4 楼；纯放置：5 / 10 楼
- **调试满星后门**：状态栏星标区 1.5s 内连点 5 次切换 `系统._调试满星`（MVU 持久，防 reload），
  开启时两角色均按满星结算（星全亮 + "⚙ 调试满星"注记；培育+1/楼、疑心+1/楼照常）——
  正式数值落地后测试快节奏专用，发正式版前记得确认关闭状态

---

## v0.23 — 打断/苏文视角 + 疑心闭环 + 录像 + 装备扩充（已发布）

- **调试满星后门改为永久保留**（用户决定）——撤销 v0.22 的"发正式版前关闭"提醒，
  连点 5 次星标区的满星模拟作为长期玩家功能存在
- **货币后门**：顶栏货币区 1.5s 内连点 5 次 → 货币 +500，可无限重复；
  成功时"+500"绿色浮字动画反馈。与调试满星同款手势→写 MVU 通道
- **换装事件改云霜凝式物理骨架**（0.22 试用反馈：首穿文案"她穿上丁字裤做家务"是
  既成事实，跳过了赠送环节，正文里玩家从没拿出过衣服）：
  - 装备（首穿）：`{{user}}把新买的「X」交给她，让她换上（槽位；他第一次送她这样的东西）
    ——本轮演绎交付与她按当前阶段的反应`；复穿/卸下同款 {{user}} 动作句
  - 事件=玩家的交付动作本身，她的反应交 AI 按阶段生成（角色无关原则，对齐云霜凝
    itemPhysicalSkeletons）；装备类的 22 条 首穿 专属文案弃用（保留在数据里）
  - 体改事件加前缀"在{{user}}的安排下，"——锚定改造由玩家推动，不凭空发生
- **打断系统 + 苏文视角（重新设计，取代旧案"越级惩戒打断"触发）**：
  - 触发：疑心每上升跨过一个 10 点档（10~90）触发一次打断，**每档一生一次**
    （`_已触发打断档位`，疑心降回再涨不重演）；一次跨多档只演最高档；100 仍是爆表坏结局
  - 打断事件 = 9 条独立**方向**文案（偶然路过→蹲点查岗递进），只给方向和规则，
    台词/时机/她的反应交 AI 按上下文与当前阶段演绎；他无实据、不揭穿
  - 打断后苏文卡点亮**发光跳动按钮"👁 苏文视角"**（常驻到点击；多次打断只保留最新一次），
    点击进入 3 幕插叙 POV：铺垫违和感→线索发酵→收束在推门那一刻（同一场面从他的眼睛重看）
  - POV 期间主线引擎**全冻结**（培育/疑心/作息/心防松动/判定任务全停；`isPovFloor` 覆盖
    已计数楼层的 ROLL 重生成，防幕数错位/漏冻结），快照只注入本幕指引；素材限上下文
    真实发生过的剧情；变量只许更新苏文情绪/心理想法
  - 疑心值不因打断变动（纯剧情演出，用户定）；借口机制另行设计（待办）
- **疑心主通道：堕落度折算 + 下限棘轮 + 苏文安抚道具**（核心玩法定调：一边攻略一边瞒）：
  - 主通道：她的堕落度每 +2 → 疑心 +1（×0.5 折算，`_疑心已结算堕落度` 基准水位持久化，
    念头成熟/体改/卖习惯补转全部来源统一覆盖；UI 侧改动下一楼补收；老存档首次初始化不补收历史）
  - 下限棘轮：回落与降疑手段都降不破 **堕落度×0.25**（`getSuspicionFloor`）——
    看见了就无法当没看见；疑心成为"有松紧的棘轮"
  - 满星 +1/楼 保留（附加项）；冻结期堕落度增量挂账，解冻后补收
  - 苏文安抚道具 3 件（消耗品），分两种形态（用户定：剧情型只是其中一类，不是全部）：
    **剧情型**（她去执行，弹一楼安抚剧情，方向不定细节）精心家宴 150（-10）、
    周末全家出游 400（-25 + 冻结5楼）；**静默型**（直接生效不占剧情，对标借口短信）
    贴心小礼物 250（-15）
  - 商店总数 35 → 38 件
- **装备款式扩充 +12**（只加款式，不加槽位/字段——用户明确）：装备 22 → 34，商店总数 50。
  内着 +3：连体塑身衣200(阶2)/黑丝吊袜带320(阶3)/珍珠链内裤520(阶4越级)；
  外装 +4：情侣家居服120(阶1,**陪伴交流**首次覆盖)/包臀热裤220(阶2)/侧开衩旗袍350(阶3)/
  他的白衬衫480(阶4越级,家庭替代)；饰品 +3：珍珠耳坠120(阶1)/纤细脚链180(阶2,**亲密接触**)/
  玫瑰锁骨链300(阶3)；妆容 +2：水光唇釉160(阶2,亲密接触)/桃花眼妆260(阶3)。
  全部有 CLOTHING_WRITE 映射（现有字段）；新批次不写首穿文案（换装事件已骨架化）；
  10 大念头类型自此全部有对应装备（补齐 陪伴交流/亲密接触 两个空窗）
- **录像系统（特别分类，云霜凝留影石原理的秦璐版）**：商店总数 51。
  - 设计裁决：A 重看认知冲击 ✅ + E 跨角色观看 ✅（合一：影像可给任一女角色看，
    给自己看=认知冲击，给另一位看=耳濡目染带坏——"先堕落的带坏后堕落的"落地）；
    B 匿名上传 ❌ / C 把柄要挟 ❌ / D 持有隐患 ❌
  - 流程：云台微型相机 600 一次买断（`道具状态`）→ 商店按钮 ●开始录制/⏹停止录制
    （`_录像.录制中`，顶栏红色 ● REC 闪烁指示）→ 录制中快照注入"镜头正在记录"一行
    → 停止生成 `影像_N`（待摘要）→ **判定任务 C：影像归档**（AI 写 20 字摘要，
    白名单已补：变量更新规则 + 变量输出格式，**需手动同步酒馆**）→ 已就绪
  - 观看：特别页"影像档案"卡列出摘要，按钮"给{目标角色}看" → 观看者堕落度 +4
    （走主通道自动折算疑心）+ 阶段校正 + 弹一楼观看剧情（引用摘要），影像即看即毁
  - AI 不懂楼号 → 影像的"内容"就是 AI 自己归档的摘要，每个玩家的影像都来自
    自己的剧情；录制期间无数值效果（镜头感纯演出）
  - 调整（用户反馈）：摘要 20字 → **50字**（三处同步：判定任务C/两条世界书）；
    录制绑定"为谁选购"目标，**每角色同时只存一份影像**（key 固定 `影像_角色名`），
    重录覆盖旧影像（前端提示"覆盖旧影像"）
- **酒红缎面裙·苏梦引场隐藏钩子**（阶3外装 320，商店总数 52）：
  秦璐首次穿上 → `_苏梦引场` 倒数 3 楼 → 注入一次性事件：苏梦不经意撞见母亲此刻的
  样子（推门/厨房/走廊按场景自选）并由衷赞美——目的是引出苏梦的存在，登场方式完全
  按上下文演绎不硬编码；一生一次；**无任何 UI 暗示**（潜伏式道具原则），简介只写
  "好看到家里人都会注意到"作暗线

**发版前全量校对（v0.23 收尾）**——逐文件重读 index/shopSystem/thoughtEngine + UI 改动区 + schema 交叉一致性：
1. 🔴 **事件清空时序 bug（会吞核心功能）**：写阶段 4.5 的 `_待发送道具事件=''` 会把
   同周期 4.25/4.4 新产生的**苏文打断事件、苏梦引场事件全部吞掉**（UI 写入的事件不受影响，
   因为跨周期）。修复：清空前移到写阶段开头 1.4（语义=擦掉上一轮已注入的），
   BE/POV 分支的重复清空一并去掉——**此前测试里打断/引场从未真正生效过**
2. 🟡 每楼限一次打断：调试满星下两角色同楼跨档会挤出两条打断事件 + 苏文视角互相覆盖；
   现在第二位不标记档位、顺延下一楼
3. 🟡 判定任务 A 撤掉"植入后≤3楼"窗口：POV 3 幕 + 打断楼会烧穿窗口，念头永久卡判定中
   还占培育槽；改为判定中即注入、判出即消失
4. 🟡 老存档守卫：toggleRecording/showTape/酒红裙钩子对裸 stat_data 上可能缺失的
   `_录像/影像列表/_苏梦引场` 补防御初始化（0.22 存档升级后、首个 AI 周期前点按钮不再崩）
5. 🟢 queueItemEvent 消毒 `|`（防 AI 摘要含分隔符导致事件串裂）；文档笔误
   getImplantLimit→getCultivationSlots
   其余通过项：BE/POV 引擎冻结路径、疑心结算顺序（回滚→结算）、幂等标记、
   换装写回/恢复、38+13+1 道具映射完整性、schema 7 个新字段全消费点一致、build EXIT=0

---

## v0.24 — 越级闸门/阶段约束恢复 + 三振坏结局实装

**背景**：测试期关闭两闸门的唯一理由（培育要快）已被 v0.23 后门消灭（货币连点+调试满星），
用户拍板全部恢复并实装三振。

- **越级闸门恢复**（resolveThoughtType）：类型推荐阶段 > 有效阶段 → 退回（未达标）。
  有效阶段 = 基础 + 心防松动窗口(+1) + 药效（安神药+1/头孢酒+2）+ **越级钥匙装备**
  （v0.24 首次接入：装备中且类型倾向匹配的越级钥匙 +1，`hasEscalationKey`），封顶 5。
  安神药/头孢酒/越级钥匙自此从"惰性数据"转正
- **阶段约束恢复**：`ENABLE_STAGE_RESTRAINTS = true`，快照按在场角色注入 5 档
  允许/禁止清单（念头决定她想什么，阶段决定她敢什么）
- **三振坏结局**（诱惑派入口 + 惩罚亮牌，镜像原则）：
  - 未达标念头行出现红色呼吸"强行植入"按钮（title 诱惑话术；已有计数时亮牌 ⚠n/3）
  - 前 2 次：假意转培育中（`_强植` 标记）+ 注入排异反应事件（失控/抗拒/痛苦，
    第 2 次追加"眼神出现裂纹"），下一楼 tick 必被心智弹回未达标
  - **连续第 3 次 → `_坏结局 = 三振崩溃·角色名`**，锁档；坏结局快照文本按类型分支
    （三振=人格崩解终局 / 疑心爆表=摊牌终局）
  - 连续语义：正常植入成功转培育中 → 该角色 `_强植三振` 清零（用户定）
- 无世界书改动（阶段约束走快照动态注入），本版**不需要**手动同步

---

## v0.25 — 高阶装备补全 + 装备页折叠 + 引场放宽 + 商店页货币后门（已发布 tag 0.25）

- **货币后门补商店页**（用户反馈"点击货币没效果"）：+500 连点后门原只挂在顶栏货币胶囊，
  ShopPanel 页头的货币显示无 handler——补上同款（+500 浮字 + success toast）。两处后门通用：
  1.5s 内连点 5 次、须在最新楼层面板操作
- **装备页槽位分组折叠**（用户反馈页面太长）：内着/外装/饰品/妆容组头默认收起，
  点击展开/收起（▸/▾ + 件数），展开态高亮；其他分类页不折叠
- **高阶装备 +6（41 装备 / 全店 58）**：填终局空档（原阶4无饰品、阶5全线只有素圈戒指）。
  阶4饰品：铃铛脚链/皮质颈环（越级）；阶5：镂空连体网衣（内着，越级）/暗扣高领裙（外装）/
  同源沐浴露/发情红晕妆（妆容）。阶5主题=表里反差：暗扣高领裙特意低风险（端庄下的机关
  苏文看不出），同源沐浴露反向高风险（他可能闻出儿子的气味）。全部有 CLOTHING_WRITE 映射，
  无新槽位/新字段
- **苏梦引场钩子放宽**（用户定，防玩家漏触发）：原仅酒红缎面裙 → 秦璐穿上**任意阶段2+外装**
  即启动 3 楼倒数（阶1外装太日常不算"变化"；一生一次、无 UI 暗示不变）

**云霜凝借鉴批次**（本场对云霜凝全功能盘点后用户选定 6 项，老楼折叠核查发现 v0.17 已有）：

- 🔴 **重roll 事件保护**（补真实缺口）：`_待发送道具事件` 注入后不再直接清空，改为**转存**
  `系统._已注入事件{楼层,内容}`；同楼重生成（转存楼层≥本次生成楼层）时快照重放转存内容——
  此前重roll 会丢失换装/打断/引场事件，AI 口胡。重放时刻意忽略待发送队列
  （那是引擎为下一楼产出的）；写阶段 1.4 改三分支：重roll 不动 / 过楼转存 / 过期清除
- **经济结算 v1**（"她的堕落是你的资本"落地，写阶段 4.45，坏结局楼不发）：
  ① 堕落度增量 ×20 折算货币（独立水位线 `_货币已结算堕落度`，镜像疑心结算，旧档校准不补发）
  ② 阶段突破一次性奖励：升 2/3/4/5 阶 → +200/400/600/800（`_已奖励阶段` 水位）。
  此前货币唯一来源是卖习惯 300，前期穷死
- **消耗品楼层冷却**（对标云霜凝，防降疑连刷/药效叠加）：ShopItem 新增 `冷却楼数`，
  借口短信10/安神药5/头孢酒5/精心家宴6/贴心小礼物8/周末全家出游15；
  `系统._消耗品上次使用楼层` 记录，前端按钮显示"冷却 N 楼"
- **在场角色锁定按钮**（对标云霜凝 2.0.31）：tabs 行 🔒 按钮 → 浮层（仅秦璐/仅苏梦/两者/解锁），
  直写 message_id=-1；`_在场锁定` 期间 在场角色 转脚本管理（进保护快照回滚 AI 改动）+
  快照注入【在场锁定】指令；苏梦引场触发时自动解锁（防顶掉她的登场）
- **脚本心跳监察**（对标云霜凝 2.0.32）：脚本每 5s 写 `_top.sessionStorage` 心跳 +
  Mvu 初始化失败弹错误 toast；状态栏 iframe 15s 后检测心跳空/陈旧 → 弹"脚本未加载"
  （sessionStorage gate 防重弹）
- 老楼折叠核查：v0.17 tab 重构时已实现（App.vue storedTab/localTab/isLatest），无需新做

**打断余波系统**（用户发现"打断后下一楼就能继续攻略"+ 打断时不检查苏文位置的连贯性 bug）：

- **打断落地强制在场**：打断触发时直接写 苏文状态=在家@世界.地点——人都进门了，
  位置数据不能还写外出（修复：疑心跨档可能发生在他"外出"的楼，事件与快照自相矛盾）
- **余波期 = 打断楼+后 3 楼**（`_打断余波至楼层 = 触发楼+4`）：
  ① 作息游标暂停（他滞留家中，窗口过后从暂停处恢复流动）
  ② 快照注入收敛提示："越界收进眼神与暗流（行为出口临时收敛一档）"——**培育不冻结**
  （念头在内心，他在附近反而吃偷腥悖论加速；玩家损失的是尺度不是进度）
  ③ POV 插叙占用窗口时按楼顺延（判定任务 A 烧穿教训）
  ④ UI：苏文速览条整条琥珀色脉冲 + "👁 余波·他还没走远（剩N楼）"
- **打断 9 档文案去具象化**（用户点破"递水果"是硬编码 → 教训7 固定模板风险）：
  删掉全部具体理由/道具/引语（递水果/充电器/"你们在聊什么呢"等），只保留猜疑强度与姿态；
  事件模板追加"理由/借口必须从当前上下文与家庭日常生成，不要凭空发明道具或情节"
- **POV 文案核查**（用户问"发现踪迹的原因是否硬编码"）：本就上下文驱动
  （规则明写"素材只用上下文里真实发生过的剧情"），第1/2幕方向再拧紧——
  明确线索=正文里他可能接触到的痕迹、选丈夫最可能注意到的
- 无世界书改动，本版不需要手动同步

---

## v0.26 — 裂纹窗口：强植从纯陷阱变"暴力破窗"（已发布 tag 0.26）

**背景**：用户反馈"强植完成后下一楼还是未达标，强植没作用"。v0.24 设计里强植本就永不成功
（纯诱惑派陷阱：排异剧情 + 三振崩溃），但按钮只有惩罚没有收益，玩家体感被骗。方案三选一
用户定"裂纹窗口"。

- **机制**：强植被排异弹回时在她心防上砸出裂缝——`_裂纹至楼层 = 弹回楼+3`，
  窗口内该角色**有效阶段 +1**（getEffectiveStage 新通道，可与心防松动/药效/越级钥匙叠加）
- **趁隙再植**（thoughtEngine.retryImplant）：未达标念头在窗口内出现绿色脉冲按钮，
  点击重过越级闸门（不重新判型，占培育槽校验）；成功 → 培育中（进度清零、刷新植入楼层）
  + **三振计数清零**（等同正常植入成功的连续语义）
- 排异事件文案追加"这次粗暴的撞击也在她的心防上留下了一道裂缝"（AI 侧暗示）；
  未达标行文案窗口内变"她的心防上有一道裂缝（剩N楼）——趁现在"
- **强植的完整循环**：强植(⚠1/3) → 排异剧情 + 裂缝 → 3楼内趁隙再植（差1阶直接过，
  差2阶叠药/钥匙）→ 成功即清零计数。收益真实存在，但连按三次强植仍是崩溃——赌的是节奏
- 无世界书改动，不需要手动同步

---

## v0.27 — 苏文视角按钮立即触发第一幕（已发布 tag 0.27）

- 用户反馈：点"苏文视角"后没有直接弹出回复，还要手动发一条消息才开始第一幕
- 修复（对标云霜凝 退出神魂空间按钮）：enterSuwenPov 写完 MVU 状态后直接
  `triggerSlash('/send （进入苏文视角）|/trigger')` 立即生成——PROMPT_READY 检测
  剩余楼>0 注入 POV 第一幕指引；toast 从"发送任意消息开始演绎"改为"第一幕开始"
- 第 2/3 幕仍由玩家正常发消息推进（幕间留给玩家的阅读/反应节奏，有意不连发）

---

## v0.25 补 — 打断状态重roll保护（已随 tag 0.25 发布）

**背景**：用户报"苏文视角不支持重新ROLL"。排查结论：POV 三幕本身 roll 安全
（幕数推进有 上次处理楼层 防重扣 + 快照分支覆盖已计数楼层，双基准幂等）；
真实的洞在**重roll 打断触发楼**——待看/_已触发打断档位/_打断余波至楼层 都是该楼
写阶段写入该楼变量的，重roll 时 MVU 从上一楼变量重建、保护快照又不覆盖这些字段
→ 打断状态整体丢失：按钮永远不亮（疑心已在档位之上不会再跨档补发）、档位记录
丢失（未来可能重复触发）、余波消失。

- 修复：`_苏文视角`/`_已触发打断档位`/`_打断余波至楼层` 纳入保护快照
  （快照来自本次生成时的最新楼数据，永远 ≥ 重roll 的重建基准），回滚时
  pov 整体覆盖、档位并集、余波取大；随后引擎照常推进（原有防重扣守卫不受影响）
- 附带效果：重roll 打断楼时打断事件会从旧楼数据重放（v0.25 注入逻辑读到 待发送
  非空），打断剧情在重roll 版本里提前一楼出现——事件不丢，可接受

---

## v0.28 — 苏文心理活动锚定 + 苏梦气质描述修正 + 后门移动端加固（本次发布 tag 0.28）

用户测试反馈两个 bug + 一个移动端加固。

**Bug1 苏文外出却在想"再睡一下"**：苏文 `当前状态/当前位置` 由脚本作息游标黑盒算出，
但 `当前心理想法` 是 AI 每轮自由写的，此前只要求"维持心理轨迹"、没绑定到脚本位置。

- 修复（双保险）：① 快照 `buildStatusSnapshot` 在【苏文】位置行下按状态追加**心理活动锚定**
  指令（外出→只能想外面的事、绝不写居家/赖床；睡眠→睡前思绪；在家→符合具体房间）；
  ② `变量更新规则.yaml` 对应条目补"必须与脚本给出的当前状态/位置一致"

**Bug2 苏梦气质描述错成"温柔贤淑的家庭主妇"**：根因是 `initvar.yaml` 苏梦状态缺
`气质描述` 字段。schema 的 prefault 虽给苏梦"活泼开朗的大学生"，但因 initvar 提供的是
**部分对象**（非 undefined），prefault 被跳过，字段回落到**共享字段级默认**（那是秦璐的
'温柔贤淑的家庭主妇'）。

- 修复：① initvar 给苏梦补 `气质描述: 活泼开朗的大学生`，顺手给秦璐也显式写上，消除
  "依赖字段默认恰好等于某角色值"的脆弱性；② 加幂等旧档迁移——检测到苏梦气质描述
  等于泄漏的家庭主妇串就纠正（该字段 AI/脚本均不写，旧档否则永远错）

**后门移动端加固**（用户问"作弊功能手机端支持吗"）：调试满星（星标区）+ 货币+500
（顶栏/商店页）三处后门都用 `@click`，手机 tap 会合成 click 本就能触发；为防旧机 300ms
点击延迟与双击缩放吞点，把连点窗口 1.5s→2.5s，并给三处热区加 `touch-action: manipulation`

- 注：Bug2 的 initvar/变量更新规则改动属世界书，需手动同步进酒馆；Bug1 的心理锚定与
  苏梦迁移在脚本内，随 tag 的 dist 自动生效

---

## v0.29 — 苏文状态预演注入：修复 v0.28 锚定锚在上一楼（本次发布 tag 0.29）

用户回报 v0.28 未生效：状态栏显示"外出工作@外面"，心理活动仍是"综艺没什么好看的，
回卧室躺一会儿…明天周一又得早起"这类居家内容。

**根因（用户怀疑正确）**：读写分离的时序性滞后。PROMPT_READY 注入快照时从
`message_id=-1` 读到的是**上一楼**的苏文状态；作息游标推进（`advanceSuwenRoutine`）
发生在写阶段（AI 回复落地后），新状态写进**本楼**变量、状态栏渲染本楼。于是每个
跨段楼快照必错一段，"第二天"跳转楼错开一整天——v0.28 的锚定行生效了，但锚的是
旧处境，反而把 AI 摁在"周日晚客厅看电视"上写心理活动，与本楼落地的"周一外出"矛盾。

- 修复：`suwenRoutine.ts` 新增 `previewSuwenPosition()`（纯计算不写变量），
  PROMPT_READY 注入前**预演本楼推进结果**，【苏文】行 + 锚定行都用预演值：
  - 新楼（chat 末条是玩家输入）：`detectJump(玩家输入) ?? 游标+1` → `getSuwenPosition`，
    与写阶段同输入同算法，预演 = 落地
  - 重roll/继续（chat 末条是 AI 消息）：不预推，重roll 的推进基准与原次相同
  - 打断余波窗口：冻结在家（条件对齐快照余波提示行 `promptFloor < _打断余波至楼层`）
  - 写阶段照旧不动，仍是唯一写入方；疑心打断那种"写阶段才产生的强制在家"预演不了，
    但打断自带下一楼事件文本兜底，属已有设计
- 顺手：锚定行补一句"若他上一轮的心理想法与此处境已不符，本轮必须重写该字段"，
  压 AI 偷懒不更新导致旧想法挂在新位置旁的次要面
- 纯脚本改动，随 tag 的 dist 自动生效，无需手动同步世界书

---

## v0.30 — 去预置发情：人设/开场白清洗 + 阶段约束常驻与心理来源白名单（本地 tag 0.30，未推送）

玩家反馈：秦璐/苏梦太容易发情，不撩也"内心虚空发情"，防发情提示不顶用；指出人设里
"潜意识中有着超越母子的情感萌芽"疑似源头。诊断确认，且问题比那一句大——发情素材
弥漫在整个常驻人设与开场白里，与"植入念头→培育→堕落"的核心玩法自相矛盾（人设把
终点状态写成了初始状态，阶段1「抵抗」无物可抵抗）。

**A. 世界书清洗**（需手动同步进酒馆：秦璐/苏梦/开场白 三个条目）：

- `角色/秦璐.yaml`：删"性需求/理智与欲望斗争/情感萌芽/身体敏感度随开发提升/主动寻找
  独处机会"，interview 删燥热·心跳·身体敏感段。换成土壤型描述：寂寞空虚 + 情感寄托
  过重 + 边界感模糊的依赖。加【重要】声明：当下真实心理以游戏状态数据为准，本设定
  只是出厂底色（寂寞但完全正常的母亲，无逾越念头）
- `角色/苏梦.yaml`：删"超越姐弟的亲近感/亲密感/性话题开放/身体未开发的敏感度/肢体
  接触频繁"，interview 删钻被窝·靠怀里·眼神不一样·不介意被看·"{{user}}是我的"。换成
  无话不谈的玩伴 + 大大咧咧不设防。同款【重要】声明
- `系统设定/开场白.yaml`：删"更隐秘更柔软的表情/有机可乘/演得真好/心跳攀升·苏醒/
  享受门前等待/异样光泽的眼睛"——原版开局即密谋，AI 续写全程照此调子。同场景同
  钩子重写：底色改为寂寞 + 送水果是她一天里最有盼头的时刻
- 注：玩家侧数据库/总结插件从旧人设自动提炼的"[背景设定] 情感萌芽/燥热"条目在旧档
  会继续注入，旧档想彻底干净需玩家手动清插件条目；新开档无此问题
- `主角.yaml` 的"暗含欲望"描述的是玩家自己（攻略者），有意设计，不动

**B. 脚本：阶段约束常驻 + 心理来源白名单**（`buildStatusSnapshot` 影响块重构）：

- 原缺陷①：阶段约束挂在影响块末尾，无培育中念头/习惯时整块跳过——"没植念头也没撩
  还发情"的场景恰好什么约束都没注入；缺陷②："影响只在内部呈现（走神/脸红…）"是
  允许清单，AI 读作"每轮写点内部春心是合规的"
- 有影响时：清单末尾加"▷ 心理来源限定：超出阶段基线的暧昧/欲望心理只能来自已列
  念头与习惯，没列出的 = 不存在"
- 无影响 + 阶段1：注入"平常心基线"——她就是普通的母亲/姐姐，内心戏是家务/学业/
  琐事，不要自发生成暧昧/性张力/身体意识
- 无影响 + 阶段2+：注入"心理基线以阶段约束为准，不要自发加码"（阶段2+的堕落基线
  由阶段承载，不能说"普通"）
- 阶段约束行移出条件分支，在场即注入；STAGE_RESTRAINTS[1] 措辞改绑定念头（"已列
  念头/习惯的影响只在内部呈现…没有念头时她的内心就是纯粹的日常"）

C（心防松动文案降噪）用户决定不做。

**v0.30 补（同 tag）：越界应对 + 苏梦边界感隐藏入口**

- 越界应对（prompt 层，系统层数值代价用户决定先不做）：阶段约束后常驻追加一行——
  苏斌言行超出本阶段禁区时她本轮不会配合，但**只给底线不给动作剧本**（教训6/7：
  写"推开/呵斥/离场"会坍缩成固定戏码），回应形式由人设/感情/情境现挂；并明确
  "拒绝不损害她对他的爱，也不点燃欲望"——保攻略性，被拒信号是"她现在还不行"
  而非"这条线死了"。第一版文案因太硬编码（惊慌推开/夺门而出/不给台阶）被用户
  否掉，据此重写
- 苏梦补"边界感弱"作为攻略隐藏入口（用户指示：可以边界感弱，但不要强调）：只在
  behavioral tendency 加一条"打闹惯了，挨着坐/抢东西/勾肩搭背从没多想过"——
  无性意味、不进 key trait、全文仅此一处

**v0.30 补3（同 tag）：打断冷却存量制（用户设计）——修复打断连环**

问题：打断余波中培育不冻结（v0.25 有意设计），3 个培育中念头在余波窗口内接连成熟
= 堕落度 +18~24 → 疑心 +9~12，立刻跨下一个 10 点档 → 苏文人还没走又"突然出现"
打断，且新打断整体覆写 `_苏文视角`，未看的 POV 插叙直接丢失。

方案演化：①存量判据+余波后补触发 → 用户否（因果断开，体验差）；②余波内就地升级
（疑心加深事件）→ 用户提出更优的③冷却存量制，采纳：

- 新增 `系统._打断冷却至楼层`（打断楼+12，全局共享，纳入重roll保护快照取大恢复）
- 冷却内跨档**不触发也不标记**（疑心值照涨，UI 可见=欠账可见）
- 冷却外每楼按**当前疑心值**结算：最高未触发档 ≤ 当前值 → 触发并连带标记其下档位；
  非冷却期首次涨过某档=当楼即触发，与旧跨档瞬间语义一致
- **拆弹玩法**：冷却 12 楼内玩家把疑心降回档下（自然回落 0.5/楼、借口短信等）→
  到期不触发，该档等疑心再涨上来才演——打断从纯惩罚变成可运营的威胁
- 疑心触顶 100 坏结局不受冷却影响，照旧即时
- 已知保留：POV 待看超 12 楼一直不点开，新打断触发时仍会覆写旧 POV（不做"待看
  阻塞冷却"——会被玩家用"永不点开"无限拖住打断）

---

## v0.31 — 旧楼层状态栏只读（本地 tag 0.31，未推送）

用户问旧楼网店能不能用道具。排查结论：数据层本就安全——所有操作（购买/装备/消耗品/
体改/录像/观影/植入/在场锁定/两个后门）读写全固定 `message_id=-1`（永远最新楼），
领域函数在最新数据上复验（货币/阶段/冷却），旧楼变量不会被写。但 UI 层没有闸门：
老楼面板默认折叠可手动展开，按钮按旧楼历史数据亮灭，点了生效在最新楼且旧楼面板
不刷新——"点了没反应"式困惑。

- App.vue 内容区加旧楼只读：`!isLatest`（现有 2s 轮询保活的判断）时面板整块
  `pointer-events:none` + 降饱和降透明 + 顶部提示条"旧楼层仅供回看，操作请回到
  最新楼层"。CharPanel/ShopPanel 内的一切操作（含苏文视角按钮、调试满星、货币
  后门）一并覆盖，组件零改动
- 遮罩外的两个操作单独闸：顶栏货币后门 `coinTap` 开头 return；在场锁定按钮/浮层
  `v-if="isLatest"` 直接隐藏
- 旧楼查看功能保留（这是 store 绑定本楼的有意设计），只禁交互

**v0.31 补（同 tag）：疑心分期补收 + 挂账透明化（B+D，用户选定）**

玩家反馈：冻结道具到期后疑心 7→16 一楼跳变。排查：不是 bug，是 v0.23"挂账补收"
设计——冻结期间水位线不推进，解冻一次性收齐（7 + 18堕落×0.5 = 16，数字吻合）。
但"冻结=保护伞"的直觉预期变成"解冻=账单日"，体验反直觉。方案 A免除/B分期/
C折扣/D透明化中用户选 B+D（总量守恒保"瞒不住"主题，去掉跳变惊吓）：

- B 分期：`settleSuspicion` 主通道每楼最多 +2（`SUSPICION_RISE_CAP_PER_FLOOR`），
  水位线只按实收推进（`+= min(collect*2, owed)`，防奇数欠账过推），余账逐楼续收。
  **通用生效**：不只解冻账单，多念头同楼成熟的 +3~4 也被摊平（本身也是跳变源）；
  满星 +1/楼 是活账不占额度。总量不变，只影响到账节奏；配合打断冷却存量制，
  跨档打断在值真正爬到档位那楼触发
- D 透明化：速览条 ❄ 旁显示"挂账+N"（冻结中实时计算 (堕落度-水位线)×0.5），
  tooltip 注明"解冻后每楼最多补收+2"

**v0.31 补2：疑心/打断在场门槛——修复缺席角色被动涨疑心+凭空打断（玩家实测）**

玩家报告：苏梦引场后离场，全程零互动，苏文对她的疑心仍持续上涨，跨档后打断
砸进正在攻略秦璐的剧情——苏梦根本不在场。两个叠加的坑：

- 根源：`_调试满星` 是全局旗标，开着时 `getOutfitStars` 对两个角色都返回满星 →
  缺席的苏梦也吃满星 +1/楼，十几楼即跨档（疑心只有堕落度增量和满星两个涨源，
  苏梦无堕落度增长，锁定此路径）
- 修复①满星附加仅对在场角色生效（`getPresentCharacters` 判定，全 false 回落秦璐
  的兜底保留）——调试满星只影响在场者，合法满星在她离场期间也暂停压力（语义：
  不在场谈不上"被他看见"）
- 修复②打断触发加在场门槛：不在场不触发不标记，档位欠着等她下次在场且冷却外
  按存量补触发——打断的语义是"中止她当前的场面"，没有场面就没有打断
- 玩家侧存量疑心值不回收（可用安抚道具压），已触发档位照旧不重演

**v0.31 补3：苏梦初始服装与秦璐同款——气质描述 bug 的服装版（玩家报告）**

同 v0.28 Bug2 机制：initvar 苏梦状态是部分对象 → schema 苏梦 prefault（含专属
服装）整体被跳过 → `服装细节` 回落到照秦璐写的字段级默认（米色针织开衫/深灰长裙/
肉色丝袜/**婚戒**/居家贤妻），妆容同理（素颜的大学生带着淡粉唇彩）。

- initvar 给秦璐/苏梦都显式补齐 服装细节+妆容细节（消除"字段默认恰好是某角色值"
  的脆弱性，同 v0.28 处理气质描述的思路）——**世界书改动，需手动同步 initvar**
- schema 苏梦 prefault 妆容补齐为自洽素颜（底妆素颜/无唇妆/无腮红）
- 脚本幂等旧档迁移（1.2）：完整签名判定（上装+下装+整体风格三键齐中，防误伤 AI
  后续换装）→ 命中整体换苏梦青春休闲初始装 + 素颜妆容

**v0.31 补4：苏文安抚道具叙事化 + 冻结冷却拉开真空期（用户选定）**

- 叙事化（2a，用户："静默改值像作弊，没有沉浸感"，原静默设计取消）：借口短信/
  贴心小礼物走 `queueItemEvent` 注入一次性事件——只给方向（短信由头/礼物内容从
  上下文生成），要求"轻描淡写带一笔，不喧宾夺主"。家宴/出游原本就有剧情；
  安神药/头孢酒保持静默（药效是植入辅助的隐形窗口，文中点破会改变剧情走向）
- 平衡（2b 方案①）：借口短信冷却 10→18（冻结10楼后 8 楼裸奔期，8币/楼永冻引擎
  作废）；周末全家出游冷却 15→20。降疑类（家宴/礼物）有下限棘轮兜底，不动

---

## v0.32 — 玩家名去硬编码 + 打断余波硬约束（本地 tag 0.32，未推送）

玩家报告两个问题：①开场白被硬编码成"苏斌"（作者自己的角色名，玩家各异）；
②打断余波没有任何实际束缚，余波期亲密照旧。

- **玩家名去硬编码**：
  - 开场白 4 处"苏斌"→`{{user}}`（世界书文本酒馆自己会替换宏）——**需手动同步开场白**
  - 脚本注入链路补宏替换：脚本 push 进 prompt 的消息不经过酒馆宏替换，`{{user}}`
    会原样透传给 AI。新增 `getUserName()`（substitudeMacros 优先，SillyTavern.name1
    兜底），快照最终字符串注入前统一 replace——越界应对行（曾硬编码"苏斌"）、
    家宴/出游/短信/礼物等 queueItemEvent 文本里的 `{{user}}` 一并治愈
  - 脚本内文本约定从此统一写 `{{user}}`，注入点集中替换
- **打断余波收紧为硬约束**：原文案"行为出口临时收敛一档"太软（AI 不当回事）。
  改为明确禁令：余波期间不论阶段/念头，禁止一切亲密/暧昧/越轨的行为与话题，
  全部收进眼神与心理暗流；她们主动拉开距离、掰回家常，玩家的越界试探被
  "不是现在"式的紧张按住（注明"不是拒绝他，是怕被看见"——不伤关系语义，
  与越界应对行的原则一致）。如何演绎仍由 AI 按上下文现挂，不给动作剧本

**v0.32 补：情绪字段撑破状态栏（玩家报告）**

AI 偶尔把 当前情绪 写成整句（"克制后的疲倦与不自在，训斥后又不自觉回到分析模式…"），
角色卡情绪胶囊被撑变形。

- UI 钳制（治标）：`.emotion`（角色卡）/`.sw-emo`（苏文卡）max-width + 单行省略号，
  完整文本挂 title 悬停可看。变量数据不截断（AI 写的内容不动）
- 规则约束（治本）：变量更新规则 三处 当前情绪 追加"2~6字的情绪短语，不要写成句子"
  ——**世界书改动，需手动同步变量更新规则**（与 v0.32 开场白一起同）

---

## v0.33 — 仪容感知强化 + 装扮信号事件 + 习惯刻印（本地 tag 0.33，未推送）

玩家测试反馈：①仪容框架对游戏非常鸡肋（数值全部隐形，满星"奖励隐形惩罚显性"）；
②习惯满5取舍变卖丧失成就感。方案讨论后用户定：仪容 A+B，习惯走道具固定（比内化
制强——固定是主动选择+花钱供奉）。

**A. 加速来源明细（纯 UI）**：培育中念头进度旁 +N/楼 标签（`thoughtRateParts` 与
tickThoughtProgress 同公式：保底1 + 加速房0.5[按作息游标判定，与写阶段一致] +
装备定向 + 满星1），悬停 title 显示分解 + "剧情相关另判 +1~2"

**B. 装扮信号事件（新机制，`settleOutfitAttention` 写阶段 4.42）**：穿风险装 +
苏文在家 + 她在场 → 概率触发"他注意到了"，从被动惩罚变主动赌注：

- 触发率 = 10% + 风险总和×5%（封顶30%，`getEquippedRiskSum`）；每角色 10 楼冷却
  （新字段 `_装扮注意上次楼层`）
- 按当前疑心分岔：<30 → 70%误读（-2，注入温情小事件）/30%起疑（+1）；
  ≥30 → 30%/70%（+2）。事件只给方向；涨疑封顶99（不直接引爆坏结局，触顶留给主通道）
- 跳过场景：坏结局/苏文不在家/打断余波（已在全神贯注盯着）/疑心冻结中
- 涨疑跨档由下一楼打断存量判据接手，不与注意事件同楼撞车
- 效果：低疑心期大胆穿=主动洗疑手段，高疑心期满身风险装=火上浇油

**C. 习惯刻印（道具固定）**：

- 网店特权新道具「刻印香炉」500/次**可复购**（buyPrivilege 特例分支，不占道具状态，
  累计 `系统._刻印名额`；ShopPanel 按钮显示"购买名额（现有×N）"）
- 角色页习惯旁 📌 刻印按钮（有名额即亮）：习惯移入 `刻印习性列表`（新字段）——
  不占5条上限、金色专属区块、不可逆不可变卖；每角色上限4条（防提示词膨胀）
- 快照影响块中刻印习性置顶："已刻进她的本能，表现权重高于普通习惯，几乎每轮都应
  留下痕迹"；计入 hasInfluence（只有刻印时不会误触发平常心基线）
- 刻印习性纳入保护快照整体回滚（AI 不得增删改）；刻印腾位与变卖共用
  `backfillMatureThoughts`（从 sellHabit 抽出）补转已成熟念头
- 数值：500 比变卖300 贵——固定一条习惯的真实代价是 800（放弃回收价+倒贴）

均为脚本/界面改动，无需同步世界书。

## v0.34 — 仪容明细图标网格（本地 tag 0.34，未推送）

玩家反馈：仪容区上装/下装单列排布，右侧大片空白，浪费且不美观。

- 原 `dl.attire` 单列改为 `repeat(auto-fill, minmax(118px, 1fr))` 自适应网格
  （窄屏 1 列 / 常规 2 列），每项变图标卡片：emoji 图标（👚👗👙🩲🧦💍⛓️💄👁️✨🌸）+
  小标签 + 值；长值单行省略号 + title 悬停看全文
- 数据抽成 `attireEntries` computed：五件常显（上装/下装/内衣/内裤/袜），
  可选项（配饰/装饰/唇妆/眼妆/妆饰/香氛）非"无"才入列——与原显隐逻辑一致

**v0.34 补：鞋子槽位 + 商店上货 4 双（用户指出常显 5 格双列不齐）**

- 常显第 6 格「👠 鞋」（读 服装细节.鞋子，字段本就存在），双列网格齐整
- 新装备槽位 '鞋子'（EquipSlot/ClothingPart/setClothingPart/restore 全链路）：
  **不参与仪容星标**（满星仍= 4槽+体改，星标语义不动）；卸下回落 '室内拖鞋'
  （阶段默认外观未细分鞋子，居家场景通用）
- 商店 +4（装备 45 / 全店 63）：软绒家居拖鞋（阶1·陪伴/依赖·80）→ 绒球细带凉拖
  （阶2·肢体·180）→ 红底细高跟（阶3·暧昧/开放·风险2·350）→ 锁扣绑带高跟
  （阶4·性行为/服从·风险3·600）。全部有 CLOTHING_WRITE 映射；ShopPanel
  SLOT_ORDER 增设鞋子分组

**v0.34 补3：装备互斥改按仪容字段重叠判定（用户报 丁字裤+丝袜 无法同穿）**

根因：互斥判据是商店分类「槽位」，但真实身体部位是 CLOTHING_WRITE 写入字段——
粒度对不上，同槽不同部位被误伤。全部误伤面：

- 内着：袜类（黑丝吊袜带/开裆连裤袜，写袜裤）×所有内衣件；只占内衣上的
  （乳贴/开衩情趣内衣）×只占内衣下的（丁字裤/珍珠链内裤）
- 外装：低胸针织衫/露脐短打（上装）×包臀热裤（下装）
- 饰品：配饰组（手链/戒指/项链/耳坠/锁骨链）×特殊装饰组（贴颈链/肚脐链/脚链/颈环）
- 妆容最重：唇妆/眼妆/特殊妆容/香氛四个部位全互斥，只能化一样
- 鞋子无误伤（全写同一字段）

修法：toggleEquip 互斥判据改 `clothingConflicts(a,b)` = 两件 CLOTHING_WRITE
字段集有交集（同一部位只能穿一件）；无映射兜底回同槽比较。槽位字段降级为
纯商店分组/星标归类。套装语义自然正确：蕾丝内衣（占上下两格）装备时会同时
顶掉乳贴+丁字裤；魅惑晚妆（眼+唇）顶掉口红+桃花眼妆。

副作用（有意接受）：同时在身装备上限从 5 件（每槽一件）升到 12 件
（每仪容字段一件），加速/风险总和上限随之提高——加速数值本就标记待平衡期
统一调；风险进 装扮信号 概率已有 30% 封顶。星标判定 `.some()` 语义不变。
同字段仍互斥是数据模型限制（每字段一条文本），如 耳坠+项链 同写配饰不能共存，
可接受。

---

## v0.35 — 玩法路线满星重做（隐藏机制）+ 路线补货 25 件（本地 tag 0.35，未推送）

用户设计：商店分类代表游戏路线，**同路线凑齐才能满星**（"有些玩法玩的是性奴化，
那就要装备仪容都是这类的装备才能满星"）。路线是隐藏机制——商店不展示，
让玩家自己研究为什么满不了星；满星后状态栏亮出路线名。

**四条路线**（10 个类型倾向的划分）：

- 💗 恋人（纯爱恋慕）：陪伴交流/情感依赖/肢体亲近/亲密接触
- 💍 爱妻（妻位取代）：身份认同/家庭替代
- 🔥 淫荡（露出开放）：暧昧互动/身体开放
- ⛓️ 性奴（调教服从）：性行为/绝对服从

**机制**（shopSystem `ITEM_ROUTE` 映射表 + `getOutfitStars` 重写 + `ROUTE_FULLSTAR` 配置）：

- 6 星制：内着/外装/饰品/妆容/**鞋子（收编）** 各有装备 + 体改
- 纯度制：装备中每一件必须同路线，穿混直接 0 星（隐藏研究点）；顺带把互斥修复
  （v0.34补3）带来的 12 件堆叠通胀重新约束住
- 体改例外：永久改造不算"穿混"（异路线体改不破功），但体改星只认本路线体改
- 满星效果差异化（替代旧无差别 培育+1/疑心+1）：本路线类型定向 培育+2/楼；
  疑心按路线分档——淫荡/性奴 +2、恋人 +1、**爱妻 +0**（在苏文眼里"越来越贤惠"，
  位置却正被取代，爱妻线专属爽点）。数值待平衡
- 快照注入【路线共鸣】行：按路线的整体气质方向文案（方向性不写死动作）+
  整套清单，独立于装扮意识行（恋人线整套风险可为 0）；私密认知条款保留
- 调试满星后门保持旧口径（route=null → 全类型+1/疑心+1）
- UI：星标 6 颗、n/6 计数；满星显示「图标+路线名+效果简述」；
  培育速率明细"满星共鸣+1"→"路线共鸣+2（仅本路线类型）"
- 商店 UI 零路线痕迹（ShopPanel 不改）

**路线补货 +25**（装备 45→66，体改 7→11，全店 88；全部有 CLOTHING_WRITE/首穿）：

- 恋人 +8：少女感内衣(2)/心形锁扣内衣(3)/成对情侣内裤(4)/约会级精心妆(4)/
  并肩散步小白鞋(3)/刻生日对表(5) + 体改 第一对耳洞(2)/腕内侧星痕纹身(3)
- 爱妻 +11：鹅黄围裙裙装(2)/冠姓刺绣内衣(3)/新婚风白蕾丝(4)/传家玉镯(3)/
  刻姓吊坠(4)/持家淡妆(2)/正室红妆(5)/女主人拖鞋(2)/细带玛丽珍(4) +
  体改 耳后姓氏微纹(4)/无名指戒痕纹身(5)
- 淫荡 +1：恨天高防水台(5)
- 性奴 +5：古典女仆装(3)/绑带束腰连衣裙(4)/拘束式立领长裙(5)/泪痣媚点(3)/
  锁链踝环厚底鞋(5)
- 存量 52 件全部打路线标（旗袍/真空围裙归爱妻、催情香水归性奴等按气质定）；
  校验 77/77 无缺漏。恋人线阶5 仅 1 件（对表），后续可补

**旧档影响**：判定变严——原混搭满星的玩家会掉到 0 星（需在更新说明讲清楚）；
数据无需迁移，无世界书改动。

---

## v0.36 — 云霜凝灵感补货 +12（全店凑整 100）（tag 0.36）

参考云霜凝商店的三个强项移植：暴露度阶梯的顶点款（灵纱/镂空系）、
穿戴器具（震动器/尾栓/大腿皮环）、特殊配饰的私密部位分布。
纯目录扩充，零引擎改动（现有槽位/字段/路线机制全兜住）。

- 💗 恋人 +4：他生辰石耳钉(饰3)/情侣浴袍(外4)/纪念日真丝睡裙(外5)/专属调香(妆5)
  ——补上恋人外装阶4-5 与妆容阶5 的空档，恋人线终局不再薄
- 💍 爱妻 +2：盘发玉簪(妆3，主母仪态)/新婚夜真丝亵衣(内5)
- 🔥 淫荡 +3：双层身体链(饰4)/透纱居家长袍(外5)/全透蕾丝套装(内5)
  ——"像穿了又像没穿"的暴露顶点（云霜凝 灵纱飘带/透明胸纱 思路）
- ⛓️ 性奴 +3：大腿束环(饰4)/贴身遥控珠(饰4·风3·越级)/绒尾饰栓(饰5)
  ——穿戴器具系："裙下的、目光不及的"（云霜凝 身体器具/大腿皮环 思路）

装备 66→78，全店 100。校验 89/89 路线+CLOTHING_WRITE 无缺漏。
未移植：云霜凝环境布置类（隔音/纱帘）与场景器具（口枷/缚灵缎）——前者需要
新效果逻辑（降风险钩子），后者是场景道具不是日常穿戴，都留待后续批次。

---

## v0.37 — 玩家反馈批次：打断重复触发修复 + 越级封顶 + 手动晋阶（tag 0.37）

玩家反馈五条，本版落三条；"常识改变玩法"（认知歪曲轴）用户定为下版立项；
"五阶段太快"先靠手动晋阶观察体感再决定动阈值。

**1. 修 BUG：疑心10·打断同档重复触发**

根因：档位标记/冷却是 MVU 楼级字段没错，但重roll时 MVU 从上一楼重建变量，
全靠 v0.28 的**模块内存**保护快照并集回来——页面刷新/切聊天/重启后快照归零，
此时重roll打断楼 → 标记+12楼冷却全丢 → 同档重发（模块变量幂等门控陷阱，
这次踩坑的是保护快照自己）。

修法：`syncInterruptMirror()`——打断状态（档位/冷却/余波/苏文视角+楼层戳）
镜像到 **chat 级变量** `秦璐重置版_打断镜像`，每次捕获快照时：
镜像楼层 ≤ 当前楼 → 并集回填内存快照（rollback 沿用既有并集/取大逻辑）；
镜像楼层 > 当前楼 = 玩家主动回档到打断前 → 不回填、直接覆盖（保留回档重演语义）。
苏文视角按"活跃优先"合并（待看或剩余楼>0 才算活跃，防复活已看完的 POV）。

**2. 越级加成封顶 +2（阶段闸门被玩家指"形同虚设"）**

此前 心防松动+1 / 裂纹+1 / 头孢酒+2 / 越级钥匙+1 可叠到 +5——阶1能收阶5念头。
getEffectiveStage 重构：越级钥匙并入函数内（原在两处调用点外挂），
加成总和 `min(2, Σ)` 封顶。UI 同步：未达标退回显示具体原因
（「类型」通常要到第N阶段+当前阶段，title 注明封顶规则），CATEGORY_STAGE 导出。

**3. 手动晋阶（替代自动跃迁，玩家提议）**

- 堕落度越过下一阶段门槛后**不再自动跳阶**：五处自动跳阶全拆
  （念头成熟/体改/观影/习惯腾位补转 → 只记待晋日志；每楼阶段校正 → 只向下钳制防超前）
- 状态栏阶段区亮金色呼吸按钮「晋阶 ▲」（坏结局不亮；旧楼只读全局已挡）→
  `promoteStage()`：一次一阶（跨两档点两次、各演一次转变），
  注入方向性晋阶事件（"她终于承认了已经发生的变化"，不写死动作）
- 滞留低阶自带代价：高阶念头被闸门退回、高阶商品买不了——不构成刷子策略
- 旧档零迁移：当前阶段==派生档的档不受影响；此后堕落度先涨、阶段等玩家点

无世界书改动。数值不动（阈值 20/40/60/80 保持，观察手动晋阶后的节奏体感）。

**v0.37 补：手动"重新处理变量"改为只恢复不推进（玩家实测漂移）**

此前 VARIABLE_UPDATE_ENDED 守卫在非 AI 生成周期直接跳过 → 手动重处理时 MVU 按
AI 指令从上一楼重建，苏文位置/疑心/货币等脚本管理字段全部漂移（玩家看到
"生成显示在家、重处理变外面，其他变量也变"——重处理后的值不是作息真值）。
改为：非生成周期若有内存快照 → `rollbackProtectedFields` 拉回真值写回（幂等，
可反复点），引擎不推进；刷新后无快照则维持原状。
另：玩家澄清"苏文位置对不上"是旧版本未更新（v0.29 已修的滞后），时段跳转词
扩充（清晨/傍晚/晚上等，现仅认"第二天"系）降级为可选优化，攒着后做。

**v0.37 补2：阶段"接受上限"硬约束（玩家反馈"阶2用各种方式就能上全垒"）**

漏洞：STAGE_RESTRAINTS 各阶只写她"主动"的禁区，没写她"接受"的上限——玩家
主动推进时文本上不算越界，prompt 闸门被磨穿。修法（纯脚本快照文案，无世界书）：

- 各阶追加"接受上限"（行为出口的物理边界，用户定档）：
  阶1 家人常态接触｜阶2 依偎拥抱 + 短暂的、可推说冲动的亲吻（深吻以上不发生）｜
  阶3 亲吻 + 隔衣抚触（衣下与性行为不发生）｜阶4 私下性行为解禁（苏文可感知
  范围内不发生）｜阶5 无
- 越界应对行升级【硬约束】：点名常见包装手段（酒意/游戏惩罚/按摩借口/装睡/
  "就一下"）一律不越上限，"身体先于意识停下"；反应仍从人设/感情/情境长出
  （方向性不写死动作，拒绝≠关系受损保留）
- 说明：prompt 层闸门在持续硬推下仍可能渗漏，疑心/打断是系统兜底——本次目标
  是把"阶2上全垒"抬高成"要玩到阶4"

---

## v0.38 — 毒快照防御：修"状态栏突然初始化"（tag 0.38）

玩家反馈：玩着玩着状态栏数值突然变回初始值，**只有习惯和念头没变**。

症状即诊断：变回初始的恰好是保护快照回滚的覆盖面（堕落度/阶段/依存/疑心/
货币/苏文位置——强制回滚的数值字段），习惯/念头列表回滚从不整体覆盖所以幸存。
根因：三处入口都是 `Schema.parse(_.get(vars,'stat_data') ?? {})`——某个时刻
读到的楼层变量 stat_data 缺失（酒馆/MVU 偶发竞态、消息被插件处理等），
`parse({})` 凭空造出全默认值数据 → 被捕获成保护快照（毒快照）→ 下一轮回滚
把初始数值盖回真实存档。机制自 v0.28 就存在（非 0.37 引入），0.37补的
"只恢复不推进"多了一条盖章路径，一并堵上。

修法：PROMPT_READY / VARIABLE_UPDATE_ENDED（两分支共用）/ MESSAGE_RECEIVED
全部加防御——stat_data 缺失或为空 → 跳过本轮捕获/结算/写回，保留旧快照，
console.warn 留痕。全仓已无 `?? {}` 兜底 parse。无世界书改动。

已受损存档的玩家自救：回档到数值还正常的楼层重新生成即可（数值字段会被
该楼的真实数据重新锚定）。

**v0.38 补：晋阶镜像（玩家反馈"手动晋阶过一两楼被打回"）**

判断：玩家在 0.37 上，症状与毒快照同源——快照被污染后回滚把 当前阶段 连同
其他数值一起盖回默认（该玩家刚手点晋阶所以对阶段最敏感）。0.38 主修复已堵根源；
本补丁给阶段再上一道保险带：当前阶段 是玩家手点的单调状态（与打断标记同类），
chat 级 `秦璐重置版_晋阶镜像` {楼层, 秦璐, 苏梦}——脚本每次捕获快照时按镜像
取大并回（镜像楼层 ≤ 当前楼才并；回档到镜像前则作废，保留回档重演）；
UI 晋阶成功时同步直写镜像，连"晋阶后下一次捕获恰好读坏"的窗口也关掉。
任何未知的旧值路径都无法再把阶段盖回去。

---

## v0.39 — 修"货币下轮回来 / 刻印下一楼消失"：MESSAGE_SENT 误伤根因（tag 0.39）

玩家反馈：①刻印留不住，下一楼就消失；②每轮花掉的货币下轮会回来（道具还在，钱退回）。

根因（读 MagVarUpdate 源码确证）：MVU 对**玩家刚发送的用户楼**（MESSAGE_SENT）
也会跑一遍完整变量处理，且 VARIABLE_UPDATE_ENDED **无条件触发**（哪怕消息里没有
任何指令）。此刻 `_isInAiCycle=false`，于是每次发消息都走进 v0.37补 加的
"非生成周期→按快照恢复"分支——而内存快照定格在上一轮 MESSAGE_RECEIVED，
玩家之后在 UI 里做的操作全晚于快照：

- 货币在恢复面 → 扣款被盖回旧值；装备状态/道具状态不在恢复面 → 道具保留、钱退回
- 刻印习性列表是整体回滚字段 → 被盖成空；_刻印名额不在恢复面 → 名额白扣
- 当前阶段在恢复面 → **这也是 v0.38补"晋阶被打回"的真正根因**（镜像是治标，保留作保险带）

被盖回的数据写进用户楼，作为下一楼生成基准顺着传下去——所以症状是"下一楼/下轮"。
v0.37补 之前该分支是直接 return，故为"最近几个 tag 引出"（玩家猜测正确）。

修法（只动 index.ts 的 VARIABLE_UPDATE_ENDED 处理器）：
1. 顶部加用户楼守卫：末楼 is_user = MESSAGE_SENT 路径 → 一律放行（用户楼变量
   本就是 MVU 刚从上一楼含全部 UI 写入拷贝的真值，无需恢复也不该推进引擎——
   顺带堵住"事件迟到进生成周期、引擎在用户楼多推一轮"的潜在竞态）
2. 真正的手动"重新处理变量"（末楼=AI楼）保留恢复，但恢复源优先改读该楼当前
   （重建前）的变量——含快照之后的 UI 写入，比内存快照新；读不到才退回内存快照

无世界书改动。受损存档无需自救：修复后正常游玩，下次购买/刻印即持久。

---

## v0.40 — 竞态窗口加固 + 晋阶镜像扩堕落度（tag 0.40，两项合并发布）

起因：玩家报告"0.39 上晋阶下一楼仍被打回"。先查出**版本错配**（玩家只改了
一处 @tag，脚本实际仍停 0.38；注意 0.38/0.39 前端零差异，致病的只可能是脚本
那条 import）；玩家随后补述真实复现路径＝"出售习惯攒够晋阶资格 → 重roll 后
被打回"，指向第二个真窗口（见本节后半）。两项修复合并为 0.40。

### 其一：关死 0.38 快照防御的竞态窗口

排查中确认该窗口真实存在（它就是 0.38 之前"毒快照/状态栏突然初始化"偶发
报告的源头），本版将其关死：

根因＝0.38"缺失即跳过、保留旧快照"防御的残余窗口：MVU 往刚发送的用户楼拷贝
变量是**异步**的，与提示词构建存在竞态——竞态轮 PROMPT_READY 读 -1 楼 stat_data
为空 → 跳过捕获，保留的旧快照定格在晋阶**之前**；本轮回滚把阶段连同堕落度一起
盖回（堕落度缩水还会再触发"阶段超前钳回"，0.38 晋阶镜像只在捕获时取大、
回滚路径不查镜像，挡不住）。同窗口也会顺带回退购买/刻印等一切 UI 后写，
且该轮快照注入整个丢失（AI 看不到状态）。

修法：新增 `读最近有效stat()`——从末楼往前回溯（上限 10 楼）取最近一楼含有效
stat_data 的数据；PROMPT_READY / 手动重处理真值读 / MESSAGE_RECEIVED 三处入口
全部由"读 -1 缺失即跳过"升级为"回退取楼"。竞态时上一楼（含全部 UI 写入）就是
真值：快照恒新鲜，注入不再丢轮；近 10 楼全空（全新对话）才跳过。毒快照语义
不变（仍绝不 parse({})）。

### 其二：晋阶镜像扩堕落度——修"出售习惯攒够晋阶资格，重roll 后被打回"

玩家实测：出售习惯后堕落度达到可晋"疯狂"，**重roll 这条消息**后晋阶资格
消失、退回"沉溺"。

根因＝重roll 场景下 UI 写入的固有盲区（与其一的发送竞态同族不同窗）：
出售习惯 → 腾位补转把已成熟念头转入习惯，**UI 侧即时 堕落度+6/8**（体改的
堕落度加成、依存度变化同理）。重roll 时 MVU 按设计从上一楼重建变量（swipe 间
互不污染），内存快照又定格在出售**之前**（上一次 MESSAGE_RECEIVED）——这次抬升
两头都没有；即使 0.38 晋阶镜像保住了 当前阶段，"阶段超前钳回"也会按缩水的
堕落度把它拉回去。

修法（延续 0.38补 镜像范式）：晋阶镜像扩展为**阶段+堕落度双单调镜像**
（chat 级 `秦璐重置版_晋阶镜像` 加 秦璐堕落/苏梦堕落 字段，旧档缺字段 ?? 兜底）——
- shopSystem 新增共享助手 `直写晋阶镜像()`（键 PROMOTE_MIRROR_KEY 同处导出，
  脚本/UI 统一引用）；UI 四个抬升点写镜像：手动晋阶（原内联逻辑收编）、
  变卖习惯、刻印腾位、体改改造
- 脚本每次捕获快照时按镜像对 阶段+堕落度 取大并回（回档到镜像前作废语义不变）

边界（接受）：重roll 后被卖习惯随上一楼基准回到列表、依存度变化不复原——
堕落度/阶段由镜像保住（晋阶资格不丢，这是玩家真正感知的东西）；理论上可
"卖→重roll→再卖"重复刷堕落度，单机卡且有调试后门，不做反作弊。
**本版改了状态栏 UI——玩家须把正则和脚本两处 @tag 一起更到 0.40**。无世界书改动。

---

## 无关的辅助工作

以下不属于秦璐重置版代码本身，但过程中有做：

### Pioneer 路由模型代理
- 目的：让 Kilo Code / 酒馆等通过 `pioneer/anthropic/pioneer-auto` 时固定路由到 `claude-opus-4-7`
- 创建：`pioneer-proxy.mjs`（本地 Node HTTP → HTTPS 代理，注入 `models: [...]` 到请求 body）
- 修改 `~/.config/kilo/kilo.jsonc` 的 `baseURL` 指向本地代理
- 端口切换：初始 8787 → 与 gproxy 冲突 → 改 8788

### gproxy 升级到 v2.0.8
- 数据库迁移记录冲突：删除 `m20260420_000001_strip_realtime_routing_rules` 缺失记录（旧库已备份）
- v1 → v2 迁移脚本报缺列 → 全新初始化数据库
- v2.0.8 移除 `--admin-api-key` 参数、密码要求 ≥12 字符 → 更新 `start.bat`
- 编译 v2.0.8：console 前端 `pnpm build` → `sync-to-embed.mjs` → `cargo build --release`

---

## 已知待办 / 待恢复项

- [x] ~~念头培育速度不平衡~~ **v0.22 已定稿**：基数 简单5/困难10 + 满配下限 3/4 楼
      （见 v0.22 节"培育速度平衡"）。
- [ ] **v0.14 越级闸门**：测试完毕后恢复 `resolveThoughtType` 中的越级判定，让越级念头转"未达标"
- [ ] 世界书条目手动同步到酒馆（`tavern_sync.yaml` 里没配秦璐重置版，只能手动更新）
- [ ] 越级惩戒打断、借口机制、商店道具系统整块尚未实现（设计文档已定 / 部分待定）
