# rq0.90.4 后本地 CG 淘汰与产品化审计

记录日期：2026-09-04。

## 一、审计边界

本审计只覆盖 `rq0.90.4` 正式发布之后在本地形成或接入的新功能 CG。判断顺序为：

1. 当前运行时生产者与消费者；
2. 当前状态机及旧档迁移入口；
3. 权威 manifest、任务状态、审核记录与 SHA；
4. `rq0.90.4` 标签和之后的 Git 历史；
5. 文件名、目录名与修改时间只作辅助，不单独决定淘汰。

未经本清单分类并取得对应用户确认的图片，禁止转换、删除、覆盖或改动接线。`rejected`、`superseded`、旧任务图和制作中间层继续按证据保留，不得因为不进入产品就删除原件。

## 二、状态定义

- **当前保留**：权威清单选中，且存在当前运行时消费者。
- **待产品化**：权威清单已选中，但尚未生成正式产品副本或发布闭包。
- **待用户确认**：代理审核已完成，但 manifest 仍明确要求用户终审。
- **公开旧档兼容**：新流程已取代，但 `rq0.90.4` 公开存档仍可能进入当前兼容路径。
- **已被取代但保留证据**：不再进入当前产品，仍须保留来源、审核、SHA或失败证据。
- **明确淘汰／拒绝**：manifest、审核或后继选择已明确排除；不参与转码和产品安装。

## 三、当前保留与产品化结果

当前权威清单共选中 **183 张运行画面**，现已全部采用WebP产品格式：

- 母亲视频通话：54张。`product-v1/source-map.json`、产品 manifest、54个运行ID和54张产品WebP四向闭合；产品约15.40MB。53张当前源图全部来自 `candidates-v2`，另复用1张preview正式源。
- 沈静仪《第二机位》：9张正式WebP，约1.43MB。
- 母亲《回国》：9张正式WebP，约1.89MB。
- 母亲《双重继承》：11张正式WebP，约2.80MB。
- 302结局后九图包：4张亲密开场加5张共居背景，共9张正式WebP，约1.88MB；权威 manifest 状态为待外部素材仓发布。
- 许曼君《分居》：12张正式WebP，约2.94MB。
- 许曼君《离婚》：15张正式WebP，约2.21MB。
- 周小满《不再留门》：8张产品WebP，合计2,000,490字节（约1.91MiB）；8个ID均有当前运行时消费者，产品manifest、逐文件SHA与源码路由已经闭合。相较17,306,046字节PNG源图节省88.441%，源图及2张拒绝图继续保留在制作审核区。
- 安若妍《不必停》：18张产品WebP，合计3,591,384字节（约3.43MiB）；18个ID全部进入当前系统白名单和剧情映射，产品manifest、逐文件SHA与源码路由已经闭合。相较35,384,050字节PNG源图节省89.851%，源图、被取代候选与技术审核证据继续保留在制作审核区。
- 102／202共享《录像带》V4：38张产品WebP，合计6,975,484字节（约6.65MiB）；102／202各19幕，候选源、产品、镜头卡与双层SHA已经闭合。相较66,892,443字节PNG源图节省89.572%，38张源候选与逐图provenance继续保留在制作审核区。

原有119张WebP产品未重复转码。用户于2026-09-04确认保留《不再留门》8张、《不必停》18张和录像带V4 38张后，新增64张统一以Pillow 12.3.0、WebP quality 93、method 6、exact alpha、不缩放完成确定性产品化。转换只生成产品副本，没有覆盖审核原图；每组的精确体积与质量证据分别写入自身产品manifest。

## 四、明确淘汰、拒绝或非产品

以下文件不参与任何产品格式转换：

- 《回国》拒绝图5张：`output/imagegen/return-home/rejected/*.png`。
- 《分居》A6钥匙柜第一版1张：`output/imagegen/xumj-separation/candidates/分居_A6_钥匙柜前最终会面_r1.png`；`generations.json`明确登记当前采用revision 2，并把该文件列为`originalCandidate`。
- 《离婚》顶层拒绝图10张，加局部分镜拒绝图1张；以 `xumanjun-divorce-ending/manifest-v2.json` 的两组 `rejectedAttempts` 为准。
- 《离婚》非生产食物参考实验1张：manifest明确登记于`nonProductionExperiments`，不进入运行时。
- 《不再留门》拒绝图2张：`ZXM-NMD-06-draw1.png`与`ZXM-NMD-07-draw1.png`；当前产品分别采用draw2。
- 《不必停》被取代候选1张：`ARY-NBS-09-N` draw1；当前manifest明确登记为`superseded_candidate`。
- 《不必停》本地材质冒烟图1张：位于`.codex-tmp/301-local-model-material-test`，manifest登记为`rejected_smoke_only`。
- 录像带V4被取代的B08旧输出2张：`VTR-V4-102-B08.png`与`VTR-V4-202-B08.png`；当前v2清单只选用相应`-draw2`文件。
- 录像带显式拒绝路径8张：根`rejected`目录6张、`local/rejected`1张、V4 tablet-exterior rejected子目录1张。

以上合计32张当前仍可定位的显式拒绝、取代或非产品图片。它们是“不得产品化”，不是“允许删除”；成人CG拒绝原件继续服从保留规则。

## 五、录像带版本链

旧产品树合计是48个文件路径，但只有38个唯一SHA：v1十个文件全部与v2中的十个文件逐字节重复。下文“十张／三十八张”分别按各版本路径计数，不应相加理解为48张不同画面。

### 5.1 v1十张试播图

`src/人妻公寓/素材/丈夫结局/录像带/SCREEN-*.png`共10张、约16.72MB：

- 十张全部与v2中的对应帧字节完全相同；
- 未进入`rq0.90.4`正式标签；
- 当前仓库没有内建v1事件生产者，只剩版本缺失载荷的解析兼容和公开导出帮助函数；
- 当前任务与测试已经把它定义为五格技术试播，而不是完整故事。

结论：用户已于2026-09-04审阅去重后的旧图并明确确认淘汰。录像带独立任务提交`b24dd8f6`完成清理，合入主工作树后的提交为`4cfec9be`；它删除这10个重复产品路径，并同步移除版本缺失视觉载荷消费者、公开帮助函数和旧视觉测试。历史内容仍可从Git与`output/imagegen/video-tape-ending/**`审核链追溯。

### 5.2 v2三十八张产品图

`src/人妻公寓/素材/丈夫结局/录像带/v2/**/*.png`共38张、约65.70MB：

- 已被V4取代，不代表最终游戏效果；
- 但`rq0.90.4`正式代码已经能够保存`录像带:102`、`录像带:202`前置与旧录像带进行中状态；
- 旧档兼容分支仍保留场次标识、双户硬状态、失败／超时／取消后的复锁核验、幂等键和完成结算；
- 用户确认退场后，兼容分支不再返回v2视觉载荷，也不再要求旧产品副本存在。

结论：38张v2旧产品图已明确淘汰，不做WebP转换。主工作树提交`4cfec9be`删除全部38个v2产品路径和旧事件／队列／URL接线，同时保留无图的旧档状态兼容；专项、全量、静态检查与隔离生产构建均已通过。

### 5.3 v3制作中间图

`tablet-exterior-v3`与`outer-v3`合计62张PNG、约108.43MB。两套manifest均未解锁生产，当前V4是后继实现；这些文件不属于运行时产品。部分文件仍参与V4来源与审计链，因此只归“已被后继方案取代但保留证据”，不转码、不删除。

### 5.4 V4正式产品

V4的v2源清单精确选中38张候选，用户已逐项审阅并确认全部保留。产品化后状态为：

- `user-confirmed-product-webp-ready-awaiting-external-publish`；
- `formalAccepted=true`；
- `installed=true`；
- `productionUnlocked=false`。

38张源PNG共66,892,443字节，Q93 WebP产品共6,975,484字节，节省59,916,959字节（89.572%）。产品目录只含38张稳定ID WebP和一份产品manifest；运行时改用`__RQGY_VTR_V4_ASSET_BASE__`，外部不可变标签为空时保持监控占位。结论：用户终审和本地产品安装完成，外部素材发布仍锁定。

### 5.5 此次用户确认的精确边界

第一次审阅页展示的是v2旧产品20张平板画面与18张外层画面，共38张唯一旧图；用户确认这些旧产品路径及旧视觉接线退场。V4终审页随后展示V4的38张候选；机器比对指出其中`102／202-B01`与`102／202-B17`共4张是V2画面的逐字节复用。用户明确说明这4张效果合格、正因如此一直由V4引用，不需要重做，并最终确认V4全部38张保留。因此边界是：旧版本产品树与运行协议淘汰，V4中有意继承的4个独立副本合法保留并进入WebP产品。

## 六、302背景的两版候选

权威九图包 manifest 选中的五张背景SHA全部对应 `output/imagegen/302-daily-backgrounds` 第一组及其WebP副本；不对应 `302-daily-backgrounds-v2`。

`302-daily-backgrounds-v2`包含5个不同画面、PNG和WebP共10个文件，约13.61MB，且没有当前运行时产品清单消费者。

结论：v2五个画面属于明确未采用候选，不参与转码；当前按制作证据保留整个候选目录。以后若单独建立外部长期归档，可以迁出，但该存储位置选择不阻塞本轮产品闭包。

## 七、母亲视频通话旧任务图

当前产品来源是`candidates-v2`现存53张全量加preview复用1张。权威`CURRENT_TASK.md`明确记录：

- 用户最终保留的53张即当前`candidates-v2`全部文件；
- 用户已删除23张不合格候选，不恢复；
- `approved/母亲`不再作为最终版来源，仅保留旧30任务生成与审核历史。

当前`approved/母亲`物理存在43张PNG，约153.14MB。结论：这43张属于旧版审核历史，不参与产品转换；当前继续作为证据保留，以后是否迁到外部长期归档属于独立存储治理任务，不阻塞本轮产品闭包。

## 八、验证证据

分类确认前只读执行下列八组素材专项：

- `302结局后CG资产包.test.mjs`
- `第二机位.test.mjs`
- `不再留门事务与资源.test.mjs`
- `安若妍不必停资源.test.mjs`
- `录像带完整叙事分镜.test.mjs`
- `母亲线CG资产生产链.test.mjs`
- `许曼君201离婚CG产品化.test.mjs`
- `许曼君分居资源.test.mjs`

实测68／68通过。该结果证明当时的选中源、产品文件、SHA和既有兼容契约保持闭合，不等于授权删除或转换待确认图片。

录像带旧产品经用户确认后另在隔离任务分支完成退场，原任务验证为：录像带／特殊场景专项87／87、《人妻公寓》全量2313／2313、定向Vue／TypeScript、ESLint与diff检查通过；逻辑端和客户端隔离生产构建通过，正式`dist/**`前后72／72文件精确不变。原任务提交为`b24dd8f6`，主工作树合入提交为`4cfec9be`。

《不再留门》产品化由主工作树提交`c0216416`落地，专项48／48、类型与静态检查、隔离生产构建及`dist/**`72／72精确不变核对通过。《不必停》完整功能和18张WebP产品由提交`11d19f88`落地，专项11／11、相关契约85／85、类型与静态检查及隔离生产构建通过。

三项提交组合后的主工作树再次完成最终验收：`tests/人妻公寓`全量2324／2324通过；定向Vue／TypeScript通过；ESLint零错误（仅保留两份测试中共28条既有默认导入警告）；`git diff --check 5dd46d84..HEAD`通过；逻辑端与客户端生产模式隔离构建通过，正式`dist/**`构建前后均为72个文件且逐字节一致。

录像带V4产品化在独立任务worktree中完成：V4专项48／48、《人妻公寓》全量2324／2324、定向Vue／TypeScript、ESLint与diff检查通过；逻辑端和客户端生产模式隔离构建通过，正式`dist/**`前后72／72精确不变。产品质量与接线细节见[《录像带V4：38张WebP产品化与验收》](./录像带V4_38张WebP产品化与验收_2026-09-04.md)。

分类确认阶段另以`.codex-tmp/verify-cg-retirement-audit.py`执行机器复核，输出
`.codex-tmp/cg-retirement-audit-verification.json`；当时结果为`verdict=pass`、
`conversionFrozen=true`，用于证明用户确认前没有提前转码。该阶段门随后被三组用户确认和正式产品manifest取代，不再代表当前产品状态；临时校验器与结果不进入产品提交。

## 九、可选的长期存储治理

当前已经没有待分类或待确认的运行时产品图片。下列两组只剩可选的物理存储位置调整：

1. `302-daily-backgrounds-v2`五个未采用画面当前留作候选证据，以后可以迁入外部长期归档；
2. 母亲视频通话`approved/母亲`43张旧任务图当前留作审核历史，以后可以迁入外部长期归档。

两组均无当前运行时消费者，不参与转换或发布；继续保留不会形成第二套产品接线。录像带旧产品退场、录像带V4、《不再留门》与《不必停》WebP产品化均已形成边界清楚的本地任务提交。全程只做隔离构建，没有构建或改写正式`dist`，也没有组卡、tag、push或发布。
