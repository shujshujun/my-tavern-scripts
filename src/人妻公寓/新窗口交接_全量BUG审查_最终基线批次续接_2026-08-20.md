# 《人妻公寓》全量 BUG 审查——最终基线闭合批次续接

> 日期：2026-08-20  
> 唯一允许功能范围：`P001、P002、P004～P010`  
> 唯一未闭合产品文件：`F079 src/人妻公寓/脚本/游戏逻辑/依赖版本.ts`  
> 本交接由第 9 批完成窗口创建；创建时第 9 批 P236～P244 与全部批次 9 产品文件已经闭合，最终基线批次尚未开始源码审查。

---

## 一、强制任务边界

下一窗口只允许完成最终基线闭合批次：

```text
P001 客户端启动与挂载
P002 首次准备设置
P004 新局默认数据
P005 当前 v9 存档载入
P006 启动错误与降级反馈
P007 状态水合与界面刷新
P008 当前聊天与分支识别
P009 全局反馈提示
P010 版本与发布标识显示

F079 src/人妻公寓/脚本/游戏逻辑/依赖版本.ts
```

`P003 序章与标题屏` 已完成，只允许作为新局／启动调用链的必要反证，不得重新计数或从头审查。

必须在本窗口完成：

1. F079 逐导出、逐私有函数、逐网络／超时／解析分支与逐消费者审查；
2. P001、P002、P004～P010 的真实入口、状态转换、失败／取消／超时／切聊天／重复挂载／旧档边界全部闭合；
3. 真实 BUG 先补失败反例，再做最小修复；
4. 更新唯一台账；
5. 产品文件达到 `192／192`；
6. 功能达到 `244／244`；
7. 全部强制门禁与全量测试通过；
8. 在聊天中给出最终全量审查统计与仍存在的外部环境提示（如有）。

只有同时满足：

```text
产品文件 192／192 闭合
功能 244／244 闭合
定位中 0
真实 BUG 待修 0
设计待拍板 0
实机待验 0
最终全量测试全绿
```

才允许宣告本轮全量 BUG 审查完成。

不得因为只剩一个未审产品文件而跳过九个基线功能的集成生命周期；也不得把已经闭合的前九批重新全部审一遍。已闭合文件只允许按当前九个功能做窄调用链反证，发现真实根因时才可最小修改。

---

## 二、连接与强制读取顺序

使用 DevSpace2 打开：

```text
D:\SillyTavern\tavern_helper_template-consolidated
```

连接后第一件事运行：

```bash
git status --short
```

随后确认：

```bash
git branch --show-current
git rev-parse HEAD
git diff --cached --name-status
```

再依次完整读取：

1. `AGENTS.md`
2. `.cursor/rules/破限.mdc`
3. `src/人妻公寓/新窗口入口_精简.md`
4. `docs/BUG语义排查协议.md`
5. `src/人妻公寓/新窗口交接_全量BUG审查_最终基线批次续接_2026-08-20.md`
6. `src/人妻公寓/全量BUG审查台账_2026-08-19.md`

随后完整读取唯一未闭合产品文件：

```text
src/人妻公寓/脚本/游戏逻辑/依赖版本.ts
```

再按本交接第七节的功能矩阵窄读生产者、消费者和既有测试。不得另建台账，不得从 Schema、第 1 批、手机、关系、亲密、孕产、客户端或世界书整批重头开始。

---

## 三、Git 与保护现场

创建本交接时确认：

```text
分支：release/rq083
HEAD：2973cbf45375e6b99278e7356d72db267e7a33dc
短 SHA：2973cbf4
暂存区：0
发布基线：rq0.83
数据版本：9
```

工作树非常脏，包含用户原有改动、第 1～9 批修复、大量测试、素材与交接文件。当前脏工作树是唯一真实现场。

明确禁止：

```text
git reset
git checkout
git restore
git stash
git clean
删除或清理 untracked
覆盖现有源码、测试或素材
覆盖或回退 dist
整文件无脑 prettier --write
正式构建 dist
组卡
commit
tag
push
发布
```

只允许在现有差异上做最小增量。任何看似“旧改动很多”的文件都可能同时含用户改动和前九批已验证修复，不得用 HEAD 覆盖当前版本。

---

## 四、当前唯一权威进度

唯一台账：

```text
src/人妻公寓/全量BUG审查台账_2026-08-19.md
```

### 产品文件

```text
总数：192
已完成：109
已审无 BUG：82
闭合：191／192
定位中：0
真实 BUG 待修：0
未开始：1（仅 F079）
```

### 功能

```text
总数：244
已完成：161
已审无 BUG：74
闭合：235／244
定位中：0
未开始：9（P001、P002、P004～P010）
```

### 缺陷分类

```text
真实 BUG：69
P0／P1／P2／P3：0／43／22／4
测试过时：8
设计待拍板：0
新增测试：105
实机待验：0
```

下一编号：

```text
下一真实 BUG：BUG-070
下一过时测试：TEST-STALE-009
```

最新验证真值：

```text
测试文件：183
第 9 批专项：8／8
第 9 批去重关联矩阵：113／113
全量 tests/人妻公寓/*.test.mjs：1624／1624
fail：0
cancelled：0
skipped：0
todo：0
总耗时：约 5.98 秒
产品 TypeScript：通过
Vue TypeScript：通过
受影响测试 ESLint：0 error
第 9 批改动 YAML：12／12 可解析
git diff --check：通过，仅既有 CRLF 提示
```

最终批次产生任何源码或测试修改后，必须重新运行并记录新的真实数字，不得沿用 1624／1624 作为最终结论。

---

## 五、第 9 批完成边界——不得回退

第 9 批 P236～P244 已全部闭合：

```text
已完成：P238、P240、P241、P244
已审无 BUG：P236、P237、P239、P242、P243
```

批次 9 的 19 个登记产品文件全部闭合；F022 沿既有数据库结论只做静态反证，本窗口新增闭合 18 个产品文件。

第 9 批新增真实 BUG：

```text
BUG-067（P1）变量输出世界书仍公开旧 <Analysis> 协议，并把任意 JSON 值预先字符串化。
BUG-068（P3）未索引的陆嘉明登记档案与 101 当前夫妻事实整套冲突。
BUG-069（P2）叙事世界书把脚本、系统注入、动态快照和状态数据直接暴露给正文模型。
```

新增过时测试：

```text
TEST-STALE-008 `快照着装` 把“动态快照”实现字样当成角色世界书必须保留的公开契约。
```

必须保留的第 9 批核心结论：

1. 世界书索引只装载登记文件；变量条目同深度按 100→99→98 稳定展开，普通丈夫不双重注入。
2. 变量公开协议唯一为 `<UpdateVariable>` 内含 `<JSONPatch>` 的 RFC 6902 replace 数组；值占位符代表完整 JSON 字面量，不得再恢复 `<Analysis>` 或预先加引号。
3. 五对夫妻与 302 父母的门牌、年龄、婚龄、职业和作息已与 `stageConfig`／周作息统一；陆嘉明真值为 30 岁机械厂技术员、夏乔 28 岁。
4. 叙事世界书只表述当前场景／剧情事实和隐藏真相门槛，不得恢复“脚本注入、系统节点、动态快照、状态数据”等实现术语。
5. 成人 CG 清单 1448 项与 `qgy-assets@cg3` 发布树字节级一致，缺失 0、孤儿 0、重复 ID／path 0。
6. 静音会议五户完整覆盖 10 个双人＋10 个三人 CLEAN 组合，本地和 `rq0.70` 发布标签均缺失 0、孤儿 0；六户周作息均为 42 时段。

第 9 批直接修改／新增的现场必须保留：

```text
src/人妻公寓/世界书/变量/变量输出格式.yaml
src/人妻公寓/世界书/系统设定/世界观.yaml
src/人妻公寓/世界书/角色/何俊生.yaml
src/人妻公寓/世界书/角色/周小满.yaml
src/人妻公寓/世界书/角色/夏乔.yaml
src/人妻公寓/世界书/角色/安若妍.yaml
src/人妻公寓/世界书/角色/江辰.yaml
src/人妻公寓/世界书/角色/沈静仪.yaml
src/人妻公寓/世界书/角色/许曼君.yaml
src/人妻公寓/世界书/角色/赵国强.yaml
src/人妻公寓/世界书/角色/陆嘉明.yaml
src/人妻公寓/世界书/角色/顾国栋.yaml
tests/人妻公寓/第9批世界书静态配置.test.mjs
tests/人妻公寓/快照着装.test.mjs
src/人妻公寓/全量BUG审查台账_2026-08-19.md
```

---

## 六、唯一未闭合产品文件：F079

```text
F079
src/人妻公寓/脚本/游戏逻辑/依赖版本.ts
```

必须完整展开：

1. 全部 export、私有 helper、常量、远端地址、当前版本和模块级状态；
2. 游戏版本、酒馆助手／依赖版本、数据库版本各自的数据来源和消费者；
3. 稳定版筛选、前缀识别、预发布过滤、重复值、非数字段、缺失字段和畸形 payload；
4. 版本比较必须按数字段而不是字典序，重点覆盖 `0.9`、`0.10`、`0.83`、`0.84-beta`、带 `rq／v／xdy` 等前缀的候选；
5. 当前版本、最新稳定版、无更新、存在更新、未来／测试版混入时的真实语义；
6. fetch 的 `cache: 'no-store'`、非 2xx、网络异常、JSON 解析失败、超时／AbortController、迟到完成和重复检测；
7. 外部 manifest、宿主脚本树与数据库版本候选的强／弱身份边界；
8. F079 与 `首次准备.vue`、数据库版本模块、发布版本提示和测试之间的全部调用链；
9. 版本诊断失败是否只是可恢复提示，哪些依赖缺失会阻止首次完成，必须以真实消费者和产品设计为准，禁止凭函数名猜测；
10. 网络环境失败、远端临时不可达与本地产品 BUG 必须分类，不得用实时公网不稳定替代确定性行为测试。

高风险反例至少包括：

```text
版本数组为空／缺失／非数组
候选包含 null、对象、空白、重复项
0.9 与 0.10 数字段比较
稳定版与 beta／rc／dev 混合
错误前缀和相似脚本名
停用文件夹中的旧数据库脚本
HTTP 404／500
fetch reject
response.json reject
请求超时后底层迟到 resolve
连续两轮检测时旧轮迟到
当前已经是最新版本
远端只含比当前更旧版本
```

现有直接测试入口：

```text
tests/人妻公寓/依赖版本检测.test.mjs
tests/人妻公寓/App拆分A3.test.mjs
tests/人妻公寓/v083发布版本契约.test.mjs
```

不得只满足源码字符串断言；需要补足能执行实际导出与失败生命周期的行为反例。

---

## 七、最终九个功能的闭合矩阵

### P001 客户端启动与挂载

设计目的：打开角色卡／iframe 后，等待必要依赖，超时可控降级，且同一页面只保留一个有效 Vue App、监听集合和启动任务。

窄读重点：

```text
src/人妻公寓/界面/客户端/index.ts
src/人妻公寓/界面/客户端/启动等待.ts
src/人妻公寓/界面/客户端/App.vue
src/人妻公寓/界面/客户端/viewport.ts
src/人妻公寓/脚本/游戏逻辑/index.ts
```

必须反证：首次挂载、重复执行、HMR、卸载、依赖迟到、等待超时、旧启动 Promise 迟到、致命异常、父页／iframe 跨域失败和 CHAT_CHANGED 后仍只有当前实例。

F060 的 BUG-059 和第 8 批单实例结论已闭合，不得退回旧直接挂载或叠监听实现。

### P002 首次准备设置

设计目的：首次准备页把依赖诊断、数据库路线、解析配置和完成状态作为一个可恢复流程；只有必要配置真正成功后才允许进入游戏。

窄读重点：

```text
src/人妻公寓/界面/客户端/components/首次准备.vue
src/人妻公寓/界面/客户端/components/设置弹窗.vue
src/人妻公寓/MVU解析模式.ts
src/人妻公寓/脚本/游戏逻辑/数据库脚本写入能力.ts
src/人妻公寓/脚本/游戏逻辑/数据库版本.ts
src/人妻公寓/脚本/游戏逻辑/依赖版本.ts
```

必须反证：首次／已完成分流、检测重试、同轮旧请求迟到、数据库唯一支持路线、外置变量模型说明、设置原子持久化、失败不写完成键、页面重开和无依赖／无 SQLite 时的明确恢复说明。

现有直接测试入口：

```text
tests/人妻公寓/首次准备数据库单路线.test.mjs
tests/人妻公寓/依赖版本检测.test.mjs
tests/人妻公寓/App拆分A3.test.mjs
```

### P004 新局默认数据

设计目的：创建新聊天时写入与当前 Schema 完全一致的数据版本 9 默认对象，空动态户表、系统、资源和内部事务状态不分叉。

窄读重点：

```text
src/人妻公寓/schema.ts
src/人妻公寓/schema.json
src/人妻公寓/世界书/变量/initvar.yaml
src/人妻公寓/脚本/游戏逻辑/index.ts
src/人妻公寓/脚本/游戏逻辑/mvuIO.ts
P003 固定序章成功提交链（只做反证）
```

必须反证：`Schema.parse({})`、发布 `schema.json` 和 `initvar.yaml` 三方深一致；新局失败／取消不留下半档；序章成功才消费一次开场状态；数据版本必须为 9。F002／F003／F006 已闭合，只有集成断点才可最小修改。

### P005 当前 v9 存档载入

设计目的：当前 v0.83／数据版本 9 的完整存档经结构与版本验证后原样继续，不默认化真实进度，也不误走旧版迁移。

窄读重点：

```text
src/人妻公寓/schema.ts
src/人妻公寓/脚本/游戏逻辑/mvuIO.ts
src/人妻公寓/脚本/游戏逻辑/index.ts
src/人妻公寓/界面/客户端/store.ts
```

必须反证：完整 v9、损坏尾楼回退、未来版本硬拒绝、非空损坏快照失败关闭、空新局与损坏旧档不混淆、载入不原地改写输入、v7／v8 才进入迁移。不得把 P015／P016 已闭合的迁移整批重做。

现有直接测试入口至少包括：

```text
tests/人妻公寓/v080存档策略.test.mjs
现有 Schema／版本迁移／变量契约测试
```

### P006 启动错误与降级反馈

设计目的：初始化、依赖、版本、MVU、数据库或挂载异常不污染存档，并向玩家给出可恢复、与真实严重度一致的提示。

窄读重点：

```text
src/人妻公寓/界面/客户端/index.ts
src/人妻公寓/界面/客户端/启动等待.ts
src/人妻公寓/界面/客户端/components/首次准备.vue
src/人妻公寓/界面/客户端/components/反馈提示.vue
src/人妻公寓/界面/客户端/App.vue
src/人妻公寓/脚本/游戏逻辑/依赖版本.ts
```

必须区分：致命无法挂载、可降级版本诊断、数据库不可写但可进入主步骤、可选通知／媒体失败、用户取消和旧请求失效。提示不得冒充已经提交或回滚硬状态。

### P007 状态水合与界面刷新

设计目的：宿主 MVU／chat 变量变化后，单一持久真值同步到 Pinia／App 派生状态；不保留第二套业务状态，也不让旧订阅覆盖新聊天。

窄读重点：

```text
src/人妻公寓/界面/客户端/store.ts
src/人妻公寓/界面/客户端/App.vue
src/人妻公寓/界面/客户端/场景状态同步.ts
src/人妻公寓/脚本/游戏逻辑/mvuIO.ts
```

必须反证：首次水合、变量变化、聊天切换、同聊天分支切换、空／损坏 stat、刷新失败、卸载后事件、重复监听和 A→B→A；地图、档案、手机与正文读取同一当前对象。

### P008 当前聊天与分支识别

设计目的：所有异步任务冻结当前聊天、楼、分支／swipe 和共享世代；切换后旧结果静默失效，A→B→A 也不得重新认领。

窄读重点：

```text
src/人妻公寓/脚本/游戏逻辑/时间线切换协调.ts
src/人妻公寓/脚本/游戏逻辑/index.ts
src/人妻公寓/界面/客户端/App.vue
src/人妻公寓/界面/客户端/回合恢复缓存.ts
现有共享世代／客户端事件总线测试
```

该功能是前九批时间线修复的最终横向验收，不得发明第二个世代体系。必须覆盖切聊天、swipe、删楼、加楼、同值恢复、旧回调迟到和宿主事件顺序。

### P009 全局反馈提示

设计目的：成功、失败、取消、拒绝、超时与已提交后呈现失败都使用有生命周期的玩家反馈；提示不写硬状态、不把可选失败说成业务失败，也不会永久遮挡或吞掉重要拾获卡。

窄读重点：

```text
src/人妻公寓/界面/客户端/components/反馈提示.vue
src/人妻公寓/界面/客户端/App.vue
所有全局 toast／拾获卡生产者的统一入口
```

必须反证：同一事件重复提示、timer 清理、组件卸载、切聊天、sending 门、手动收取、空文本、已提交后素材失败、失败重试和旧 timer 迟到。

### P010 版本与发布标识显示

设计目的：设置／首次准备／更新提示对当前发布显示 `rq0.83`，持久数据显示版本 9；稳定更新判断与 F079 的真实版本选择一致。

窄读重点：

```text
src/人妻公寓/脚本/游戏逻辑/依赖版本.ts
src/人妻公寓/界面/客户端/components/首次准备.vue
src/人妻公寓/界面/客户端/components/设置弹窗.vue
src/人妻公寓/schema.ts
src/人妻公寓/schema.json
src/人妻公寓/世界书/变量/initvar.yaml
src/人妻公寓/组卡.mjs
```

必须反证：卡体版本、组卡标签、素材标签、UI 当前版本、远端最新稳定版和数据版本不混淆；beta／rc 不冒充稳定更新；检测失败显示“无法确认”而不是伪造“已最新”或强制升级。

现有直接测试入口：

```text
tests/人妻公寓/v083发布版本契约.test.mjs
tests/人妻公寓/依赖版本检测.test.mjs
tests/人妻公寓/App拆分A3.test.mjs
```

---

## 八、测试可信度与失败分类

最终批次仍必须遵守 BUG 语义五问：

1. 设计初衷是什么；
2. 正常运行时真实状态如何变化；
3. 对游戏和玩家有什么作用；
4. 根因位于哪个生产—持久—消费链；
5. 最小修复会影响哪些兼容与已闭合行为。

每个失败必须分类为：

```text
真实 BUG
测试过时
设计待拍板
环境／网络／夹具问题
```

禁止：

- 为了让旧源码形状断言变绿而回退单实例、共享世代、结构校验或失败关闭；
- 把公网请求偶发失败直接登记为产品 BUG；
- 只扫 TODO、只看静态字符串或只跑已有测试就宣称 244／244；
- 把测试 mock 的空壳当作真实 v9 存档或宿主脚本树；
- 因 F079 只有一个文件就忽略 P001～P010 的集成边界。

真实 BUG 必须有修复前稳定失败、修复后通过的行为证据。旧测试若锁定已被更安全设计替代的字面形状，登记 `TEST-STALE-009` 起的记录并改为语义／行为断言。

---

## 九、强制验证门禁

至少执行：

```bash
# F079 与最终基线专项
node --test tests/人妻公寓/依赖版本检测.test.mjs
node --test tests/人妻公寓/App拆分A3.test.mjs
node --test tests/人妻公寓/v083发布版本契约.test.mjs
node --test tests/人妻公寓/客户端稳定性.test.mjs
node --test tests/人妻公寓/首次准备数据库单路线.test.mjs
node --test tests/人妻公寓/v080存档策略.test.mjs

# 产品 TypeScript
npx tsc -p .codex-tmp/tsconfig.rq082.json --noEmit --pretty false

# Vue TypeScript
npx vue-tsc --noEmit -p .codex-tmp/tsconfig.scene-story-vue.json --pretty false

# 受影响 TS／Vue／测试 ESLint
npx eslint <本批实际修改的受支持文件>

# 差异完整性
git diff --check

# 最终全量
node --test tests/人妻公寓/*.test.mjs
```

如新增或修改测试，最终测试文件数与测试总数必须重新统计。不得只报告“全绿”，必须记录：

```text
测试文件数
tests
pass
fail
cancelled
skipped
todo
总耗时
```

若仓库对某类文件没有 ESLint 配置，必须明确说明并用实际解析器／类型器补足语法门禁，不得把 ignored warning 说成已 lint。

---

## 十、唯一台账的最终写回要求

只能更新：

```text
src/人妻公寓/全量BUG审查台账_2026-08-19.md
```

必须写回：

1. F079 的完整审查登记与最终状态；
2. P001、P002、P004～P010 的最终状态；
3. P003 只保留既有完成结论和必要反证；
4. 新增 BUG、测试过时、设计待拍板、环境说明；
5. 最终专项／关联／类型／Lint／diff／全量数字；
6. 产品表反算必须是 192 个唯一 F ID，且闭合 192／192；
7. 功能表反算必须是 244 个唯一 P ID，且闭合 244／244；
8. `未开始`、`定位中`、`真实BUG待修`、`已修待回归`、`设计待拍板`、`实机待验` 均为 0；
9. 第 0 批“基线、库存与测试可信度”状态由定位中改为已完成，并记录最终验证真值；
10. 新增“最终基线闭合批次”记录，说明九个集成功能如何逐一闭合，而不是只写 F079；
11. 只有所有门禁通过后，才把本轮全量审查状态写成完成。

建议用脚本从 Markdown 表格反算，禁止手工只改顶部数字：

```text
产品：已完成 + 已审无BUG = 192
功能：已完成 + 已审无BUG = 244
BUG 标题数 = 顶部真实 BUG 数
TEST-STALE 标题数 = 顶部测试过时数
唯一 F ID = 192
唯一 P ID = 244
```

---

## 十一、完成定义

最终窗口结束前必须同时给出：

```text
F079 最终结论
P001、P002、P004～P010 各自最终状态
新增真实 BUG／测试过时数量与编号
最终产品文件 192／192
最终功能 244／244
最终测试文件数与 tests/pass/fail/cancelled/skipped/todo
产品 TypeScript
Vue TypeScript
ESLint
解析器（如适用）
git diff --check
是否存在环境或实机待验
```

达到上述条件后，不再创建下一批交接；直接在聊天中明确宣告本轮 192／192 产品文件、244／244 功能的全量审查已经闭合，并列出最终统计。

在此之前，禁止使用“全部完成”“全量审查已结束”等表述。
