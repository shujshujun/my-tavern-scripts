# 修复代码复审问题复修记录（r3f1，2026-09-08）

## 1. 任务与定位

用户授权：修复上一轮独立复审发现的问题，并把修复报告记录保存到本地。

- 续接工作树：`C:\Users\Administrator\.devspace\worktrees\tavern_helper_template-consolidated-5d7f765f`
- 复用工作区：`ws_0a48808756`
- 开工及末次核对分支：`task/unified-repair-20260907`
- 开工及末次核对 HEAD：`c5943ef6`
- 原复审报告：`src/人妻公寓/修复代码独立复审_2026-09-08_s2v6.md`
- 原统一记录：`src/人妻公寓/统一修复记录_最新补审_2026-09-07.md`

本轮延续当前混合未提交工作树，没有切分支、新建工作树、回退或整理删除其他修改。原复审结论和失败证据保留；本文件记录后续修复增量，不改写历史测试结果。

已读取 AGENTS.md、项目入口和 BUG 语义排查协议等开工文件。更细目录规则与统一记录的组合定位调用被工具安全网关拦截，未取得结果；因此未盲写统一记录，也不声称已核对并行窗口的全部最新进度。本增量另存本文件，并在原独立复审报告追加入口。

## 2. 本轮结论——不得整体标记四项完成

| 原编号 | 本轮产品修改 | 当前证据及状态 |
| --- | --- | --- |
| PLAY-023 | 已保存否定副词与询问宾语作用域修复 | 三个专项 111/111；原独立诊断 16/16。此次复审反例在已执行范围内修复通过。 |
| PLAY-030 | 已保存当前倾听邀请识别修复，追加12个回归检查 | 修后测试调用被工具拦截；源码落地、类型检查通过，但业务回归待补验，不关闭原编号。 |
| PLAY-031 | 已保存共享结局与各户承接经历分层投影 | 修后专项调用被工具拦截；源码落地、类型检查通过，但业务回归待补验，不关闭原编号。 |
| PLAY-013 | 未修改产品源码 | 完整入口仍在模型调用之前因演员校验失败，继续保留原未解决状态。 |

本轮没有增加新的项目 BUG 编号。PLAY-023 开发中新增控制反例先失败，随后修复并保留该用例；具体经过见第4节。

## 3. 修前复现

执行当前真实工作树测试：

```text
node --test --test-reporter=spec tests/人妻公寓/离婚主入口生命周期.test.mjs tests/人妻公寓/安抚行动意图_PLAY030.test.mjs tests/人妻公寓/结局承接经历投影_PLAY031.test.mjs
```

实际退出码1：86项，75通过、11失败、0取消、0跳过。对应原专项为 PLAY-013 0/1、PLAY-030 70/71、PLAY-031 5/14。

工具保存的完整修前输出位置：

```text
C:\Users\ADMINI~1\AppData\Local\Temp\pi-bash-398b6b438fcd2750.log
```

该位置是工具临时日志，不作为永久证据的唯一来源。关键诊断在本报告保留如下：

```json
{"success":false,"modelCalls":0,"businessCommits":0,"rows":33,"legalBefore":false,"legalAfter":false,"busy":false,"leaseHeld":false,"warnings":["[人妻公寓] 回合执行失败: Error: 办理现场必须关联且只能关联赵国强"]}
```

## 4. PLAY-023：否定副词与询问宾语作用域

### 设计、价值与真实逻辑

父亲知情分类应区分真实肯定、明确否定、其他人的知情及普通楼务话题。合法的否定台词通过电话格式门后，不应被模式语义门反向判为知情。修复不修改原消息、引用内容或游戏状态，也不放宽所有候选。

根因：原否定规则要求否定词几乎紧邻谓语，“并未真正知道”“未曾真正察觉”等普通副词打断了匹配。

### 已保存修改

产品文件：`src/人妻公寓/脚本/游戏逻辑/父亲关系知情.ts`

1. 只在谓语前的分析副本末尾移除明确的程度/方式副词；单重否定和双重否定使用同一分析副本。
2. 保留明确知情、双重否定知情、主语归属、普通话题、引用及疑问等既有判断。
3. 新增控制反例“爸爸没问过同事但已经真正知道这段秘密关系”首次失败，原因是询问宾语“同事”被当成主语。随后仅隔离紧贴转折的明确询问宾语，并增加主语换成母亲、同事、父亲的朋友等反向控制；不跨任意叙事文本扩大豁免。

新增测试：`tests/人妻公寓/父亲知情否定副词复修_PLAY023.test.mjs`，32项。

覆盖原四种否定、多个副词/带“地”的形式、真实电话格式与模式校验、双重否定、不同主语、询问宾语、反转肯定、普通楼务、重复调用和视角切换。没有删除或放宽原断言。

### 修后证据

首次新旧专项组合：107项，106通过、1失败；失败即上述询问宾语控制。修复该点并增加4项反向控制后重新执行：

```text
node --test --test-reporter=spec tests/人妻公寓/父亲知情否定副词复修_PLAY023.test.mjs tests/人妻公寓/父亲知情复审反例_PLAY023.test.mjs tests/人妻公寓/父亲知情对象与嵌套语义_PLAY023.test.mjs
```

结果：**111/111，0失败、0取消、0跳过**。包括新增32项及原有29+50项。

另执行未改写的原独立诊断：

```text
node .codex-tmp/repair-code-review-20260908-s2v6/knowledge-negation-probe.mjs
```

结果：10个分类检查、6个真实电话消费者检查全部匹配预期，`classifierFailures=0`、`consumerFailures=0`。HEAD对照仍显示原四种否定错误，而当前源码均正确；未覆盖或改写原复审结果。

该次诊断的产品文件 SHA-256：

```text
cf034ed0cc5d05973bc02e67153e8e4d4fc90172aa80fda25d01574d0541c099
```

边界：111项与16项诊断有业务内容重叠，不应宣传为127个独立业务场景。没有调用真实在线模型或玩家存档。

## 5. PLAY-030：恢复当前倾听邀请

### 设计、价值与根因

“你说吧，我会认真听。”是玩家当下邀请对方表达，应进入有效回应候选；“你说过……”和转述他人话语不能替代玩家本次行动。

根因：他人转述正则把所有以“你说”开头的句子整体排除，原有效安抚谓语还没机会检查就已返回空分句。

### 已保存修改

产品文件：`src/人妻公寓/脚本/游戏逻辑/冷落系统.ts`

只在“你说吧”后紧接句界或结束时，将其从他人转述中排除；“你说过”“你说：……”及“你说吧台……”仍按转述处理。后续明确拒绝倾听、拒绝关心的否定校验继续执行。没有修改安抚阈值、首次诉苦、同楼去重、送礼上限或成长结算。

原测试文件：`tests/人妻公寓/安抚行动意图_PLAY030.test.mjs`

追加4项当前邀请、6项转述/拒绝反例、两个真实消费者块的完成与重复不计数检查。原71项及原断言保留，文件按新增用例预期包含83项；这不是83项通过的声明。

### 验证状态

修后测试调用被工具安全网关拦截，没有返回测试统计，也没有换报告器、改入口或混入总跑来绕过拦截。定向项目类型检查通过，但不替代业务回归。

当前状态：**源码和回归用例已落地，业务验收待补；不能写为已通过。**

## 6. PLAY-031：共享结局与真实承接经历分层

### 设计、价值与根因

旧共享结局依然是合法的已完成结局，不要求玩家重玩，不撤回收益。但它不能单独证明102新版承接、202新版承接及202停止默认等待等事实均发生过。

根因：共用结局后生活语义函数只按“录像带结局”阶段选择文案，无条件同时写入新版承接事实；正文、快照、私聊、群聊方向和朋友圈因共用此函数而一并继承错误。

### 已保存修改

产品文件：`src/人妻公寓/脚本/游戏逻辑/结局后生活社交语义.ts`

- 共享结局开启与正式结局已完成判定保持原状；旧“录像带”“录像带结局”标记继续承认。
- 102承接投影读取本户阶段或稳定完成ID，与原第二机位已完成的判定条件一致；直接只读，不引入运行时路线依赖。
- 202承接投影复用已读到的严格`不再留门已完成`契约，不能只凭旧显示标记补造完整经历。
- 既有V4终态继续作为新版承接已完成的兼容依据；原V4终态的收束叙述保留。
- 202“停止默认等待”与整条承接完成分开：有效实例已使用道具且已有该决定时，可以投影部分进度，不冒充整条路线完成。
- 无该决定的旧共享档不再被正文、主动私聊主题、群内可见后效或公开朋友圈安排为已经完成该转变；已成立决定的原表达保留。
- 不新增或补写Schema字段、完成ID、物品、存档迁移或收益，不缓存结果。

### 验证状态

原`tests/人妻公寓/结局承接经历投影_PLAY031.test.mjs`的14项及全部原断言保持不变；覆盖两种旧共享标记、单户完成、部分202进度、V4终态、Schema重载和回档。

修后执行该专项被工具安全网关拦截，没有统计结果，不能把修前5/14或静态类型通过当成修后业务通过。关联路线生产路径的扩展检查也受限，因此未修改那些生产模块。

当前状态：**共用语义源码已落地，业务验收待补；不能写为已通过。**

## 7. PLAY-013：未解除的完整入口阻塞

已读取原生命周期测试、真实入口环境、事实验收夹具、完整引擎的快照注入校验以及焦点检测消费逻辑。

本轮确认：回合在快照组装的演员校验处失败，模型调用0次、业务提交0次，33条原记录和未成立的法律状态保持；锁已释放。不能把此失败归咎于模型生成正文质量，也不能凭局部85/85旧证据放行完整入口。

后续需要在真实事件生产、事件演员绑定及焦点消费者之间核对丈夫身份，而不是放宽“必须且只能关联201丈夫”的检查。相关生产/绑定定位调用被工具安全网关拦截，本轮没有为凑成功而改守护、夹具或提交条件。

当前状态：**本轮未修复，保留原编号待处理。**

## 8. 其他复审项

本轮没有处理微信持久化测试的旧固定字符串断言。旧报告已说明该失败不足以证明手机硬保存失效；没有修改该测试来凑全绿，也未修改相关发送/落库实现。

## 9. 静态检查、Git与边界

本轮三个产品补丁保存后执行：

```text
pnpm exec vue-tsc --noEmit --pretty false -p .codex-tmp/unified-repair-20260907/tsconfig.task.json
```

结果：正常返回、无诊断，定向Vue/TypeScript检查通过。

末次定向`git diff --check`无诊断；注意Git diff不会检查未跟踪测试文件，不能把此项当作未跟踪文件的检查证明。

同次末检返回：

- 分支`task/unified-repair-20260907`、HEAD `c5943ef6`。
- `git diff --cached --name-only`没有输出，暂存区为空。
- `git diff --name-only -- dist`没有输出，当前已跟踪官方dist相对HEAD无差异；该命令不证明不存在未跟踪dist文件。

没有正式构建、隔离生产构建、组卡、commit、tag或push；未操作玩家实时存档、数据库和在线模型。类型检查不等于完整运行时验收，本轮也不是全游戏全量回归。

## 10. 本轮文件增量与续接入口

本轮有意修改/新增的业务相关文件仅为：

```text
src/人妻公寓/脚本/游戏逻辑/父亲关系知情.ts
src/人妻公寓/脚本/游戏逻辑/冷落系统.ts
src/人妻公寓/脚本/游戏逻辑/结局后生活社交语义.ts
tests/人妻公寓/父亲知情否定副词复修_PLAY023.test.mjs
tests/人妻公寓/安抚行动意图_PLAY030.test.mjs
```

另新增本记录，并在原独立复审报告末尾追加指向本文件的增量入口。没有覆盖原报告、历史失败输出、旧测试断言或helpers。

后续按本文件状态续接：PLAY-013仍需修复；PLAY-030和PLAY-031先补修后业务验收，失败继续修、不删除断言；PLAY-023保留本次111/111与16/16证据。读取统一记录权限恢复后，应把本增量链接及状态追加到原统一记录，而不是重算旧批次为全绿或重复增加BUG编号。
