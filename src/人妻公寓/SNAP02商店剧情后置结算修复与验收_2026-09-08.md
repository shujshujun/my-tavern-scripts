# SNAP-02 商店剧情后置结算修复与验收

本项为新修。主候选 C:\Users\Administrator\.devspace\worktrees\tavern_helper_template-consolidated-5d7f765f，当前HEAD 394817a5；本项涉及原混合源码，全部保持未提交，未发布。

## 设计、根因与影响

原第一轮快照报告§3/N05及当前源码均证明：旧201账本购买会先扣800、排事件，随即运行配置结算清欠租/写唯一完成ID，并发参与妻堕落+2；正文尚未生成。即时开演先将该业务及活动事务落地，之后才生成；失败重试保留购买后快照，所以提前的成功后果无法由回合失败纠正。

正常用途是购买后演出当前剧情，正文成功才成立清账、完成和奖励，原费用、剧情、配置结算及后续线路完成标记继续使用。完成标记供分居资格及阶段线路特殊节点补账读取；阶段线路每次新线路事件会读取当前完成表，不要求重新购买或重演。此项解决提交时点，原场景后续重设计仍按其独立计划处理。

## 实现与状态生命周期

新增商店剧情结算.ts，准备时生成唯一票号并与商品/参与妻/待演状态写入系统凭据，导演事件携带机器标记。购买只扣款并产票；现有即时开演负责将它和场景事务同次保存。待演时重复购买被拒。Schema添加默认无凭据状态，旧v9可按已有默认补字段，不抬版本、不猜测历史欠租或补造已完成奖励。

普通回合引擎与原生index均在本轮有效正文且实际队首消费成功后调用同一提交函数，配置结算、奖励和凭据已结算状态都写入同一候选。票号、配置及参与者不匹配时失败，完整同票重放无副作用；没有新标记的旧票保持原已结算历史。机器标签进入现有标题过滤，不代替可见剧情标题。原生坏结局早退前已应用本项结算，经济结算随后读取新账；没有新增独立保存或放入可选提交后任务。

关键差异：普通通道先计算焦点疑心再消费事件，原生先消费再计算疑心。提交函数返回本次实际脚本堕落增量；原生在原有增量中精确减去这部分，保留原AI贡献及100上限截断。保证原来购买奖励不触疑心的语义不会因后置而改变。

## 两轮与验证

首批3项红测为1通过/2失败，真实购买和重试均复现提前后果；首轮准备修后3/3。双通道装配初次出现两处新夹具错误：惰性结算户误从时钟模块导入、原生局部变量名未提供；修正测试装配后49/49，未为这两项改变产品。实际安全壳/即时开演/落地扩展后67/67。

随后补两条凭据参与者反例（空数组及跨户），修前0/2；第二轮在任何结算前对照当前配置参与者，两个反例解决。最终新专项69项进入8文件联合289/289，0失败/取消/跳过。保留所有原始失败日志。类型曾报移走奖励后遗留的一项未使用import，精确移除后通过；并非业务断言失败。最终8个源/测试目标ESLint均0错误0警告，vue-tsc exit0。

行为覆盖：购买与待演分离、JSON重载、同票重试和重放、无有效正文、旧已结算票、迟到普通请求世代、参与者缺失/跨户、标题过滤；两通道在50/98/99/100基准与-2/0/1/2/4 AI增量下奖励及疑心一致；票保存失败、结果保存前失败、生成失败/停止/超时返回失败、切聊天/回档世代/换房使候选失效。旧设计缺口测试的购买即完成断言已按新契约改成真实消费后结算，保留原价、原效果与完成ID检查。

真实执行范围：新专项加载真实Schema、商店、场景事务、结算模块、惰性时钟/疑心、事件冻结和消费；AST执行完整普通回合结算、完整原生成功块及其实际焦点风险循环，以及安全操作/即时开演/落地。邻接非活动剧情、随机打断、经济通知、生成入口及存储提供者使用明确隔离适配；没有把被测队首、结算或疑心函数替换成恒定成功。生成停止/超时用入口返回失败契约测试，未运行真实模型计时器。原生完整浏览器回调、MVU物理保存和写后报错补偿、连续真实游玩不在本次运行范围；相关既有租约回归同时通过，最终候选仍需联合宿主验收。

## 构建与所有权

最终双入口生产隔离构建build-1788828897428-88e223通过，游戏逻辑0错误3警告、客户端0错误2警告，Node模块类型警告保留。禁schema dump/监听/同步，输出和声明只写scratch；主仓/候选正式dist各72文件及受监控源码构建期前后不变。新模块与所有改动源SHA均匹配该构建。

开工备份六个既有目标（五源一旧测试），新差异逐项复核并制成owned-existing.patch；在隔离副本应用后六文件逐字节匹配当前内容，保留原index/回合引擎/商店/场景事务混合差异。新模块与新专项另存after副本。凭据模块SHA ba2d72ae0c7e539dbbe4e4e43a716a3839a73f7557953b53c9e85581fef82e53；新专项SHA 0e938e98d642dce8ca7079439c2d0a0dab99400f6bf0714b8b67ac6fe8df3281；既有文件增量补丁SHA 1a60b6fc3435b054431f55ab8714ce86c46472de26d411c1c00ab718541910b5。因共享源码混合所有权，本项不整文件暂存或提交。

证据目录.codex-tmp/codex-takeover-20260908/snap02：baseline.json/before、before.tap、round1-channels.tap、round1-fixture-fixed.tap、lifecycle-round1.tap、participants-before.tap、final-related.tap、final-lint.json/runtime-lint.json、types.log/round2-types.log、final-build.log、owned-existing.patch、ownership.json、replay和after副本。较早build-1788828654827-809bbf对应移除未使用import前源码，仅保留历史，不用于最终签发。

SNAP-02本地代码验收完成；总目标、旧场景独立设计和最终联合验收仍未完成。
