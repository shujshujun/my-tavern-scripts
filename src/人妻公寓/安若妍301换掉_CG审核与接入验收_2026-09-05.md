# 安若妍301《换掉》CG审核与接入验收

日期：2026-09-05。

## 交付

31张现稿已逐张以1536×1024原始分辨率打开复核，用户确认全部接入。正式目录为`素材/特殊场景/安若妍换掉/`，包含19张剧情／照片素材、12张持久背景和`安若妍换掉CG.manifest.json`。

产品统一采用无损WebP，全部保留1536×1024尺寸。31张产品解码后的RGB像素与各自PNG逐字节相同；总计58,724,615字节降至39,923,840字节，节省32.015%。8张结局后背景的相框外像素与对应结局前产品完全相同。

## 原图复核

检查范围包括角色和人数、画面结构、场景、道具、原图可读性、版本对应、照片来源及剧情时序。31张均可用于当前运行位。机器清单逐张保存源图路径、源图SHA、原审核记录SHA、产品SHA、解码像素SHA、本次原尺寸复核结果和备注。

以下观感问题保留在交付记录中，不阻断本批接入：

- `ARY-RPL-07-N`、`ARY-RPL-07-P`、`ARY-RPL-09-P`的摄影者渐隐叠层有可见拼接感。
- 8张POST背景的竖框内照片较小，横向画面存在压缩感；对应照片版本与换照结果仍可辨。

01—05、06-N/P、07-N/P、08-N/P、09-N/P、10-N/P、11-N/P、12—13，以及BASE／BABY1／BABY2／BABY3各自PRE／POST-N／POST-P，均已完整复核。相似的背景和照片属于指定剧情及体态差分，保持独立运行ID；31张产品没有精确文件重复。

## 接入行为

`安若妍换掉资源.ts`声明产品目录、清单名、31张文件数及待不可变标签状态，既有剧情CG、图库回忆、手机图片和301背景继续通过该资源函数解析地址。

- 11个剧情节点消费13个CG运行位，完整覆盖19张剧情文件。
- P2冻结的10-N／10-P对应最终照片；后续显影和换照背景继续使用该版本。
- 结局前使用4张PRE；玩家换照完成后，以普通房间／前三胎房间和冻结照片体态组合选择8张POST。
- 姐妹群保存的`@ending/安若妍换掉/…`资源键使用同一产品路由。
- 存档及回档沿用既有完成凭据和照片体态；缺照片数据的旧完成档保留既有降级行为。

七份当前源清单按实际路径及SHA登记，其中H1明确采用`h1-manifest-v2.json`，P2普通／孕态事件图分别采用当前draw3／draw4。候选、底板和旧版本继续保留在制作区。生产运行依赖正式产品目录，制作区路径只用于来源追溯与离线核验。

## 验证

- 接入前结局专项基线：27／27通过。
- 接入后结局、恢复边界与资源专项：34／34通过。
- 31张实际HTTP资源逐张实取并校验SHA；手机图片解析返回相同地址。
- 独立像素复验：31／31产品与源图相同，8／8背景相框外像素相同。
- 定向TypeScript和ESLint通过。
- `tests/人妻公寓`全量2370／2370通过，失败与跳过均为0。
- 逻辑与客户端两入口生产模式隔离构建通过；输出位于`.codex-tmp/ary-replace-cg/isolated-build/`。schema dump、监听／同步插件和自动声明写回均已禁用。
- 正式`dist/**`构建前后各72个文件，逐文件SHA完全相同；自动导入与组件声明文件也未变化。webpack仅保留包体积建议警告。

验证日志保存在`.codex-tmp/ary-replace-cg/`。原有用户配置、正式dist漂移和制作区文件保持原状；此前未跟踪的CG生产清单仅在本地同步交付状态，整份制作稿保留原所有权，不纳入本任务提交。

## 发布与宿主边界

产品已安装到本地，外部素材仓库和不可变标签等待统一素材发布。验收环境可通过`__RQGY_ARY_REPLACE_ASSET_BASE__`指向实际提供这31张WebP的HTTP基址。本次本地HTTP验证不代表外部素材已上线。

真实SillyTavern桌面／手机的19回合连续游玩、真实AI演绎和中断恢复实测仍属于既有宿主验收待办。本次交付是素材审核、产品安装与现有代码路由闭合。
