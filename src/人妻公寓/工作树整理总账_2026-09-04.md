# 《人妻公寓》工作树整理总账

记录日期：2026-09-04。

## 一、整理口径

本账以当前磁盘、Git索引、提交历史、各任务worktree与实际文件体积为准。整理完成不等于强行让`git status`空白，而是要求：

- 已经完成且能够验证的功能、BUG或设计批次形成边界明确的本地提交；
- 暂存区为空；
- 剩余tracked／untracked内容都有明确所有者、用途和下一处理门槛；
- 制作母版、拒绝图、审核记录与其他窗口改动不因“清理”被删除、覆盖、隐藏或混入提交；
- 未经发布授权不写正式`dist/**`、不组卡、不tag、不push。

## 二、已经收口的本地提交

当前主分支`develop/rq-next`已经依次包含：

- `11d19f88`：301《不必停》完整承接线与18张WebP产品；
- `c0216416`：《不再留门》8张WebP产品；
- `4cfec9be`：录像带旧v1／v2视觉退场，保留无图旧档状态兼容；
- `66c6f3cf`：CG分类、淘汰与产品化总账；
- `4faca104`：录像带V4 38张WebP产品、manifest与生产地址；
- `137c2ce1`：角色页补齐301《不必停》及102／202录像带V4攻略投影；
- `37306c04`：301正式结局《换掉》玩法设计封板；
- `9ff63844`：停用旧`2026-08-03`总待办入口并登记原位归档。

`137c2ce1`补验结果为专项3／3、`tests/人妻公寓`全量2327／2327、定向Vue／TypeScript、ESLint与提交差异检查通过。CG批次另有各自专项、全量和隔离生产构建记录；正式`dist/**`未由这些任务写回。

## 三、剩余tracked改动：六项

### 3.1 代理配置与本地记忆：三项

- `.cursor/rules/破限.mdc`：大范围改写代理内容规则，属于用户／本地代理配置，不是产品源码；不能随功能提交。
- `CLAUDE.md`：新增内联规则并强依赖尚未跟踪的`.cursor/rules/规则.mdc`。其中记录的界面规模为28个组件、6个composable、`App.vue` 8943行／318KB，而当前磁盘为29个组件、7个composable、9787行／349546字节，尚未达到可提交的一致状态。
- `CODEX_MEMORY.md`：本地记忆文件，当前比HEAD增加104行。项目规则明确要求本任务不自动读取；因此未读取正文、未暂存、未提交。

这三项的下一所有者是用户或专门的代理规则维护任务。只有明确确认规则内容、消除重复入口并更新过时架构数字后，才能形成独立配置提交。

### 3.2 正式构建目录漂移：三项

- `dist/脚本示例/index.js`：5808字节变为5003字节，是一次完整的示例脚本重新打包，不是单行元数据变化。
- `dist/脚本示例/index.js.map`：15441字节变为14241字节，与上述示例脚本重新打包配套。
- `dist/角色卡示例/脚本/立即事件/index.js.map`：字节数保持1187，只把source map路径从`src://tavern_helper_template/示例/...`改为`src://tavern_helper_template/src/...`。

这些文件不属于《人妻公寓》产品提交，也没有正式发布授权。当前既不提交，也不擅自恢复；下一处理必须由对应示例项目所有者决定是丢弃构建噪声，还是按正式发布流程重新生成完整产物。

## 四、剩余untracked内容：6866个文件

机器清点结果为6,701,684,464字节，其中：

- `output/imagegen/**`共6862个文件、6,545,875,918字节，主要是PNG母版、候选、拒绝图、provenance、任务JSON、审核记录和制作脚本；产品需要的WebP已经按任务边界进入正式目录或既有tracked产品路径。这些制作证据继续保留，不批量提交、不删除，也不通过忽略规则把未审候选永久藏掉。
- 根目录漫画参考压缩包`[Sound MILK ...].zip`为155,765,013字节，属于外部参考源，不是产品依赖；当前保留在本地，不提交。
- `.cursor/rules/规则.mdc`为36,620字节，是`CLAUDE.md`当前引用但尚未跟踪的代理规则文件；与上文配置批次一起等待用户确认。
- `PRODUCT.md`为4,044字节，仍声明`v0.82／rq0.82`，低于当前正式基线`v0.90.4／rq0.90.4`，不能作为当前产品权威或直接提交。
- `新窗口执行模板.md`为2,869字节，记录旧成人CG断点`165／330`与`PXQ-SEX-034`，已被当前磁盘任务状态和专用工作流取代；当前只视为未归档的历史交接草稿，不作为执行入口。

完整机器清点保存在`.codex-tmp/worktree-audit/untracked-inventory.json`，该临时报告不进入产品提交。

## 五、worktree与分支登记

- 已清除两个只指向不存在目录的陈旧Git worktree登记：`D:/rq073-audit-781205`、`D:/rq074-audit-300326`；没有删除任何现存目录。
- `codex/nmd-webp-productization`与`codex/vtr-v4-webp-productization`的提交均已在主分支找到补丁等价提交；对应worktree的tracked状态为空。
- `codex/vtr-old-assets-retirement`的主分支合入曾处理共享文件冲突，因此不是字节级patch-id等价；主分支`4cfec9be`是经过组合验证的最终版本，原任务worktree继续保留作追溯。
- 五个`.devspace/worktrees/**`由宿主应用管理，当前不移除。
- 三个本次CG任务worktree均含未跟踪临时验证或证据联接；未经明确授权不执行`git worktree remove`。

## 六、当前结论

本窗口能够自动判定并安全提交的功能、BUG、设计与归档批次已经全部收口。暂存区应保持为空。剩余内容不是“漏提交的一锅改动”，而是三类有意保留项：用户代理配置、未授权的示例`dist`漂移，以及本地制作／审核证据与外部参考。

若目标是让`git status`最终完全干净，仍需要用户分别决定：

1. 代理规则三件套是提交、重写还是保留为纯本地配置；
2. 三份示例`dist`是恢复到HEAD还是走对应项目的正式构建提交；
3. 6.55GB制作证据与155.77MB参考压缩包是否迁入外部归档；
4. 三个已完成CG任务worktree是否删除。
