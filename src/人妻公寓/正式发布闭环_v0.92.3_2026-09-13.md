# v0.92.3 发布记录

发布页：https://github.com/shujshujun/my-tavern-scripts/releases/tag/rq0.92.3

- 产品提交：`4547a9905292a4cec64694e16a6b515842a7fbbf`。
- 源码与发布配置提交：`9c0465998478b6448601e086acbd4e3ec2bf4ee4`。
- 注解标签对象：`c575d5782468209a5c0a6a83f8e28ca637015f82`。
- 发布分支：`release/rq0923`，工作树复用 `D:/SillyTavern/tavern_helper_template-official-db-restore`。
- 发布时间：2026-09-13 21:28:47（Asia/Shanghai）。
- GitHub Actions：`34759942706`，`publish-rq0923`，结果 success。

范围包括游戏侧官方数据库持久恢复点、仅0楼重开及迟到清场、已打开裂缝保留，以及恢复重试、不再留门控制落地和正文AI话题误判三项修复。数据库继续使用官方更新渠道，本版不附带非官方数据库脚本；旧版本的标签和附件未修改。

## 验证

- 发布版本门禁69项、全量游戏6675／6675通过，零失败、零取消、零跳过。
- 产品TypeScript与客户端Vue类型检查通过；定向ESLint零错误，组卡脚本保留4条既有Node内置模块导入警告。
- 两入口隔离构建与正式构建零错误、5条体积／性能提示；源码和声明文件未被构建改写。正式代码与隔离产物逐字节一致，其他79个既有dist文件保持原样。
- PNG所有块CRC正确，唯一chara块与JSON逐字节一致，角色卡及两个代码入口统一为0.92.3／rq0.92.3。
- 分支与注解标签通过Git原子推送成功，远端标签对象及产品提交与本地一致；旧rq0.92.2标签对象核对未变。
- 三个Release附件通过GitHub API下载回读，与本地逐字节及SHA-256一致。
- testingcf.jsdelivr.net上的客户端与游戏逻辑均HTTP 200，内容哈希与发布产物一致。

证据位于本工作树 `.codex-tmp/release0923/`：`gate.log`、`all-tests.log`、`lint.log`、`tsc.log`、`vue-tsc.log`、`isolated-stats.json`、`formal-stats.json`、`local-verification.json`、`online-verification.json`。SHA-256清单随Release的 `rqgy-0.92.3-checksums.json` 提供，源码中位于 `dist/人妻公寓/发布校验_0.92.3.json`。

## 更新与边界

附件为 `rqgy-0.92.3.png`、`rqgy-0.92.3.json`、`rqgy-0.92.3-checksums.json`。可导入新卡，或把游戏脚本与界面正则中的代码标签都改为 `rq0.92.3`，随后完整刷新酒馆页面；旧档无需重开，数据库仍保持单实例。

2026-09-13 21:29:54线上验证时，实际游戏版本查询仍返回0.92.2，属于版本索引缓存尚未传播；新标签CDN及下载附件已经可用。没有把索引缓存滞后记成通过，也未创建后台检查任务。验证未使用玩家原始出错存档，不保证找回已丢失且无备份的历史记忆。
