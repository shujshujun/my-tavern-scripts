# 《人妻公寓》v0.92 正式发布闭环

日期：2026-09-12（Asia/Singapore）

## 一、发布结论

《人妻公寓》v0.92 已完成正式发布。正式不可变发布基线为：

- 发布标签：`rq0.92`
- 注解标签对象：`3cdcf6378cf81aa1b64b0fd1cda39fe5913cb0e8`
- 标签剥离后的正式打包提交：`98c4848ea58ea90a08fa50b6aaacf166fa8b81dc`
- 发布分支：`release/rq092`
- 发布后说明提交：`d854d05402d49d9598138de0276d31a4e789ca0a`
- 源码／测试／版本准备提交：`d4383aa42987e4968ca801288fa2f5c1a906de6e`
- 存档数据版本：`9`

注解标签始终固定在五项正式产品已经落盘的打包提交。发布后说明与本闭环记录只继续推进 `release/rq092`，不移动 `rq0.92`。

## 二、发布前候选与门禁

本次发布沿用已完成的 v0.92 全量复审候选：

- `tests/人妻公寓` 全量：`6608 / 6608` 通过；`fail=0`、`cancelled=0`、`skipped=0`；
- v0.92 发布契约、依赖版本检测与组卡正则门禁通过；
- 定向 TypeScript：0 error；
- 客户端定向 Vue TypeScript：0 error；
- 修改文件 ESLint：0 error，仅保留既有 Node 导入与 TypeScript 默认导入提示；
- `git diff --check` 通过；
- 双入口隔离 production build：游戏逻辑 0 error／3 warning，客户端 0 error／2 warning；构建前后正式 `dist` 与受保护源码不变；
- 正式 `pnpm build`：成功，0 error；39 条 warning 均为仓库其他项目既有 Sass 弃用或 webpack 体积提示，《人妻公寓》相关编译 warning 为 0；
- 正式组卡成功：世界书 13 条（启用 8）、正则 5 条、脚本 2 个；
- 卡规范：`chara_card_v3`、`spec_version=3.0`、`character_version=0.92`；
- PNG 只有一个 `chara` 数据块，解码 JSON 与独立 JSON 逐字节一致；
- 客户端构建只含 `RQGY_GAME_VERSION:0.92`；
- 卡内客户端和游戏逻辑入口均固定到不可变标签 `@rq0.92`；
- 素材继续复用不可变 `cg5`／`cg6`，未复制、伪造或重发 imagegen 制作证据。

## 三、Git 推送与不可变标签

由于当时 DevSpace 的 DNS／Git libcurl 到 GitHub 连接异常，本轮使用仅位于 `.codex-tmp` 的本地临时反向通道，让原生 Git 智能 HTTP 经系统 curl 访问 GitHub。Git Credential Manager 凭据只在进程内读取，没有写入项目文件、日志或提交。

首次推送以原子操作同时建立：

- `refs/heads/release/rq092` → `98c4848ea58ea90a08fa50b6aaacf166fa8b81dc`
- `refs/tags/rq0.92` → 注解对象 `3cdcf6378cf81aa1b64b0fd1cda39fe5913cb0e8`
- `refs/tags/rq0.92^{}` → `98c4848ea58ea90a08fa50b6aaacf166fa8b81dc`

这样工作流收到分支 push 时，所检出的不可变标签已经存在。发布后说明修正提交随后将 `release/rq092` 推进到 `d854d05402d49d9598138de0276d31a4e789ca0a`，标签未移动。

## 四、GitHub Actions

- 工作流：`publish-rq092`
- Workflow run ID：`34659292511`
- Job ID：`103458259365`
- 事件：`push`
- 分支：`release/rq092`
- Head SHA：`98c4848ea58ea90a08fa50b6aaacf166fa8b81dc`
- 开始：2026-09-12 07:46:15（Asia/Singapore）
- 完成：2026-09-12 07:46:34（Asia/Singapore）
- 状态：`completed`
- 结论：`success`

以下步骤全部 `completed/success`：

1. Set up job；
2. Check out immutable release tag；
3. Prepare release assets；
4. Create or update GitHub Release；
5. Verify published assets；
6. Post checkout cleanup；
7. Complete job。

## 五、GitHub Release

- Release ID：`387391733`
- 标签：`rq0.92`
- 标题：`《人妻公寓》v0.92`
- `draft=false`
- `prerelease=false`
- 发布时间：2026-09-12 07:46:30（Asia/Singapore）
- GitHub `releases/latest` 已指向 `rq0.92`

发布后已将 Release 正文从“本地候选／尚未发布”修正为正式发布状态，并再次确认正文不含“尚未创建或推送”等旧候选措辞。

## 六、正式附件与下载回读

| 附件 | 字节 | SHA-256 | API状态 |
|---|---:|---|---|
| `rqgy-0.92.json` | 99,177 | `aff527a743219b2977b40bcf645f296bbc73a0ad803bddda1269c52e3dc65cad` | uploaded |
| `rqgy-0.92.png` | 2,390,335 | `6037c449491d76192b96dce3413d0c870c002e29a60467e3a8f922920e96c2ef` | uploaded |
| `rqgy-0.92-checksums.json` | 1,753 | `bb12179be6165cdd218645c635327283de4e26cf7953193ce5d05c79b8c70ef0` | uploaded |

三个附件均从公开 Release 地址实际下载回读：

- 下载字节数与本地完全一致；
- 下载 SHA-256 与本地及 GitHub Release API `digest` 完全一致；
- 远端 PNG 只有一个 `chara` 数据块；
- 远端 PNG 内嵌 JSON 与远端独立 JSON 逐字节一致；
- 回读卡体为 `chara_card_v3`／`3.0`／`0.92`。

## 七、不可变标签 CDN

| 入口 | HTTP | 版本响应 | 字节 | SHA-256 | 结果 |
|---|---:|---|---:|---|---|
| testingcf 客户端 | 200 | `x-jsd-version: rq0.92` | 2,368,503 | `7d66ca4f099af5c487687ed59600d98727c4a669862a0cc7d0e59b52aeb99b16` | 与本地一致 |
| testingcf 游戏逻辑 | 200 | `x-jsd-version: rq0.92` | 3,015,297 | `43772cd3fceaf020830aa0d3f139cbe6d5de97b5166bfb3e0f2879cbd7008066` | 与本地一致 |
| cdn.jsdelivr 客户端 | 200 | `x-jsd-version: rq0.92` | 2,368,503 | `7d66ca4f099af5c487687ed59600d98727c4a669862a0cc7d0e59b52aeb99b16` | 与本地一致 |
| fastly.jsdelivr 客户端 | 301→200 | 最终文件已取回 | 2,368,503 | `7d66ca4f099af5c487687ed59600d98727c4a669862a0cc7d0e59b52aeb99b16` | 与本地一致 |

三路客户端文件逐字节一致。testingcf／cdn.jsdelivr 的精确标签响应使用一年不可变缓存；角色卡引用的正式 testingcf 客户端与游戏逻辑已经可用。

## 八、版本索引与更新提示状态

截至本闭环记录形成时：

- GitHub 标签、Release、附件与精确 `@rq0.92` CDN 文件均已上线；
- jsDelivr 旧版本列表接口 `/v1/package/gh/...` 暂时仍以 `rq0.91.6` 为最新 rq 标签；
- jsDelivr 新版本列表接口 `/v1/packages/gh/...` 暂时也仍以 `rq0.91.6` 为最新 rq 标签；
- 精确包元数据接口因仓库整体超过 50 MB 返回既有的 package-size 403，但精确标签静态文件已经 200 且逐字节核验通过。

项目当前的更新检测先读取 jsDelivr 版本列表，再回退 GitHub refs。因此在 jsDelivr 版本列表完成传播前，v0.91.6 玩家可能不会立即显示 v0.92 更新提示；这不影响直接下载、导入或运行 v0.92。版本关系函数的本地门禁已经验证：

- `0.91.6` 对 `0.92` = `当前较旧`；
- `0.92` 对 `0.92` = `相同`；
- `0.92` 对暂时落后的 `0.91.6` = `当前较新`。

不得把版本列表暂未传播写成附件或 CDN 发布失败，也不得在索引实际出现前宣称旧版更新提示已经线上闭环。

## 九、正式本地文件

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| `dist/人妻公寓/界面/客户端/index.html` | 2,368,503 | `7d66ca4f099af5c487687ed59600d98727c4a669862a0cc7d0e59b52aeb99b16` |
| `dist/人妻公寓/脚本/游戏逻辑/index.js` | 3,015,297 | `43772cd3fceaf020830aa0d3f139cbe6d5de97b5166bfb3e0f2879cbd7008066` |
| `dist/人妻公寓/人妻公寓.json` | 99,177 | `aff527a743219b2977b40bcf645f296bbc73a0ad803bddda1269c52e3dc65cad` |
| `dist/人妻公寓/人妻公寓.png` | 2,390,335 | `6037c449491d76192b96dce3413d0c870c002e29a60467e3a8f922920e96c2ef` |
| `dist/人妻公寓/发布校验_0.92.json` | 1,753 | `bb12179be6165cdd218645c635327283de4e26cf7953193ce5d05c79b8c70ef0` |

`发布校验_0.92.json` 记录的四项正式产品字节与 SHA 已在发布前和发布后独立重算通过。

## 十、现场保护与排除

整个正式发布过程没有使用 `reset`、`checkout`、`restore`、`stash` 或 `clean`，没有删除 untracked，也没有使用 `git add -A`、`git add .` 或 `git commit -am`。

以下内容均未进入 `rq0.92` 标签、Release 附件或正式产品提交：

- 13 个既有／全仓构建产生的无关 tracked `dist/**` 修改；
- 《人妻公寓》客户端与游戏逻辑的三个 source map；
- DevSpace probe；
- `.codex-tmp/**`，包括本次临时网络反向通道；
- 测试 `chat.json`；
- 旧交接词、未选择纳入的专项报告和本地测试卡；
- imagegen 原图、候选、审核制作证据；
- 其他角色卡的 HTML、JS、CSS 与 source map。

## 十一、最终状态

v0.92 的不可变标签、发布分支、GitHub Actions、GitHub Release、三个附件、下载回读、PNG／JSON一致性以及精确标签 CDN 均已完成并通过。

唯一尚由第三方缓存异步传播的项目是 jsDelivr 版本列表。它只影响旧版本自动发现 v0.92 的时机；正式 v0.92 下载件和游戏运行入口已经上线。后续复核版本列表时，只需确认新旧接口出现 `rq0.92`，不得移动标签或重新上传相同附件。
