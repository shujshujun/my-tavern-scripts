# PLAY-001 监控启动互斥修复与验收

2026-09-08，统一候选开工HEAD `7f3934d7`。本项首次修复。

监控新场次应在已有普通场次结束后建立。原前端就绪值表示录像带筹备凭据，并用于分流普通监控；真实index监听器在MVU队列内重读状态，但最终准备函数没有检查普通场次，可能同时建立两套场次。

唯一产品修改为`录像带V4状态.ts`三行：新启动时`_性爱场景.状态`不是`空闲`就返回失败、零变动，在任何录像带消耗或特殊槽覆盖之前结束。原监听器传递失败提示。观看中相同场次ID的幂等早返回保留；不改前端就绪值、筹备凭据、医院／剧情门、安全中断或旧场次资源。

新`监控启动普通场次互斥_PLAY001.test.mjs` **18/18通过**；加载主代理保存的真实修前状态源码时11通过／7行为断言失败。覆盖空闲启动、真实普通建账、进行中／收尾中、同ID幂等／不同ID拒绝、原资格、取消／安全中断重开、普通结束后再开、存档恢复、真实index安全壳／监听器在MVU队列内复核、地点变化及保存失败。收尾状态在真实建账后设入夹具；宿主存储、生成入口和忙态为隔离边界，没有执行模型或浏览器事务。

相关四文件**26/26通过**，与新专项合计44项。vue-tsc、产品与新测试lint均通过，0错误0警告。证据在`.codex-tmp/codex-takeover-20260908/`：`play001-red.tap`、`play001-green.tap`、`play001-related.tap`、`play001-typecheck.log`、`play001-source-eslint.log`、`play001-test-lint.txt`。未修改旧测试。

真实双入口生产隔离构建`build-1788822980333-851d0a`通过：游戏逻辑0错误3警告、客户端0错误2警告；主目录与候选正式dist各72文件及受监控源码前后不变。当前产品SHA与构建一致，见`play001-final-source.json`；差异仅本项三行，未携带其他混合修改。

本项本地代码验收完成，未正式发布。此前PLAY-004的产品与证据仍保留工作区待整合，PLAY-013已证失败及SNAP-25／26暂缓状态继续保留。用户要求本项完成后以精简交接转新任务，后续由新任务承接全部未完成队列及持续目标。
