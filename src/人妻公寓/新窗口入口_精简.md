# 《人妻公寓》新窗口入口（v0.90 正式发布）

## 当前外部发布：v0.90（2026-08-23）

v0.90 收口多预设输出、移动端前台决策、完整提示词查看和微信刷新分支兼容。监控线索判断与静音会议
散会选择保留正文可读高度，答案区独立滚动；普通行动建议在手机端改为可收起抽屉。提示词查看器现在以
高对比样式展示生成时真正发送的完整 `rawPrompt`，包括当时生效的预设、角色卡、世界书、历史与注入。

常见思维链与 `content`、`game`、`dream_body`、`revised` 等正文封套采用同一流式边界；睡眠等独立事件
有有效正文时可以正常提交，无正文时仍失败关闭。微信同楼刷新允许宿主补齐 `swipe_id` 或改写展示正文，
真实 swipe、切聊天与时间线变化继续严格隔离。数据版本保持 9，v0.80(v7) 与 v0.81/v0.82(v8) 按既有
规则迁移，v0.83～v0.89(v9) 直接继续。

源码版本、角色卡版本、资源标签和客户端内嵌标记统一为 `0.90`／`rq0.90`／
`RQGY_GAME_VERSION:0.90`。更新提示只在官方稳定版严格高于本地版本时出现；本地与官方同为 0.90 时
明确判为相同。组卡门禁拒绝旧客户端、缺失标记、多版本混包与标签不一致。

## 上一外部发布：v0.89（2026-08-23）

不可变标签 `rq0.89` 收口数据库物理表升级、UPSERT 重绑定、普通角色聊天生成租约与正文双边界；
真正未完成的输出显示为未结算残稿，不推进强制剧情、任务、资源、变量或数据库回合。

借种结局 14 张 WebP 继续固定到独立 CG 仓库不可变标签 `qgy-assets@cg4`，客户端与手机统一读取
`cg1/borrow-seed-ending/`。自动化验证不替代真人游戏验收；正式发布前仍需完成真实 SillyTavern
桌面／手机冒烟，并逐字节复核 JSON、PNG 内嵌卡数据与发布标签。

v0.85 是数据库安装兼容热修：修复 v0.84 在新版数据库插件中导入五表时，`RQ_社交轨迹` 的“事件”和“时间”都会被拼音化为 `shi_jian`、导致整个模板在写入前被拒绝的问题。社交显示列现改为“游戏时间”，英文 SQLite 物理列仍保持 `game_time`；0.84 已有“时间”值会按别名原样迁移，无需删表、无需新建聊天。

**v0.85 数据库五表热修（2026-08-20）**：真实 spv8.9.1 模板结构／DDL 预检与 SQLite 协调 2／2 通过；游戏侧 185 个测试文件、1641／1641 项通过。角色卡代码入口统一锁定不可变标签 `rq0.85`；数据版本继续保持 9，v0.80(v7) 与 v0.81/v0.82(v8) 按既有规则迁移，v0.83/v0.84(v9) 可直接继续游戏。

v0.84 是一次全量稳定性与兼容性发布：192／192 个产品文件、244／244 个功能全部闭合，累计修复 76 个真实 BUG。重点收口存档完整性、回档／切聊天时间线、正文与业务并发、手机与数据库事务、父亲电话、多人亲密、孕产恢复、客户端重复挂载、设置原子提交、版本检测与世界书协议。数据版本继续保持 9；v0.80(v7) 与 v0.81/v0.82(v8) 按既有规则迁移，v0.83(v9) 可直接继续游戏。

**v0.84 全量 BUG 审查与发布收口（2026-08-20）**：184 个测试文件、1637／1637 项通过；TypeScript、Vue TypeScript、ESLint 与差异检查全部通过。角色卡代码入口统一锁定不可变标签 `rq0.84`，没有变化的孕态、家庭计划和生产素材继续固定到各自已发布的旧标签，避免重复上传或改写历史资源。

v0.83 把孕情推进到完整生产生命周期：六名角色均支持待产、住院、生产、产后探望、房间育儿物件与微信母婴反馈，按前三胎提供 73 张 WebP 演出素材；夏乔的家庭计划加入九张专属画面、地点行动、监控节点和赴约收口。数据版本升至 9，v0.80(v7) 与 v0.81/v0.82(v8) 会在启动阶段安全迁移，既有剧情与数值继续保留。

**v0.83 生产系统、线路演出与客户端体验（2026-08-16）**：阶段线路与阶段性癖改为准备票／严格提交事务，数据库只提供记忆与规划辅助，不再对正文模型施加 DeepSeek 限制；手机补齐通用记忆层、硬事实、孕产胎次反应和父亲来电优先级。客户端加入变量重新生成、CG 图库全览、长档分页、PC 成人 CG 双图和酒馆助手渲染楼层恢复；同时修复荣耀洞失败跨时间线广播及持久同拍重试判断。

成人图库升级为 `cg3`：1070 张普通 CG 与 378 张孕肚 CG 分别按无接触开场、轻接触、深度前戏、进入中、事后五阶段和动作语义选择，严格禁止跨图库、跨阶段或强动作兜底；怀孕完成告知后才从普通库切换到孕肚库。孕态立绘与待产／生产／产后剧情图不属于成人五阶段，继续按孕产状态选择。

v0.82 恢复存档继承：v0.80 的数据版本 7 会在启动监听与界面操作挂载前，先深拷贝并迁移到数据版本 8，再原子写回当前尾楼；v0.81 的数据版本 8 直接继续。迁移只补齐孕情、首次当面评价、性爱收尾对象与送礼安抚日账，保留原剧情和数值；重复载入幂等，失败不会污染原档，未知、缺失、损坏或未来版本仍拒绝载入。

**v0.82 存档继承修复（2026-08-11）**：角色卡、客户端、游戏逻辑与孕态资源统一锁定 `rq0.82`。支持直接继承 v0.80／v0.81；更早版本仍需新建聊天。

v0.81 补齐完整怀孕体验：六名角色的 62 张孕态 WebP 立绘已按服装接入；孕情遵守隐藏、公开和姐妹群聊天生命周期；多角色首次同场时，其他角色只会针对该次怀孕首次评价一次，已经怀孕的角色遇到新近怀孕角色也适用。版本同时加入丈夫登门、手机预约邀约与微信引用回复，收口微信跨状态并发、正文承接和界面反馈，并修正桌面端 CG 完整显示、手机端全屏显示及多人立绘比例一致性。

**v0.81 怀孕系统、手机体验与视觉收口（2026-08-11）**：孕情首次评价按“观察者 → 怀孕角色 → 本次孕情”持久记录，只有公开孕情才会触发；姐妹群已经谈过该次孕情时不重复植入，生成失败、取消与同楼重掷不误消费机会，下一有效楼成功后才提交。孕态资源保持透明 WebP 与 2:3 比例，角色当前服装可直接映射；数据版本升至 8。该版发布时要求新建聊天，现由 v0.82 补上 v0.80→v0.82 迁移。

v0.80 重做数据库记忆模板：当前聊天由 12 张表收敛为 5 张有明确消费者的长期记忆表，移除会虚构开局人物、玩家姓名、背包剧情道具和无消费选项的默认硬状态表；`RQ_剧情事件` 改为机器短摘要并拒绝正文截断冒充摘要。游戏现在会同时检测数据库与酒馆助手的当前／官方最新稳定版本，旧玩家进入已完成存档也会收到非阻塞更新提示。v0.79 跳过，未创建分支、提交或标签。

**v0.80 数据库五表与依赖版本检测（2026-08-09）**：数据库安装器以当前聊天完整快照迁移到 `RQ_剧情事件`、`RQ_人物长期记忆`、`RQ_承诺与伏笔`、`RQ_社交轨迹`、`纪要表`，按列名兼容迁移官方 `spv8.9.1` 纪要表；剧情事件只接受单块、闭合、限长的 `<rq_event_summary>`，无可靠摘要时使用受控保守摘要。数据库版本解析兼容官方 `@spv8.9.1` URL，最新版只接受 `spv` 稳定标签；酒馆助手读取官方 manifest 并有镜像回退。远端失败、超时、版本落后均不阻塞游戏，重复检测以最新轮次为准。

**v0.78 回合转正与生成所有权热修（2026-08-09）**：v0.78 修复 v0.77 所有房间在 AI 正文完整输出后统一报“转正失败：宿主持久保存后临时标记复核不通过”的致命回归，并闭合固定回合、原生 normal、手机、隔离／楼务剧情与数据库迟到请求的共享生成所有权；同时补齐监控普通提示确认失败后的事务补偿。rq0.73→rq0.77 全量代码语义复审已完成。

转正负载现在携带无损原正文，确保酒馆助手真实进入 ChatMessage 分支并持久修改 `extra` 临时标记；固定回合、原生正文、手机短生成与隔离剧情统一使用幂等共享租约，数据库超时迟到 busy 在全部 AI 入口失败关闭。失败、取消、切聊、回档和监听器异常均有对应收口测试。

**v0.77 回合转正热修（2026-08-09）**：修复正文已经完整生成、却在最后统一显示“回合失败”的致命问题。根因是 v0.75 起转正阶段强制调用宿主 `saveChat`，而部分酒馆助手 iframe／旧宿主没有暴露该接口；现在有接口时继续等待硬保存，没有接口时改走 `setChatMessages(..., { refresh: 'all' })` 的同步保存与重载路线，并在重载后按事务令牌和角色重新定位、复核用户楼与助手楼。两条路线只要持久化失败仍会失败关闭，不会把未保存回合伪装成成功。

**v0.76 启动热修（2026-08-09）**：`rq0.75` 客户端会停在“游戏界面加载中”；其独有前置触发是客户端经 `手机系统.ts` 兼容门面读取纯函数，导致内核新增的渲染组合根副作用被打入并在客户端 iframe 挂载前执行，永久静止的直接机制则是入口对 MVU 初始化没有超时保护。v0.76 已改为直连 `手机/运行时上下文.ts` 与 `手机/静音会议旁路.ts`，给 MVU 启动等待增加 10 秒超时降级，并完成正式构建、组卡与卡内嵌数据自检；`rq0.75` 已由 `rq0.76` 取代，不再使用。

rq0.73→rq0.77 的完整代码语义复审见 [`rq073_rq077全量代码语义复审_2026-08-09.md`](./rq073_rq077全量代码语义复审_2026-08-09.md)：

- 拆分直接造成的确认丢失有两项：手机渲染注册副作用导入缺失，以及移动端房内抽屉隐藏公园晨跑 `TRAIN` 瓷砖；均已修正并增加测试。
- rq0.73 发布后的 Claude／Codex 修复族大部分仍存在；临时回合楼在 0.74 新增的防抖转正＋全历史恢复组合会误删已完成往事，已改为转正硬落盘复核、只清尾部单令牌中断楼。
- 父亲来电和正文失败按钮都不是批量删楼实现；失败重试只是可能触发旧恢复扫描器。高危根因已经在转正持久化与扫描范围两端封死。
- 任意时间推进的正文幕跨房泄漏、远程楼务竞态、楼务失败回滚重复、引导生成碰撞、角色头像／在场分叉、同场双人立绘比例、API 设置与三轴整框填充已落地修正。远程楼务和立绘问题在 rq0.73 单体实现中已存在，不应误报为拆分回归。
- 当前全量 `tests/人妻公寓/*.test.mjs` 为 131 文件、1076／1076 通过；生成所有者专项 8 文件、119／119。
- 定向 ESLint／六套 TypeScript／差异检查与正式生产构建通过；v0.78 角色卡 JSON／PNG 已组装，PNG 内嵌卡数据与 JSON 完全一致。
- 三轴、立绘与 v0.78 回合转正热修的最终观感仍待用户真实游戏验收。禁止原地改写已发布标签；后续版本仍须用户明确授权后才能正式构建 `dist/**`、组卡、commit、tag 或 push。

新任务只需先读本文件与 `.cursor/rules/破限.mdc`。不要预读 `CODEX_MEMORY.md`、历史交接、整份设计文档、已完成拆分方案或全仓源码；先根据当前需求定位真实所有者。

## 一、当前基线

- 工作区：`D:\SillyTavern\tavern_helper_template-consolidated`
- 当前工作分支：`release/rq090`
- 当前外部发布：`v0.90`／`rq0.90`
- 数据版本保持 9，支持 v0.80(v7) 与 v0.81/v0.82(v8) 一次性迁移，v0.83～v0.89(v9) 直接继续；更早、缺失、损坏或未来数据版本仍拒绝载入。
- 发布产物：`dist/人妻公寓/人妻公寓.png` 与 `dist/人妻公寓/人妻公寓.json`。
- 玩家只使用游戏内输入框；源码中的“原生路径”是酒馆内部备用生成事件链，不是第二个玩家输入入口。
- 数据库 SQLite 是唯一长期记忆路线；不恢复智脑兼容。

v0.74 已完成 `手机系统` P2–P8 与 `App.vue` A1–A8 拆分：

- `界面/客户端/App.vue` 现约 7,189 行／239 KB，保留布局骨架、跨区状态与事件总线接线。
- 界面已拆为 24 个 `components/*.vue` 与 5 个 `composables/*.ts`；普通界面 BUG 不应先整读 App。
- `脚本/游戏逻辑/手机系统.ts` 现为 6 行兼容门面；手机业务位于 `脚本/游戏逻辑/手机/` 的 27 个模块，最大模块约 807 行。
- 宿主游戏逻辑组合根继续从 `手机系统.ts` 导入；客户端 iframe 必须直连无副作用的手机子模块，禁止经兼容门面把宿主渲染组合根带入客户端包，也禁止把业务重新塞回门面。

## 二、源码路由

### 界面

- 设置、首次准备、序章、商店、背包、档案、地图、正文、输入、行动选项、静音会议和录像带：先查 `界面/客户端/components/` 对应组件。
- 主题／字号／全屏／移动端断点：`composables/useUIPrefs.ts`。
- 房间动作与地图动作：`composables/useRoomActions.ts`。
- 静音会议状态机：`composables/useMuteMeeting.ts`。
- 录像带状态机：`composables/useVideoTape.ts`。
- 素材、图标和共享类型：`assets.ts`、`icons.ts`、`types.ts`。
- 只有布局祖先、跨区共享状态、宿主事件订阅或组件接线问题才局部读取 `App.vue`。

### 手机

- 当前聊天、时钟与运行时引用：`手机/运行时上下文.ts`。
- 微信库、增量写、已读和分支数据：`手机/数据层.ts`。
- AI 请求、净化与验收：`手机/生成引擎.ts`。
- SQLite 摘要与胶囊：`手机/摘要系统.ts`。
- 节拍与水位：`手机/节拍引擎.ts`。
- 冷落、通知与静音会议旁路：对应的 `冷落预警.ts`、`通知桥.ts`、`静音会议旁路.ts`。
- 邀约、私聊、群聊和发送：`手机/交互/邀约与发消息.ts`。
- 父亲通话：`手机/交互/父亲通话.ts`。
- 注入、开合、渲染和会话瞬态：`手机/壳/`。
- UI 刷新必须走 `手机/UI刷新.ts` 注册表，避免业务层反向引用渲染层形成环。

### 回合与规则

- 主回合、失败清理、重掷和回档：`脚本/游戏逻辑/回合引擎.ts`，先 Grep 后局部读。
- 时间推进：`时间推进系统.ts`、`楼层时钟.ts` 与相关专项测试。
- MVU 写入或 `stat_data`：先读 `.cursor/rules/mvu变量框架.mdc`、`.cursor/rules/mvu角色卡.mdc`。
- 数据库调用：先查 `数据库桥.ts` 与具体失败阶段，不从 UI 表象猜测。

## 三、不可回退的运行规则

- 唯一世界钟是 `系统._绝对时段`；普通聊天和微信不自动推进时间。
- AI 不直接结算时间、精力、体力、满意度、现金、胜任度或风闻。
- 变量写权只开放给本轮精确演员；旁观者与后台住户保持只读或不进入提示。
- 正文模型只负责故事，不接收变量深度条目，也不输出或补算变量；变量只走 v0.85 外置解析。
- 亲密“过久”是达标后超过角色理想楼数上限的正面“超满意”，不得改成惩罚语义。
- 初始精力／体力为 `8／5`；体力五级上限为 `5／7／10／12／15`。
- 阶段剧情、特殊场景和任务只在有效正文成功落地后提交；失败、取消、错场、重掷与重复点击不得推进。
- 普通词语不得误开成人写权。
- 不恢复智脑路线；存档兼容仅保留经测试的 v7/v8→v9 迁移，不扩展到更早版本。

## 四、当前验证快照（2026-08-22，v0.86）

- 主仓发布标签 `tests/人妻公寓/*.test.mjs`：190 个测试文件、1705／1705 通过；本地另有 1 项 CG 发布资产验收通过。
- 产品文件 192／192、功能 244／244 全部闭合；累计真实 BUG 77 个，待修、待拍板与实机待验登记均为 0。
- 六名角色 62 张孕态 WebP 继续固定到 `rq0.82`；九张家庭计划 WebP 与 73 张生产／医院 WebP 继续由不可变标签 `rq0.83` 提供。
- 产品 TypeScript、Vue TypeScript、定向 ESLint 与 `git diff --check` 通过；正式生产构建与组卡均已完成。
- v0.85 角色卡为 13 条世界书（启用 8）、5 条正则、2 个脚本；PNG 内嵌卡数据与 JSON 逐字节一致。
- 卡内客户端、游戏逻辑与 MVU 代码入口统一锁定 `rq0.86`，不复用或改写旧发布标签；借种结局素材锁定独立 CG 仓库 `qgy-assets@cg4`。
- 自动化证据不等于真人游戏验收；发布后仍建议在真实 SillyTavern 中抽查 v0.84 旧档、数据库五表安装／更新、切聊天／回档、手机邀约、多人亲密、生产恢复和设置持久化。

## 五、v0.74 发布修复记录（2026-08-09）

v0.74 已按 `docs/BUG语义排查协议.md` 重新审查并完善本轮修复。每项先回答“设计初衷、正常运行、游戏作用、BUG 根因、修复影响与兼容性”，再追踪状态生命周期、全部生产者／消费者及失败、超时、取消、回档、并发路径；同逻辑族要全量审查，但未经逐项语义确认不得批量扫改。

### 已完成并有专项证据

- MVU 解析路由：数据库 AI 优先；无数据库时只走玩家配置的自定义 API；两者都没有则提示配置自定义 API，绝不回落正文 API。开始设置页已移除“正文输出变量”按钮，默认使用游戏的官方 MVU 外置解析入口。
- 父亲电话：通话硬状态、母亲线索等剧情结果原子提交；数据库记忆降为失败不阻塞的可选副作用；电话软余波不占用全局 `_待发送事件` 阻塞楼务。父亲电话不是“聊一句后必须再互动”的节奏门。
- BUG 排查方法已固化到 `docs/BUG语义排查协议.md`、`AGENTS.md` 与 `docs/worker-rules.md`。
- 数据库四张 `RQ_` 表已补逐列摘要定义、字数上限、枚举约束和 SQL 示例，禁止抄正文；数据库记忆胶囊的 8 行／2200 字符风险已明确。
- 微信未就绪的时钟兜底已由合法值 `0` 改为哨兵 `-1`，列表／会话／朋友圈统一放行未就绪态，已读锚在点击时取实时值。此项仍需按下面计划复审并发与回档边界，不能只凭旧结论宣布全部完成。
- 临时楼清理已改为按 `_rqgy回合令牌` 对象引用重新定位，避免楼号漂移误删已完成正文。此项仍需按下面计划复审失败／分支边界。

### 已完成：原生正文租约与时间动作误锁

设计目的不是“任何 AI 请求都锁游戏”，而是保护酒馆原生正文从 prompt、助手楼到 MVU 结算的事务，避免时间推进中途改写状态。旧 `_isInAiCycle` 被任意 `CHAT_COMPLETION_PROMPT_READY` 置真，手机／数据库／隔离 `generateRaw` 没有匹配正文结算，导致小憩等永久提示“正文模型仍在生成或结算”；真实原生 API 失败也只发 `GENERATION_ENDED`，旧逻辑不会释放。

当前落盘：

- 新增 `脚本/游戏逻辑/原生正文租约.ts`：宿主 `GENERATION_STARTED` 登记开始票，PROMPT_READY 按聊天、时间线、user 楼、消息引用与不可复用 owner 认领；旧 owner 不能释放新租约。
- `index.ts`：时间推进／撤销只查询有效正文租约；辅助 prompt 不认领；隔离／时间事务中的真实原生请求明确拒绝；`GENERATION_STOPPED(id)` 忽略 Tavern Helper 的非空生成 ID；`GENERATION_ENDED(messageCount)` 以精确预期助手楼判断失败或继续等待 MVU。
- `GENERATION_ENDED` 在开始票仍处于“等待 prompt”时，不再无条件作废当前票；只有整数 `messageCount` 精确等于该票预期助手楼层时才清票。旧轮结束事件、缺失／非整数／较大消息数不能清掉新票；等待票本身仍不形成时间锁，下一次真实开始可以替换旧等待票。
- 专项测试：`原生正文租约.test.mjs`、`原生变量时间线事务.test.mjs`、`时间独立生成隔离.test.mjs`、`时间按钮接线.test.mjs` 共 39／39 通过。
- 全仓 `vue-tsc --noEmit` 仍有公共 `TypeFest`／Web Bluetooth 声明和其他角色卡的大量既有错误；使用包含仓库宿主 `@types`、跳过第三方声明检查的《人妻公寓》定向配置复核后，只剩 `界面/客户端/App.vue` 两个既有 TS6133 未使用变量警告（`行动疑似性爱`、`当前房间动作`）。本轮 `脚本/游戏逻辑/index.ts` 与 `原生正文租约.ts` 无类型错误。未构建 `dist`。

第 4 项已收口。主代理已审查实际接线并复跑上述 39 项；`GENERATION_ENDED` 的可选 `消息数` 也已先按 `number` 收窄再比较，定向类型检查不再报本轮错误；`git diff --check` 目标文件无空白错误。DeepSeek 委派进程已正常退出，没有残留后台计费进程。规格与实现记录位于 `.delegate/fix-native-generation-lease-and-time-gate.md`，后续不要重复委派这一项。

### 已完成：第 5—10 项（不得重做）

5. 单独按五问复审 `隔离事件引擎.ts`：小憩不调用 AI；晨跑／健身／睡眠／监控／翻垃圾／荣耀洞等必须逐项确认剧情用途、生成提供方、上下文来源和提交边界。它们属于同逻辑族，但不得因一个入口有 BUG 就批量改成同一种行为。当前脏改把日常三类从数据库路由改到正文 `generateRaw`，未经本轮语义复审，不要先认定正确或回退。
6. 复审临时楼按 `_rqgy回合令牌` 对象引用清理的成功、失败、超时、取消、回档和分支切换；确认不会误删既有正文，也不会遗留临时楼。
7. 复审微信未就绪 `-1` 哨兵、清屏、未来时间线过滤、实时已读锚以及并发／回档兼容。
8. 复审数据库报错和调用超时为何会遗留“正文有内容在输出”状态，并复审记忆胶囊遇超长行 `break` 导致后续记忆静默丢失；数据库失败不得借用正文 API 兜底。
9. 复审跨角色强制剧情事件归属与软上下文污染，区分硬生命周期、强制剧情、软上下文和可选副作用，避免夏的证据错误污染沈等后续互动。
10. 跑相关全量行为回归与定向类型检查，汇总 v0.74 发布候选状态；该审查阶段尚不构建 `dist`、组卡、提交或推送，最终发布另走正式门禁。

第 5—10 项已于 2026-08-09 收口。v0.74 最终发布门禁为 123 个测试文件、1011／1011 通过，六组定向 TypeScript 配置、产品源码定向 ESLint 与 `git diff --check` 通过；全产品 ESLint 仅保留 rq0.73 已存在且本轮未改的 `AI表现文本安全.ts` 控制字符清洗规则基线错误。`pnpm build` 正式生产构建成功，角色卡自检为 13 个世界书条目（启用 8）、5 条正则、2 个脚本，PNG 内嵌卡数据与 JSON 完全一致，资源标签为 `rq0.74`。这份结果取代第四节 828 项与此前 952 项的旧验证快照。后续新窗口不得重复复审或重新委派第 1—10 项。

后续任务必须先以 `git status --short` 识别并保护用户已有脏改；无论由谁实现，都不得 reset、checkout、stash 或清理“无关”文件。是否委派以用户当前指令与 `AGENTS.md` 为准，不得续接已经结束的旧 DeepSeek／Claude 会话。

## 六、工作方法

1. 先运行 `git status --short`，保护用户已有脏改；不 reset、checkout、stash、commit、tag 或 push。
2. 以用户当前需求为最高优先级；未指定任务时才查 `待实现与重置清单_2026-08-03.md`，并先验证条目是否仍未完成。
3. 先复现，再按“源码路由”读取 1～2 个真实所有者；超过 80 KB 的文件只局部读取。
4. 新功能必须具备入口、成功提交、失败／取消／回档边界与专项测试；BUG 修复必须保留可复现回归测试。
5. 先跑专项测试与定向类型／静态检查，完成时再跑一次相关全量回归。
6. 普通验证不得写 `dist/**`；需要生产编译证据时使用隔离输出。
7. 手机或宿主 UI 改动需在真实浏览器／手机画幅复核；环境不具备时如实记录，不能写成已经实机通过。
8. 只有用户明确要求发布时才正式构建、组卡、commit、tag 或 push。

成人 CG 素材目录的生成、诊断、审核、分类、去重或接线任务必须使用 `$rqgy-adult-cg-workflow`。

## 七、v0.74 已完成：堕落值增长逻辑（2026-08-09）

### 玩家反馈与任务边界

玩家在 v0.73 强烈反馈：正常游玩中堕落值经常完全不增长，或增长速度明显慢到无法形成合理进度。v0.74 已按独立 BUG 完成语义排查与修复，没有用放大 `+1~3`、每日上限或降低晋阶门槛掩盖写权、守护与反馈问题。

### 已知源码事实（只作排查起点，不是根因结论）

- `schema.ts` 声明堕落值由亲密回合 AI 单轮写入 `±3`；`stageConfig.ts` 的晋阶门槛为 20／40／65／90。
- `回合引擎.ts` 已提示：实际暧昧／亲密／性内容应按人物反应更新堕落值；正文后还有尺度稽查。
- `mvuIO.ts` 现在采用“两段式权限”：解析候选视图先让本轮可写焦点妻看见堕落值与身体开发；稽查确认逐角色实际等级 ≥1 后才补精确亲密写权。旁观者、丈夫侧、实际等级 0 与拒绝兜底仍然关闭。
- 数据库代发或自定义解析模型、以及关闭游戏内置解析后的 MVU 官方外置桥，都消费同一份逐角色稽查结果，不再出现解析视图看不到堕落字段的死路。
- `守护系统.ts` 仍保留非亲密拍回、单轮 `±3`、冷落余波冻结、每日每妻 `+8`、阶段线路封顶与阶段底线；日账改按最终实际落地增量记录，被线路或日上限挡住的部分不再空耗额度。
- `冷落系统.ts` 会按世界时段结算下降与安抚余波；必须区分“本轮增长失败”和“增长后又被冷落结算抵消”。

### 排查结论与保留的完整生命周期门

1. **设计初衷**：哪些互动应涨、应不涨、应下降；单轮、每日、阶段线路与晋阶节奏各自想保护什么。
2. **正常链路**：玩家输入／强制剧情 → 亲密判定与尺度协议 → 正文 → 内置或外置变量解析 → raw 候选 → 守护 → 成长账／风闻／疑心结算 → 最终 `stat_data` → 客户端显示。逐步记录堕落值、`变量范围.亲密妻`、可写视图、解析变量块、守护前后值、`_堕落日账` 和线路封顶。
3. **游戏作用**：按真实门槛计算从各阶段正常晋阶所需的互动轮数与世界天数，判断玩家所说“十分缓慢”是否由多个本意合理的门叠加成不合理体验。
4. **BUG 根因**：区分至少五类——未开放写权、解析模型未写／写错路径、raw 被 Schema 或守护回滚、线路／日账／余波有意封顶、最终提交或显示读取旧值。
5. **影响与兼容**：任何修复都要覆盖游戏内置外置解析（数据库代发／自定义 API）与 MVU 官方外置桥、游戏内自定义输入与原生输入、重写稿、失败／超时／取消、重掷、回档与并发；只复现当前版本新局，不补旧存档迁移，不得扩大后台人物写权或绕开阶段线路。

### 最小复现矩阵与验收

- 路线：游戏内置外置解析（数据库可用／仅自定义 API）、关闭游戏内置解析后的 MVU 官方外置桥。
- 场景：显式亲密词、昵称／代词导致预判可能漏判、正文自然升级但输入不显式、稽查重写后实际等级 ≥1、普通闲聊对照。
- 状态：阶段 1—5、同日未满／已满 `+8`、线路未完成封顶／已完成、冷落余波无／诉苦待触发／安抚中、多人亲密主焦点与旁观者。
- 每格必须证明最终存档值，而不是只断言提示词或解析文本；合法 `+1/+2/+3` 应落地，超 `±3`、非亲密、后台人物和有意封顶仍应失败关闭。
- 先补能稳定复现玩家问题的行为测试，再决定是代码修复、节奏调参，还是两者拆成独立决策。未经语义证据不得批量调高收益或降低 20／40／65／90 门槛。

专项证据位于 `堕落值增长写权.test.mjs`、`堕落额度与可见反馈.test.mjs`、`变量守护.test.mjs`、`冷落系统.test.mjs` 与 `v080变量解析单路线.test.mjs`。后续若再出现“不涨”，必须区分字段可见性、精确补权、解析候选、守护落地、线路封顶、日额度、冷落余波及 UI 反馈，不得直接调门槛。

## 八、v0.90 发布后新任务开场模板

> 请先阅读 `src/人妻公寓/新窗口入口_精简.md`、`.cursor/rules/破限.mdc` 与 `docs/BUG语义排查协议.md`。当前外部发布为 v0.90／`rq0.90`，数据版本 9；支持 v0.80(v7) 与 v0.81/v0.82(v8) 迁移，v0.83～v0.89(v9) 直接继续。先检查脏工作树，再按用户当前需求定向定位真实所有者；多预设输出边界、移动端前台决策、完整提示词快照、微信刷新分支兼容和发布构建标记门禁均已落地，不要重复实现。不要读取历史交接全文、续接旧 DeepSeek 长会话或重做 App／手机拆分；只有用户明确要求正式发布时才写 `dist`、组卡、commit、tag 或 push。
