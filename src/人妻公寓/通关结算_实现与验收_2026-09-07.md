# 通关结算：实现与验收

日期：2026-09-07。开发基线：`483dc7a7`，任务分支：`task/settlement-celebration-20260907`。

六名角色对应五条结局线；沈静仪、周小满共享《录像带》。检测使用现有正式完成凭据，最后一名角色完成时记录世界时段T，从T+2起等待可操作的前台再展示。活动剧情、当前地点等待票、连续锁场、特殊场景、普通亲密和通话，以及生成、写入和前台弹窗都会让结算顺延；远处的未激活预约不阻断当前前台。

## 成绩与展示

- C/B/A随角色阶段与正式结局推进；全员完成获得S，探索分40获得SS，80获得SSS。
- 探索分：完成静音会议20分；按每位角色的不同已赠服饰计1分，上限40；每3张本局CG计1分，上限40。服饰与CG可以在通关后继续收集。
- `系统._通关纪念`保存起点、本局CG、首次成绩和已庆祝评级，沿用数据版本9，以默认值兼容旧档，跟随消息快照回档。
- 首次成绩保持原值；晋级展示当前成绩。关闭立即恢复操作，确认保存失败时在当前分支后台重试，不重复强弹同档。
- 右上角评级入口可以查看旅程进度、首次成绩和探索规则。桌面与手机使用同一个Vue组件；有键盘焦点管理、Escape关闭、减少动画支持和图片加载失败反馈。
- 普通CG在正文画面成功加载后登记；跨局浏览器收藏和图库全览不计入本局。旧档的CG从本功能接入后开始记录，已有剧情与服饰结果直接读取存档。

## 代码与素材

`通关纪念存档.ts`定义存档；`脚本/游戏逻辑/通关结算.ts`计算成绩与时段资格；`通关纪念服务.ts`使用现有MVU串行写入与提交校验。脚本签发权威时间线身份，客户端通过握手取得，避免两个iframe各自的模块变量被误当作共享状态。成功脚本写入与原生变量提交路径均同步完成起点。

界面位于`界面/客户端/components/通关结算.vue`，控制器位于`composables/useSettlement.ts`。批准的庆典CG以1672×941 WebP接入，336310字节，直接随界面内嵌；制作提示词与SHA记录位于`素材/结算/`。

## 验证

- 三个专项测试文件共60项通过：通关计算29、记录服务18、Vue控制器生命周期13。
- 游戏全量回归2482/2482通过；独立工作树以原目录的只读制作证据补齐素材校验依赖，未修改既有素材。
- Vue/TypeScript检查通过。定向ESLint无错误；App原有613行`v-show`属性排序警告保留。
- Playwright运行真实Vue组件、控制器和记录服务的隔离演示存档：首次关闭、首次/当前切换、晋级、保存失败后恢复、回档、T+2强剧情顺延及Escape均通过；1440×900和390×844画面已检查。
- 独立界面复审结论为ship，范围是提供的截图和新增界面源码。
- 真实客户端与游戏逻辑双入口生产模式隔离构建通过；禁用schema dump、监听、同步器和声明写回。正式`dist`的72文件在构建前后SHA、大小、mtime均不变。

验收证据位于`.codex-tmp/settlement-20260907/`，画面位于`output/playwright/settlement-20260907/`。本次未做正式构建、组卡、标签或推送；真实SillyTavern长存档全剧情流程仍由发布验收覆盖。
