# 《人妻公寓》新窗口交接：全量 BUG 审查续接至 244／244（2026-08-19）

> **本文件是当前续接真值。** 旧交接中“重新建台账、从 Schema 第 1 批开始”的开场步骤已经过时。新窗口必须在现有台账上继续，不能重头审、不能把已完成项重置为未开始。

## 一、唯一任务与完成定义

继续审查当前工作树中的《人妻公寓》，直到：

- 192／192 个产品 TS、Vue、YAML、JSON 文件全部闭合；
- 244／244 个功能生命周期全部闭合；
- 台账中不存在 `未开始`、`定位中`、`真实BUG待修`、`已修待回归`；
- `设计待拍板` 必须获得明确结论后才能清零；
- `实机待验` 若自动化环境不能覆盖，必须真实记录并由用户决定是否接受，不能伪装已验；
- 全量测试、定向 TypeScript、受影响 ESLint 与 `git diff --check` 通过。

“测试全绿”“看过主要文件”“找到很多 BUG”都不等于完成。最终必须以台账中 **244 条功能逐项闭合** 为准。

若一个新窗口仍未做完，必须更新：

```text
src/人妻公寓/全量BUG审查台账_2026-08-19.md
src/人妻公寓/新窗口交接_全量BUG逐文件逐功能审查_2026-08-19.md
```

然后再向下一窗口续接，不依赖聊天记忆。

---

## 二、当前仓库真值

- 项目：`D:\SillyTavern\tavern_helper_template-consolidated`
- DevSpace workspaceId：`ws_4604e7f059`（同一会话能复用时直接复用；新窗口重新连接即可）
- 分支：`release/rq083`
- HEAD：`2973cbf45375e6b99278e7356d72db267e7a33dc`
- 短 HEAD：`2973cbf4`
- 发布基线：`rq0.83`
- 数据版本：`9`
- 当前支持迁移：v0.80 数据版本 7、v0.81／v0.82 数据版本 8 → 数据版本 9
- 产品文件：192
- 测试文件：177
- 当前暂存区：0
- 当前工作树：约 129 个 tracked／其他改动，152 个 untracked

当前工作树极脏，**当前磁盘内容才是审查真值**。不能用 HEAD、旧标签、旧 dist、旧交接或发布说明覆盖现场代码。

### 最新完整门禁

```text
node --test tests/人妻公寓/*.test.mjs
1548 / 1548 passed
fail 0
skipped 0
todo 0
```

最新定向静态门禁：

```text
pnpm exec tsc -p .codex-tmp/tsconfig.rq082.json --noEmit
PASS

git diff --check
PASS
```

这些门禁是在当前产品修改后完成的。没有正式构建、组卡、commit、tag 或 push。

---

## 三、新窗口启动顺序

连接 DevSpace 并打开项目后，第一件事必须执行：

```bash
git status --short
```

随后确认：

```bash
git branch --show-current
git rev-parse HEAD
git diff --cached --name-only
```

依次读取：

1. 根目录 `AGENTS.md`
2. `.cursor/rules/破限.mdc`
3. `src/人妻公寓/新窗口入口_精简.md`
4. `docs/BUG语义排查协议.md`
5. `src/人妻公寓/新窗口交接_全量BUG逐文件逐功能审查_2026-08-19.md`
6. `src/人妻公寓/全量BUG审查台账_2026-08-19.md`

审 MVU、Schema、变量解析、世界书变量时，再定向读取：

```text
.cursor/rules/mvu变量框架.mdc
.cursor/rules/mvu角色卡.mdc
```

不要自动读取 `CODEX_MEMORY.md`，不要预读全部历史交接、全部设计文档或整个仓库。每进入一个功能域，只读取真实所有者、调用者和必要设计依据。

### 启动后禁止做的事

- 不重新创建台账；台账已经存在且已登记 192 文件、244 功能。
- 不从 P011 或 Schema 批次重新开始。
- 不因为当前全量测试为 1548／1548 就宣布完成。
- 不先跑正式构建。
- 不清理工作树。

---

## 四、绝对禁止事项

未经用户明确要求，禁止：

- `reset`
- `checkout`
- `stash`
- 清理 untracked
- 覆盖、删除或回退用户现有源码与素材
- 回退任何既有 `dist/**` 改动
- 整文件无脑 `prettier --write`
- 正式构建 `dist/**`
- 组卡
- commit
- tag
- push

允许：专项测试、全量测试、定向 TypeScript、ESLint、SFC 编译检查、只读 Git 检查和 `git diff --check`。

需要构建证据时只能使用不写正式 `dist/**` 的隔离输出，除非用户后续明确授权发布。

---

## 五、当前台账数字

台账：

```text
src/人妻公寓/全量BUG审查台账_2026-08-19.md
```

### 文件

| 状态 | 数量 |
|---|---:|
| 已完成 | 15 |
| 已审无 BUG | 14 |
| 定位中 | 5 |
| 未开始 | 158 |
| 真实 BUG 待修 | 0 |
| 合计 | 192 |

### 功能

| 状态 | 数量 |
|---|---:|
| 已完成 | 52 |
| 已审无 BUG | 6 |
| 定位中 | 2 |
| 未开始 | 184 |
| 已闭合合计 | **58／244** |

### 已发现问题

- 真实 BUG：25
- 测试过时：2
- 设计待拍板：0
- 严重度：P0 0、P1 18、P2 5、P3 2
- 台账登记新增测试：34
- 实机待验：0

后续发现真实 BUG 从 `BUG-026` 继续编号；测试过时从 `TEST-STALE-003` 继续编号。

---

## 六、当前仍为“定位中”的对象

### 产品文件

1. `F025` `src/人妻公寓/界面/客户端/App.vue`
   - 已审生成忙态、Dock、商店／背包／监控入口等子链。
   - 其余跨区状态、事件总线、正文幕和客户端总收口仍待第 8 批完成。

2. `F046` `src/人妻公寓/界面/客户端/components/设置弹窗.vue`
   - 已审变量解析开关与 MVU 自动请求自愈。
   - 其余 UI 偏好、重开、恢复和设置错误态待第 8 批完成。

3. `F073` `src/人妻公寓/脚本/游戏逻辑/index.ts`
   - 已审启动迁移、MVU 自愈、`安全操作`、部分业务入口和 CHAT_CHANGED 收口。
   - 组合根其余事件注册与调用链必须在第 2～7 批按功能继续闭合。

4. `F090` `src/人妻公寓/脚本/游戏逻辑/回合引擎.ts`
   - 已审主回合启动、忙态、生成／取消租约、流式选择、变量重生成、临时双楼、转正、失败回滚、回档、序章、重开、最终整表和父亲三方合并。
   - 其余确定性结算、重掷、swipe／删楼／切聊全链、角色近期正文和幕归属仍待继续。

5. `F108` `src/人妻公寓/脚本/游戏逻辑/手机/交互/邀约与发消息.ts`
   - 多人邀约子链已闭合。
   - 普通发送、引用、群聊和其余手机批次在第 5 批继续。

### 功能

- `P003` 序章与标题屏：定位中
- `P060` 快照创建：定位中

这 5 个文件和 2 个功能不能被遗忘，也不能因部分子链已有测试就提前标“已完成”。

---

## 七、已经闭合、不得从头重做或回退的范围

### 第 1 批 P011～P041 已全部闭合

包括：

- Schema、JSON Schema、initvar 契约
- v7／v8→v9 迁移
- 损坏／未来版本拒载
- MVU 解析模式、数据库／自定义解析路由
- `<UpdateVariable>`／JSON Patch 协议
- 候选净化、精确演员写权、亲密事后补权
- 守护、日账、阶段封顶
- 三方并发合并与 `stat_data` 原子写回
- 变量重新生成及事务门
- 数据库时间线栅栏与数据库 AI 租约
- 父亲通话写租约
- 生成通道互斥
- 时间线切换协调

### 第 2 批已经闭合的子链

- P046～P056
- P061～P063
- P068
- `AI表现文本安全.ts`
- `严格正文清洗.ts`
- `临时回合楼.ts`
- `原生正文租约.ts`
- `正文模型识别.ts`
- `正文生成完整性.ts`
- `预设桥.ts`
- `预设输出兼容.ts`

### 其他已经闭合的功能

- P099 数据库剧情规划辅助
- P119 父亲电话数据库记忆副作用
- P159～P167 多人邀约与共同赴约完整子链
- P241 数据库五表模板

### 已修 BUG-001～BUG-025

不要在新窗口逐项重做。需要联查时以台账五问和现有回归为现状：

- BUG-001～007：写权、数据库栅栏、MVU 并发／自动请求、Schema 默认值、损坏存档和布尔迁移。
- BUG-008～012：父亲三方合并、切聊 ABA、变量重生成损坏快照、邀约数据库幂等键、并发投诉单槽。
- BUG-013～017：残缺协议清洗、异常遗留临时楼、模型 getter、英文流式收句、Unicode／控制字符。
- BUG-018～019：数据库 API 层级发现与数据库版本候选作用域。
- BUG-020～023：临时楼完整 stat 继承、回档回调 CAS、失败 finally 动态世代、序章选项迟到写。
- BUG-024：主行动命中已有回合／时间线协调时静默返回，客户端永久卡 `发送中`。
- BUG-025：重开核心已经提交后，派生失败仍伪报失败并停留旧界面。

### 三项不可回退的产品语义

1. **场景剧情事务**
   - 远处结构化等待票不全局阻塞无关互动。
   - 到达目标地点才激活；一次只消费队首。
   - 业务已提交但正文失败时保留原票重试，不重复业务。
   - 活动事务按地点、事务 ID 和请求世代提交。

2. **多人邀约／多人亲密**
   - 单聊进入邀约，当前角色固定必选，可追加至最多五人。
   - 每人独立接受／拒绝，只有接受者共同赴约。
   - 赴约不等于同意亲密。
   - 多人亲密逐人尺度、逐人账本；CG、射精、受孕与收尾只跟当前主焦点。
   - `snapshotSystem.ts` 当前已为每名亲密焦点妻分别注入婚姻／人格边界，不能再拿主焦点规则覆盖其他人。相关反例在 `AI状态快照矩阵.test.mjs`，当前全量已通过。

3. **父亲电话频率**
   - 经营周期仍为 9 时段。
   - 普通例行电话配置 42 时段，经期界约 6～7.5 天一次。
   - 重要／紧急责任仍可即时来电。
   - 普通电话用压缩生活摘要，不逐条念未到期楼务。

---

## 八、新窗口第一阶段：必须先完成第 2 批

不能直接跳到手机、客户端、亲密或世界书。先把第 2 批剩余 14 个功能闭合。

### 待完成产品文件

```text
F073  src/人妻公寓/脚本/游戏逻辑/index.ts
F075  src/人妻公寓/脚本/游戏逻辑/snapshotSystem.ts
F090  src/人妻公寓/脚本/游戏逻辑/回合引擎.ts
F151  src/人妻公寓/脚本/游戏逻辑/打断系统.ts
F177  src/人妻公寓/脚本/游戏逻辑/结算系统.ts
F183  src/人妻公寓/脚本/游戏逻辑/角色近期正文.ts
```

必要消费者联查：

```text
F025  App.vue
F046  设置弹窗.vue
相关 tests/人妻公寓/*.test.mjs
```

### 待闭合功能

```text
P042 游戏内玩家输入
P043 行动选项提交
P044 酒馆原生输入备用链
P045 固定回合生成
P057 正文后变量解析接线
P058 稽查与角色尺度确认
P059 回合确定性结算
P060 快照创建（定位中）
P064 重掷与旧请求隔离
P065 swipe 分支切换
P066 删楼与楼号漂移
P067 切聊天中止旧回合
P069 角色近期正文记忆
P070 正文幕房间／角色归属
```

### 推荐执行顺序

1. **P042／P043**：从客户端 `发出()`／行动瓷砖，经事件总线到 `执行回合()`，检查乐观忙态、空输入、重复点击、过期选项、失败事件和最终解锁。
2. **P044／P045**：原生 normal 与系统固定回合，核对开始票、辅助请求隔离、变量结算、失败／停止和正式楼所有权。
3. **P057／P058／P059**：正文成功 → 外置变量 → 稽查 → 守护 → 资源／任务／亲密等确定性结算 → 最终整表，确认每个副作用唯一所有者。
4. **P060／P064～P068**：把快照创建彻底结案，再审重掷、swipe、删楼、切聊和回档；所有异步 `updateVariablesWith` 回调必须在真正写入前复核聊天／世代／楼／引用。
5. **P069／P070**：角色近期正文的角色隔离、容量、回档／删楼裁剪，以及客户端正文幕按原房间、原时间线显示。
6. 展开 `打断系统.ts`、`结算系统.ts` 的全部导出、关键私有函数、状态生产者／消费者和失败边界；不能因为专项测试已有覆盖就直接标完成。

第 2 批完成后：

- 更新所有相关 F／P 行；
- 把 F073、F090 或 P003、P060 中实际已闭合的部分结案；
- 跑本批专项、相关族回归、定向 TypeScript／ESLint、`git diff --check`；
- 再跑一次全量测试并记录新数字；
- 报告实际文件数、功能数、真实 BUG／测试过时／无问题分类。

---

## 九、第 2 批后必须按顺序继续，直到 244／244

### 第 3 批：P071～P099

场景剧情事务、入住、丈夫登门、侦探、隔离事件、睡眠／晨跑／健身、监控、翻垃圾、荣耀洞、录像带、静音会议及强剧情期间的手机／时间／特殊场景边界。P099 已闭合，其余逐项审。

### 第 4 批：P100～P121

唯一世界钟、时间推进／撤销、资源、周作息、管理任务、经济／收租、胜任、风闻、父亲电话频率／责任／UI、危机和坏结局。P119 已闭合。

### 第 5 批：P122～P167

手机 34 个模块：挂载、导航、会话、朋友圈、设置、数据层、分支隔离、已读、分页、格式／安全、生成引擎、租约、上下文、硬事实、摘要、通知、冷落、每日联系、好友、引用、撤回、压缩、私聊图、玩家发送、妻回复和群聊。P159～P167 多人邀约已闭合；F108 其余子链必须完成。

### 第 6 批：P168～P192

好感／堕落／婚姻／身体开发、满意度、阶段晋阶、24 条线路、阶段性癖、角色表现、冷落／诉苦／安抚、商店、送礼、换装、丈夫作息／疑心／打断、裂缝／线索／信件／调查、雌竞及六名角色差分。

### 第 7 批：P193～P216

单人／多人亲密 1～5 人、逐人同意／退出／账本、主焦点、成人 CG、射精／收尾、保护／体力／中止、受孕、怀孕隐藏／公开／评价／立绘、待产／住院／生产／产后／孩子／前三胎／再次受孕、姐妹群、73 张孕产素材和夏乔家庭计划。

成人 CG 素材目录、分类、去重、审核、移动或接线必须遵守 `$rqgy-adult-cg-workflow`；普通代码审查不能顺手批量改素材。

### 第 8 批：P217～P235

客户端 App、地图、房卡、操作抽屉、行动选项、输入、正文卷轴、商店／背包、档案、CG、设置、监控、录像带、静音会议、家庭计划、读信、首次准备、响应式布局、加载／空／错／取消态和迟到回调。

逐个闭合 45 个 TS／Vue 文件。能用 SFC 编译和行为测试验证的，不只肉眼看源码；真实浏览器不可用时如实标实机待验。

### 第 9 批：P236～P244

17 个 YAML、3 个 JSON、`stageConfig.ts` 与关键静态清单：世界书顺序、变量规则／输出格式、世界观／开场／主角、六户角色事实、数据库模板、成人 CG 清单、周作息／静音会议配置及技术标签泄露。P241 已闭合。

### 第 10 批：跨系统模拟与最终收口

至少覆盖：

- 新局 → 多户入住 → 中后期长期推进；
- 任务、经济、电话、冷落、阶段、亲密、怀孕并行；
- 多人邀约 → 多人同场 → 多人亲密 → 切焦点 → 收尾／受孕；
- 强剧情等待／失败／重试期间使用手机、时间和特殊场景；
- API busy、失败、超时、取消、刷新和迟到请求；
- 回档、重掷、swipe、删楼、切聊天；
- v7／v8 旧档迁移和损坏／未来档拒载；
- 无数据库、数据库失败、数据库可选副作用失败；
- 重要状态 Schema／JSON 往返；
- 测试文件与产品模块映射、源码字符串断言和过时测试最终清点。

---

## 十、每个文件／功能的审查纪律

严格执行 `docs/BUG语义排查协议.md` 五问：

1. 设计初衷是什么？
2. 正常应该怎样运行？
3. 对游戏有什么作用和影响？
4. BUG 为什么出现？
5. 修复影响谁，如何兼容？

每项必须实际检查：

- 导出函数、关键私有函数、模块级可变状态和副作用注册；
- 全部调用者、生产者和消费者；
- `eventEmit`／`eventOn` 是否成对；
- 创建、进行中、成功、清理、失败、超时、取消、刷新、重试；
- 回档、重掷、swipe、删楼、切聊天、A→B→A；
- 并发、迟到回调、重复点击和幂等键；
- 旧档、损坏值、默认值和失败关闭；
- 硬生命周期、强制剧情／确定性账目、软叙事上下文、可选副作用分层。

真实 BUG 必须：

1. 先补行为复现或可执行的紧范围复现；
2. 在台账写明五问、根因和影响链；
3. 做最小修复；
4. 加反例证明没有误删／误放行；
5. 跑同逻辑族回归；
6. 看实际 diff；
7. 更新统计和 BUG 编号。

不得只扫 TODO、只看测试、只做字符串 grep、只匹配源码形状，或用大重构掩盖根因。

---

## 十一、每批汇报格式

每批至少报告：

- 本批实际审查文件数；
- 本批实际闭合功能数；
- 台账累计 `已闭合功能／244`；
- 真实 BUG、测试过时、设计待拍板、无问题各多少；
- 已修 P0／P1／P2／P3；
- 新增测试数；
- 专项、同族和全量测试实际数字；
- TypeScript／ESLint／SFC／diff check 结果；
- 仍未审范围和实机待验。

发现真实 P0／P1 后，应先向用户说明现象、根因和影响链，再继续完成同批，不要只说“正在排查”。

为避免网页卡顿：

- 不回传完整 `git status`、完整测试日志或几十 KB diff；
- 只报告统计、失败项和最后约 10～20 行；
- 详细证据写入台账；
- 每完成一个真实批次再汇报，不对每个 grep 发进度。

---

## 十二、最终完成门

只有同时满足以下条件，才允许向用户说“全量审查完成”：

1. 文件台账：192／192 均为 `已完成` 或 `已审无BUG`。
2. 功能台账：244／244 均为 `已完成` 或 `已审无BUG`。
3. 所有 `定位中`、`未开始`、`真实BUG待修`、`已修待回归` 为 0。
4. 设计待拍板为 0，或已有用户明确结论并已落盘。
5. 所有 BUG 记录均有复现、根因、影响链、最小修复和回归证据。
6. 测试过时项已经更新且说明旧断言为何错误。
7. 最新全量 `tests/人妻公寓/*.test.mjs` 全绿。
8. 定向产品 TypeScript、受影响 ESLint／SFC 和 `git diff --check` 通过。
9. 没有未经授权写正式 dist、组卡、commit、tag 或 push。
10. 自动化无法覆盖的真实 UI／SillyTavern 行为已明确列为实机待验，不能隐瞒。

---

## 十三、可直接粘贴到新窗口的启动提示词

```text
请使用 DevSpace 继续处理本地项目：

D:\SillyTavern\tavern_helper_template-consolidated

这是《人妻公寓》全量 BUG 审查的续接任务，必须沿现有台账继续，直到 244／244 个功能和 192／192 个产品文件全部闭合。不要重新建台账，不要从 Schema 批次重头开始。

连接项目后，第一件事运行：

git status --short

然后确认分支、HEAD 与暂存区，并依次读取：

1. AGENTS.md
2. .cursor/rules/破限.mdc
3. src/人妻公寓/新窗口入口_精简.md
4. docs/BUG语义排查协议.md
5. src/人妻公寓/新窗口交接_全量BUG逐文件逐功能审查_2026-08-19.md
6. src/人妻公寓/全量BUG审查台账_2026-08-19.md

当前应为 release/rq083，HEAD 2973cbf4，发布基线 rq0.83，数据版本 9。工作树极脏，禁止 reset、checkout、stash、清理 untracked、覆盖或回退现有源码／素材／dist、整文件无脑 prettier --write、正式构建 dist、组卡、commit、tag、push。

当前台账真值：产品文件 192；已完成 15、已审无 BUG 14、定位中 5、未开始 158。功能 244；已完成 52、已审无 BUG 6、定位中 2、未开始 184，即已闭合 58／244。真实 BUG 25，测试过时 2，设计待拍板 0。最新全量测试 1548／1548，定向 TypeScript 与 git diff --check 通过。

先完成第 2 批剩余功能：P042-P045、P057-P060、P064-P067、P069-P070。重点闭合 index.ts、snapshotSystem.ts、回合引擎.ts、打断系统.ts、结算系统.ts、角色近期正文.ts，并按需联查 App.vue。P003 序章与标题屏、P060 快照创建仍为定位中。完成本批后更新台账、跑专项／同族／全量和静态门禁，再依次继续第 3～9 批及跨系统最终模拟，不能在 244／244 前宣称完成。

所有真实 BUG 都必须先补复现、写五问与影响链，再做最小修复；从 BUG-026 继续编号。每批报告实际审查文件数、闭合功能数、发现分类、测试数字和剩余范围。为避免网页卡顿，不输出完整日志，只输出统计与失败摘要；详细证据写入台账。
```
