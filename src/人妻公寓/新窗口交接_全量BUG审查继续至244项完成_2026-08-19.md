# 《人妻公寓》新窗口交接：当前 94／244，继续至全部闭合（2026-08-19）

> **这是当前最新交接真值。** 旧交接中的 `58／244`、`BUG-026`、`1548／1548` 已过时。新窗口必须沿现有台账继续，不重新建台账，不重做第 1、2 批。

## 1. 唯一任务与完成门

继续当前脏工作树中的《人妻公寓》全量 BUG 审查，直到同时满足：

- 192／192 个产品文件全部进入终态；
- 244／244 个功能全部进入终态；
- `未开始`、`定位中`、`真实BUG待修`、`已修待回归` 全部为 0；
- 每个真实 BUG 都有复现、五问、影响链、最小修复与回归证据；
- 全量测试、定向 TypeScript、Vue TypeScript、受影响 ESLint、`git diff --check` 通过；
- 自动化不能覆盖的真实宿主／浏览器行为如实登记 `实机待验`，不得伪装已验证。

测试全绿不等于全量审查完成。最终只能以台账中的 **192／192、244／244** 为准。

---

## 2. 仓库与现场真值

- 项目：`D:\SillyTavern\tavern_helper_template-consolidated`
- 分支：`release/rq083`
- HEAD：`2973cbf45375e6b99278e7356d72db267e7a33dc`
- 短 HEAD：`2973cbf4`
- 发布基线：`rq0.83`
- 数据版本：`9`
- 当前支持迁移：v0.80(v7)、v0.81／v0.82(v8) → v9
- 暂存区：0
- 本交接前工作树：133 个 tracked 改动、156 个 untracked

当前磁盘工作树才是源码真值。不得用 HEAD、旧标签、旧 `dist`、旧交接或历史说明覆盖现场内容。

### 绝对禁止

未经用户明确要求，禁止：

- `git reset`、`git checkout`、`git stash`；
- 清理 untracked；
- 覆盖、删除或回退现有源码、测试、素材、文档、`dist/**`；
- 整文件无脑 `prettier --write`；
- 正式构建 `dist/**`；
- 组卡、commit、tag、push。

允许专项／同族／全量测试、只读 Git 检查、定向 TypeScript／Vue TypeScript／ESLint／SFC 检查与 `git diff --check`。

---

## 3. 新窗口启动顺序

连接 DevSpace 后，第一件事必须运行：

```bash
git status --short
```

随后确认：

```bash
git branch --show-current
git rev-parse HEAD
git rev-parse --short=8 HEAD
git diff --cached --name-only
```

依次读取：

1. `AGENTS.md`
2. `.cursor/rules/破限.mdc`
3. `src/人妻公寓/新窗口入口_精简.md`
4. `docs/BUG语义排查协议.md`
5. `src/人妻公寓/新窗口交接_全量BUG审查继续至244项完成_2026-08-19.md`
6. `src/人妻公寓/全量BUG审查台账_2026-08-19.md`

原始总纲：

```text
src/人妻公寓/新窗口交接_全量BUG逐文件逐功能审查_2026-08-19.md
```

只在需要方法背景时定向读取；其中旧进度数字已过时。不要自动读取 `CODEX_MEMORY.md`，不要预读所有历史交接或整仓源码。

---

## 4. 当前进度真值

### 产品文件：50／192 已闭合

| 状态 | 数量 |
|---|---:|
| 已完成 | 23 |
| 已审无 BUG | 27 |
| 已闭合合计 | **50／192** |
| 定位中 | 3 |
| 未开始 | 139 |
| 真实 BUG 待修 | 0 |

当前仍为 `定位中` 的产品文件只有：

1. `F025` `src/人妻公寓/界面/客户端/App.vue`
   - 第 2 批涉及的序章、输入、移动事务门、正文幕、事件 CG 子链已闭合；整文件留第 8 批完成。
2. `F046` `src/人妻公寓/界面/客户端/components/设置弹窗.vue`
   - 变量解析／MVU 自愈子链已闭合；其余设置、重开、恢复和错误态留第 8 批。
3. `F108` `src/人妻公寓/脚本/游戏逻辑/手机/交互/邀约与发消息.ts`
   - 单人／多人邀约已闭合；普通发送、引用、群聊等留第 5 批。

### 功能：94／244 已闭合

| 状态 | 数量 |
|---|---:|
| 已完成 | 83 |
| 已审无 BUG | 11 |
| 已闭合合计 | **94／244** |
| 定位中 | 0 |
| 未开始 | 150 |

### 发现分类

| 分类 | 数量 |
|---|---:|
| 真实 BUG | 40 |
| P0／P1／P2／P3 | 0／27／11／2 |
| 测试过时 | 4 |
| 设计待拍板 | 0 |
| 新增测试 | 45 |
| 实机待验 | 0 |
| 当前测试文件 | 180 |

下一真实 BUG 从 `BUG-041` 编号；下一测试过时从 `TEST-STALE-005` 编号。

### 最新门禁

```text
node --test tests/人妻公寓/*.test.mjs
1563 / 1563 passed
fail 0
skipped 0
todo 0
```

最新还通过：

- 产品 TypeScript；
- Vue TypeScript；
- 受影响源码／测试定向 ESLint；
- `git diff --check`；
- 仅出现既有 CRLF 转换提示，无失败。

没有正式构建、组卡、提交或推送。

---

## 5. 已闭合、不得重头再审的范围

### 第 1 批

`P011～P041` 全部闭合：Schema、迁移、MVU、变量解析、协议、候选净化、精确写权、守护、原子写回、变量重生成、数据库栅栏／租约、父亲三方合并、生成互斥和时间线协调。

### 第 2 批

`P003`、`P042～P070` 全部闭合。此次续接实际审查 12 个产品文件、闭合 15 个此前未结项功能，修复 `BUG-029～BUG-040`，更新两项过时测试。

关键不可回退结论：

- 游戏输入与行动选项共享同一提交入口，移动写入期间不能按旧场景启动回合；
- 原生逃生舱与固定回合成功链都要写角色在场凭据并执行完整确定性结算；
- 外置解析完成门只认完整变量块或真实 `stat_data` 变化，外围元数据不能提前收口；
- 稽查的拍摄题材免词必须逐角色应用，不能替混合多人场景全局豁免；
- 失败／取消快照覆盖连续反感可能提前改写的共同邀约计划；
- 序章脚本心跳失效时不得进入发送态；
- 独立事件撤回删除正文后必须作废正文幕；
- 生产／家庭计划 CG 联合载荷类型与未消费计算值的 Vue TypeScript 问题已闭合。

### 第 3 批已闭合的核心范围

功能表已经闭合：

```text
P071～P086
P091
P093～P095
P098～P099
```

包括场景剧情票、远处票边界、到场激活、队首消费、事务冻结／重试、入住、丈夫登门、侦探、特殊场景策略、监控、荣耀洞、录像带、静音会议、隔离事务与数据库剧情规划核心。

其他已闭合：

```text
P119
P159～P167
P241
```

`BUG-001～BUG-040` 均已完成，详细五问与证据在台账；除非后续兄弟消费者提供新反证，不得回退既有语义。

---

## 6. 当前真正断点：先补齐第 3 批剩余 7 项

台账此前有一句“第 3 批已闭合、直接进入第 4 批”，但功能表与 94／244 统计证明这句话过时。已经修正台账批次说明。

以下 7 项仍为 `未开始`，必须先实际审查，不能仅凭已有测试直接改状态：

```text
P087 小憩
P088 公园晨跑
P089 健身
P090 睡眠
P092 翻垃圾
P096 强剧情期间手机边界
P097 强剧情期间时间动作边界
```

### 推荐收口顺序

1. `P087～P090、P097`
   - 联查 `时间推进系统.ts`、`时间撤销系统.ts`、`楼层时钟.ts`、`index.ts`、`App.vue`、玩家资源／经济／冷落／阶段线路消费者；
   - 逐项核对只读预检、唯一世界钟、脚本结算／独立睡眠演出、双存储事务、撤销、重复点击、失败、切聊、回档和强剧情远近票边界。
2. `P092`
   - 联查翻垃圾入口、场景地点复核、即时场景事务、背包收获、强事件、生成失败／重试和换房隔离；
   - 不能把收获或正文带到离开垃圾房后的其他地点。
3. `P096`
   - 联查活动票、同场等待票、未知旧票、远处结构票对手机打开、手动消息、自动节拍、父亲通话、群聊与特殊场景的分层限制；
   - 远处票不应全局锁死无关普通互动，但不能启动另一套互斥连场状态机。

完成后更新这 7 个 P 行、相关 F 行和批次记录，再进入第 4 批。若实际证据证明它们早已完整审过，也必须把函数、调用者、失败边界和测试证据补进台账后才可结案。

---

## 7. 随后进入第 4 批：P100～P121（P119 已闭合）

### 主要产品文件

```text
F107  手机/交互/父亲通话.ts
F115  手机/壳/渲染/call.ts
F122  手机/壳/渲染/talk.ts
F132  手机/父亲通话优先级.ts
F158  时间推进系统.ts
F159  时间撤销系统.ts
F161  楼层时钟.ts
F174  管理任务系统.ts
F175  管理任务通知.ts
F176  经济系统.ts
F178  胜任系统.ts
F191  风闻系统.ts
```

必要调用者／消费者：`index.ts`、`App.vue`、手机节拍／数据层、周作息、玩家资源、冷落、丈夫、生产、阶段线路与数据库桥。

### 必须闭合的语义

- P100 唯一世界钟；
- P101 手动时间推进；
- P102 普通正文／微信不自动推进时间；
- P103 时间撤销；
- P104 精力／体力消耗与恢复；
- P105 周作息与角色位置；
- P106～P108 管理任务生成、逾期、补办；
- P109～P110 经营、租金、欠租、上交缺口；
- P111～P112 胜任与风闻事实／责任；
- P113～P118 父亲例行／紧急电话、事实合并、未接责任、压缩摘要和硬状态提交；
- P120 父亲通话 UI／语音；
- P121 危机、最后通牒和坏结局。

既有产品语义不得回退：经营周期 9 时段；普通父亲例行联络约 42 时段、经期界约 6～7.5 天；重要／紧急责任可即时来电；普通电话不逐项念未到期楼务。

---

## 8. 后续批次

第 4 批完成后连续推进，不等待重新授权：

- 第 5 批 `P122～P167`：手机完整子系统，`P159～P167` 已闭合；
- 第 6 批 `P168～P192`：关系成长、阶段线路、冷落、商店、送礼、丈夫、调查与六户差分；
- 第 7 批 `P193～P216`：单人／多人亲密、CG、收尾／射精／受孕、怀孕、生产、孩子与家庭计划；
- 第 8 批 `P217～P235`：客户端全部 TS／Vue，收口 F025、F046；
- 第 9 批 `P236～P244`：YAML／JSON／静态表／数据库模板／资源清单；
- 最终跨系统长期模拟：新局至中后期、时间／任务／电话／冷落／阶段／亲密／怀孕并行，API busy／失败／取消／迟到，回档／重掷／swipe／删楼／切聊，旧档迁移与无数据库路径。

成人 CG 素材的生成、诊断、审核、分类、去重、移动或接线必须使用 `$rqgy-adult-cg-workflow`；普通代码审查不得顺手批量改素材。

---

## 9. 审查与修复纪律

严格执行 `docs/BUG语义排查协议.md` 五问：

1. 设计初衷；
2. 正常运行；
3. 对玩法／剧情／数值／时间／任务／记忆的作用；
4. 根因与完整影响链；
5. 修复影响、兼容入口、旧档和时间线边界。

每项必须查：导出函数、关键私有函数、模块级状态、副作用注册、全部生产者／消费者／调用者，以及成功、失败、超时、取消、重复点击、刷新、回档、重掷、swipe、删楼、切聊、并发、迟到与旧档。

真实 BUG 必须按顺序：

```text
补复现 → 写五问与影响链 → 最小修复 → 反例／同族回归 → 查看实际 diff → 更新台账
```

测试失败必须分类为真实 BUG、测试过时、设计待拍板、环境／夹具问题；不得为了旧源码形状断言回退正确产品语义。

---

## 10. 每批验证与汇报

每批完成后至少执行：

1. 新增复现专项；
2. 同逻辑族回归；
3. `node --test tests/人妻公寓/*.test.mjs`；
4. 产品 TypeScript／Vue TypeScript；
5. 受影响 ESLint／必要 SFC 编译检查；
6. `git diff --check`。

不得正式构建。

向用户只报告：实际审查文件数、闭合功能数、累计闭合数、分类、测试数字、静态门禁、剩余范围和失败摘要。不要输出完整 `git status`、完整测试日志或大段 diff；详细证据写入台账。

---

## 11. 可直接粘贴到新窗口的启动提示词

```text
请使用 DevSpace 继续处理本地项目：

D:\SillyTavern\tavern_helper_template-consolidated

这是《人妻公寓》全量 BUG 审查续接。必须沿现有台账继续，直到 244／244 个功能和 192／192 个产品文件全部闭合；不要重新建台账，不要从 Schema 或第 2 批重头开始。

连接后第一件事运行 git status --short，然后确认分支、HEAD、暂存区，并依次读取：
1. AGENTS.md
2. .cursor/rules/破限.mdc
3. src/人妻公寓/新窗口入口_精简.md
4. docs/BUG语义排查协议.md
5. src/人妻公寓/新窗口交接_全量BUG审查继续至244项完成_2026-08-19.md
6. src/人妻公寓/全量BUG审查台账_2026-08-19.md

当前应为 release/rq083，HEAD 2973cbf4，发布基线 rq0.83，数据版本 9，暂存区 0。工作树极脏，禁止 reset、checkout、stash、清理 untracked、覆盖或回退现有源码／素材／dist、整文件无脑 prettier --write、正式构建 dist、组卡、commit、tag、push。

当前权威进度：产品文件已完成 23、已审无 BUG 27、定位中 3、未开始 139，即闭合 50／192；功能已完成 83、已审无 BUG 11、未开始 150，即闭合 94／244。真实 BUG 40，测试过时 4，设计待拍板 0。下一 BUG 从 BUG-041，下一过时测试从 TEST-STALE-005。

最新全量测试 1563／1563，产品 TypeScript、Vue TypeScript、定向 ESLint 与 git diff --check 通过。

第 1、2 批不得重做。第 3 批核心已闭合，但功能表仍有 P087～P090、P092、P096～P097 共 7 项未开始。先实际闭合这 7 项并更新台账，再进入第 4 批 P100～P121（P119 已完成），随后连续推进第 5～9 批与最终跨系统模拟。不能因为批次说明或测试全绿跳过未开始行，也不能在 244／244 前宣称完成。

所有真实 BUG 先补复现、写五问与影响链，再做最小修复。每批报告实际审查文件数、闭合功能数、分类、测试数字和剩余范围。为避免网页卡顿，只输出统计和失败摘要，详细证据写入台账。未经明确要求，不正式构建、不组卡、不提交或推送。
```
