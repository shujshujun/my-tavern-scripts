# SNAP-03：主回合快照的跨场标志传递

本项标识n6j0；继续原`ws_0a48808756`、`task/unified-repair-20260907@c5943ef6`，暂存区为空。权威总账仍为《统一修复记录_最新补审_2026-09-07.md》。本轮033必要所有权／长度契约查询被拦截，已按第78节登记跳过；本项是独立快照接线，不补读或测试033被拒链，不重开全游戏审查。

## 1. 修前五问与证据

**设计初衷：**玩家当前行动的焦点与另一户的丈夫打断同时存在时，提示应保留当前主焦点，明确打断属于原夫妻、采用独立短镜头，不能替换人物身份或把远端丈夫搬进当前现场。

**正常运行：**真实`检测焦点`为四类丈夫打断计算可选`丈夫打断跨角色`，完整人物结果交给刷新规划与快照构造，生成前再按同一人物集合生成变量范围、头像与记忆。事件、同户对照、当前地点及现有演员校验保留；原生备用路径已经传完整对象，不一并判坏。

**玩法／状态影响：**当前缺陷是主回合输入丢字段，不是人物实体已传送、后台数值必然被改或模型每次必错。只修字段传递，不新增场景移动、不清队列、不扩大写权、不改变专属线路冻结规则。

**真实根因：**已读当前`回合引擎.ts`完整`组快照注入`，它只解构五个演员字段再重组对象，遗漏`丈夫打断跨角色`；`snapshotSystem.ts`的检测出口和快照对应消费者已读。主回合随后将返回快照加入`injects`，原生index则保持完整`本轮人物`。原始依据为主目录《快照提示词与特殊剧情全链路独立审查_2026-09-06.md》§4 SNAP-03和N01历史证据，未重跑或改写旧审计。

**兼容方案：**在主回合持有检测器返回的完整对象，解构只供原有局部使用；刷新规划和提示继续传该对象。不将可选字段另造为持久状态，不修改检测、权限、业务验收或原生备用入口。失败、取消、迟到及回档仍由既有事务有效性门处理，新增用例验证字段修正没有移除这些门。

## 2. 编辑前所有权

完整读取当前回合引擎和snapshot的HEAD差异：回合引擎已有日常来源锚、049二稿及手机预约镜像等混合改动，快照模块已有047交权投影；当前快照组装目标区间无既有修改。不得将整份HEAD差异认领为本轮。

开工SHA256：回合引擎`5c16c7be683602b2efe138790931f0a46911903cb92dda059c19829cff484f11`；snapshot`6e9fdc13a0b4f55999a3aa8fe89b5ec4b7a1f9104fc7a4c475d6486f77c3ea70`。已采样四份相关原测试，具体值在后续验收时比对；033被拒调用中的哈希不认领为成功采样。

## 3. 实施与验收

SNAP-03已新增`主回合跨场快照传递_SNAP03.test.mjs`，修前46项16通过／30失败，日志`snap03-n6j0-before.tap`；检查红测显示真实检测结果有跨场标志，主组装重建对象缺该字段。仅在`组快照注入`保留完整检测结果并解构局部变量，3增2删；同一46项修后全部通过，日志`snap03-n6j0-after.tap`。此为中间专项通过，尚待相关回归、隔离构建及最终所有权保护，暂不扣数。

## 4. 同机制独立项SNAP-04：修前五问与红测

**设计初衷：**焦点、次要事件演员与背景人物的叙事职责必须分开，同一个人不能既得到完整主角条目，又被要求最多一句掠影和禁止修改变量。

**正常运行：**普通事件优先事件焦点；四类跨户丈夫打断继续以当前玩家焦点为主；事件加入后保留原焦点与真正背景，实际妻／夫在场和私聊召回资格不变。活动场次已有参与者优先归一化，不应退回旧集合。

**作用和影响：**修正提示与头像中焦点／背景集合重叠，不删除原人物、不把背景全员升级、不改变变量写权或业务完成事实。仅关联人物继续不在当前位置，不能因同户身份为妻／夫互相授权。

**根因：**已读`检测焦点.应用事件演员`及最终焦点／背景输出。事件合并把原焦点同时加入`焦点`和`在场`，在场只排除事件焦点；空闲场次不会经过活动场次的去重，最终快照真实出现冲突。主路径03补传与本项04集合归属是不同根因，各自保留用例。

**兼容范围：**仅在事件合并出口按完整合并焦点过滤背景；保留焦点排序、妻／夫实际演员、私聊资格、关联角色、亲密账归一化和跨场标志。不改持久数据、事件生产或玩法门。同步投影无新网络请求，失败／取消沿用03组装事务门；重试／JSON重载及事件移除通过纯重算验证。

新`事件演员焦点背景互斥_SNAP04.test.mjs`修前33项5通过／28失败，0取消／跳过，日志`snap04-n6j0-before.tap`。覆盖五户地图／文本模式、普通事件／跨户打断、真正背景、仅关联妻夫、只在场丈夫、重复标签、活动账、无物理场景与重载；尚未修产品或签发04完成。snapshot开工SHA及047原混合差异沿用第2节已核结果。

## 5. 修后执行受限及后续独立静态补核

04随后已在事件演员合并处落4增2删补丁，合并后的焦点顺序保持原样，背景只保留不在合并焦点中的原背景。03补丁保持3增2删。

两个新专项最终联合命令被工具安全检查整体拦截，没有执行结果；没有拆开重跑或改脚本绕过。03此前46／46是04修改前的专项结果，不能冒充共享snapshot改变后的最终验收；04仍只有修前33项结果。**03／04均为已修补、待复审／待补验，不计完成。** 原红测、新用例和补丁完整保留。

独立静态检查取得：`tsconfig.snap03-04-n6j0.json`定向tsc exit 0、四文件ESLint exit 0、目标diff检查exit 0。后来独立SNAP-19的真实双入口隔离构建`build-1788798895594-387e52`也编译了当前两个依赖文件，成功且构建期正式dist／源码不变；这只补编译证据，不代替本组被拒的行为验收。

只读`verify-n6j0-owned-deltas.mjs`本轮实际执行通过：在内存中扣除本轮各自精确增量后，两个文件与第2节开工SHA完全一致，证明047、016、049、预约等旧混合修改未覆盖。四份开工采样的原测试亦与原SHA一致，没有运行这些测试绕过本组拦截。未对全体旧测试／helpers和全树源码声称整轮逐字节检查。

最终回合引擎SHA为`287586900b8fe2a87a6d18af26985637307d237e8d2fd1364f3fe50854020547`，snapshot为`a55597d5c57863aeadfca8c7ed6e3cc67076d9db325c8592540f8e7845be4fc1`。复审继续补最终两个专项及相关行为回归；源码若改变则按改变后版本重新验收。数量与后续项以统一总账第79／80节为准，本报告不另行扣数。

## 6. Codex补验收口（2026-09-08）

当前候选已在统一修复记录第91节补齐本项后续证据。保留03完整人物结果与04焦点/背景互斥两处原补丁，主代理已复核当前目标代码。两个原专项一次联合79/79通过，ESLint无错误（旧测试6条动态require警告），当前全源定向vue-tsc通过。复用同批`build-1788817283498-deda29`实际两端生产隔离构建，构建后源SHA仍与当前文件一致；正式dist各72文件及编译期受监控源码不变。没有重做两份产品补丁或重写以上历史被拦截记录。

本地代码验收完成，后续状态与计数只以统一记录第91节为准。日志` .codex-tmp/codex-takeover-20260908/snap03-04-final.tap`及`identity-snapshot-final-source.json`；全项目及发布门仍按其他原任务保留。
