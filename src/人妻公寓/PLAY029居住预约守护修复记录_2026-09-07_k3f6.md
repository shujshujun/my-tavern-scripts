# PLAY-029 居住／预约字段守护修复记录

日期：2026-09-07。继续原工作树 `ws_0a48808756`，开工实际核对 `task/unified-repair-20260907@c5943ef6`，暂存区为空；完整读取统一记录第69—70节，未见更后章节。当前计数55项／本地验收完成PLAY20项，039已计入，不重复抵扣。042及其他待复审状态保留。本轮仅针对029，不借此重取前轮被拒目标。

## 1. 修前五问

1. **设计初衷：**丈夫的居住模式及预约起止是路线脚本签发的现实状态，不是AI可自由修改的表现文字。已有关系和预约不能因普通变量候选丢字段而回到默认作息。
2. **正常运行：**脚本操作／正文入口捕获可信整表；Schema解析AI候选后，同一守护按真实演员写权接受合法心理／情绪，其余丈夫机械字段恢复可信值。路线脚本在守护后继续自己的确定性提交；回档／新聊天采用其新可信快照，不沿用未来预约。
3. **影响：**`丈夫在楼`、房间在场／作息、201线路外住／交接／退居、301预约及结局后到访、202承接门、丈夫风险和档案显示。不能把守护缺口扩大为每次普通回合必坏；需要错误候选前提。
4. **根因：**当前 `schema.ts`新增 `_居住模式`、`_预约回楼起`、`_预约回楼至`，但 `守护系统.ts`丈夫逐项机械恢复遗漏这三项。焦点内即使夫无写权，也仅先恢复心理／情绪，然后使用同一不完整恢复表。后台整户恢复正常。
5. **兼容方案：**只在既有丈夫机械恢复处补三个赋值，不扩大为全字段重构、不改Schema默认值／路线同步／时间规则／演员写权。增加按当前Schema丈夫键枚举的行为门，新增字段若未守护将被测试发现；保留正常文字更新、预约右开区间和所有现有脚本写入。

## 2. 当前证据与调用链

只读主目录取得原玩家报告PLAY-029（928—942行）、Codex第9节最终结论及最新逐项索引。候选已核目标守护及Schema的diff为空；已读完整守护公共入口和户字段恢复、可信快照捕获、丈夫Schema、`丈夫在楼`完整实现、原生index手动重处理完整分支，以及真实楼层写权读口。普通固定回合、原生回合及变量重生成均调用同一守护，重生成另传显式原回合快照。

字段读写定向检索覆盖游戏逻辑与客户端：生产者为201分居／离婚、301不必停／换掉及Schema旧档迁移；直接读者为楼层时钟、丈夫线路风险策略、202不再留门入口、201线路状态及配偶档案显示。最小补丁只恢复输入真值，不改这些生产者或消费者。进一步验证与局限在后续追加，不将原报告复现写成本轮实测。

## 3. 开工保护采样

- 守护系统SHA256：`55a6019db5c416f84ca10e31e626eea26fcef6b0ba9149208038fbc28ade70e7`。
- 原测试／helpers逐文件聚合：`57a08c1ad1cdea0b1ca4a683c4ed6d6660a24c6fcaf1dd49244a3fdbd35bec2a`。
- 正式dist逐文件聚合：`33077f117411824101955cb5447ebeef04a77760a080979c23b31253be17d999`。
- 原报告a4e6：`40c8e53036ff116edd32d4e536c04e774c9aed65540a4c17da8595005987ac5e`；c72d：`c4588d97f9d1114ea9a2e86a2bac43479bbd61d7a4bd566873c0587cb12bba01`。

聚合使用find逐文件排序后sha256sum列表再哈希；不与不同算法的历史聚合混比。不新建工作树、不清理混合差异、不暂存／提交／发布，不修改原报告或旧测试。

## 4. 实际修补与最终验证

产品仅`守护系统.ts`增加三字段恢复和一行说明，共4增0删。新建`tests/人妻公寓/丈夫居住预约守护_PLAY029.test.mjs`；原测试、helpers、Schema、路线生产者、原生入口与回合引擎均未修改。

修前同一184项专项：**20通过／164失败，0取消／跳过**；修后三赋值最终版本：**184／184通过**，分别保留`play029-k3f6-before.tap`和`play029-k3f6-after.tap`。范围包括201／301的六种居住模式、删除三叶子／覆盖／替换或删除丈夫对象、丈夫可写／仅妻可写／无人可写；另枚举六户当前Schema所有丈夫字段（除合法心理／情绪）验证守护，不只检查三个问题字段。后台、未提供焦点、旧调用缺raw、两种原始候选封套、旧档缺字段、显式快照优先、重复、回档重新捕获、后置合法脚本改约、301完成后的真实两线时间同步均有对照。

原生手动重处理测试通过AST提取当前index实际完整分支（包括无快照冷启动恢复），接真实Schema、守护、快照和`读取AI可写变量范围`；不把被测守护／权限读口替换为桩。宿主聊天变量与视图同步为明确内存适配。另验证他楼遗留权限不给本楼授权、视图失败后重试仍保留可信基底。直接消费者使用真实`丈夫在楼`／风险策略／配偶档案函数，对预约前、起点、窗口内、右端点及之后作对照。

最终相关15文件联合：**323／323通过，0失败／取消／跳过**，包含184，不相加；日志`play029-k3f6-related.tap`。其他组覆盖原守护、变量删除／写权、作息、丈夫线路保护、原生变量时间线／确定性结算、变量重生成／事务门、301两线／201作息以及配偶档案。测试中的模拟存储失败日志属于断言通过的失败路径，不计成测试失败。

定向类型配置`tsconfig.play029-k3f6.json`实际tsc退出0；ESLint两文件均0错误／0警告，`play029-k3f6-eslint.json`。实际生产模式双入口隔离构建`build-1788791005399-56157b/summary.json`成功：游戏逻辑0错误／3警告，客户端0错误／2警告，Node模块类型提示保留。真实生产转换保留，schema dump／同步／监听写回hook禁用，产物和声明均在隔离目录。构建期间主目录与候选受监控源码未变、正式dist各72文件逐字节不变。

## 5. 生命周期与验收边界

本补丁是同步字段恢复，没有新请求、计时器或持久写口，不改现有取消／超时／迟到的事务所有者。专项目测“已切换可信快照后旧候选”“显式原回合快照”“其他楼遗留权限”，相关回归覆盖实际事务门及失败／超时／回档；不冒称用此三赋值修好了所有并发写或真实宿主的整个模型请求流程。此项不修复已经失去可信快照的历史坏档，也不按完成ID猜测旧预约；旧档原本缺字段则以现有Schema迁移后的可信状态为准。

原生分支测试不运行完整index宿主初始化；视图失败是受控失败注入，非真实SillyTavern物理存盘。没有请求玩家模型／API，未扩展为真实宿主连续游玩。PLAY-041派生资格、042终验断点及其他原待复审项目不据本项通过关闭。

## 6. 最终差异、保护与结论

末次Git核对仍为`task/unified-repair-20260907@c5943ef6`，暂存区为空；`git diff --check`通过，完整产品diff已读并确认仅4增0删。最终守护SHA256：`f61fec778921c5c690f64d72dad9516650a41c5ddd0d2b2ad05c0268a20e5895`。本輪沒有安全拦截；当前验收不是引用历史通过。

排除本轮新测试后原测试／helpers聚合、正式dist聚合、a4e6／c72d原报告SHA均与本记录开工值一致；未改旧测试或降低断言。其余全树源码仅声明构建期间的实际采样保护，不夸大为整轮全树字节比对。

**PLAY-029本地代码验收完成，未提交／整合／发布。** 以统一记录第70节55项为起点只减029一次：**49项未闭环代码＋4项条件待证＋1项素材＝54个主要原编号，本地验收完成PLAY21项**。039不重复抵扣，022／025／033／038／040／041／042／046及其他旧状态保留。后续按阶段1原顺序核SNAP-25／26当前断点，先查最新记录，不重开全游戏审查。

## 7. 029完成后已继续的SNAP-25／26取证

已读取最新索引185—186行和主目录《全游戏提示词第八轮_睡眠提交与撤销恢复_2026-09-06.md》完整报告；保留其条件性故障前提、正常反例与S01／S02／S04／S06历史证据，不重跑旧审计或伪称本轮复现。

当前候选成功取得 `时间撤销系统.ts` 无未提交差异，读取1—125和280—530行，含时间推进恢复记录结构／读写校验、双存储提交与补偿及撤销点判定；当前index已读979—1021（启动恢复）、3058—3115（聊天补偿及时间入口开头）、3248—3476（推进准备、最终chat写入、双存储提交及回滚）、3476—3639（反向撤销及完成／失败处理）。因此本轮不再沿用第10节“这几段当前源码均未取得”的旧状态。

**SNAP-25当前定位：**最终chat updater在建立撤销点的同一回调内删除本次恢复记录；宿主应用后再抛错时，双存储控制器先恢复MVU，再调用要求同一记录仍存在的聊天恢复函数，确有补偿拒绝路径。修复不能只移除事务ID核验或将保存报错都认作成功，需用可验证的当前事务／最终提交状态及原聊天、分支身份保留精确补偿。

**SNAP-26当前定位：**反向撤销将推进后数据和聊天备份仅放在局部变量中，先恢复旧MVU，身份失效时catch正确拒绝跨线补偿；启动恢复只识别持久`_时间推进事务`，本逆向入口在首写前未建立对应恢复资料。需复用既有事务结构明确反向恢复身份和数据方向，保留手机双轴裁剪、玩家新消息使撤销点失效及原恢复键边界；不粗暴清历史，不把它与25同根因关闭。

两项当前仅完成上述定向取证，**没有新产品修改、新测试或新行为实测，不计闭环**，也未发生本轮新的工具拦截。下一实施从SNAP-25开始，先核index混合差异的目标区间所有权与现有时间专项，补“最终chat应用后抛错”的红测及同事务／异事务／切聊对照，再做最小补丁；26另建逆向中断和重启恢复用例，不能由25通过代验。019／022／042等已有index相关差异必须保留。本轮最终新增完成仍仅029，主要原编号54项。
