# PLAY-042 挂断请求寿命修复记录

日期：2026-09-07。继续 `ws_0a48808756` 原工作树，未新建或切换。实际开工 `task/unified-repair-20260907@c5943ef6`、暂存区为空。当前统一记录最新为第68节后的h8d4补记，没有发现已将039计入数量的后续章节；第68节56项应先仅扣上轮已完成039一次，得到50项未闭环代码＋4项条件待证＋1项素材＝55项，本地验收PLAY20项。此为补计上轮039，不算本轮新增修复。042尚待下述实际实施与验收。

## 修前五问及实际链路

1. 设计：父亲通话待回复序号是持久重试身份；挂断已成立后旧回复不再有消费者，不能继续占用手机生成槽。正常失败仍保留当前待回复，不能让取消变成吞掉有效会话。
2. 正常运行：刚接通／玩家应答建立同一通话ID下的待回复；小生成使用独立ID与共享手机租约，完成后提交端再次校验通话、序号、玩家原话、时间线和冻结事实。挂断先写收尾态与空待回复，完成消息后主入口原子归档清空通话。
3. 影响：普通正文与变量重生成读取手机租约，旧回复等待会在业务结束后继续阻挡；普通手机可嵌套令牌，不能描述为所有手机请求也不可用。机场视频不是同一个普通挂断UI，保留其独立状态机。
4. 根因：四个台词小生成调用只传允许场景剧情期间，未传当前通话寿命；await后防迟到写并不取消等待。生成层已有250ms轮询有效性的受控等待，但调用者没有使用。按ID停止失败还会回退全局停止，042不能因此误杀别的请求。
5. 兼容：通过共享父亲回复所有者传入只读当前ID／模式／序号／原话／聊天及时间线／本地请求身份校验，保留原硬事实变化后重试、失败重试和持久提交门。只对本请求停止采用按ID尽力停止；其他请求令牌不清空。数据库callAI无法按ID取消时只结束本地等待，真正数据库AI占用保护仍由原数据库租约持有，不放行仍有底层占用的数据库。

已完整读取父亲通话模块、生成引擎相关三路与受控等待、共享生成互斥、运行时数据源；读取index实际父亲通话结束回调4206—4280、MVU串行与提交校验实现、原父亲竞态及相关测试。原042及Codex证据只读引用，不重做全游戏审查，不修改原失败报告。

## 开工保护指纹

- 父亲通话.ts：`1df39222f869c15d2f6fd250db887f5c5e5a2a79138a83b2288c03a47ad7684a`，开工目标diff为空。
- 生成引擎.ts：`02119b13c5e3a4428ede5f5ab4134ac9a0d623da5ac9bdde34fc07dc66c98f29`，开工目标diff为空。
- 数据库桥.ts（保留039）：`ed3c26d6fce5701f815db75c4226e93c147eee5fa79e8b1bc8933842cd0ee198`。
- 原a4e6报告：`40c8e53036ff116edd32d4e536c04e774c9aed65540a4c17da8595005987ac5e`。
- 原c72d报告：`c4588d97f9d1114ea9a2e86a2bac43479bbd61d7a4bd566873c0587cb12bba01`。
- 全部开工tests/人妻公寓文件列表聚合：`11a84aa1054eccf2116384c579e3114bdde03011ec3ec1ff4c3edde1f671063c`。
- 候选正式dist文件列表聚合：`33077f117411824101955cb5447ebeef04a77760a080979c23b31253be17d999`。

列表聚合算法为find/sort/sha256sum管道，与构建器JSON聚合不同。仅声明实际采样范围，不能扩大为全部并行差异所有权。

## 实施与验收

### 本轮实际补丁

已修改 `手机/交互/父亲通话.ts` 与 `手机/生成引擎.ts`。父亲四个台词生产分支接收同一请求控制，按当前聊天、时间线世代、通话ID／模式、待回复序号与玩家原话以及不可复用本地对象令牌校验；挂断成功、被替换或其他实例已消费后取消本地等待。await后与排队取写锁后均重新验明请求身份。旧finally只释放自己的本地请求键，不清新请求；已取消回复不弹“信号断请重试”，有效提供方／保存失败仍保留原待回复。

生成层新增可选`仅停止本请求`：042按真实生成ID尽力停止，false／抛错／缺少停止API均不回退全局stopAllGeneration。其他未选择该控制的调用保留原停止兼容。数据库路径只用同一个受控等待结束手机本地等待，不调用不存在的数据库生成ID、不清真实数据库AI迟到租约。原通话剧情文案、父亲知情模块、已提交记录、Schema、计费与主入口归档均未编辑。

### 修前、最终行为和静态结果

新增 `tests/人妻公寓/父亲通话挂断请求寿命_PLAY042.test.mjs`，完整载入真实父亲控制器、小生成、解析／模式验收、手机数据层、生成互斥和受控等待；实际提取MVU串行队列／提交校验以及index父亲结束回调。外围宿主I/O、模型／插件返回、时钟、守护快照通知和无关线索／阶段奖励明确隔离。不是把目标解析器或验收函数替换为桩，也不冒称真实宿主保存与所有玩法奖励已测。

初版34项修前4通过／30失败（`play042-j2e5-before.tap`），首版补丁34／34（`play042-j2e5-after-1.tap`）。扩展至42项通过后，额外加入“旧结果已返回但仍排队，同ID重开”的新场景，实际43项42通过／1失败（`play042-j2e5-queued-before.tap`）；随后在写锁内追加原请求身份校验，最终**43／43**（`play042-j2e5-final.tap`）。以上失败用例和日志均保留，没有降低断言或使用中间版本结果代替最终版本。

覆盖普通语音／三种新旧家常路由、正文／自定义、首句／应答、未返回提供方、失败／迟到成功／迟到拒绝、挂断保存失败、重复恢复／挂断、切聊／世代／ID／模式／序号／原话变化、并行旧新模块、其他手机令牌、同ID重开、排队旧写、硬事实变化正常重试、仅关闭手机不取消、机场有效首句与固定告别分支、数据库不可取消占用。机场使用真实预约与接听函数，最终告别是明确的隔离检查点，不称完整CG流程。四分钟分支由受控虚拟时钟推进，不称实等四分钟；取消检查仍为原250ms轮询，不承诺后台浏览器无调度延迟。

最终相关组合**247项：246通过／1失败，0取消／跳过**（`play042-j2e5-related-final.tap`），包含43，不相加。唯一失败是原`生成通道互斥.test.mjs:302—318`的D1源码断言，仅检查未被042修改的`手机/交互/邀约与发消息.ts`字符串：期待`return;`，而既有034接受凭据补丁在1020—1023行返回`{ 已接受: false }`。该分支仍在写库前提示并返回。此静态断言不依赖042两个改动文件，不将其算作042行为回归；没有修改它或据此关闭034其他已挂账问题。中间79项78通过／1失败和246项245通过／1失败亦分别保留原日志，不替换成全绿。

最终定向tsc退出0（`tsconfig.play042-j2e5.json`），最后一次ESLint退出0（`play042-j2e5-eslint-final.json`）；之前已读的同范围ESLint为各0错误／0警告，最终JSON计数的末次补查受下述整条调用拦截，不能将之前计数冒充末次读回。`git diff --check`在最后补丁后已通过。

双入口隔离production构建 `build-1788787849868-024bb3/summary.json`成功：游戏逻辑0错误／3警告、客户端0错误／2警告。原schema dump／同步／监听写回hook禁用，产物及声明仅写隔离目录；构建期间主目录／候选受监控源码不变，正式dist各72文件前后不变。Node模块类型警告保留。此为构建期保护证明，不扩大成任务期所有并行差异已最终核对。

### 最终保护核对拦截与状态

最后一条组合bash请求包括：两产品最终git diff，目标及039／原a4e6／c72d的SHA，旧测试／helpers与dist聚合，分支／HEAD／暂存区及最终ESLint JSON计数。工具返回“因 OpenAI 无法确定请求的安全状态，已拦截此工具调用。”整条无结果，不认领其中子命令执行；没有拆分重跑、换工具或生成脚本绕取。

**PLAY-042：补丁已写，最终43项专项、相关行为、定向类型／ESLint和隔离构建取得上述证据；最终产品diff审阅／任务期保护哈希补核受限，标记待复审，不计完整闭环。** 复审断点是原样核对两文件最终差异与保护范围、读回最后ESLint计数；如发现补丁变化，需对变化后版本重新验收，不能用本轮结果替新版本签发。保留全部已写补丁、新测试及失败日志，按用户规则转下一独立项。

计数：本轮只补计上轮039一次；042不扣。以第68节56项为起点得到**50项未闭环代码＋4项条件待证＋1项素材＝55个主要原编号，本地验收完成PLAY20项**。原022／025／033／038／046等仍保持各自待复审状态，不重复重开或扣除。

## 下一独立项PLAY-040：来源与撤回证明链查询受限，未改产品

042登记后实际继续040。已从只读主目录的最新逐项索引及原P0报告第29节核实：合法原文压缩413→402后，个人摘要仍保留实际接收的话题，但同一句群追问由接受变拒绝；撤回与未接收者不能借修复取得知情。此为旧审查证据，本轮没有重跑旧Codex诊断或以原数字称本轮测试。

当前候选已读 `手机/姐妹群阶段验收.ts`81行完整模块、`手机/姐妹群已知事实.ts`完整模块、`手机/微信记忆上下文.ts`180行完整模块。当前生成读取`读取角色群聊见闻胶囊`，阶段解析只把`角色可知群消息`原文送给跨线门，后者的玩家提起仅扫描这些当前消息，代码方向仍与原报告相符。首次按原报告简称猜到根目录同名已知事实文件不存在，为普通路径错误；随后按实际import定位到手机目录并正常读取，不记为安全拦截。

随后一次组合bash查询摘要系统的逐人编译／来源／确认／压缩所有者、群解析完整消费者、原摘要／见闻／姐妹群测试以及已知事实模块的当前diff-stat时，被安全检查拦截，整体无结果。未拆分、換读口、调用旧脚本或用新增测试重取被拒目标。

**PLAY-040本轮产品0修改、新测试0、实际测试0，必要来源／撤回／分支证明链及所有权未核清，待复审，不扣数。** 不能直接把全群摘要或可读胶囊文本喂给所有成员放行，也不能把已压缩话题伪造成当前可引用原消息或公开事实。下一实施需核实际每人持久来源、已确认摘要、撤回与分支过滤，再把有限话题资格接给真实验收；保持引用仍只认当前可验证的原始消息。040不代替046署名契约或S08窗口预算。

本轮到此仅新增042产品补丁与43项专项，042因末次差异／保护补核受限仍待复审，040未改。上轮039计数补入一次后仍55个主要原编号、本地验收完成PLAY20项。下一独立候选转PLAY-041：按原报告六户正常回合／变量重生成差异，先定位`重算变量重生成派生`和原回合资格生产者；不使用当前后来阶段倒灌，不重修已完成的冷落接管。041在本段写入时尚未读源码；其后取得的证据另见下一节，不改写先前阶段记录。

## PLAY-041独立续查：原回合票据未存线路资格，混合差异／消费者补核受限

继续只读主目录P0报告1269—1280及当前候选。已实际读取：

- `变量重新生成核心.ts`357行完整文件，当前diff-stat为空；重算只从每户派生前快照调用无线路参数的成长／单妻冷落函数。
- `回合引擎.ts:3110—3274`原票据生产和正常结算链；票据在脚本剧情提交后、成长结算前冻结妻快照和母亲入列／微信保护，但不保存该时刻线路资格。普通结算却使用整个newStat。当前回合引擎完整未提交diff已读取，含既有016、049及预约镜像等差异，本轮未编辑、未认领。
- `回合引擎.ts:1250—1404`变量重生成真实消费与成套持久化链：重算在实际写入之前执行，失败可由外层收口，不需要通过清旧账或写后补偿掩盖资格缺失。
- `冷落系统.ts:1—125、250—504`实际窄类型、成长、冻结与全楼冷落：线路接管时保留历史余波，母亲正式共居有单独清遗留余波规则，住院有计时校准规则，不能把三个分支粗暴等价成同一个全局关闭。
- `丈夫线路风险策略.ts`完整窄事实类型及六户实际接管判定。资格依阶段／已完成凭据／真实使用及居住安排，不是仅按当前人物数值猜测。

根因方向已获当前源码支持：原票据的妻快照不足以重放普通回合当时真正使用的路线判定。后续应冻结原回合有限资格或可重放窄事实，并复用相同派生语义；不能使用重生成时后来开线／共居／住院事实倒灌旧楼。旧无字段票据须明确可证明的兼容策略，不能全部假定未开线、全部已接管或从结果推造新剧情完成ID。

随后组合bash查询冷落系统现有完整diff、原变量重生成测试中票据／候选断言、全部票据／重算消费者及入口错误提示位置时，被安全检查拦截，整体无结果，没有拆分重取、改运行器或用新测试打印被拒源码。**041产品0修改、新测试0、实际测试0；必要混合所有权与完整消费者／旧票兼容证据未齐，待复审，不扣数。** 原报告六户100→90仍是旧审查数据，本轮不冒称重新实测。冷落系统原030补丁和回合引擎混合差异继续保留。

最终本轮状态：042实改保留，43／43及247项246通过／1既有静态断言失败证据保留，042末次diff／任务期保护补核待复审；040与041仅续查，均未改产品。补计上轮039一次后仍为**50项未闭环代码＋4项条件待证＋1项素材＝55项；本地验收完成PLAY20项**。未完成项不减数。下一独立候选为原计划PLAY-029的脚本所有居住模式／预约字段写保护，仍需先按原号取得当前证据和差异，不以其验证替代本轮被拒查询。
