# 时间事务与阶段现场接续修复

> **当前状态更正（2026-09-08）：**用户明确实际宿主为TauriTavern，MVU由第三方作者维护，游戏必须兼容其他玩家使用的原版框架。下文MVU改版与配套通过结果只保留为历史实验，不能作为公共分发方案或SNAP25/26完成证明。游戏新增的自定义终写接口接线已移除，对应框架补丁/测试/构建包归档至 `.codex-tmp/repair-20260908-time-stage/mvu-experiment/`。原有游戏内时间恢复保护保留，PLAY037保留。SNAP25/26重新列为兼容缺口待复查，最新总账以§124为准。

用户本轮解除SNAP25、SNAP26、PLAY037暂停，并明确同意将MVU依赖补丁纳入本地修复；仍只隔离构建，不替换玩家脚本或发布。候选保持 `task/unified-repair-20260907@394817a5` 完整混合工作树。

## SNAP25、SNAP26

SNAP25处理时间推进失败时的半完成状态；SNAP26处理撤销时间失败时的半完成状态。此前已经实现正向/反向恢复记录、身份校验、补偿、镜像和普通写入隔离。本次开工的六份现有专项75/75通过，没有重写这些已经生效的机制。

剩余根因在外部MVU：`handleVariablesInMessage`等待`BEFORE_MESSAGE_UPDATE`后才调用变量updater；无变量变更或用户楼又不触发该事件。时间操作在此间完成后，旧候选仍可能写回旧世界钟和数值。使用完整原样公共函数的新9项测试，修前4通过5失败，日志为 `final-write-before`。

配套MVU补丁新增同步终写校验注册事件，逐个变量updater及正文写回前执行。游戏侧完整index安装监听，回调复用当前`mvuIO`候选身份校验。函数保留在本次调用闭包中，持久数据仍为普通可序列化对象；不修改玩家变量结构，不创建第二套时间真值。

新增14项最终通过：写入前事件之后、真实updater之前、chat兼容镜像与message两次写入之间、无变更/用户楼、合法普通提交、正文写回前、候选序列化、完整游戏index注册器及脚本中途重载。未观察到该次解析开始时，拒绝给旧候选补认新身份；这两项边界修前均失败，修后通过。已经先提交的合法变量在后续时间操作中保留。原9项红证、原外部函数及MIT许可证均保留；新版函数从配套补丁产物提取，未用目标验收恒定成功替身。

MVU源版本为 `b42817925d0391c15fa242a8238d2bbe28eb6319`，源码补丁见 `patches/mvu-time-final-write/`。在独立完整源码副本上实际应用补丁后，生产模式构建 `mvu-build-1788866904095` 成功，0错误/0警告；bundle.js为143246字节，SHA256为 `e91da4158832b746f3aa50cd23c587ae7603e17e41f4969b251a128acc3ac7d1`。原MVU源码及artifact不变。

**这是游戏+MVU配套候选的本地通过。** 旧运行MVU没有新终写事件，应用游戏单侧改动不能补全该窗口；本轮没有替换运行脚本。正式联合部署与真实宿主版本验证保留。

## PLAY037

该号是攻略阶段的地点剧情及晋阶口径问题。102沈静仪的“入户检修、她知道镜头却没有要求拆掉”属于阶段3→4的第二节点；原代码允许真实自然同场，但剧情票始终宣称固定周历预约已生效。部分最终节点已正常演完后，晋阶门又要求再等固定星期。

依据《阶段攻略线路设计》关于固定时段及错过后继续开放的规则，保留每个节点原有最低等待、允许时段、地点匹配、妻子真实位置和丈夫状态。周历仍在指定格提供到场保证。修复只对齐消费者：

- 周历窗口内生成原预约说明；自然同场时生成当前时段/地点说明。
- 晋阶沿用最后节点的允许时段及原地点/人物/丈夫条件，消除额外精确星期门。
- 档案里的周历标签改为“保证到场”。母亲早餐两幕专用现场门保留，未清旧档完成位或补造进度。

新增43项，修前20通过23失败。修后完整遍历40个地点节点×84时段，核候选/产票/提交/重复及最终节点的晋阶一致性；另覆盖102检修、102最终节点及非自然到场的管理员室预约，旧档JSON重载也保留新的一致口径。修前各项遇首错停止，不能把23个失败说成3360个组合全失败。

## 验证与状态

阶段专项与相关组合139/139；时间/阶段/MVU相关联合580/580。游戏生产类型与定向lint通过。两处既有源码形状测试依赖旧注释或已被受控写口取代的局部变量，已保留保护目标并改为核实际启动顺序、反向恢复记录与补偿写口；原失败日志保留。

游戏双入口隔离生产构建 `build-1788867198721-c89b85` 成功，0错误、3/2警告；主目录与候选正式dist各72文件SHA不变，schema dump/监听/同步/正式声明回写禁用。所有日志、开工原文件、SHA、源码副本和配套包在 `.codex-tmp/repair-20260908-time-stage/`。

本轮三项本地配套修复完成。按此前暂行6项口径，剩 **PLAY026、PLAY043、PLAY050**；前两个仍需对应真实宿主条件证据，最后一项待孕态素材。这里的PLAY026不是本次修复的SNAP26。真实宿主/在线模型/真机及统一发布仍独立待验收，未提交或发布。
