export interface 朋友圈隐私记录 {
  /** `仅你可见` 标记；值损坏为 null 等形状时仍按隐私优先处理。 */
  私?: unknown;
}

/**
 * 只有完全没有私密标记的公开动态，才允许展示邻居点赞和评论。
 * `仅你可见` 的语义是发布者只向玩家本人开放，其他住户不可能看见，更不可能留下互动。
 */
export function 朋友圈允许公开互动(动态: 朋友圈隐私记录): boolean {
  return 动态.私 === undefined;
}
