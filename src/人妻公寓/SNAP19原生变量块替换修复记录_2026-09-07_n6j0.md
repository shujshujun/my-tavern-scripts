# SNAP-19：原生非外置兼容变量块的替换边界

沿用统一修复任务日期2026-09-07，实施标识n6j0。继续原修复树`ws_0a48808756`、`task/unified-repair-20260907@c5943ef6`。本项为第79节之后的独立兼容修复，不借此重跑被拒的03／04行为，也不处理033数据层。

## 1. 修前五问

**设计初衷：**仅保留的原生非外置兼容入口，把世界书已经展开的旧变量视图替换为本轮精确演员视图。受支持的默认外置路线不走此函数，不能报告为所有玩家正常路线必坏。

**正常运行：**在system字符串消息中的旧`status_current_variable`块换入当前实际可写视图，保留块前后其它指令及其他角色消息；原本没有块时追加一份当前视图。合法字段中的美元符号只是文字，不能参与JavaScript替换模板展开。妻／夫／只读及候选视图权限仍由现有构造函数决定。

**影响：**旧实现将JSON字符串直接作为replace替换值，`$&`／前后缀替换符可能把旧户内容带回；旧块只有开始无结束时，根本未匹配却把`已替换`置真，造成当前视图缺失。问题限请求字符串，不代表守护允许越权，也不代表默认外置模式失效。

**真实根因及取证：**已读主目录第五轮原报告§3和当前`覆盖原生本轮变量视图`、非外置调用门及错误后缀，当前函数仍有上述两问题；已完整核index混合diff，现有日常锚及Persona回调不在目标区间。已读真实`构造AI可写变量视图`与原提示负担／写权接线测试，不改其过滤或授权规则。

**最小修复和兼容边界：**以替换回调返回当前块，只有实际匹配时登记已替换。完整旧块仍以结束标签为边界；开始标签未闭合时，按该标签仍打开的结构语义，将同一system消息中从它开始到消息尾的残块替换为完整当前块。保留其之前的前缀及其他消息；不猜测残块内部的文字何时已经“出块”，不对世界书原文件／存档做修复，不删整条system消息或整个请求。重复完整块逐个替换，混合完整＋残块分别处理；非system和非字符串消息保持原样。

此策略不新建解析引擎、状态、网络请求或持久票。重试是同一字符串变换的幂等重算；换聊天／回档时以调用传入的当前data与范围重新生成，不记跨请求缓存。最终生成／宿主持久保存不是本兼容纯函数验收范围，既有外置门及守护仍保留。

## 2. 实施和验证

唯一产品修改为`src/人妻公寓/脚本/游戏逻辑/index.ts`内的兼容替换函数，**6增2删**（包含两行说明）。原完整块替换改为返回当前块的回调，未闭合块处理到本条消息尾，`已替换`仅在实际匹配回调中置真。原生非外置调用门、原Actor范围、Persona字面替换和日常来源锚保持原样。本轮没有修改世界书、Schema、素材或手机数据层。

新增`tests/人妻公寓/原生变量块字面替换_SNAP19.test.mjs`，直接AST提取完整当前生产函数，连接真实Schema／当前可写视图构造及候选范围；另提取实际非外置调用门执行正反例，没有把被测替换器或权限构造替换为桩。测试不运行完整index初始化、不连接玩家宿主／模型，不代验033或03／04被拒的执行范围。

本轮实际结果：

| 检查 | 结果 | 证据（均位于`.codex-tmp/unified-repair-20260907/`） |
|---|---|---|
| 修前新专项 | 58项：18通过／40失败，0取消／跳过 | `snap19-n6j0-before.tap` |
| 修后同一专项 | 58／58通过，0取消／跳过 | `snap19-n6j0-after.tap` |
| 五文件相关联合 | 83／83通过，含上述58项，0取消／跳过 | `snap19-n6j0-related.tap` |
| 定向类型检查 | exit 0；改用本轮隔离声明后再次exit 0 | `tsconfig.snap19-n6j0.json` |
| index及新增专项ESLint | 0错误／0警告 | `snap19-n6j0-eslint.json` |
| 最终diff空白检查 | exit 0 | 本轮`git diff --check` |
| 双入口隔离生产构建 | success true；游戏逻辑0错误3警告，客户端0错误2警告 | `build-1788798895594-387e52/summary.json` |
| 精确增量所有权保护 | 通过 | `verify-n6j0-owned-deltas.mjs` |

58项包括九种普通／美元符号／Unicode／JSON转义值与五种完整／重复／缺失／未闭合／混合块形状的45例，六种妻／夫／同场／只读／候选权限对照，以及多消息尾部残块、非system／多模态消息保持、重复幂等、当前数据重入、视图构造失败后重试、真实外置门等边界。原缺块追加行为仍保留；坏模板的残块处理按第1节明确的标签边界，不声称能恢复作者原本缺失的结束位置。

相关联合完整运行新专项及原`变量块标准协议.test.mjs`、`v080变量解析单路线.test.mjs`、`原生快照Persona字面替换_CODE_IDENTITY_001.test.mjs`、`变量契约对齐.test.mjs`。没有修改旧测试／helpers、降低断言或跳过失败；83包含58，不能相加成141。原红测日志保留，修后通过不是删除失败样本取得的。

没有新增网络／时钟等待，取消／超时和宿主请求寿命不由这个同步替换函数负责；测试仅覆盖明确传入的当前请求数据与重试，不冒称服务端持久保存或真实游戏连续游玩。默认外置模式的实际调用门对照没有进入替换函数。

## 3. 隔离构建与最终所有权

复用已经审阅的`isolated-build.mjs`，生产入口、loader和压缩保持当前webpack配置；创建编译器之前禁用schema dump、宿主监听及同步器，声明和产物只写`build-1788798895594-387e52`。Node模块类型提示和五条构建警告保留，不能写成零警告。

构建期间主目录和候选受监控源码、正式dist均保持不变。主目录正式dist为72文件／19484503字节，候选为72文件／19486508字节。这里是构建期采样，不冒称整个任务期全树哈希验证。

最终完整index diff已读；只读所有权程序在内存中扣除本次6增2删后，index还原出的SHA与本轮之前实际混合版本一致：`243a3a2c8dcb5e56d5b1894b8925af1ff752323b0350237449dcccee6a2416e1`。文件没有实际回退或重写；既有日常来源锚和Persona补丁逐字节保留。修后index SHA为`9397d8e4e523c402384267990a5b3457195d25bb577a47d8b36677bbc5cfb2fe`。

同一静态程序也核对了本轮03／04的精确增量和四份修前采样的原测试，均保持各自原差异及字节；没有运行03／04被拒测试。原a4e6／c72d报告未编辑；本轮初始033组合哈希请求受限，故不宣称这两报告或全部旧测试／helpers完成了整轮首尾哈希。未改的旧证据仍保留。

末次Git核对仍为`task/unified-repair-20260907@c5943ef6`、暂存区为空。未暂存、提交、正式构建dist、组卡、tag或push。

## 4. 结论与后续断点

**SNAP-19本地代码验收完成，尚未整合／发布。** 本项只是低优先级非外置兼容替换边界，不冒称修复当前默认外置路线的所有提示问题，也不等同旧CODE-IDENTITY编号再完成一次。

03／04的补丁及静态／编译／所有权证据保留，但最终联合行为被拦截，继续待复审／待补验，不因本次构建或19专项通过关闭。033未写产品补丁，仍待必要数据层取证；SNAP-25／26及022／042等待复审状态不变。

本轮仅扣SNAP-19一个原编号：**46项未闭环代码＋4项条件待证＋1项素材＝51个主要原编号；本地验收完成PLAY仍为23项**。后续独立断点为SNAP-18的内置变量解析字段说明与真实可写视图一致性；先核当前说明、实际权限与出厂规则，不扩大字段写权、不借此重跑被拒任务。权威状态只追加到统一记录第80节。
