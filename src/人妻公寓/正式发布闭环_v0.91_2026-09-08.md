# 《人妻公寓》v0.91 正式发布闭环

- 闭环日期：2026-09-08（Asia/Taipei）
- 结论：**正式发布闭环完成**
- 游戏版本：`0.91`
- 正式标签：`rq0.91`
- 正式发布页：https://github.com/shujshujun/my-tavern-scripts/releases/tag/rq0.91

本记录只核对已经发布的不可变对象、下载件、运行时素材与旧版更新检测。`rq0.91` 标签保持冻结；本记录、待办核销和补充测试作为标签后的文档／测试维护提交推进 `release/rq091`，不移动或重打正式标签。

## 1. 游戏仓库与正式标签

- 远端仓库：`shujshujun/my-tavern-scripts`
- `rq0.91` 为 annotated tag。
- 标签对象：`26c481b900e873895292a2b103d43849e2dbea53`
- 标签指向提交：`7e03a238de8d541b502bb956e1e8d90003f8a8dd`
- 提交标题：`build(release): package rq0.91`
- `release/rq091` 在闭环开始时与该提交一致；标签对象及其目标提交已由本地与远端 `ls-remote` 双向核对。

## 2. cg5／cg6 素材闭包

素材仓库：`shujun8520-design/qgy-assets`

| 标签 | 标签对象 | 提交 | 发布清单 | WebP |
| --- | --- | --- | --- | ---: |
| `cg5` | `4e5529c44df889ff4700d43f79d4f06ee6664d7e` | `80b2f0cacf7cebe9175c9f159eb929c21cb80b1f` | `rq091/manifest.json` | 350 |
| `cg6` | `914e934e53c082f9d7cff56a325ff0c210608151` | `a01ab5f5220bc188c663f8a32ea0eb6acdf4aa52` | `rq091/ui/manifest.json` | 8 |
| **合计** |  |  |  | **358** |

清单与文件实核结果：

- `cg5` 清单：350个唯一路径，99,244,738字节，清单SHA-256为 `aebbee0c81ff49b7ca29657a36980af4e663aaab18e9fc2ee667b7f7596bda0e`。
- `cg5` 的350张由 `rq091/**` 的296张与继续复用 `cg1/mother-video-call/**` 的54张组成；54张复用途径属于清单内容，不是缺件。
- `cg6` 清单：8个唯一路径，8,394,438字节，清单SHA-256为 `fc4eefa39cec7948f71e963f43a5561e57636373d7554b38f0206521cc4288ba`。
- 两个标签合计358张、107,639,176字节；逐文件SHA-256为358/358一致，WebP解码与清单尺寸为358/358一致，路径无重复。
- `cg5 → cg6` 只新增8张WebP和1份UI清单，没有改写前350张素材。
- 两张由PNG转出的素材按清单采用无损WebP，8张UI素材均登记 `pixelIdentical: true`。
- 仓库内 `素材发布锁定_cg5.json` 与远端 `cg5` 清单逐字节一致；`初始界面素材发布锁定_cg6.json` 与远端 `cg6` 清单字段一致，字节差异只来自本地换行格式。

运行时使用的两个不可变CDN清单均返回HTTP 200、缓存命中，并由 `x-jsd-version` 分别锁定为 `cg5`、`cg6`：

- https://testingcf.jsdelivr.net/gh/shujun8520-design/qgy-assets@cg5/rq091/manifest.json
- https://testingcf.jsdelivr.net/gh/shujun8520-design/qgy-assets@cg6/rq091/ui/manifest.json

按生产代码生成的358个CDN地址完成逐URL检查。首轮有3条本机Schannel并发握手瞬断；降低并发逐条重试后均返回 `200 image/webp`，最终结果为 **358/358可达，0个404或缺件**。

## 3. GitHub Release 下载件

GitHub Release `人妻公寓 v0.91` 已发布，状态为非草稿、非预发布。三个资产均已从公开下载地址实际下载，不只检查元数据。

| 下载件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `rqgy-0.91.png` | 2,379,995 | `5bb4185c3c848214f7f212a003c387f2e224a465170438bc301d35526ac9ff9d` |
| `rqgy-0.91.json` | 91,421 | `882cf2c5ae8c0f381dc5724fe491bd9031f6e899af874fa1d8efcb190be63100` |
| `rqgy-0.91-checksums.json` | 954 | `ff8e6e856609bb9e13603564c0681a7ffee655e9a6b2cc3d4ccc5db0199802f5` |

核对结果：

- 三个下载件的字节数与SHA-256均和GitHub Release资产元数据一致。
- 三个下载件分别与 `rq0.91` 标签中的 `dist/人妻公寓/人妻公寓.png`、`人妻公寓.json`、`发布校验_0.91.json` 逐字节一致。
- PNG含且只含一个 `chara` 数据块；Base64解码后为91,421字节，与JSON下载件逐字节一致。
- 校验文件中的游戏逻辑、客户端、JSON、PNG四项均重新从 `rq0.91` 标签树读取并复算，字节数和SHA-256全部一致。

正式下载入口：

- PNG角色卡：https://github.com/shujshujun/my-tavern-scripts/releases/download/rq0.91/rqgy-0.91.png
- JSON角色卡：https://github.com/shujshujun/my-tavern-scripts/releases/download/rq0.91/rqgy-0.91.json
- SHA-256校验文件：https://github.com/shujshujun/my-tavern-scripts/releases/download/rq0.91/rqgy-0.91-checksums.json

## 4. 旧版更新检测

发布源码中的当前游戏版本为 `0.91`，组卡显示版本与资源标签均为 `0.91`／`rq0.91`。版本源使用jsDelivr包版本接口，并在失败时回退GitHub `rq*` 标签接口；只接受正式稳定标签。

本次对正式远端执行真实查询，返回最新稳定版 `0.91`：

- `0.90.4 → 0.91`：`当前较旧`，旧版会出现升级提醒。
- `0.91 → 0.91`：`相同`，当前版不会重复提醒。
- 更新查询失败仍只显示非阻塞提示，不成为开始游戏的门槛。

补充回归断言已加入 `tests/人妻公寓/依赖版本检测.test.mjs`，明确锁定 `0.90.4 → 0.91`、`0.91 → 0.91` 以及远端缓存落后时不得提示降级。

## 5. 验证口径

- 发布专项：`v091发布版本契约`、`cg5发布资源路由`、`依赖版本检测`、`发布后玩家流程回归`，共19/19通过。
- 发布校验文件保留打包前候选的6472/6472回归、类型检查和生产构建通过记录。
- 本次闭环独立完成：远端标签、358张素材及两份清单、358条生产URL、Release三下载件、四项校验文件内容、PNG内嵌JSON、真实更新查询与发布专项复核。
- 发布标签后的全量通配测试不能作为独立复现门：其中若干素材生产测试依赖未纳入Git的 `output/imagegen/**` 原图／制作清单，另有一条旧客户端体积断言按整个bundle禁止任意 `data:image`，与当前正式包中保留的小型内嵌图标口径不一致。它们不影响已发布运行资产，但需要在后续测试基础设施整理时改为读取受跟踪发布锁或按场景位图阈值判断。本记录没有把该类制作证据缺失冒充产品运行失败，也没有伪称本次重新跑出了6472/6472。

## 6. 闭环结论

`cg5`／`cg6` 的358张压缩WebP与两份清单、`rq0.91` 正式标签、GitHub Release PNG／JSON角色卡和校验文件、PNG内嵌JSON一致性、旧版 `0.90.4 → 0.91` 更新检测均已核验通过。v0.91正式发布闭环完成；后续开发与真实宿主长期观察按统一待办另行跟踪，不回写或移动本次正式标签。
