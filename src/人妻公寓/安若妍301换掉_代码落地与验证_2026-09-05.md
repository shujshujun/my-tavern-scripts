# 安若妍301《换掉》代码落地与验证

本次在`a5ed2802`基础上实现《换掉》的玩法代码。固定剧情提示围绕本拍动作、数值结算边界和末帧组织；素材生产、素材发布与真实宿主完整游玩另行验收。

## 已接入

- 商品沿用`角色路线:301:结局剧情`，价格1500；购买只取得背包票。相机套装配置价600，在公寓外部采购，不能作为普通礼物送出。
- `_安若妍换掉`与`schema.json`、`initvar.yaml`同步，沿用v9存档版本。旧档默认不开线，仅有旧完成ID时不虚构照片版本。
- 三日流程通过真实日期边界推进：采购后次日试机；预约后次日晚间到访。预约遵守301周作息，来电、医院等占用期间不能开场，错过后顺延。
- 固定拍只在对应持久票匹配且成功提交后推进。两条正文路径使用同一状态机；取消、失败与旧票重复提交不计固定拍。
- H1创建零进度普通场次；P1、P2暂停普通资源账。前半至少4个个人有效楼、P1后1楼、P2后2楼；满意目标冻结为6，收尾前至少7个个人有效楼。主动收尾和七点体力后的免费收尾均可完成；过早耗尽只重排预约。
- P2冻结照片体态和`ARY-RPL-10-N/P`，只有玩家最终换照才写入结局ID。场次异常失效保留采购、试机与拍摄历史，重排当夜，不伪造合格收尾。
- 13个剧情运行位、19个文件ID与12个房间背景ID完成路由。背景优先级覆盖普通301及前三胎房间状态。P1/P2事件画面按队列播放，关闭末帧后恢复原场次；图库可查看已经记录的结局回忆。
- 姐妹群先单独提交唯一照片消息，下一节拍才生成反应。6～9条、至少3名成员、至少4条来自安若妍之外；不合格整批拒绝，不消费反应键。成员结局差分实时派生，群内公开事实只由已发送消息授予。
- 当前聊天世界书覆盖启动、正文、时间推进、撤销、删楼、重开与切聊路径；超时释放队列，过时请求失效，失败可重试。结局后301提供一块“和她亲密”，二选一只决定零进度开场。

## 验证证据

- 新线专项：14／14通过，包含购买幂等、三日完整链、暂停账本、主动／耗尽收尾、失效重排、照片冻结、开场、演员门、世界书失败／超时／切聊、群消息重试。
- `node --test --test-concurrency=4 'tests/人妻公寓/*.test.mjs'`：2350／2350通过。
- 定向`vue-tsc`通过。定向ESLint无错误；保留既有App属性顺序及测试TypeScript默认导入的4条警告。
- 两入口生产模式隔离构建通过，产物位于任务工作区`.codex-tmp/ary-ending/build/`。禁用schema dump、同步器、监听器及声明文件自动写回。
- 任务工作区和主工作区正式`dist/**`各72个文件，构建前后SHA-256完全一致。webpack保留包体积建议警告。
- 实际diff已按商品、Schema、事务、资源、界面、手机与世界书消费者逐项复核。用于全量测试的既有制作证据通过本地引用／补缺副本读取，不进入本任务提交。

## 2026-09-05公开事实与状态恢复复审

2026-09-05补充复审覆盖本任务新增的提示生产、群聊解析／验收、结局入口、普通场次门、时间门、前台强制操作和恢复写回。群聊应承接已发送照片及各成员既有公开事实，原先使用普通群聊与302专场验收器会拒收本场已公开内容；现改为本线公开事实校验，并只向专场传递群内所需信息。普通群聊和302原有调用由反例测试继续保护。

结局后入口现在只消费结局完成凭据和当前可行动条件。301普通回合、收尾、时间与前台操作门只作用于实际绑定场次；失效绑定恢复会精确清理本线活动票，保留其他剧情、相机和拍摄历史。已完成ID优先保护冻结照片。启动及删楼／回档收口会保存修复；当前未完成票按现行定义重建文案，保留事务ID、目标、请求世代和剧情进度，已完成历史不重写。

新增边界专项13／13通过，与原专项合计27／27；全量2363／2363、Vue／TypeScript、定向ESLint及两入口隔离生产构建通过。本轮ESLint仅保留既有App属性顺序警告。主工作区和任务工作区正式`dist/**`各72个文件，构建前后SHA-256均不变。新测试仅替代外部生成与记忆读取，群消息解析及生产验收使用真实实现，覆盖拒收／重试、停止生成、迟到结果、旧档、其他住户隔离、活动票清理、完成照片保护和待演票迁移。

## 剩余边界

《换掉》的31张现稿已于2026-09-05复核并获用户确认，19张剧情／照片素材和12张持久背景已安装为无损WebP；详见[CG审核与接入验收](./安若妍301换掉_CG审核与接入验收_2026-09-05.md)。资源配置等待统一外部素材发布，验收环境可设置`__RQGY_ARY_REPLACE_ASSET_BASE__`；真实仓库与不可变标签配置完成后启用正式地址。

尚未进行真实SillyTavern桌面／手机端的19回合连续游玩、真实AI措辞验收与生成中断实测；自动化结果不能替代这些验收。本次不写正式dist、不组卡、不打tag、不push。
