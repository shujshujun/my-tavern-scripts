# SNAP-20 手机并行修复

状态：本地代码专项通过，交主窗口串行整合；未发布。分支 `task/phone-parallel-20260908`，冻结基线为 `.codex-tmp/parallel-phone-20260908/inherited-baseline.json` 的167文件及HEAD `d29e2ded`。本项仅认领 `手机/节拍引擎.ts` 中一行条件增量。

设计与正常运行：回国茶话会“回应回国”选取尚未回应的最多两名成员，要求目标完整回复，母亲发言可选。玩家真实插话触发生成；解析后逐条保存，全部实际气泡存在才签发批次凭据，路线提交记录已回应成员。用途是推进茶话会，同时保留其他任务的母亲发言和人物约束。

根因：提示和目标选择允许母亲不发言，但统一验收要求至少一条母亲消息，因此两名目标完整回复被整体丢弃。修复只让“回应回国”使用既有目标成员完整性检查，其他任务继续要求母亲。未改提示、路线字段、幂等键、解析器或批次提交。

影响链已核：`回国茶话会一拍`→真实`微信群文本`→`手动群接话`→数据层增量写入→`构造回国提交凭据`/`回国提交凭据有效`→`提交回国茶话会批次`。自动改名反应、坦白、点评、转正事、收束及后续私聊不因此获得豁免。无新增持久字段，旧存档重试继续使用当前代码。

新测试 `手机并行回国目标_SNAP20.test.mjs`：17/17。原样读取冻结节拍源码为15/17；失败为无母亲完整批次及该批次的中途取消场景。覆盖目标完整/缺一、外来人、可选母亲、其他任务必答、同批次幂等、取消、生成超时、写入失败、回档、切聊天、swipe和部分送达不签完整凭据。宿主存储与生成提供者被隔离；被测任务选择、解析、发送、实际存储和凭据/路线函数均真实执行。后续PLAY-040装配通过后已移除本项临时普通摘要适配，最终84项中本项17例仍全部通过。没有执行真实SillyTavern界面或完整index事件宿主。

最终16文件联合275/277，只有SNAP-12两条已登记失败，本项17例通过。ESLint 0错误0警告，定向vue-tsc通过。生产隔离构建 `build-1788825970892-dfd9b6` 两入口通过，游戏逻辑3警告/客户端2警告，无错误；独立目录与参考主仓正式dist各72文件及受监控源码前后不变。使用该构建生成的本地声明重新类型检查通过。

证据目录 `.codex-tmp/parallel-phone-20260908/`：`snap20-baseline.tap`、`snap20-green.tap`、`related-final-corrected.tap`、`typecheck-built.log`、`eslint.log`、`build.log`。最初夹具未加入302及新增相邻依赖未装配的失败日志保留，不当作产品失败。最终所有权SHA、分项最小补丁和构建匹配由交回清单记录。
