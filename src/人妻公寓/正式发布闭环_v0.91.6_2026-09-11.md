# 《人妻公寓》v0.91.6 正式发布闭环

- 完成日期：2026-09-11（Asia/Taipei）
- 正式版本：`0.91.6`
- 正式标签：`rq0.91.6`
- 上一正式标签：`rq0.91.5`
- 发布工作树：`C:\Users\Administrator\.devspace\worktrees\tavern_helper_template-consolidated-151e9f65`
- 开工定位：detached HEAD `81c88728429fb197962a5b4b0bfa075a942ba69e`
- 正式打包提交：`18aef2fe28dc82796c6a8a1213d73587a9cadb3c`
- 注解标签对象：`52477679d43b875d46f113be87f5d323aaaae051`
- 标签解引用提交：`18aef2fe28dc82796c6a8a1213d73587a9cadb3c`
- 发布分支：`release/rq0916`
- GitHub Actions 工作流提交：`c4daa4ca333a99786bd07b654cf60b6ec913a178`
- 闭环记录提交：本文件所在的 `docs(release): close rq0.91.6 publication` 提交；正式标签不随该提交移动
- GitHub Actions 运行：https://github.com/shujshujun/my-tavern-scripts/actions/runs/34521233275
- GitHub Actions 作业：https://github.com/shujshujun/my-tavern-scripts/actions/runs/34521233275/job/103018973136
- GitHub Release：https://github.com/shujshujun/my-tavern-scripts/releases/tag/rq0.91.6
- Release 发布时间：2026-09-11 03:34:57（Asia/Taipei）

## 一、发布范围与最终语义

本版从 `release/rq0915` 闭环提交 `81c88728...` 的 detached HEAD 混合工作树继续，正式整合两批已完成修复：

1. **特殊剧情自解锁按钮门控修复**：修复周小满《不再留门》A5／A7／A9 当前确认、暂缓与合法撤回被自身普通场景锁反向拦截；同步统一房内操作抽屉的动作数组、数量与可见性，并清除许曼君《离婚》自有票期间的重复起场按钮。
2. **脚本隔离生成数据库身份修复**：睡眠、监控、荣耀洞、母亲视频、录像带等脚本辅助演出会在 TavernHelper 广播真实 `GENERATION_AFTER_COMMANDS` 事件时，由一次性队首监听给同一个 options 对象标记 `automatic_trigger=true`，数据库后续监听不再把它误认成玩家普通正文并启动额外召回／骨架／自动填表链。

本版继续严格保留以下边界：

- 《不再留门》只允许 `确认当前决定`、`暂缓`、`撤回许可` 三类白名单动作，并且必须匹配当前实例、修订、场景、拍次、绝对时段、目标地点及活动票／队首等待票；错地点、旧票、队尾票、其他路线、生成中、移动中、界面事务中与 MVU 保存冲突仍失败关闭。
- 《双重继承》最终“归位总钥匙”的既有专属精确穿锁例外完整保留，没有被抽屉统一过滤误伤。
- 真实删楼、重掷、swipe、回档、切聊天和重开触发的数据库时间线安全栅栏没有放宽；数据库尚未证明回放到目标楼层时，召回与新写仍保持关闭，避免未来分支记忆污染当前分支。
- `RQ_社交轨迹` 的群级摘要、实际接收者以及各接收者个人见闻之间的知情隔离不变。
- 存档数据版本继续为 `9`；v0.80(v7)、v0.81／0.82(v8) 按既有迁移规则处理，v0.83～v0.91.5(v9) 可直接继续，无需因本次行为热修重开。

## 二、工作树与提交链

开工时：

- detached HEAD：`81c88728429fb197962a5b4b0bfa075a942ba69e`；
- `git branch --show-current` 为空；
- `git describe` 为 `rq0.91.5-2-g81c88728-dirty`；
- 26 个 tracked 修改、11 个 untracked、暂存区为空；
- 其中 13 个 tracked 源码／测试修改与 1 个 untracked 专项测试属于本版，两批修复之外的脏 `dist`、source map、probe、交接资料与 `.codex-tmp` 均需保留并排除。

发布提交链：

1. `f099ee2ca06832d34d191135a62ef8833ded0a5a` — `fix(story): repair self-unlocking special scene controls`
2. `b1d4e73502f11094a58125ac8f532badee31d700` — `fix(database): isolate scripted generation from auto-fill`
3. `b48bb8d2a9548d22d14e67c4a17aed90072e366c` — `chore(release): prepare rq0.91.6`
4. `b957a4d769f527e5c700bcc0796225a09900a796` — `test(story): align legacy gates with rq0.91.6 controls`
5. `18aef2fe28dc82796c6a8a1213d73587a9cadb3c` — `build(release): package rq0.91.6`
6. 在第 5 条正式打包提交上创建注解标签 `rq0.91.6`
7. `c4daa4ca333a99786bd07b654cf60b6ec913a178` — `ci(release): publish rq0.91.6 assets`
8. 本文件所在提交 — `docs(release): close rq0.91.6 publication`

`rq0.91.6` 始终固定在正式打包提交 `18aef2fe...`。工作流与闭环记录位于标签之后，不修改标签内容。

### 第一批修复提交文件

`f099ee2c...` 只纳入：

```text
src/人妻公寓/特殊剧情自解锁按钮门控同类BUG专项排查记录_2026-09-10.md
src/人妻公寓/特殊剧情自解锁按钮门控同类BUG修复记录_2026-09-10.md
src/人妻公寓/界面/客户端/App.vue
src/人妻公寓/界面/客户端/components/不再留门进展.vue
src/人妻公寓/界面/客户端/composables/useRoomActions.ts
src/人妻公寓/脚本/游戏逻辑/不再留门系统.ts
src/人妻公寓/脚本/游戏逻辑/许曼君离婚系统.ts
tests/人妻公寓/App拆分A6b.test.mjs
tests/人妻公寓/特殊剧情自解锁按钮门控修复.test.mjs
tests/人妻公寓/移动端房内操作抽屉.test.mjs
```

### 第二批修复提交文件

`b1d4e735...` 只纳入：

```text
src/人妻公寓/玩家反馈_过夜后数据库不召回不更新_专项排查_2026-09-11.md
src/人妻公寓/玩家反馈_过夜后数据库不召回不更新_修复记录_2026-09-11.md
src/人妻公寓/脚本/游戏逻辑/数据库辅助生成标记.ts
src/人妻公寓/脚本/游戏逻辑/隔离事件引擎.ts
tests/人妻公寓/数据库辅助生成标记.test.mjs
tests/人妻公寓/时间独立生成隔离.test.mjs
tests/人妻公寓/隔离行动语义边界.test.mjs
tests/人妻公寓/隔离请求玩家宏_PLAY045.test.mjs
```

### 版本准备与兼容测试

`b48bb8d2...` 更新 `依赖版本.ts`、`组卡.mjs`、精简入口、0.91.6 发布说明、历史版本契约兼容断言、新 `v0916发布版本契约.test.mjs` 与依赖版本检测；旧 0.91.5 发布说明、闭环记录、工作流和标签内容保持冻结。

`b957a4d7...` 只调整四份已经过时、仍强制旧前端接线形状的兼容测试：`App拆分A7a`、`App拆分A7b3`、发布后玩家流程回归和远处等待票／地点入口 PLAY004；业务安全断言没有被删除或放宽。

## 三、完整门禁

正式打包前的最终候选门禁结果：

- `tests/人妻公寓` 全量：`6554／6554` 通过，0 失败、0 取消、0 跳过；
- 两批与版本发布合并后的定向门禁：`453／453` 通过；
- 特殊剧情新增专项：`7／7`；特殊剧情相关联合回归：`293／293`；
- 数据库／睡眠／隔离／录像带／取消／回档相关：`143／143`；
- 发布后再次运行 `v0916发布版本契约` 与 `依赖版本检测`：`13／13`，0 失败、0 取消、0 跳过；
- 定向产品 TypeScript：通过；
- 客户端 Vue TypeScript：通过；
- 本版全部修改的 TS／Vue／MJS ESLint：0 错误，4 条组卡脚本既有 `import-x/no-nodejs-modules` 警告；
- `git diff --check`：通过。

全量中的历史素材验收继续只读复用既有审核证据覆盖层；没有伪造审核清单，也没有把 `output/imagegen`、外部证据或本地测试卡提交到正式标签。

## 四、隔离构建、正式构建与组卡

隔离 production build 证据：

```text
.codex-tmp/rq0914-regression-fix/build-1789068436618-1a4e93
```

结果：

- 游戏逻辑：0 错误、3 条警告；
- 客户端：0 错误、2 条警告；
- 主仓库正式 `dist`：72 文件、19,484,503 字节，构建前后聚合 SHA-256 均为 `417d5fc4e7a8c45de92c1ba00f990586cde4b8e261df0950e2832dd16120d752`；
- 候选工作树正式 `dist`：77 文件、26,565,538 字节，构建前后聚合 SHA-256 均为 `d924ad012b3bfafccc17ebb8ec08f44ed4885f6b3c7689ab9f12065de7ed245a`；
- 主仓库与候选工作树源码聚合哈希均未变化。

正式构建与组卡：

- `pnpm build` 成功；
- 39 条警告均为仓库其他角色卡既有 Sass 弃用／webpack 体积提示，没有《人妻公寓》编译错误；
- `node src/人妻公寓/组卡.mjs` 成功；
- 正式卡规范：`chara_card_v3`，`spec_version=3.0`，`character_version=0.91.6`；
- 世界书 13 条、启用 8 条；正则 5 条；脚本 2 个；
- 客户端只包含一份 `RQGY_GAME_VERSION:0.91.6`；
- 游戏逻辑 bundle 实际包含 `automatic_trigger` 辅助生成身份、身份建立失败的安全提示以及《不再留门》精确控制接线；
- 卡内正式运行入口只有 `@rq0.91.6`，没有 `@rq0.91.5` 运行 URL；
- 卡内没有“勿发布”、`localhost`、`127.0.0.1`、`.codex-tmp`、`file://`、测试卡名或本地临时客户端；
- PNG 只有一个 `chara` 数据块，解码后为 97,195 字节，SHA-256 与独立 JSON 相同并逐字节一致；
- 素材继续复用不可变 `cg5`、`cg6`，共 358 张压缩 WebP。

正式打包提交只纳入五项《人妻公寓》产物；与 `rq0.91.5` 比较，三个《人妻公寓》source map 的 Git 内容无变化，未进入 `rq0.91.6` 标签。

## 五、正式本地文件与 SHA-256

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| `dist/人妻公寓/人妻公寓.json` | 97,195 | `327c5865e7b98d17c2ff203b0e908f4385613f63820ef38c3fd87b419218ab0c` |
| `dist/人妻公寓/人妻公寓.png` | 2,387,695 | `593c98f8d0ce558665bbb90750dcdff0f9a079120242c68a580e3ffb6c0e7127` |
| `dist/人妻公寓/界面/客户端/index.html` | 2,366,831 | `b99dde6ec099df99bd6c274486456d695a7015a96a17b1ced2b9a0206d938559` |
| `dist/人妻公寓/脚本/游戏逻辑/index.js` | 2,975,327 | `fea4779d69afe46d2ad92a299794f8c7c01099e36d008bce5c238a09adef6fe3` |
| `dist/人妻公寓/发布校验_0.91.6.json` | 1,193 | `01eb4ce3e1c0846eac9e0c29f7d25d072f78fa3336d95e85105fc8530d598f24` |

`发布校验_0.91.6.json` 同时记录：构建来源 `b957a4d7...`、全量 6554、定向 453、TypeScript／Vue TypeScript／正式构建／隔离构建均通过、PNG／JSON 一致、无关 dist 保留。

## 六、注解标签、分支与旧版本冻结

本地与远端复核：

- `refs/tags/rq0.91.6` 注解对象：`52477679d43b875d46f113be87f5d323aaaae051`；
- `refs/tags/rq0.91.6^{}`：`18aef2fe28dc82796c6a8a1213d73587a9cadb3c`；
- 发布工作流触发时 `refs/heads/release/rq0916`：`c4daa4ca333a99786bd07b654cf60b6ec913a178`；
- 本闭环记录推送后，`release/rq0916` 前进到本文件所在提交；`rq0.91.6` 不移动。

上一版保持不变：

- `refs/heads/release/rq0915` 仍为 `81c88728429fb197962a5b4b0bfa075a942ba69e`；
- `refs/tags/rq0.91.5` 注解对象仍为 `87bbb4dd82a441c935b7a857912ff62f8fa16e8e`；
- `refs/tags/rq0.91.5^{}` 仍为 `e9201f4fe736ba468185115076c84b7482fcf567`。

本次没有移动、覆盖或重建 `rq0.91.5`。

## 七、GitHub Actions 与 Release

GitHub Actions：

- 工作流：`publish-rq0916`；
- 运行 ID：`34521233275`；
- 作业 ID：`103018973136`；
- 事件：`push`；
- 分支：`release/rq0916`；
- head SHA：`c4daa4ca333a99786bd07b654cf60b6ec913a178`；
- 运行状态：`completed`；
- 结论：`success`；
- 作业开始：2026-09-11 03:34:48（Asia/Taipei）；
- 作业完成：2026-09-11 03:35:02（Asia/Taipei）；
- 不可变标签检出、附件准备、Release 创建／更新、发布后附件验证及收尾步骤全部 `completed/success`。

公开 Release：

- Release ID：`386567454`；
- 标签：`rq0.91.6`；
- 标题：`《人妻公寓》v0.91.6`；
- `draft=false`；
- `prerelease=false`；
- GitHub `releases/latest` 指向本 Release；
- 发布时间：2026-09-11 03:34:57（Asia/Taipei）；
- 三个附件状态均为 `uploaded`。

## 八、Release 附件回读

| 附件 | 远端字节 | 远端 SHA-256 / API digest | 与本地 |
|---|---:|---|---|
| `rqgy-0.91.6.json` | 97,195 | `327c5865e7b98d17c2ff203b0e908f4385613f63820ef38c3fd87b419218ab0c` | 逐字节一致 |
| `rqgy-0.91.6.png` | 2,387,695 | `593c98f8d0ce558665bbb90750dcdff0f9a079120242c68a580e3ffb6c0e7127` | 逐字节一致 |
| `rqgy-0.91.6-checksums.json` | 1,193 | `01eb4ce3e1c0846eac9e0c29f7d25d072f78fa3336d95e85105fc8530d598f24` | 逐字节一致 |

三个下载均返回 HTTP 200。GitHub Release API 的 `digest` 与本地 SHA-256 全部一致；远端 PNG 中只有一份 `chara` 数据块，其 Base64 解码内容与远端独立 JSON 逐字节一致。

## 九、不可变标签 CDN

精确标签资源验证：

| CDN 资源 | HTTP | `x-jsd-version` | 字节 | SHA-256 | 与本地 |
|---|---:|---|---:|---|---|
| `testingcf.jsdelivr.net` 客户端 `index.html` | 200 | `rq0.91.6` | 2,366,831 | `b99dde6ec099df99bd6c274486456d695a7015a96a17b1ced2b9a0206d938559` | 一致 |
| `testingcf.jsdelivr.net` 游戏逻辑 `index.js` | 200 | `rq0.91.6` | 2,975,327 | `fea4779d69afe46d2ad92a299794f8c7c01099e36d008bce5c238a09adef6fe3` | 一致 |
| `cdn.jsdelivr.net` 客户端 `index.html` | 200 | `rq0.91.6` | 2,366,831 | `b99dde6ec099df99bd6c274486456d695a7015a96a17b1ced2b9a0206d938559` | 一致 |
| `fastly.jsdelivr.net` 客户端 `index.html` | 200 | `rq0.91.6` | 2,366,831 | `b99dde6ec099df99bd6c274486456d695a7015a96a17b1ced2b9a0206d938559` | 一致 |

精确标签客户端响应带不可变长缓存策略：`public, max-age=31536000, s-maxage=31536000, immutable`。

## 十、版本索引与旧版更新检测

远端版本发现最终结果：

1. GitHub `refs/tags/rq0.91.6` 已公开，指向注解对象 `52477679...`；
2. jsDelivr 旧接口 `/v1/package/gh/...` 已列出 `rq0.91.6`；
3. jsDelivr 新接口 `/v1/packages/gh/...` 已列出 `rq0.91.6`；
4. 项目自身真实执行 `查询游戏官方最新版本()` 返回 `0.91.6`；
5. 项目自身比较函数返回：
   - v0.91.5 对远端 0.91.6：`当前较旧`，会显示非阻塞更新提示；
   - v0.91.6 对远端 0.91.6：`相同`，不会重复提示；
   - v0.91.6 遇到暂时落后的 0.91.5 缓存：`当前较新`，不会提示降级；
   - beta／rc／dev 与非 `rq` 标签继续被正式版本筛选排除。

旧版更新检测线上门禁通过。

## 十一、现场排除与最终工作树边界

整个发布过程没有使用 `reset`、`checkout`、`restore`、`stash`、`clean`，没有删除 untracked，没有使用 `git add -A`、`git add .` 或 `git commit -am`。

以下内容没有进入两批修复提交、版本提交、正式标签、工作流提交、Release 附件或本闭环提交：

- 13 个既有／正式全仓构建产生的无关 tracked `dist/**` 修改；
- 《人妻公寓》客户端与游戏逻辑的三个 source map；
- 根目录 `__devspace_probe_should_not_create__`；
- `.codex-tmp/**` 隔离构建、只读证据覆盖层、远端核验脚本与本地测试卡；
- 所有名称含“勿发布”的本地测试卡；
- 0.91.4／0.91.5 旧交接资料；
- `新窗口交接词_0.91.6正式发布整合_2026-09-11.txt`；
- 其他角色卡、HTML、JS、CSS、source map、日志、设计稿或素材证据。

闭环提交前，剩余现场为 13 个无关 tracked `dist` 修改、6 个保留的 untracked 文件，暂存区为空；本次只显式暂存本闭环文档。闭环提交后仍保持 detached HEAD，未为了显示分支名而切换工作树。

## 十二、结论

《人妻公寓》v0.91.6 的两批修复整合、版本准备、历史兼容测试、全量与专项门禁、TypeScript、Vue TypeScript、ESLint、隔离 production build、正式 production build、角色卡组装、注解标签、发布分支、GitHub Actions、GitHub Release、三个附件逐字节回读、不可变标签 CDN、jsDelivr 新旧版本索引和旧版更新检测均已完成并通过。

正式不可变发布基线为：

- 标签：`rq0.91.6`
- 标签对象：`52477679d43b875d46f113be87f5d323aaaae051`
- 正式打包提交：`18aef2fe28dc82796c6a8a1213d73587a9cadb3c`
- 发布分支：`release/rq0916`

`rq0.91.5` 未移动、未覆盖；既有正式附件与标签保持不变。
