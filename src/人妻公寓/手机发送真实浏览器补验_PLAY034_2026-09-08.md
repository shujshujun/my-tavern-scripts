# PLAY034：发送等待与真实浏览器补验

在当前完整混合候选 `task/unified-repair-20260907@394817a5` 继续修复，未重建工作树、提交或发布。

## 设计与根因

玩家明确发送后，真实消息身份入库才表示接受；未接受的草稿和引用必须可重试。保存、回复、撤回和时间线租约继续沿用原有所有者，草稿不参与剧情或时间结算。

真实 Chrome 中复现：玩家气泡已经显示，原输入节点已脱离文档，但宿主保存永久 pending 时，按钮一直“发送中”，草稿票及批次写入数保持占用。实际生成器返回并写出回复气泡后，末尾保存也会继续占红灯。另用真实变量回调证明：提交后抛错时虽然能够确认接受，原路径却未再请求保存；变量回调本身不返回时也没有等待上限。

## 当前修复

- 发送与完整回复在确认入库后独立请求原串行持久保存，完成草稿/批次收口。保存失败继续给出“请勿重复发送”提示，保存调用不再决定是否接受。
- 保存请求及延迟补存传入同一发送时间线校验，排队期间世代失效后不启动旧请求。已交给宿主的保存无法物理撤销，不宣称已经取消。
- 玩家变量写入用现有受控等待器限定十秒，并每250毫秒检查时间线。失败时重新核对真实消息身份；尚未提交则撤销本条迟到回调的写入资格、保留草稿并释放租约；已经提交则确认接受并继续请求保存。
- 只修改 `手机/交互/邀约与发消息.ts` 的技术生命周期；原会议/生成互斥、批次顺序、引用、撤回墓碑及剧情规则保留。六处旧源码形状断言按新的受控写入和独立保存调用更新，原行为断言未删除。

## 证据

新增 `手机发送保存等待_PLAY034.test.mjs`。第一组7项修前2通过5失败；补充永久变量等待后10项中7通过3失败；修后新增10项与原草稿26项、受控等待3项合计39/39。

独立浏览器夹具直接加载完整生产模块，保留真实挂载、UI注册表、调度、DOM、焦点、事件、计时器、发送、生成器、批次、租约、数据层与镜像。宿主变量/保存、模型返回和可选记忆来源为显式隔离适配器，网络请求禁用，未接入玩家存档。旧受控Element helper保持原样。

最终真实 Chrome **14/14**：桌面1440×1000和移动390×844，实际重绘/焦点、迟到成功/失败、引用ABA、输入法/回车、旧节点、关闭重开、切联系人、切世代、停止/迟到模型结果、模型拒绝、回复落库后的保存pending、十秒写入超时。时间上限用浏览器受控时钟推进，正常界面使用真实计时器。移动截图为视口模拟，不能等同真机软键盘测试。

手机相关联合 **830/830**，0失败/取消/跳过；生产入口类型检查通过。原始命令、stdout/stderr、源SHA、完整浏览器结果和截图位于 `.codex-tmp/repair-20260908-ui/`。首轮浏览器中的引导层拦截及受控时钟安装时点错误属于夹具问题，保留日志，未计为产品缺陷。

双入口隔离生产构建 `build-1788862670823-2899e3` 成功，0错误、3/2警告；主目录与候选正式dist各72文件SHA不变，源码/配置也未被构建回写。

## 边界

PLAY034的代码、当前候选集成及隔离浏览器补验完成。真实SillyTavern服务端、在线模型及真机联合验收仍独立保留。发送接受不等于服务端保存成功；永久pending的宿主保存仍留在原串行队列，不擅自并发重发，已有浏览器恢复镜像承担未确认窗口。会议摘要与强剧情跨容器提交没有因本次修复被改成普通可选保存。
