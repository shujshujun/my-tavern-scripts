# 《人妻公寓》v0.91.5 正式发布闭环

- 完成日期：2026-09-10（Asia/Taipei）
- 正式版本：`0.91.5`
- 正式标签：`rq0.91.5`
- 上一正式标签：`rq0.91.4`
- 正式打包提交：`e9201f4fe736ba468185115076c84b7482fcf567`
- 注解标签对象：`87bbb4dd82a441c935b7a857912ff62f8fa16e8e`
- 标签解引用提交：`e9201f4fe736ba468185115076c84b7482fcf567`
- 发布分支：`release/rq0915`
- GitHub Actions 发布提交：`747e4da21beb82eafc2896c1f0ccca71bb934af2`
- GitHub Actions 运行：https://github.com/shujshujun/my-tavern-scripts/actions/runs/34396710252
- GitHub Release：https://github.com/shujshujun/my-tavern-scripts/releases/tag/rq0.91.5
- Release 发布时间：2026-09-10 03:43:18（Asia/Taipei）

## 一、发布范围

本版继续 `release/rq0914` 的隔离候选工作树，整合两批已经在真实 TT 酒馆和自动测试中验收的修复：

1. 0.91.4 回归修复：恢复 `RQ_剧情事件`、`RQ_社交轨迹` 的数据库时间线写入，修复变量完成后的假忙态、时间推进恢复门、数据库重开五表恢复，并保留既有变量 API 自动恢复与防双发；
2. 客户端热修：彻底删除“省流模式”和“减少动效”，从父页面与 iframe 共享存储及根元素 class 中清理旧偏好，以原生文档就绪和有界依赖等待修复长期停在“游戏界面加载中”。

`RQ_社交轨迹` 本次只修复时间线、写入和清场生命周期，没有改变群级摘要、实际接收者、各接收者个人见闻之间的知情隔离设计。

## 二、工作树与提交链

发布工作树：

`C:\Users\Administrator\.devspace\worktrees\tavern_helper_template-consolidated-151e9f65`

开工定位：

- detached HEAD：`0d82ff6d2e1637fa913bd9547ee0af28a93c1b8d`；
- `git branch --show-current` 为空；
- 42 个 tracked 修改、7 个 untracked；
- 暂存区为空；
- `git diff --check` 通过。

发布提交链：

- `9d0dfc29a57a89ba5446822f72dc9e264c9fab50`：整合 0.91.4 回归修复与客户端旧偏好／启动热修，共 46 个明确源码和测试文件；
- `0368f6787c6821238505b6c1d8f820f9afbbda14`：准备 0.91.5 版本号、`rq0.91.5` 组卡标签、入口、发布说明和版本契约；
- `e9201f4fe736ba468185115076c84b7482fcf567`：正式生产构建、角色卡和校验文件打包；
- `747e4da21beb82eafc2896c1f0ccca71bb934af2`：追加独立 `publish-rq0915` Release 工作流；
- 本文件所在的发布分支最终提交：追加正式发布闭环记录。

`rq0.91.5` 始终固定在正式打包提交 `e9201f4f...`。工作流和闭环记录位于标签之后的发布分支提交，不移动正式标签。

远端复核：

- `refs/heads/release/rq0915` 最终指向本闭环记录提交；
- `refs/tags/rq0.91.5` 注解对象为 `87bbb4dd...`；
- `refs/tags/rq0.91.5^{}` 解引用为 `e9201f4f...`；
- `refs/heads/release/rq0914` 仍为 `0d82ff6d...`；
- `refs/tags/rq0.91.4` 仍为 `29008b77...`，解引用仍为 `c5286453...`。

## 三、测试与静态检查

最终发布候选通过：

- 完整回归：`6541／6541`，0 失败、0 取消、0 跳过；
- 客户端发布专项：`215／215`；
- 数据库、时间线与受改回归专项：`422／422`；
- 正式版本契约：`50／50`；
- 发布后版本／更新检测复核：`15／15`；
- 本次全部修改 TypeScript／Vue／MJS 文件 ESLint：0 错误、7 条既有非阻塞警告；其中 4 条为组卡脚本 Node 内建模块规则、1 条为既有 Vue 属性顺序、2 条为测试动态 `require`；
- 定向产品 TypeScript：0 错误；
- 定向客户端 Vue TypeScript：0 错误；
- `git diff --check`：通过。

完整回归中的历史素材验收只读使用：

- 主仓库 `D:\SillyTavern\tavern_helper_template-consolidated` 中的既有结局／孕产审核清单；
- 统一修复工作树中的衣柜原图证据。

这些证据只通过 `.codex-tmp` 只读覆盖层参与测试，没有复制进正式标签，也没有使正式代码依赖 `output/imagegen`。

## 四、构建与组卡

隔离 production build：

- 游戏逻辑：0 错误、3 条警告；
- 客户端：0 错误、2 条警告；
- 构建目录：`.codex-tmp/rq0915-client-hotfix/build-1788982717244-1bb095`；
- 构建前后主仓库和候选工作树的正式 `dist`、源码聚合哈希均未变化。

正式构建与组卡：

- `pnpm build` 成功；
- 39 条警告均为仓库其他角色卡既有 Sass 弃用或 webpack 体积提示，没有《人妻公寓》编译错误；
- 正式角色卡规范：`chara_card_v3`，规范版本 `3.0`；
- `character_version`：`0.91.5`；
- 世界书：13 条，启用 8 条；
- 正则：5 条；
- 脚本：2 个；
- 资源标签：`rq0.91.5`；
- 素材标签继续复用不可变 `cg5`／`cg6`，共 358 张压缩 WebP；
- PNG 中只有一份 `chara` 数据块，Base64 解码后与独立 JSON 逐字节一致；
- 正式客户端只含一份 `RQGY_GAME_VERSION:0.91.5`，没有其他版本标记；
- 正式角色卡不含“勿发布”、`localhost`、`127.0.0.1`、`.codex-tmp` 或本地 Base64 客户端。

正式打包提交只更新：

1. `dist/人妻公寓/人妻公寓.json`
2. `dist/人妻公寓/人妻公寓.png`
3. `dist/人妻公寓/界面/客户端/index.html`
4. `dist/人妻公寓/脚本/游戏逻辑/index.js`
5. `dist/人妻公寓/发布校验_0.91.5.json`
6. `src/人妻公寓/发布说明_v0.91.5_2026-09-10.md`

全仓构建产生的其他角色卡差异和三份《人妻公寓》source map 均未进入正式打包提交。三个 source map 在 0.91.4 基线与 0.91.5 标签中的 Git blob 完全相同。

## 五、正式文件与 SHA-256

| 文件 | 字节 | SHA-256 |
|---|---:|---|
| `dist/人妻公寓/人妻公寓.json` | 95,575 | `7fe5fbc9f92a91c4d2e60bbcebe800b271d6450269bb42f61d61c6ccc89e04f2` |
| `dist/人妻公寓/人妻公寓.png` | 2,385,535 | `1710ceb79a3c9afc882ba28c7dd0522eeb1ffe10053577e024be8c500e5a1480` |
| `dist/人妻公寓/界面/客户端/index.html` | 2,365,836 | `62c12a168c3072e857188ce960b3f39208e98db6b0d63b36e09045d10a38dd29` |
| `dist/人妻公寓/脚本/游戏逻辑/index.js` | 2,974,646 | `7e73f6f79c72e2ff672d96b93794301b9819aadf4756ae3e18dd8c8c4af597ec` |
| Release 校验附件 `rqgy-0.91.5-checksums.json` | 1,193 | `964e75a77affd84ac11d0d96466ef7483f5d16e10a049796a053e90b0bea83ba` |

`dist/人妻公寓/发布校验_0.91.5.json` 中记录的四项文件字节数和 SHA-256 与实际文件完全一致。

## 六、GitHub Actions 与 Release

GitHub Actions：

- 工作流：`publish-rq0915`；
- 运行编号：`#34396710252`；
- 作业 ID：`102618121305`；
- 分支：`release/rq0915`；
- head SHA：`747e4da21beb82eafc2896c1f0ccca71bb934af2`；
- 状态：`completed`；
- 结论：`success`；
- 作业开始：2026-09-10 03:43:08（Asia/Taipei）；
- 作业完成：2026-09-10 03:43:21（Asia/Taipei）；
- 不可变标签检出、附件准备、Release 创建／更新、发布后附件验证及收尾步骤全部 `completed/success`。

公开 Release：

- 页面：https://github.com/shujshujun/my-tavern-scripts/releases/tag/rq0.91.5
- PNG：https://github.com/shujshujun/my-tavern-scripts/releases/download/rq0.91.5/rqgy-0.91.5.png
- JSON：https://github.com/shujshujun/my-tavern-scripts/releases/download/rq0.91.5/rqgy-0.91.5.json
- 校验文件：https://github.com/shujshujun/my-tavern-scripts/releases/download/rq0.91.5/rqgy-0.91.5-checksums.json

Release 状态：

- Release ID：`385797264`；
- `draft=false`；
- `prerelease=false`；
- 标题：`《人妻公寓》v0.91.5`；
- 最新 Release 指针：`rq0.91.5`；
- 三个附件状态均为 `uploaded`。

远端下载回读：

- PNG：2,385,535 字节，远端 SHA-256 `1710ceb7...a1480`，与本地逐字节一致；
- JSON：95,575 字节，远端 SHA-256 `7fe5fbc9...9e04f2`，与本地逐字节一致；
- 校验文件：1,193 字节，远端 SHA-256 `964e75a7...83ba`，与本地逐字节一致。

GitHub Release API 返回的三个 `digest` 也分别与上述本地 SHA-256 一致。

## 七、标签 CDN 与 jsDelivr 版本索引

以下正式标签资源均已公开返回 HTTP 200：

- `https://testingcf.jsdelivr.net/gh/shujshujun/my-tavern-scripts@rq0.91.5/dist/人妻公寓/界面/客户端/index.html`
- `https://testingcf.jsdelivr.net/gh/shujshujun/my-tavern-scripts@rq0.91.5/dist/人妻公寓/脚本/游戏逻辑/index.js`

复核结果：

- 两项响应均带 `x-jsd-version: rq0.91.5`；
- 客户端远端文件 2,365,836 字节，SHA-256 与本地一致；
- 游戏逻辑远端文件 2,974,646 字节，SHA-256 与本地一致；
- `cdn.jsdelivr.net`、`fastly.jsdelivr.net` 和 `testingcf.jsdelivr.net` 的客户端入口均返回 HTTP 200、`x-jsd-version: rq0.91.5`；
- 缓存策略为不可变静态标签缓存。

仓库体积超过 jsDelivr GitHub 包目录列举上限，因此标签根目录和完整文件树接口返回 403；精确标签文件不受影响。标签专用数据接口对 `rq0.91.5` 返回“版本存在但目录超限”的 403，而不存在的 `rq0.91.999` 返回 404。

jsDelivr 根版本数组曾在精确文件已经上线后短暂停在 `rq0.91.4`。持续破缓存核对确认这不是边缘缓存；其内部 GitHub 标签缓存重新验证完成后，于 2026-09-10 04:14:57（Asia/Taipei）同步：

- 旧接口 `https://data.jsdelivr.com/v1/package/gh/shujshujun/my-tavern-scripts` 已列出 `rq0.91.5`；
- 新接口 `https://data.jsdelivr.com/v1/packages/gh/shujshujun/my-tavern-scripts` 已列出 `rq0.91.5`；
- 旧接口 ETag 更新为 `W/"104e-wnrUMK3nJHuXRtvs9LvbyAZVtqE"`；
- 新接口 ETag 更新为 `W/"142e1-tQMsTPYpxFEXrPlW8OJVqElh+QA"`。

最终远端综合校验在第一次重试即确认两个标签 CDN 文件、三个 Release 附件、最新版 Release 指针和 jsDelivr 版本数组全部正确。

## 八、旧版更新检测

发布后在线验证：

1. GitHub `matching-refs/tags/rq` 已列出 `refs/tags/rq0.91.5`，并指向注解对象 `87bbb4dd...`；
2. jsDelivr 新旧版本清单接口均已列出 `rq0.91.5`；
3. 直接运行项目自身 `查询游戏官方最新版本()` 得到 `0.91.5`；
4. 项目自身比较函数实际返回：
   - v0.91.4 对远端 0.91.5：`当前较旧`，会显示非阻塞更新提示；
   - v0.91.5 对远端 0.91.5：`相同`，不会重复提示；
   - v0.91.5 遇到暂时落后的 0.91.4 远端缓存：`当前较新`，不会提示降级；
   - beta／rc／dev 和非 `rq` 标签不会被误判为正式游戏版本。

旧版更新提示线上门禁已通过。

## 九、边界与现场保留

本次发布没有使用 `reset`、`checkout`、`stash`、`clean`，没有删除 untracked，没有整文件格式化，也没有使用 `git add -A` 或 `git add .`。

以下内容均未进入提交、标签或 Release：

- `.codex-tmp` 中的隔离构建、只读证据覆盖层、远端校验脚本与本地“勿发布”测试卡；
- `__devspace_probe_should_not_create__`；
- 四份未跟踪交接资料；
- 全仓构建产生的其他角色卡 HTML／JS 及 source map 差异；
- 《人妻公寓》三份 source map 差异。

按用户约束，上述既有或构建产生的现场差异保留在工作树中，没有删除、覆盖或伪装为干净工作树。

## 十、结论

《人妻公寓》v0.91.5 的两批修复整合、完整门禁、隔离构建、正式构建、角色卡打包、注解标签、发布分支、GitHub Actions、GitHub Release、三个附件逐字节回读、jsDelivr 标签客户端／游戏逻辑、GitHub 与 jsDelivr 版本索引、旧版更新提示均已完成并通过。

正式发布基线为：

- 标签：`rq0.91.5`
- 标签对象：`87bbb4dd82a441c935b7a857912ff62f8fa16e8e`
- 正式打包提交：`e9201f4fe736ba468185115076c84b7482fcf567`
- 发布分支：`release/rq0915`

`rq0.91.4` 未移动、未覆盖，既有正式附件与标签保持不变。
