# PLAY-045／046 玩家身份对齐与验收

本批修复两处当前Persona与提示不一致的问题，沿用原编号。每项首版修复后专项即通过。

## PLAY-045

独立事件的自定义system消息原来保留`{{user}}`，下游自定义消息组装不负责展开它。请求构建时现在只读取一次当前Persona，按字面展开脚本模板中的主角宏；宏接口缺失、报错或仍返回占位符时使用当前Persona显示名。

普通独立事件的导演模板，以及VTR系统契约、入口胶囊和当前卡使用同一请求内身份。用户输入、既有历史、连续性摘要及其他宏保持其原处理路径。展开后的内容同时进入请求与提示词快照，不改写存档中的原模板或历史。未新增模型调用、生成通道、过滤规则或剧情条件。

新增9项实际调用生成函数的隔离回归，修前0/9、修后全通过。覆盖中文、英文、emoji、美元替换符、宏接口三种失败、改名重试、VTR同族来源、用户输入与历史原文保留。提供方返回为中性测试文本，测试捕获真实`generateRaw`参数，不是在线模型测试。

## PLAY-046

跨渠道见闻原来把结构化玩家发言写成“玩家”，同一原文的引用验收却使用当前Persona名。现在读取／编译见闻接受显式姓名；近期正文胶囊、私聊与群聊记忆、朋友圈见闻的四个生产调用传入当前`玩家名()`。直接调用的可选参数保持兼容。

只调整结构化署名和引用预览。消息标识、序号、正文、旧摘要、接收名单与撤回语义不变。新增7项修前0/7、修后全通过，使用真实见闻投影和姐妹群引用解析，覆盖当前改名、特殊字符、引用预览、撤回及跨会话拒绝。

## 验证

- 两项专项与现有见闻、引用、VTR生成通道、独立时间事件相关回归：6文件41/41通过，`play045-046-related.tap`。
- 五份生产文件与两份新测试ESLint：0错误0警告；项目定向vue-tsc通过，diff空白检查通过。
- 两端生产隔离构建：`build-1788817283498-deda29/summary.json`，success=true；游戏逻辑0错误/3警告、客户端0错误/2警告。主目录与候选正式dist各72文件及受监控源码在构建前后不变；schema dump、监听及同步写回已禁用。
- 日志统一在`.codex-tmp/codex-takeover-20260908/`。没有重新跑全游戏套件，不以这41项替代其他编号未完成的验证。

主代理已审实际差异；PLAY-046另经一次有界只读独立复审，无明确问题。两项达到本地代码交付状态，外部发布按统一版本流程另行处理。
