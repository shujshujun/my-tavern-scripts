# PLAY-048：共享地点查询只读与真实Store竞争修复

日期：2026-09-07。本轮标识：l4g8。权威状态仍只维护于《统一修复记录_最新补审_2026-09-07.md》，本文件为本项实施和验证附件，不另建总账。

## 1. 当前基线、任务边界与所有权

继续原修复工作树`C:\Users\Administrator\.devspace\worktrees\tavern_helper_template-consolidated-5d7f765f`，复用`ws_0a48808756`。开工与终验均为`task/unified-repair-20260907@c5943ef6`，暂存区为空。没有新建工作树、切换分支、回退、暂存、提交、组卡、tag或push。

本轮先按交接核对SNAP-25／26；两项必要MVU提交／手机恢复链进一步读取被工具安全检查拦截，已追加总账第74节，未写时间补丁或绕取。随后按用户“记录跳过、继续独立项”要求，转执行计划阶段1的PLAY-048。本项不修改、不补读被拒的时间事务链，不将本项通过抵扣SNAP编号。

修前完整读取目标模块当前HEAD差异：`安若妍不必停系统.ts`已有130增3删，属于PLAY-022／049等混合补丁；本轮不认领、不覆盖这些修改。`src/util/mvu.ts`与`useRoomActions.ts`无HEAD差异。本轮唯一产品增量是在共享地点候选提供方内增加两行说明及一行深复制，**3增0删**；目标文件最终相对HEAD为133增3删，不能把整个差异算成本轮。

## 2. 修前五问与真实链路

**设计初衷：**地图房卡和正文舞台查询地点动作时，应展示当前可用操作与限制；真正的签收、预约恢复和剧情提交由执行入口处理。单纯打开房卡不是玩家已经操作，不应修改权威状态。

**正常运行：**301线路到期时仍需展示大堂／信箱区签收按钮；失效检查点仍需重排预约，保留已经完成的卷宗、登记等硬事实。玩家点击后由真实动作入口先同步最新数据、复核地点和资格，再执行。重复签收不得重复给物品；忙态与离开地点不得派发原按钮。正常Store主动编辑和500ms权威拉取仍应保留。

**玩法与状态影响：**旧实现没有点击业务按钮，仅查询当前地点动作，就可能触发到期／失效恢复并修改Vue响应式Store；深监听随后把整份陈旧stat_data写回，覆盖后台新现金及新完成标记。影响不止301，也包括大堂、信箱区、管理员室、公寓外部、洗手间；远处查询会被当前地点门挡住，是必须保留的正常反例。

**真实根因：**`useRoomActions.房间动作 → 添加安若妍不必停动作 → 安若妍不必停地点动作 → 同步安若妍不必停时间节点 → Store.watchIgnorable(deep:true) → updateVariablesWith整值写回`。提供方在筛选地点前直接同步传入数据。当前真实Store为`src/util/mvu.ts`，有500ms轮询、pull和合法主动写回，不沿用旧“2秒／没有pull”的解释。

**兼容与恢复边界：**已阅读同步函数、失效检查点、当前场次匹配、重新预约、清预约状态和动作执行函数，确认同步确有业务用途。当前动作监听及`执行安若妍不必停地点动作`自身均显式调用同步，启动／回合还存在恢复调用口。修复保留全部显式恢复，不删除Store深监听，不把场次失效统一放行，不补造结局或清除其他完成记录。没有新增持久状态、迁移或第二套缓存。

原始证据只读引用主目录《Codex全量BUG独立复核_修复前置报告_2026-09-06.md》§9.3、《修复项逐项复核》PLAY-048行及旧store-room-matrix／room-store-race探针；旧探针只阅读适配范围，没有执行其原输出写入或覆盖旧结果。第3节的旧受限记录保留，本节给出后来合法取得的新证据。

## 3. 最小产品补丁

文件：`src/人妻公寓/脚本/游戏逻辑/安若妍不必停系统.ts`。

在`安若妍不必停地点动作`入口、调用原同步函数之前，令其局部参数改为`_.cloneDeep(data)`。查询后仍使用完整同步结果生成相同候选，但只修改本次临时副本，不能触发原Store的深监听。执行入口原有同步仍修改其权威事务数据，业务恢复与查询预览各守原职责。

没有改`index.ts`、时间撤销系统、守护系统、Store或房间动作组件。PLAY-022 H8／D1与PLAY-049门锁／二稿补丁完整保留；不能因本项回归通过就关闭022原来的消费者取证断点。

## 4. 新专项及测试适配范围

新增：`tests/人妻公寓/地点动作只读与Store竞争_PLAY048.test.mjs`，共60个实际测试。

- 36个当前地点样本：六地点×到期签收／失效H7／普通未开线×旧缓存／先pull。逐项断言权威完整数据、缓存完整数据及聊天变量未被查询改写，零Store写入、零业务派发；到期按钮仍可用，重复查询结果稳定。
- 15个远处查询反例：管理员室内查询其余五地点，覆盖上述三状态，保留既有零写入行为。
- 3个共享提供方反例：深冻结输入仍能查询，候选与显式同步后的结果一致。
- 6个正常／边界对照：真实500ms轮询；合法Store编辑仍写回；真实签收及重复拒绝；忙态／切房间拒绝；显式失效检查点重排、硬事实保留与重复幂等；换入另一来源数据后pull与重新查询。

真实执行的是Schema、`src/util/mvu.ts`、Pinia、Vue深监听、VueUse的watchIgnorable／useIntervalFn、useRoomActions、301候选提供方、时间节点同步与动作／恢复核心，没有用恒不写Store或恒成功业务桩替换目标。

适配仅包括浏览器对象、变量API容器I/O、UI事件出口和未参与此查询的数据库投影；查询用例对数据库调用计数为0。Node测试时钟只控制setInterval，500ms为显式推进的虚拟轮询时间。后台新值由受控I/O注入。签收样本真实运行执行核心，但外层宿主事务输入是适配的；“换来源”样本验证Store容器换入和pull，不冒充真实宿主切聊天长流程、网络保存、服务端落盘或双端实机验收。

本次查询没有AI请求，不新增超时／迟到网络机制；原按钮忙态、重复、换地点与实际状态复核仍由原路径处理。没有重开用户取消的预设／真实宿主联调。

## 5. 实际验证顺序与保留的失败

| 验证 | 本轮结果 | 证据 |
|---|---|---|
| 修前新专项 | 60项：29通过、31失败，0取消／跳过 | `.codex-tmp/unified-repair-20260907/play048-l4g8-before.tap` |
| 三行修复后同一专项 | 60／60通过，0取消／跳过 | `play048-l4g8-after.tap` |
| 初次相关联合 | 181／182通过；1个素材证据路径失败 | `play048-l4g8-related.tap` |
| 按既有素材证据入口重跑同一联合 | 182／182通过，含上述60专项；0取消／跳过 | `play048-l4g8-related-evidence-root.tap` |
| 目标模块定向类型检查 | exit 0 | `tsconfig.play022-review-f6b2.json`，本轮实际运行 |
| 本次隔离声明下三入口类型检查 | exit 0 | 新`tsconfig.play048-l4g8.json`，覆盖目标模块、useRoomActions、真实Store及依赖 |
| 目标产品＋新增专项ESLint | 0错误、0警告 | `play048-l4g8-eslint.json` |
| 目标产品diff whitespace检查 | exit 0 | 本轮`git diff --check` |
| 双入口生产模式隔离构建 | success true；游戏逻辑0错误3警告，客户端0错误2警告 | `build-1788793954733-d6e0cd/summary.json`及两入口JSON |
| 本轮所有权／保护复核 | 全部通过 | `verify-play048-l4g8.mjs`本轮实际运行 |

修前失败首例明确展示现金实际回到1000、期望12345，后台新增“第二机位”完成标记被丢失；不是仅匹配源码字符串或测试装载失败。原测试与旧helpers未修改。

初次联合的唯一失败为`安若妍不必停资源.test.mjs`读取候选目录内不存在的`output/imagegen/rqgy-reset/adult-completion/anruoyan-301-no-stop/manifest-v1.json`，错误为ENOENT。原测试本身已有`RQGY_CG_EVIDENCE_ROOT`参数，故重跑时按其原契约指定只读主目录`D:/SillyTavern/tavern_helper_template-consolidated`，生产WebP仍取候选。没有改断言、跳过测试、复制／改造素材或替换证据；第一次失败TAP原样保留。此为缺失路径适配，不是安全拦截后的变换方式绕取。

相关联合包含原`安若妍不必停`、`安若妍不必停资源`、PLAY-022三个专项和PLAY-049二稿／门锁专项，与本轮新专项一起运行。182是合计数，包含60，不能相加成242。素材来源SHA测试通过不代表完成全项目素材／外部发布闭环，也不抵扣PLAY-044／050等原号。

隔离构建使用已读过的现有`isolated-build.mjs`及当前根webpack配置；在创建编译器之前剔除schema dump、宿主监听、同步器，声明与产物仅写本次`.codex-tmp`构建目录。两处正式dist均72文件，主目录19484503字节、候选19486508字节；两处源码与dist在构建前后校验相等。保留构建警告，不冒称零警告。

## 6. 最终差异和任务期保护

只读保护程序在内存中扣除本轮唯一三行增量后，目标文件SHA256与开工完全一致：`6050c26e9feefc7d218aab1d35ef21f809bcf149feb5db509b75b3fc6d758a49`。这证明原混合PLAY-022／049补丁逐字节保留，并未实际回退或重写产品。修后目标SHA为`ab7ac18572cb526961548d98a4edf2694ac5d5651c37353ec05bbfa7510a28a9`，新测试SHA为`07c9e107f2a2429bbfd74fac121670a0844213a9d6ad4696dcac9816481b81c2`。

index、时间撤销系统、守护系统、Store、useRoomActions及原a4e6／c72d报告的开工／终验SHA全部一致，具体固定值保留在只读保护程序内。全体原测试／helpers（仅排除本轮新增专项）聚合仍为`155618a9d92cb704cef0332e935a852f84b3cfd3cbd1cd25ea295443e165eec0`；正式dist任务期聚合仍为`33077f117411824101955cb5447ebeef04a77760a080979c23b31253be17d999`，均与开工相同。此处聚合算法为原find／sort／sha256sum链，与构建JSON行对象聚合算法不同，不能混比。

## 7. 本项结论

**PLAY-048本地代码验收完成，待整合／发布，不表示已发布。** 新增完成原编号仅048一项。SNAP-25／26仍按总账74节待复审，022／042及其他原待复审编号不顺带关闭；原复审失败不改写。

按同一权威总账原54口径扣除048一次后：**48项未闭环代码＋4项条件待证＋1项素材＝53个主要原编号待处置，本地验收完成PLAY22项**。029／039等既有完成项不重复扣数。后续继续按原执行计划及总账中各编号最新断点处理独立项，不回退已完成项、不重开全游戏审查。
