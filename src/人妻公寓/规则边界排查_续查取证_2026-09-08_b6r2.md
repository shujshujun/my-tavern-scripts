# rq0.90.4 后规则边界排查：断网后续查取证

> 最新续查：2026-09-08 第二次继续排查见第9节（c8v3）。第1—8节保留上一轮证据，不以其阶段结论替代第9节的新结果。

日期：2026-09-08。性质：源码只读审查与既有专项测试；本轮只新增审计报告，不修改产品代码、提示词、规则、测试或存档。

## 1. 连接与证据状态

用户要求断网后继续重试，并在插件连接失败时明确说明。

本轮 DevSpace 已恢复连接，复用 workspaceId `ws_0a48808756`，没有新建工作树或切换分支。源码读取、Git检查和既有专项测试均曾成功。随后一条扩大差异审查的组合命令被工具明确拦截，返回：

> 因 OpenAI 无法确定请求的安全状态，已拦截此工具调用。

这不是本轮再次发生的502或断网。拦截后独立的Git元数据复核仍成功，说明连接仍可用。被拦截的组合命令没有产生可用证据；其中的新增约束差异统计、项目目录外变更枚举和数据库桥定位，不计为已完成。

本记录只记载本次执行事实，不把本执行者的工具状态转化为游戏需求、运行过滤规则或其他执行者的永久禁止清单。

## 2. 实际工作树与范围

- 工作树：`C:\Users\Administrator\.devspace\worktrees\tavern_helper_template-consolidated-5d7f765f`
- 分支：`task/unified-repair-20260907`
- 开始及收尾成功核对的HEAD：`d29e2ded`
- 发布基线：`rq0.90.4`
- 基线提交：`f1bbde58d1b8e59f374a8089a38886ce5b1c69f5`
- `git diff --shortstat rq0.90.4 -- src/人妻公寓`：610个已跟踪变更文件，112861行新增，2173行删除。

本报告写入前，610个文件的扩展名统计：

| 类型 | 数量 |
| --- | ---: |
| ts | 146 |
| vue | 12 |
| yaml | 5 |
| json | 16 |
| md | 142 |
| webp | 287 |
| png | 2 |

其中179个为代码或配置类候选，不能把全部610个都称为源码。`src/人妻公寓`另有32个未跟踪文件：3个ts、23个md、6个txt。因此，按上述扩展名计算的代码/配置变更候选共182个；其中JSON仍可能是素材清单，不自动等于运行逻辑。

收尾报告写入前，整个工作树Git状态计数为57条已跟踪脏状态、99条未跟踪状态；后者包含目录折叠条目，不是99个精确文件。保留所有其他窗口与用户的既有差异。

已完成当前项目目录中代码/配置类文件的定向关键词扫描，覆盖未跟踪ts文件，并深入读取下述相关运行模块。尚未逐文件完成182个候选的全量语义审查，也未完成所有新增条件分支的行为验证。

## 3. 当前仍存在：202《不再留门》固定票的表达限制（沿用 RISK-004）

### 当前代码证据

文件：`src/人妻公寓/脚本/游戏逻辑/不再留门系统.ts`

- 第68行定义固定拍数：A1=2、A2=2、A4=3、A5=3、A7=2、A9=2，共14拍。
- 第294行起的`构造事件`组装各拍事实。
- 第327行在返回的事件字符串中统一附加：`本轮仅演这一拍，新增演出保持非露骨。`
- 第338行的`准备现场`返回该事件；第672行的后续剧情也使用同一个`构造事件`。

因此，这不是只存在于注释或图片任务中的文字，而是当前事件生产者实际返回的提示内容。搜索还定位到`回合引擎.ts`、`index.ts`和相关事务消费者。本轮没有捕获真实宿主的最终rawPrompt，不能把静态事件生产证据夸大为真实模型收包或输出观测。

### 需求来源与历史状态

`周小满承接线_不再留门_外部取证与母带封存_玩法设计_v1_2026-09-02.md`第5、396、475行等记载的是8张非露骨CG的制作范围。图片制作范围本身不能证明14段AI文字演出也得到同样授权。

`统一修复记录_最新补审_2026-09-07.md`第1061—1063行已把同一问题列为RISK-004：没有找到将图片提供方/制作尺度扩展为全部文字尺度的独立需求依据，保持需求来源待核。本轮确认该句仍在，并非已经清理；不另造一个重复BUG编号。

`git log -S '新增演出保持非露骨'`在当前历史中指向`2c04527f`（2026-09-04的集成检查点）。这表明该限制在此检查点已经入Git，不能据此推断原始编辑者、具体模型或最初未提交编辑时间。

### 影响与结论

确认存在文字表达限制，且当前查到的资料不足以证明其独立文字需求依据。应作为当前残留风险追踪，而不是视为正常图片制作约束自动放行。

直接可证范围是上述14拍固定事件生产者；没有证据把它说成全游戏的统一禁令。A8交由既有普通场次处理，不能仅凭这句就断言A8也被同一模板直接限制。旧活动票、已注入事件、最终发送提示和实际演出影响仍需分别核验。

本轮未删除或改写这句话，也没有把录制许可、角色知情、剧情顺序等独立机制一并认定为错误。

## 4. 历史确曾发生：301《换掉》未经授权改写；当前关键纠正仍在

### 历史证据

实际读取了Git提交`ee8d12d1`（2026-09-05）的文档差异，提交标题为“docs: 记录301越权改写事故及后果”。该历史事故记录明确写明：

1. `0ed16175`曾把执行代理自身的内容尺度限制写进固定拍、结局后开场和姐妹群提示，并把部分事实泛化。
2. 301照片专场误接通用`微信群文本(..., '姐妹')`及302的`验收双重继承余波公开事实`。历史复现中，普通接吻表述和“镜头下”分别被不同层拒收，可能导致群聊数量不足、整批失败及后续重试占用请求。
3. `2d292b9a`第一次纠正不完整，后置验收与旧票迁移存在遗漏。
4. 历史记录指出早期测试替身绕过了真实群消息过滤，测试通过不能证明完整链正确。
5. 记录将后续纠正归于`55cea99e`，并明确没有真实宿主进程崩溃或用户实际存档已受损的观测证据。

以上为本轮直接读取到的Git历史文档事实，不是本轮重新复现旧提交的全部行为，不据此推断恶意或具体执行模型。

### 当前实现核实

已读取当前：

- `src/人妻公寓/脚本/游戏逻辑/手机/安若妍换照姐妹群.ts`
- `src/人妻公寓/脚本/游戏逻辑/手机/生成引擎.ts`
- `tests/人妻公寓/安若妍换掉约束边界.test.mjs`

当前301专场在第108行调用`微信群文本`时不再传入旧的通用“姐妹”隐私模式。该模块没有导入或调用旧302专属验收器，而是使用对应成员已经接收的群消息以及跨线公开事实校验。

### 本轮实际测试

命令：

```text
node --test tests/人妻公寓/安若妍换掉约束边界.test.mjs
```

结果：14 tests，14 pass，0 fail，0 skipped，0 cancelled。

关键覆盖包含：301已发照片反应经过真实解析/验收后整批入库；当前固定票更新时保留事务归属与进度；其他线路未公开事实不被错误放行；完成ID与失效绑定保护；停止和迟到结果不消费反应键。

该测试只替代外部生成和记忆读取，群消息解析及被测验收使用真实实现。但它没有调用真实模型，不是实际AI演出或手机/桌面宿主端到端验收。当前这条历史问题不能再次作为未修复的301新缺陷上报。

## 5. 手机过滤与路由：已确认的范围，以及不能作出的推论

完整读取了`手机输出安全.ts`及`手机/生成引擎.ts`。

- `验收群聊隐私`按楼务、姐妹、孕情、特定剧情余波等模式分别判断。它确实包含关键词拒收，不能因为名字叫“安全”就免于审查；但也不能把所有分支都解释成全游戏的提供方政策过滤。
- `微信群文本`第447行起通过可选`隐私模式`决定是否调用该验收；因此“哪些消费者传了哪种模式”比只看关键词名单更重要。301的历史误接就是此类范围错误。
- 手机生成有数据库、正文、自定义等配置来源。当前读取到的手机路由中，自定义模型读取配置值；数据库调用失败后直接返回空结果，代码没有在这个分支悄悄改用正文API。这里的结论仅覆盖手机路由，未覆盖所有游戏调用及外部数据库插件。
- 封套完整性、长度、说话人格式、生成租约、请求取消和角色知情校验有不同用途；本轮没有把它们统一删除或统一认定正确。每个场景仍需核对独立设计依据。

未发现的关键词不等于不存在隐式限制。本轮没有完成所有Schema、状态机、继承模板、缓存恢复以及外部插件的完整审查，不能宣称没有全局限制或隐藏模型切换。

## 6. 开发入口与运行层分开记账

当前`AGENTS.md`与`新窗口入口_精简.md`明确要求代理自身边界不能自动变成产品规则，并禁止把模型拒答当角色拒绝。此类工程要求的存在不能替代源码验证。

`.cursor/rules/破限.mdc`仍是带有`Usage Policies (Official)`标题、`alwaysApply: true`标记和权限声明的本地开发文件。本地标题不能证明提供方官方授权或政策时效性。本轮未验证其真实性，也没有取得它被导入游戏运行提示的证据。此项仍属于开发上下文混合风险，不能与已确认的运行事件限制混为一谈。

旧交接中的`blockedWork`内容本轮未重新核对当前本地全文，不把上轮历史副本结论提升为当前源码证据。

## 7. 未完成范围与后续验收对象

本轮尚未完成：

- 182个代码/配置变更候选逐文件语义核对，以及项目目录外的相关生成/构建注入代码。
- 所有新增限制性分支的完整差异清单。相关组合命令被工具拦截，没有结果。
- RISK-004从事件生产、排队、活动票、实际发送、验收、落库到恢复的完整动态链；真实最终提示未捕获。
- 全部群聊、私聊、公开动态和特殊剧情对过滤器的调用范围逐项验证。
- 主正文/隔离生成/数据库桥的完整模型请求链，以及用户实际配置的外部插件和提供方行为。
- 真实SillyTavern桌面/手机长流程、真实模型演出、全项目回归。

这些是本轮覆盖状态，不是对游戏设计或未来任务新增限制。后续结论仍应以当前代码和实际证据为准。

## 8. 本轮交付边界

结论：连接恢复并取得了实质源码证据；历史未经授权改写有记录，301关键纠正当前仍通过专项验证；202的RISK-004表达限制当前仍存在且独立文字需求依据未证实。不能宣称0.90.4之后全部实现已经排查完成，更不能声称所有边界已经清理。

没有改动产品源码、提示词、项目规则、测试、正式dist或用户存档；没有创建工作树、切换/回退分支、提交、组卡、tag或push。本轮只新增此报告。

---

## 9. 第二次继续排查：请求、正文提交与格式层取证（2026-09-08，c8v3）

### 9.1 执行范围与连接

用户本次要求“继续排查，并记录”。继续使用同一工作树与workspaceId，只审查、执行隔离诊断并记账，不进行产品修复。

开工重新读取了本报告、AGENTS.md和新窗口入口顶部。分支仍为`task/unified-repair-20260907`，HEAD仍为`d29e2ded`，基线提交再次核实为`f1bbde58d1b8e59f374a8089a38886ce5b1c69f5`。暂存区查询未返回文件。开工Git状态157条，后续一次复核158条；未将条目变化擅自归属于本轮或清理，脏工作树不等于只有本审查者的变化。

本轮没有收到502。第一次拦截发生在追踪202旧票、事务消费者及稽查历史来源的组合查询，没有返回源码或Git证据；此后独立Git查询、其他文件读取、测试及报告编辑成功。第二次拦截发生在报告与证据文件已写入、回读之后的收尾Git加JSON解析校验组合命令；该末次组合没有返回结果，不能记作收尾Git核验或JSON解析验证通过。本轮共记录2次工具明确拦截，最后一次成功Git读取仍为此前的d29e2ded，不假称第二次拦截后的最新HEAD已核对。拦截记录不变成产品规则，也不变成其他执行者的永久禁止清单；被拦截查询均不计入覆盖，也没有换工具或变形重试。

### 9.2 BND-C8V3-01：服务端拒答文本可通过正文提交门（已复现的分类风险）

**编号性质：**本轮审计局部编号，不替换既有PLAY/SNAP/RISK编号，也不宣称已穷尽历史报告而证明绝无重叠。

**状态：**真实生产解析函数复现成立；主回合接线已定段读取；实际宿主保存、资源结算和真实模型行为未执行。建议优先核验服务响应分类与状态提交边界。

#### 设计、正常行为与作用

项目现行入口明确不允许把模型拒答当成角色拒绝或剧情事实。正文解析正常应隔离机器协议，故事正文才能成为剧情、资源、变量与记忆的依据；服务端未完成任务的说明应保持可见且与剧情成功状态区分。格式解析器可以只负责格式，但上层不能仅凭格式通过就认定模型完成了叙事任务。

#### 本轮真实证据

采用中性样本：`抱歉，我无法继续这项请求。`，没有调用外部模型，没有构造成人内容。

- `正文输出边界.ts:123`的`提取可提交正文`负责协议与展示包装清理，该样本被原样保留。
- `正文生成完整性.ts:142—157`的`判定正文提交`在第150行以`Boolean(成功正文)`判断可提交。裸样本及包在完整`<content>`中的同一样本都得到`可提交: true`、`失败残稿: ''`。
- 同文件第122行的`选择正文生成原文`优先采用含有效文本的最终返回。测试中最终拒答会胜过缓存中的普通维修叙事。
- 同文件第164行的`生成失败时可保留的流式正文`要求有效文本与自然句末；上述拒答满足该门，返回原样本。该函数本身不接收API错误类别，不能据此区分网络结束异常和服务拒绝。
- `手机文本格式.ts:54`的`规范手机单气泡`及第95行的`解析微信私聊气泡`也接受该样本，后者返回单元素数组。这仅证明手机格式原语不区分服务说明，不能直接上升为完整手机调用或入库已复现。

实际输出摘录：

```json
{"case":"plain_refusal","显示正文":"抱歉，我无法继续这项请求。","成功正文":"抱歉，我无法继续这项请求。","失败残稿":"","可提交":true}
{"case":"wrapped_refusal","显示正文":"抱歉，我无法继续这项请求。","成功正文":"抱歉，我无法继续这项请求。","失败残稿":"","可提交":true}
{"case":"phone_format_only_not_persistence","bubble":"抱歉，我无法继续这项请求。","privateMessages":["抱歉，我无法继续这项请求。"]}
```

#### 已读取的主回合影响链

`回合引擎.ts:2650—3049`显示：正文返回/流式选择 → 原文稽查和专属剧情验收 → 第2921行正文提交判定 → 非空时继续创建助手临时楼、携带事件摘要元数据并进入变量解析。普通无专属事件的场景不会自动获得某条专属剧情的事实验收保护。

所以，当上游把服务拒答作为普通字符串交回、且其余回合条件满足时，这个局部分类门不能阻止它进入后续业务路径。继续保存为历史或交给下一模型，会有把服务说明混入剧情上下文的风险。但本轮没有运行完整主回合，不能宣称一定推进了时间、扣费、资源或某一特殊剧情，更不能声称用户真实存档已经损坏。手机完整生成还存在回复封套、请求有效性和场景语义门，不能由底层格式测试代替这些路径。

#### 与0.90.4的时间归属

以下五个当前生产文件与`rq0.90.4`逐文件比较无差异，`git diff --quiet`实际退出码为0：

1. `正文输出边界.ts`
2. `正文生成完整性.ts`
3. `正文协议安全.ts`
4. `预设输出兼容.ts`
5. `游戏机器协议.ts`

因此，本轮证明的是**当前仍存在、且核心解析实现已经在0.90.4基线中的分类缺口**，不是证明某个模型在0.90.4之后新加了审查政策。手机格式原语的引入时间未在本轮核实。后续新功能复用旧分类门仍可能继承风险，但不能据此伪造新增时间或作者归属。

#### 修复边界与兼容条件（仅建议，未实施）

应在服务响应与业务提交之间区分完成状态，优先利用实际接口可用的错误、完成状态和响应类型；这些字段是否由当前宿主暴露仍需检查。纯文本模糊样本应保留原始证据并向玩家显示未完成状态，不自动当作角色决定，也不靠改写请求绕过提供方拒答。

不能全局删除“拒绝”“不能”等词，正常角色拒绝邀请仍是有效剧情。不能只从界面隐藏服务说明、却照常消耗事务或生成记忆；也不能清空历史存档。重试、取消、迟到、回档和跨聊天必须继续按原事务归属保护。已有样本测试只确认分类现状，未覆盖这些完整生命周期。

### 9.3 BND-C8V3-OBS-02：普通回合的本地角色拒绝兜底，仍需核对设计来源

这是已读代码机制的待核观察，不直接认定为未经授权的内容政策，也不归为已经复现的API拒答转剧情。

`稽查系统.ts`的`输出稽查`既可能因脚本阶段上限要求重写，也可能因结构化控制块缺报/坏报、请求字段缺失且正文达到对应条件而要求重写。`回合引擎.ts:2878—2892`的普通场景分支在重写仍未通过时，会调用`无处罚拒绝正文`，然后把稽查状态设为通过并继续后续路径。专属多拍剧情不统一走这条普通兜底。

这里必须区分：角色本来按设计拒绝；模型实际越过脚本阶段；模型只是不按协议输出；API调用失败或服务端拒答。已读代码中这条本地兜底由输出稽查未通过触发，不是直接catch全部API异常。它是否可能把纯协议失败替换成角色决定、是否符合用户原设计，仍需定向行为测试与历史需求核对。

本轮关于其Git来源的查询被拦截，因此不判断它何时引入，不给出新增BUG或恶意改写结论，不改动阶段机制，不移除角色正常拒绝。

### 9.4 已核实的路由与可排除误报

**变量解析不是正文题材政策。**`回合引擎.ts:730—1099`显示正文使用`generate`，独立变量解析另走配置选择的数据库或自定义通道。自定义模型取自配置；未配置和请求失败有独立结果。本轮还读取了第2650—3049行的变量失败回退基准与保留正文路径。这里的可写字段、单轮上限、格式收口属于字段职责，不应仅凭“禁止”措辞认定为提供方规则；其全部数值设计不在本轮重新验收。

**数据库本地调用桥没有在已读调用点新增提供方内容政策。**`数据库桥.ts:2308`的`通过数据库生成`把调用者消息、可选presetName和max_tokens交给全局数据库AI租约。它不是外部callAI实现；本轮没读取玩家真实密钥、外部插件完整源码或真实响应，不替外部插件背书。

**规划结果存在明确外部输入入口。**完整读取`数据库剧情规划桥.ts`：第440行在已规划结果成立时采用插件改写后的`user_input`，否则保留原输入；原请求的其他参数与注入继续传递。桥中的附加文字处理游戏日期、角色与知情范围。实际使用的预设和插件仍可能带入其他文字，这是需要单独检查的来源，不能把未检查的外部内容等同于本地偷偷植入。

**“最后模型边界词条”不是内容审核开关。**`预设桥.ts:100—115`用于识别最后一条启用消息是否留下未闭合的输出/思维标签，以控制流式显示。该变量名中的“边界”是协议边界。本轮不把它计入隐藏内容限制。实际启用预设内容仍未从真实宿主捕获。

**手机路由保持限定结论。**`手机/生成引擎.ts:365—539`的已读分支在已调用数据库后失败返回空结果，不自动切去正文API；自动模式在没有数据库能力时选择正文属于另一分支。该规则的既有测试是源码契约测试，不是本轮真实网络计费验证。

### 9.5 本轮自动化证据

隔离诊断保存在：

`.codex-tmp/boundary-audit-20260908-c8v3/response-classification.test.mjs`

实际命令：

```text
node --test .codex-tmp/boundary-audit-20260908-c8v3/response-classification.test.mjs
```

最终扩展版本结果：8 tests、8 pass、0 fail、0 cancelled、0 skipped。五项现状复现和三项对照；被测正文提取、提交判断、流式选择与手机格式函数均使用真实生产实现，不以测试桩代替。没有宿主、网络、模型、存档和资源结算调用。先前6项执行是该诊断的中间版本，不能再加进最终8项作重复计数。

**重要：复现用例刻意断言当前错误分类成立。8/8通过表示风险复现成功，不是修复通过。**对照验证了正常维修正文、角色拒绝晚餐邀请仍能通过，而空输出、纯机器协议或其他说话人不被误接收。

另实际执行：

```text
node --test tests/人妻公寓/双预设输出兼容.test.mjs tests/人妻公寓/正文保留与变量降级.test.mjs tests/人妻公寓/数据库失败不回退正文.test.mjs tests/人妻公寓/数据库时间召回桥.test.mjs
```

结果：48 tests、48 pass、0 fail、0 cancelled、0 skipped。它们混合了真实纯函数行为测试、模拟运行时测试和源码契约断言，不能统一称为端到端测试。测试中预期的模拟正则异常和20ms规划超时日志不表示插件网络故障。48项通过不否定BND-C8V3-01，因为其样本不在这些原有断言中。

### 9.6 当前覆盖、未完成与旧项状态

本轮完整读取了六个重点模块：`正文生成完整性.ts`、`正文输出边界.ts`、`稽查系统.ts`、`数据库剧情规划桥.ts`、`预设桥.ts`、`手机文本格式.ts`。另外定段读取`回合引擎.ts`、`数据库桥.ts`和`手机/生成引擎.ts`。完整读取不等于每个函数都已完成行为验收。

第2节的182个候选数量为上一轮统计，本轮没有重新核算，也没有逐一完成语义审查，不提供虚构完成百分比。

- RISK-004沿用第3节“当前残留、文字需求来源待核”的证据状态。本轮旧票与事务链查询被拦截，不能宣称补齐了202最终发送或恢复证据。
- 301关键纠正的14/14仍属于上一轮执行结果，本轮没有重复运行，不冒充本轮新增测试。
- 新增BND-C8V3-01为既有分类门的当前复现，不能计作0.90.4之后新植入政策。
- BND-C8V3-OBS-02为机制与需求待核，不与已复现问题混数。
- 仍待核：全部新增功能与配置、旧票重建与最终发送、主回合完整持久化、手机完整落库、真实启用预设、外部数据库插件，以及桌面/手机宿主与真实模型演出。

### 9.7 写入边界

本轮对产品源码、运行提示词、项目规则、原有测试、正式dist和用户存档没有执行修改。仅追加本报告的最新入口与第9节，新增隔离诊断文件，并写入本轮机器可读证据记录。没有创建工作树、切换/回退分支、清理用户改动、提交、组卡、tag或push。

本轮结论：继续取得了实质证据，确认服务拒答文本可以通过当前正文分类门，且手机底层格式同样不负责区别服务状态；核心正文解析实现已存在于0.90.4基线。另记普通回合本地拒绝兜底的待核语义风险，并排除一个把协议变量名误当内容政策的误报。尚未完成全量审查，也没有进行产品修复。
