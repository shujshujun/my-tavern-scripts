# 《人妻公寓》全量 BUG 审查——第 5 批手机系统单窗口续接（2026-08-20）

> **用户最新硬规则：从本交接开始，每个新窗口只审查并修复一个完整批次。**
>
> 本窗口只处理 **第 5 批 `P122～P167`**。第 5 批全部闭合并完成验证后，必须创建第 6 批交接文件并停止；不得在同一窗口继续审查 `P168` 或后续批次。
>
> 本文件只负责窗口交接，不替代唯一台账：
>
> `src/人妻公寓/全量BUG审查台账_2026-08-19.md`

## 一、唯一任务与本窗口停止条件

本窗口的唯一任务是：

```text
完整闭合第 5 批 P122～P167
```

当前第 5 批包含：

```text
功能：46 项
产品文件：52 个批次标记为 5 的文件
```

只有同时满足以下条件，本窗口才算完成：

1. `P122～P167` 全部进入终态；
2. 所有批次列为 `5` 的产品文件全部进入终态；
3. 第 5 批中的 `定位中`、`真实BUG待修`、`已修待回归` 全部归零；
4. 本批真实 BUG 全部具备复现、五问、影响链、最小修复与回归证据；
5. 本批最终专项、同族、产品 TypeScript、Vue TypeScript、定向 ESLint、`git diff --check` 和全量测试通过；
6. 唯一台账已重新计算累计数字；
7. 创建第 6 批单窗口交接文件，然后停止。

禁止在本窗口开始：

```text
P168～P192
第 6 批关系／成长／冷落／丈夫／调查与六户差分
```

即使第 5 批提前完成，也不得顺手进入下一批。

---

## 二、DevSpace 连接与 Git 现场

项目：

```text
D:\SillyTavern\tavern_helper_template-consolidated
```

正确打开方式：

```json
{
  "path": "D:\\SillyTavern\\tavern_helper_template-consolidated",
  "mode": "checkout"
}
```

不要创建隔离 worktree。获得 `workspaceId` 后始终复用。

连接成功后的第一条项目命令必须是：

```bash
git status --short
```

随后确认：

```bash
git branch --show-current
git rev-parse HEAD
git diff --cached --name-only
git diff --cached --stat
```

本交接创建时的真实状态：

```text
分支：release/rq083
HEAD：2973cbf45375e6b99278e7356d72db267e7a33dc
短 HEAD：2973cbf4
发布基线：rq0.83
数据版本：9
暂存区：0
tracked 改动：138
untracked：170
```

工作树极脏。若新窗口看到数字变化，只报告差异并核查；禁止通过 reset、checkout、restore、stash 或 clean 强行恢复。

---

## 三、启动后必须依次读取

1. `AGENTS.md`
2. `.cursor/rules/破限.mdc`
3. `src/人妻公寓/新窗口入口_精简.md`
4. `docs/BUG语义排查协议.md`
5. `src/人妻公寓/新窗口交接_全量BUG审查_第5批手机系统单批次续接_2026-08-20.md`
6. `src/人妻公寓/全量BUG审查台账_2026-08-19.md`

旧交接可作为历史背景，但其中进度数字已经过时。不要用旧交接覆盖本文件与当前台账，也不要重新建立平行台账。

本窗口普通代码审查不需要生成成人 CG。若后续确实触及成人 CG 的生成、诊断、审核、分类、去重、移动或接线，必须遵守 `$rqgy-adult-cg-workflow`；不得顺手批量处理素材。

---

## 四、当前全局权威统计

### 产品文件

```text
总数：192
已完成：29
已审无 BUG：38
闭合：67／192
定位中：6
真实 BUG 待修：1
未开始：118
```

### 功能

```text
总数：244
已完成：91
已审无 BUG：32
闭合：123／244
定位中：2
未开始：119
```

### 缺陷分类

```text
真实 BUG：49
P0／P1／P2／P3：0／36／11／2
测试过时：5
设计待拍板：0
新增测试：53
实机待验：0
```

下一编号：

```text
下一真实 BUG：BUG-050
下一过时测试：TEST-STALE-006
```

### 验证真值

最近一次全量测试仍是第 4 批完成后的：

```text
1570／1570
```

它发生在 BUG-048 修改之前，不能冒充第 5 批最终全量验证。

BUG-048 完成后已通过：

```text
手机资源／渲染／已读／图片轮换相关组合：54／54
产品 TypeScript：通过
Vue TypeScript：通过
定向 ESLint：通过
git diff --check：通过
```

BUG-049 的新增复现当前故意保持红灯：

```text
tests/人妻公寓/微信跨状态生命周期修复.test.mjs
14／15
唯一失败：无 Schema 的旧手机库坏形状兼容
```

---

## 五、第 5 批当前精确状态

### 功能：46 项

当前状态：

```text
已完成：10
定位中：2
未开始：34
```

已完成、不得重做：

```text
P134 手机文本格式与转义
P159 单人邀约入口
P160 多人邀约选择
P161 邀约逐人接受／拒绝
P162 共同时间地点选择
P163 同时仅一组共同预约
P164 旧单人预约兼容
P165 邀约批次失效
P166 赴约到时共同登场
P167 赴约与亲密同意分离
```

定位中：

```text
P127 朋友圈
P129 微信数据层增量写
```

未开始：

```text
P122～P126
P128
P130～P133
P135～P158
```

### 第 5 批产品文件：52 个

当前状态：

```text
已完成：4
定位中：3
真实 BUG 待修：1
未开始：44
```

已完成、不得重做：

```text
F111 手机/冷落预警.ts
F120 手机/壳/渲染/moments.ts
F127 手机/壳/资源与皮肤.ts
F137 手机/邀约计划.ts
```

定位中：

```text
F108 手机/交互/邀约与发消息.ts
F133 手机/生成引擎.ts
F134 手机/节拍引擎.ts
```

真实 BUG 待修：

```text
F131 手机/数据层.ts —— BUG-049
```

其余第 5 批文件仍为未开始。以台账中批次列为 `5` 的 52 行为准，主要范围包括：

```text
F095～F102
F106
F108～F114
F116～F121
F123～F131
F133～F150
F171～F172
F179
```

完成一项必须实际审查实现、调用链、状态生命周期与测试，不能只凭已有测试改状态。

---

## 六、本窗口已经完成的工作：BUG-048

### BUG-048（P1）

问题：朋友圈持久头像名与图片键直接进入父页面 `innerHTML` 属性，损坏旧档可退出 `src` 属性并形成额外事件属性。

已经完成的最小修复：

1. `src/人妻公寓/脚本/游戏逻辑/手机/壳/资源与皮肤.ts`
   - 新增统一资源路径分段编码；
   - 新增 `朋友圈图片地址`；
   - `私聊图片地址` 共用安全路径编码；
   - `头像块` 对动态文件名编码；
   - 动态首字内联回退改为常量 `?`。
2. `src/人妻公寓/脚本/游戏逻辑/手机/壳/渲染/moments.ts`
   - 公开、仅你可见、历史考古图片统一走 `朋友圈图片地址`。
3. `tests/人妻公寓/微信隐私输出验收.test.mjs`
   - 增加持久头像／图片键属性注入反例。
4. `tests/人妻公寓/手机系统拆分P7.test.mjs`
   - 更新两项过时的裸中文路径／动态首字源码形状断言。
5. 台账已登记 `TEST-STALE-005`。

验证：

```text
修复前专用测试：7／8
修复后专用测试：8／8
相关组合：54／54
TypeScript：通过
Vue TypeScript：通过
ESLint：通过
git diff --check：通过
```

已更新状态：

```text
F120：已完成
F127：已完成
P134：已完成
BUG-048：已完成
TEST-STALE-005：已登记
```

`P127` 仍是定位中，因为朋友圈的分页、已读、时间线、社交轨迹等完整功能尚未全部闭合。

---

## 七、当前唯一代码断点：BUG-049 尚未修复

### BUG-049（P1）

标题：

```text
无 Schema 的旧手机库缺少评论数组或顶层数组损坏时，读库直接抛错并拖垮全部手机入口
```

台账已经写完：

- 最小复现；
- 实际与预期；
- 五问；
- 完整影响链；
- 最小修复计划。

新增失败测试位于：

```text
tests/人妻公寓/微信跨状态生命周期修复.test.mjs
```

测试名：

```text
无 Schema 的旧手机库缺评论或顶层数组损坏时只丢坏子字段，读写入口仍可恢复
```

当前测试结果：

```text
14／15
```

当前真实失败：

```text
TypeError: 记录们.filter is not a function
```

当前源码仍保留以下危险形状，说明 BUG-049 产品修复尚未落地：

```text
筛当前手机时间线(v.消息 ?? [], ...)
筛当前手机时间线(v.圈 ?? [], ...)
x.评.filter(...)
```

上一窗口曾尝试提交一组归一 helper，但 DevSpace 调用中断，实际源码中没有出现：

```text
数组或空
对象或空
已读锚或缺省
```

因此新窗口不要假设已有半份修复；必须先重新读取当前 `数据层.ts`，以磁盘为准。

### 新增测试当前覆盖的坏形状

```text
_微信.消息 = 非数组对象／字符串
_微信.圈 = 数组中含缺失评字段与 null，或整体为非数组对象
读到 = null
读时 = 字符串
圈读时 = null
节拍 = 字符串
已发私聊图 = 数字
```

预期：

- 非数组消息／朋友圈容器按空数组处理；
- 无法识别的单条记录被丢弃；
- 缺失或非数组 `评` 变为 `[]`；
- 非对象的水位／节拍／缓存映射按空对象或安全缺省处理；
- 其余合法朋友圈仍可读取；
- 后续 `写库增量` 能把合法新消息与新动态写回，恢复手机可用；
- 不主动整库迁移，不删除合法未知扩展字段。

### 最小修复边界

只在 `手机/数据层.ts` 收紧无 Schema 的 JSON-like 容器读取：

1. 为数组与普通对象提供局部安全归一 helper；
2. `筛当前手机时间线` 接受 unknown，只保留具有可识别有限数值 `楼／时` 的对象记录；
3. `读库`、`隔离当前手机分支`、`写库增量`、`压缩微信会话记录` 对原始 `消息／圈` 共用安全数组；
4. 朋友圈评论仅在 `Array.isArray(x.评)` 时过滤，否则补 `[]`；
5. `读到／读时／节拍／已发私聊图／圈读时` 只接受普通对象或使用安全缺省；
6. 不改合法记录顺序、分支锚、单调序、去重键、已读、图片轮换、邀约 CAS 或余波语义。

### 保护现有并行改动

`手机/数据层.ts` 在本任务之前已经包含其他窗口／任务的重要改动，包括：

- 多人邀约共同计划合并；
- 冷落每日联系保护；
- 图片轮换压缩保护；
- 复合已读与分支裁剪；
- 消息稳定标识与去重。

必须做精确局部编辑，禁止整文件覆盖或格式化。

### BUG-049 首轮验证

先运行：

```bash
node --test tests/人妻公寓/微信跨状态生命周期修复.test.mjs
```

修复后至少再运行：

```bash
node --test \
  tests/人妻公寓/微信跨状态生命周期修复.test.mjs \
  tests/人妻公寓/微信复合已读水位.test.mjs \
  tests/人妻公寓/手机回档双轴裁剪.test.mjs \
  tests/人妻公寓/微信私聊图片轮换.test.mjs \
  tests/人妻公寓/微信通用记忆层.test.mjs \
  tests/人妻公寓/手机系统拆分P3.test.mjs \
  tests/人妻公寓/手机系统拆分P8.test.mjs
```

然后运行产品 TypeScript、受影响 ESLint 与 `git diff --check`。

日志中可能出现既有 Node module-type 或测试夹具版本探测警告；以最终测试统计和 BUG-049 的真实断言为准，不要把警告当成功，也不要把真实 TypeError 当环境问题。

---

## 八、BUG-049 完成后的第 5 批推进顺序

第 5 批必须在同一新窗口内完整完成，但不得跨入第 6 批。推荐分组如下。

### 组 A：手机壳、页面、导航、分页与已读

```text
P122～P134
```

其中 `P134` 已完成；`P127／P129` 先收口。

重点文件：

```text
F106 UI刷新.ts
F114 壳/挂载.ts
F116 壳/渲染/chat.ts
F117 壳/渲染/chats.ts
F118 壳/渲染/index.ts
F119 壳/渲染/invite.ts
F121 壳/渲染/settings.ts
F123 壳/渲染/业务端口.ts
F124 壳/渲染/共享.ts
F125 壳/渲染/分页.ts
F126 壳/红点与开合.ts
F131 数据层.ts
F135 运行时上下文.ts
F140 手机分支隔离.ts
F141 手机已读水位.ts
F142 手机文本格式.ts
F143 手机时间显示.ts
```

### 组 B：输出安全、生成、时间线、批次、记忆与通知

```text
P135～P151
```

重点文件：

```text
F095 微信可知正文.ts
F096 微信好友规则.ts
F097 微信本地进展摘要.ts
F098 微信正文承接.ts
F099 微信每日联系.ts
F106 UI刷新.ts
F108 交互/邀约与发消息.ts
F109 内容素材表.ts
F110 内核.ts
F112 叙事硬事实.ts
F113 壳/会话瞬态.ts
F128 孕情AI通知.ts
F129 微信记忆上下文.ts
F130 摘要系统.ts
F133 生成引擎.ts
F134 节拍引擎.ts
F136 通知桥.ts
F138 配置.ts
F139 静音会议旁路.ts
F144 手机时间线租约.ts
F145 手机生成完整性.ts
F146 手机系统.ts
F147 手机群聊格式.ts
F148 手机聊天批次.ts
F149 手机触发参数.ts
F150 手机输出安全.ts
F179 胶囊预算.ts
```

### 组 C：引用、撤回、压缩、图片、发送与群聊回复

```text
P152～P158
```

重点文件：

```text
F100 微信消息压缩.ts
F101 微信消息引用.ts
F102 微信消息撤回.ts
F108 交互/邀约与发消息.ts
F116 壳/渲染/chat.ts
F133 生成引擎.ts
F134 节拍引擎.ts
F147 手机群聊格式.ts
F148 手机聊天批次.ts
F171 私聊图库清单.ts
F172 私聊图片轮换.ts
```

### 已闭合邀约组

```text
P159～P167
```

只做兄弟调用方反证核对，不得重新从头审查；只有发现新的直接反证，才回到对应行补充证据。

---

## 九、逐项审查要求

每个功能与产品文件至少核查：

- 入口是否真实可达；
- 前置条件是否与 UI、状态机和运行时一致；
- 读取与写入是否使用同一变量名、路径和时间线；
- 正常路径、失败、取消、超时、空响应是否闭合；
- 重复触发、批次重入、并发写是否幂等；
- 切聊、切档、回档、重掷、swipe、删楼、加楼、世界时段变化是否隔离；
- 已读、红点、分页、引用、撤回、压缩是否互相一致；
- 手机软叙事是否会反写硬状态；
- 住院、特殊场景、场景剧情、父亲通话和共享生成槽是否正确互斥；
- UI 文本／URL／属性是否全部经过相应转义；
- 现有测试是否覆盖现行产品语义，而非旧源码形状。

真实 BUG 必须严格按：

```text
复现 → 五问 → 影响链 → 最小修复 → 专项回归 → 同族回归 → 台账
```

不能为了快速结束本批把未确认项记为已审无 BUG。

---

## 十、测试与最终验证

每个最小修复先跑专项，再跑同族。

第 5 批全部闭合前必须运行：

```text
第 5 批相关测试族
产品 TypeScript
Vue TypeScript
受影响 ESLint
git diff --check
全量 tests/人妻公寓/*.test.mjs
```

具体命令以 `AGENTS.md`、仓库脚本和当前 `.codex-tmp` 配置为准。

禁止正式构建 `dist`。

若本批某阶段未运行全量，台账必须明确写：

```text
本阶段未运行全量
```

不能沿用 `1570／1570` 冒充当前修改后的全量结果。

---

## 十一、台账更新

只能更新：

```text
src/人妻公寓/全量BUG审查台账_2026-08-19.md
```

不得创建第二份台账。

每完成一组，立即更新：

- P 行状态；
- F 行状态；
- BUG／TEST-STALE 编号；
- 复现、五问和影响链；
- 修改文件；
- 定向与同族测试；
- 当前累计统计；
- 本批剩余范围。

统计必须重新计算：

```text
产品闭合 = 已完成 + 已审无 BUG
功能闭合 = 已完成 + 已审无 BUG
产品各状态合计 = 192
功能各状态合计 = 244
```

`定位中`、`真实BUG待修`、`已修待回归` 不计入闭合。

---

## 十二、完成第 5 批后的强制交接

第 5 批完成后：

1. 更新唯一台账；
2. 写一个新的第 6 批交接文件，例如：

```text
src/人妻公寓/新窗口交接_全量BUG审查_第6批关系系统单批次续接_2026-08-20.md
```

3. 第 6 批交接只允许范围：

```text
P168～P192
```

4. 在聊天中给出第 5 批精简统计和第 6 批交接文件路径；
5. 立即停止，不开始第 6 批任何源码读取、测试、审查或修改。

以后每个窗口都重复同一规则：

```text
一个窗口 = 一个批次
批次闭合 = 写下一批交接并停止
```

---

## 十三、禁止操作

明确禁止：

```text
git reset
git checkout
git restore
git stash
git clean
删除或清理 untracked
覆盖现有源码或素材
覆盖或回退 dist
整文件无脑 prettier --write
正式构建 dist
组卡
commit
tag
push
```

也不得：

- 重做已闭合批次；
- 覆盖其他窗口已经留下的多人邀约、冷落、父亲电话或数据层改动；
- 用 HEAD 或旧发布产物覆盖当前工作树；
- 因测试全绿跳过未开始行；
- 因工具日志很长误判为项目卡死；
- 在第 5 批未全部闭合前宣称本批完成；
- 在 192／192 与 244／244 之前宣称全量审查完成。

---

## 十四、过程回复格式

为避免网页卡顿，过程只回复简短统计，不粘贴完整源码、长 diff 或工具日志：

```text
本组实际审查：
- 产品文件：X
- 闭合功能：X
- 新增真实 BUG：X
- 新增过时测试：X
- 已审无 BUG：X

验证：
- 定向测试：X／X
- 第 5 批同族：X／X
- 全量测试：本阶段未运行，或 X／X
- TypeScript：通过／失败
- Vue TypeScript：通过／失败
- ESLint：通过／失败
- git diff --check：通过／失败

累计：
- 产品文件闭合：X／192
- 功能闭合：X／244
- 第 5 批剩余功能：X
- 第 5 批剩余产品文件：X
- 真实 BUG：X
- 测试过时：X

失败摘要：
- 仅列真实失败；无则写“无”
```

若 DevSpace 再次失败，只报告失败步骤、错误摘要、是否产生文件修改和安全恢复动作；不要连续刷长日志。
