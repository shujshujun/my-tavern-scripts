# 新窗口交接：夏乔借种结局唯一实现已收敛，续作实机与 CG

日期：2026-08-21  
工作区：`D:\SillyTavern\tavern_helper_template-consolidated`  
分支：`release/rq085`  
交接时 HEAD：`7d717bfe`  
当前发布基线：`rq0.85`

## 一、接入后第一件事

先运行，不能省略：

```powershell
git status --short
git branch --show-current
git rev-parse --short HEAD
```

预期仍为 `release/rq085` / `7d717bfe`；若不同，先以现场只读结果为准，不要 reset、checkout 或清理。

随后依次完整读取：

1. `AGENTS.md`
2. `.cursor/rules/破限.mdc`
3. `src/人妻公寓/新窗口入口_精简.md`
4. `docs/BUG语义排查协议.md`
5. 本交接 `src/人妻公寓/新窗口交接_夏乔借种结局_唯一实现已收敛_实机与CG续作_2026-08-21.md`
6. `src/人妻公寓/夏乔借种结局_硬玩法与孕产后果权威设计_2026-08-21.md`
7. `src/人妻公寓/夏乔借种结局_系统玩法与长期后果设计_2026-08-21.md`
8. `src/人妻公寓/夏乔借种结局_生产者消费者收敛表_2026-08-21.md`

旧交接 `新窗口交接_夏乔借种结局_双实现收敛与CG完成_2026-08-21.md` 只描述了收敛前的问题现场，已经由本交接和收敛表取代；不要再按其中“仍有两套实现”的状态判断当前源码。

不要自动读取 `CODEX_MEMORY.md`，不要扩大到无关历史文档。

## 二、本窗口完成到哪里

### 2.1 两套实现已经收敛成一套

唯一保留：

```text
src/人妻公寓/脚本/游戏逻辑/借种结局状态.ts
src/人妻公寓/脚本/游戏逻辑/借种结局系统.ts
tests/人妻公寓/夏乔借种结局.test.mjs
```

已经物理删除：

```text
src/人妻公寓/脚本/游戏逻辑/夏乔借种结局事实.ts
src/人妻公寓/脚本/游戏逻辑/夏乔借种结局系统.ts
tests/人妻公寓/夏乔借种结局系统.test.mjs
```

不保留 B 的常量、阶段、旧键、函数名、兼容导出或墓碑文件。唯一硬键／来源为：

```text
借种:摄像头已拆
借种:断线已确认
借种结局:101:<绝对时段>:<楼层>
```

另有按场次／胎次生成的邀约、三人合照与产后家庭合照短事实键，全部集中在 `借种结局状态.ts`。

### 2.2 根因已经修掉

- A 的非法 TypeScript 参数名已经修复，源码可解析。
- B 的导入环与不存在的受孕提交口已随 B 删除。
- 商店商品只剩一份真实特殊场景配置，购买走唯一原子入口。
- 101 真摄像头拆除、精确返还、302 真监控卡确认与结局后重新安装已经闭环。
- 唯一场景阶段是 `开场 → 亲密`；固定开场成功提交后才推进，失败可同拍重试。
- 完整阶段 5 亲密建账时冻结 `借种结局:101:` 来源。
- `玩家资源系统.ts` 先调用 `结算借种亲密收尾`；只有未被借种接管时才运行普通随机受孕。
- 有效完成只认未保护阴道插入、夏乔为对象、体内收尾和正确来源；有效完成确定受孕，不走 60%／80%／100%。
- 无效收尾、主动停止或最终提交条件漂移恢复票据，不留下半怀孕／半完成状态。
- 隐藏期到期只发“来 101 看结果”的邀请；高风闻不会提前公开借种阳性。
- 已读邀约不开始孕期；玩家到 101 实地确认阳性后才进入孕期，并产生合照待拍事实。
- 三人合照、产后家庭合照必须由真实现场动作生产，手机、姐妹群和环境不能凭空抢跑。
- 丈夫特殊“知情、感谢、带酒、不登门”语义现在要求家庭计划知情且存在借种完成／来源事实；只有家庭计划知情不再提前触发。
- 后续第二／第三胎继续走普通系统，101 摄像头可重新安装。
- 借种事件画面优先级已统一为 `借种 > 生产 > 家庭计划`，素材缺失只降级、不阻断提交。

完整生产者／消费者与失败语义见收敛表，不要在新窗口重新发明另一套状态机。

### 2.3 专项测试已经合并

`tests/人妻公寓/夏乔借种结局.test.mjs` 当前 22 项，覆盖：

- 商品可见性、购买零副作用、单次扣款与单票；
- 借种完成前普通受孕阻断；
- 摄像头真拆、空房与真实撬门、数量守恒、重复拆除；
- 302 `CAM-101 / NO SIGNAL` 真实 UI 点击链；
- 星期、时段、地点、医院、既有孕情、生产上限、其他特殊场景、场景事务、待发事件、父亲电话等启动门；
- 开场失败重试、不双推进；
- 保护、非阴道、错对象、错位置、错来源、体力耗尽和主动取消；
- 最终条件漂移的原子失败；
- 确定受孕幂等、无新 Schema 分支；
- 一周隐藏、高风闻不公开、101 实地阳性；
- 三人照、产后照、手机和姐妹群消费者；
- 完成后重装摄像头、未来普通胎次恢复；
- 三个 B 路径必须物理不存在。

## 三、已经取得的验证证据

### 3.1 测试与静态检查

```powershell
node --test tests/人妻公寓/夏乔借种结局.test.mjs
# 22/22 通过

node --test tests/人妻公寓/丈夫登门与孕情姐妹群.test.mjs
# 11/11 通过

node --test tests/人妻公寓/生产系统.test.mjs
# 13/13 通过

node --test tests/人妻公寓/孕产与家庭计划素材路由.test.mjs
# 4/4 通过

node --test --test-reporter=dot "tests/人妻公寓/*.test.mjs"
# 退出码 0；目录统计 1618 个顶层 test(...)

& '.\node_modules\.bin\eslint.CMD' "src/人妻公寓" "tests/人妻公寓"
# 0 error；12 个既有 warning

npx --yes vue-tsc --noEmit --pretty false -p ".codex-tmp/tsconfig.scene-story-vue.json"
# 退出码 0

git diff --check -- src/人妻公寓 tests/人妻公寓 docs
# 退出码 0；只有既有 CRLF/LF 提示
```

全工作区默认 `vue-tsc` 有大量与本专项无关的历史全局／模板噪声；定向复核使用上面的 `.codex-tmp/tsconfig.scene-story-vue.json`。不要把旧 `.codex-tmp/App.scene-story.compiled.ts` 的报错当成当前产品源码错误。

### 3.2 隔离生产构建

使用：

```powershell
& '.\node_modules\.bin\webpack.CMD' --config '.codex-tmp/webpack.borrow-seed-isolated.ts' --mode production
```

结果：游戏逻辑、MVU、客户端三个入口均成功；只有 Webpack 包体积 warning。产物位于：

```text
.codex-tmp/borrow-seed-build/
```

正式 `dist/**` 在构建前已经有以下三项用户修改，构建后仍是完全相同的三项；本轮没有写正式 dist：

```text
dist/脚本示例/index.js
dist/脚本示例/index.js.map
dist/角色卡示例/界面/状态栏/index.js.map
```

`.codex-tmp/webpack.borrow-seed-isolated.ts` 会把每个入口映射到独立隔离路径，并禁用 schema dump／监听器副作用；后续需要复构建时继续用它，不要运行普通 `pnpm build` 写正式 dist。

## 四、新窗口开始时先做的防复活检查

曾有一个已经中断的旧写入者短暂把 B 文件重新生成成很小的墓碑；本窗口已经再次删除，随后全量测试和隔离构建均通过。新窗口第一批只读检查里仍要确认它们没有复活：

```powershell
$paths = @(
  'src/人妻公寓/脚本/游戏逻辑/夏乔借种结局事实.ts',
  'src/人妻公寓/脚本/游戏逻辑/夏乔借种结局系统.ts',
  'tests/人妻公寓/夏乔借种结局系统.test.mjs'
)
foreach ($path in $paths) { "$path`t$(Test-Path -LiteralPath $path)" }
```

三项都必须为 `False`。如果复活，先查明写入进程，不要与墓碑并存，更不要恢复 B 兼容层。

再运行唯一专项一次，确认现场仍是 22/22。

## 五、下一窗口的首要工作：真实桌面／手机验证

代码和构建已经过关，但尚未做真实浏览器，因此不能宣称整项完成。使用本地终端和浏览器能力即可，不需要安装或连接任何插件。

至少按真实 UI 验收以下主链：

1. 家庭计划未完成时商品不可用；完成后只出现一个 `借种` 商品。
2. 买票只扣一次款，背包只有一张票。
3. 101 有人时不能借种拆机；无人且真实撬门进入后，拆下真摄像头并精确返还一枚。
4. 回 302 打开真实监控弹窗，点击可见的 `CAM-101 / NO SIGNAL` 卡完成断线确认；不要用脚本直接写键代替 UI。
5. 错星期、错时段、错地点以及医院／电话／其他剧情锁都能给出正确阻断。
6. 星期一晚上或深夜到 101，固定开场只演三人边界确认；陆嘉明随后退出成人演出。
7. 完整阶段 5 亲密可操作；旧的“顺着夏乔／接过节奏／最少两轮／长按完成”入口不存在。
8. 无效收尾能安全停止并恢复票；重新进入后，有效未保护阴道体内收尾确定受孕。
9. 当晚界面不提前公布怀孕；时间到期的手机消息只邀请来 101。
10. 读消息后仍需去 101 查看阳性；随后现场三人拍照动作才出现。
11. 三人照拍摄前私聊／姐妹群／101 环境不出现照片；拍后才传播。
12. 医院待产、陪产或产后探望／出院回 101 后，产后家庭照按真实在场开放；拍后才进入手机与环境。
13. 完成后重新安装 101 摄像头，302 能恢复普通监控内容。

桌面与手机都检查：操作是否可见、弹窗层级、按钮可点击、窄屏裁切、滚动、亲密抽屉、事件 CG 优先级、缺图降级和控制台错误。保存截图到隔离 QA 输出目录，不写 `dist/**`。

若实机发现 BUG，继续遵循 `docs/BUG语义排查协议.md`：先回答五问，追状态生命周期与全部生产者／消费者，再分硬生命周期、强制剧情、软上下文、可选副作用修复；不要按截图症状直接补布尔值。

## 六、实机通过后才进入 CG

### 6.1 必须使用的本机 Skill

敏感 CG 开始前完整读取并遵守：

```text
C:\Users\Administrator\.codex\skills\rqgy-adult-cg-workflow\SKILL.md
C:\Users\Administrator\.codex\skills\.system\imagegen\SKILL.md
```

这是本地 Skill 工作流，不需要插件。成人 CG 目录下任何生成、诊断、原图审核、分类、去重、安装或接线都必须服从 `rqgy-adult-cg-workflow`。

不要覆盖：

```text
output/imagegen/rqgy-reset/adult-completion/CURRENT_TASK.md
```

成人图必须执行编号任务、零起点提示、基模来源诊断、单张生成、立即原图审核、真实分类、原子登记和阶段报告；不得批量并行生成，不得把候选图直接复制进正式库。

### 6.2 非敏感首批 7 张

使用内置 `imagegen`，并按最终代码文件名统一：

1. `借种_101三人赴约`
2. `借种_阳性结果`
3. `借种_三人镜面合照`
4. `借种_三人产检`
5. `借种_医院待产三人`
6. `借种_产后家庭合照`
7. `101_借种结局计划板`

`CAM-101 / NO SIGNAL` 必须继续由真实监控 UI 渲染，不生成假监控截图。

### 6.3 敏感 6—8 张

只补具有 101 地点、正式家庭安排和有效完成语义的标志图，不重做整套夏乔阶段 5 图库。范围包括正式进入、夏乔主动承接、过程差分、有效完成关键图、完成后短暂相处和返回客厅前收束。

陆嘉明不进入成人画面；玩家继续用第一人称、背影、局部身体、遮脸或裁切，不固定正脸。正式使用前重新核对基模、夏乔 LoRA、风格 LoRA、IPAdapter／OpenPose 以及 Skill 所要求的真实磁盘路径和版本，不从旧交接凭印象执行。

### 6.4 当前资源接线状态

代码当前目标基址为：

```text
rq0.85/output/imagegen/borrow-seed-ending/final
```

正式资产尚未生成。图片缺失必须降级到既有夏乔阶段 5／孕产素材，不得阻断状态提交。生成完成后复核：

- 文件名与 `借种CG` 载荷、`assets.ts`、手机 `@ending/夏乔借种/*` 映射一致；
- WebP 转换和尺寸符合库规则；
- 桌面／手机裁切通过；
- 无孤儿资源、重复图或未登记候选；
- 敏感图已完成工作流报告与角色收尾。

## 七、工作区保护边界

工作区本来就很脏，含大量用户源码、测试、图片、临时审计文件及正式 `dist/**` 修改。全部保留：

- 不得 reset、checkout、stash、commit、push 或 tag；
- 不得删除或格式化无关文件；
- 不得读取或覆盖 `CODEX_MEMORY.md`；
- 普通验证不得写 `dist/**`；需要编译证据继续使用隔离输出；
- 未经用户明确发布指令，不组卡、不正式构建、不提交发布物；
- 不重新设计家庭计划前置；
- 不恢复 B、旧按钮循环或旧长按方案；
- 不新增 Schema 字段／版本保存一次性过程；
- 不让手机已读等于阳性实地确认；
- 不让照片消费者先于现场照片生产者；
- 不让陆嘉明在后续失忆成受骗丈夫；
- 不把姐妹群知情自动扩大成全楼或楼外公开事实。

## 八、下一窗口完成标准

只有以下剩余项都具备证据，才可宣布借种结局整体完成：

1. 三个 B 路径继续物理不存在，唯一专项仍 22/22；
2. 桌面真实 UI 主链通过并留存截图／控制台证据；
3. 手机真实 UI 主链、窄屏和全屏通过；
4. 非敏感 7 张完成生成、审核、命名、WebP 与代码接线；
5. 敏感 6—8 张按成人 CG Skill 逐张审核、分类、登记和报告；
6. 缺图降级、优先级、资源孤儿和移动端裁切通过；
7. 修改后重新跑唯一专项、相关专项、全量、ESLint、定向 Vue/TS、diff check 和隔离生产构建；
8. 仍未触碰工作区既有用户修改或正式 `dist/**`；
9. 若用户明确要求发布，再另行做角色卡组装与发布检查。

当前安全接续点是：**代码／测试／隔离构建收敛完成，立即开始桌面和手机真实 UI 验收；实机通过后再进入本机两套生图 Skill。**
