# 四项代码修复与三项补验（2026-09-08）

本批沿 `task/unified-repair-20260907` 当前混合候选完成4项代码修复及3项本地补验。HEAD仍为 `394817a5`，保留此前全部修改，未提交或发布。

## 代码修复

- **SNAP12：邀约回复与冻结计划一致。** 生成口向验收器传入真实计划，区分明确接受、拒绝、条件和提问；校验回复中的星期、相对日期、时段和地点。省略见面动词的“好，明天下午”也会检查；生活理由中的其他日期不改写本次约定。真实注册邀约入口验证错误回复不落预约，正确回复与原计划同次提交，重复和冲突保持CAS保护。
- **PLAY040：压缩后的个人旧话题。** 本人旧摘要与当前分支、来源锚、撤回状态一起读取，生成和验收使用同一角色的话题资格。新群摘要按完整气泡分组，保持400字符上限，内部引号与外层署名分隔符区分；胶囊在原字符预算内选择完整记录。旧v1的400字符截断记录仅恢复来源可证明的完整首条玩家引文，不能从不可靠的后续署名或残片推造资格。新成员、他人转述、撤回、换分支及未确认摘要不能获得该资格，摘要不冒充原始可引用消息或世界公开事实。
- **SNAP13：本胎事实和完成态。** 区分未来、开始、已经出生、获准出院和实际出院；兼容正常的“刚生下”“刚生产完”“已经生产了”。按本胎检查胎次与性别，允许指代宝宝的正常性别表述，避免把其他孩子或他人家庭情况混进本胎。专题群在落库前核验产妇本人消息，私聊生产通知带入真实胎次；错误或未完成文案不写通知键。
- **SNAP21：茶话会内容与实存提交。** 坦白、点评、转正事在生成后和最终实存提交前分别校验；任务、目标、批次、会话与发言人必须匹配。提交端同时复核原生成端的改名反应顺序、回应成员及母亲收束条件。有效送达凭据不再代替任务内容，调用者提供的摘要不能覆盖实存消息；当前玩家姓名由真实上下文传递。读取并验证同一份凭据快照，保持取消、回档、切聊、写入失败及重试边界。

SNAP12首轮39项中16项复现失败；PLAY040原真实400字符复现失败；SNAP13初始59项中25项失败；SNAP21原坦白/点评两例及提交端三项演员反例均复现失败。原失败证据留存，修后实际调用结果分别记录。

## 完成补验

- **PLAY006：档案的同实例响应式。** 原7项与新增3项共10/10。真实Vue组件只挂载一次，验证深层变更、退居、回档、换户、就绪开关、预约左闭右开、未知居住模式与缺户。身份/风险提示刷新，原疑心和信任读数保持。采用Vue客户端渲染器的内存节点，覆盖真实setup、依赖追踪与patch，不代表设备像素或触摸验收。
- **SNAP27：预约与成员恢复。** 与SNAP28、镜像和并发写入共同执行105/105，且包含在最终相关联合中。覆盖正常保存、抛错、吞错、无接口、延迟补存、全持久通道失败、共同成员、CAS竞争、目标地点/时段与重复恢复。
- **SNAP28：消费位恢复。** 使用真实圈晒、探针、群议生产者与恢复路径，验证消息/动态与关联消费位一致；成功恢复不重复消费，全通道失败仍可重做。新事件、旧字段差异、过期、清回false及非手机字段继续按原边界处理。

## 验证与源文件恢复

最终同一次相关联合 **1195/1195**，失败、取消、跳过、todo均为0。生产入口 `vue-tsc --noEmit --incremental false`、修改文件ESLint与`git diff --check`通过。

双入口隔离生产构建 **build-1788861312704-ef26c2** 成功，0错误，游戏逻辑/客户端警告3/2。schema dump、监听、同步及正式声明回写均禁用；主目录与候选的正式dist各72文件保持原SHA。

首次联合1179项中唯一失败来自缺失的生产源PNG。主目录 `D:/SillyTavern/tavern_helper_template-consolidated/output/imagegen/production-system/final` 保留73张PNG。恢复前逐张验证该目录配套WebP与候选同名产品的SHA完全一致，随后只复制缺失PNG，逐张复核复制后SHA。共151,441,260字节，产品WebP未变；素材路由4/4，最终联合通过。复制清单在 `production-source-restoration.json`，未生成图片或补造图像审核记录。

全部真实命令、退出码、原始stdout/stderr及构建保护文件位于 `.codex-tmp/repair-20260908-continuation/`。最终结果为 `final-joint.json`、`verified-types.json`、各lint记录、`production-build.json`及对应build目录。初轮测试沿用原档案名；最终文件名已按功能整理，历史日志保留当时实际命令。

## 剩余范围

按既有暂行清单，18项扣除本批7项后，**暂行剩11项**：

- 继续补验：PLAY002、PLAY022、PLAY034。
- 条件、设计或素材待证：PLAY037、PLAY026、PLAY043、PLAY050、PLAY044、SNAP22。
- 用户此前暂缓：SNAP25、SNAP26。

11不是对全部81个原编号重新审计的结论。真实SillyTavern会话、在线模型、浏览器/移动端交互与发布前宿主联合验收仍待完成；本批没有发布。
