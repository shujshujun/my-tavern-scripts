# PLAY-004 地点入口与远处等待票修复验收

日期：2026-09-08。统一候选开工HEAD `7f3934d7`；本项为首次修复。

## 用途、根因和修改

明确约在别处、尚未开始的普通剧情应继续排队，让玩家完成眼前地点的动作。活动事务、当前地点等待票、未知旧档和连续硬现场仍须保护；已有电话、医院、普通场次及剧情资格不因放行远处票而失效。

201离婚、离婚后日常、301不必停和换掉原先以全局非空队列阻断地点入口，导致101远处普通等待票锁住201／301乃至大堂、管理员室和公寓外部。现有地点读口只看队首，不能直接代用，否则会漏掉队尾的同地或连续现场。

- `场景剧情事务.ts`新增纯只读`有地点动作剧情冲突`：原始活动ID始终阻断（调用方原有自有票豁免除外）；逐项读取完整等待队列，复用真实元数据和旧票地点推断，阻断同地／未知／连续锁场，放行明确远处普通等待。不改旧全局门和队首门。
- 201离婚继续使用原本的自有票识别，并透传实际动作地点；日常采用已经校验过的201地点。
- 301不必停按预约实际地点301检查外部占用，覆盖时间节点及开场暂缓；换掉按动作配置的实际地点检查购买、登记和301现场，公开执行仍先核对配置与当前位置一致。

查询不消费、重排或清空队列；原有自有票续演、普通账目和医院／电话边界保持。没有引入新异步任务、重试或剧情内容。

## 上层调用链

已读index四动作事件、操作壳、即时开演与落地：均按实际地点检查活动／队首，并未再次以全局非空队列拒绝远处票。即时开演保存并核对原队列，只提取新增事件，恢复原队列后调用`激活新增场景剧情`前插当前票；成功只消费当前队首，远处票顺序保留。结局后直接回合的快照也排除远处队首。

201纯业务函数原有自有活动票豁免不等于可以在UI活动票期间另建动作；上层仍保留活动事务互斥，续演走既有重试入口。未修改这条原有职责边界。

## 验证与限制

- 新`远处等待票与地点入口_PLAY004.test.mjs` **36/36通过**；相同专项加载保存旧源码时17通过／19行为断言失败，无缺导出或环境错误。日志`.codex-tmp/play004-{red,green}-test.log`，旧源码和红灯装配在`.codex-tmp/play004-old-source/`。
- 专项执行真实Schema、场景元数据、时钟及四个公开地点显示／执行入口，覆盖九类动作、队尾同地／未知／连续锁场、四种活动状态、自有票豁免、多地点、预约到期／暂缓、原电话／医院／普通场次、保存恢复、多条同事务指令以及旧API语义。预览与执行都检查原等待票和活动归属保留；外部数据库是明确隔离边界。
- 相关七文件 **101项100通过／1失败**。唯一失败为PLAY-013完整办理现场用例，报“办理现场必须关联且只能关联赵国强”，模型调用与业务提交均为0。加载本轮修前五文件重跑同一测试，仍出现同一失败，证明不是本项引入；保留给013，不降低断言或在本项扩修。日志`.codex-tmp/codex-takeover-20260908/play004-related.tap`和`play004-preexisting-play013.tap`。
- vue-tsc、五源码及新测试lint通过，0错误0警告，diff检查通过。日志同目录`play004-typecheck.log`、`play004-eslint.log`。
- 真实双入口生产隔离构建`build-1788822259644-a07128`通过：游戏逻辑0错误3警告、客户端0错误2警告；主目录与候选正式dist各72文件及受监控源码前后不变。只在scratch写产物和声明，未发布。

本项本地代码验收完成，不代表013、所有剧情事务、玩家宿主或全队列验收完成。其余修复继续，SNAP-25／26仍暂缓。

提交边界：五文件精确任务差异保存为`play004-task.patch`；一次`git apply --cached --check`失败于日常模块导入段，因为该段依赖已有但尚未提交的旧修改。按用户额度规则不继续拆混合补丁，产品、新测试及本报告保留工作区待整合，未暂存／提交。`play004-ownership.json`记录索引前后为空且SHA相同、五个当前源文件均匹配本轮构建SHA；主代理已逐处审阅任务差异。
