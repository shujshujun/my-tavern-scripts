# PLAY-033 私聊可见长度与旧占键恢复修复记录

日期：2026-09-08；本轮标识：s2p5。

状态：**产品补丁、70项专项行为、类型／静态检查、隔离生产构建和精确差异保护已完成，本地代码验收通过，待提交／整合。联合回归两项既有静态断言失败保留，不声称全套全绿。**

## 1. 续接与修前证据

继续原工作树 `C:\Users\Administrator\.devspace\worktrees\tavern_helper_template-consolidated-5d7f765f`，复用 `ws_0a48808756`。开工及最终联合测试后均实际核对为 `task/unified-repair-20260907@c5943ef6`，暂存区为空。未切换／回退、新建工作树、覆盖其他窗口修改、提交或发布。

本窗口先完成SNAP-15原r1n4补丁的缺失补验，已追加统一记录第86节；随后实际修复PLAY-033。原 `PLAY038_033取证断点_2026-09-07_g7c1.md` 保留，不用历史取证失败代替本轮已经成功取得的代码证据。

只读主目录原 `rq0.90.4后玩家体验与世界发展大排查登记_2026-09-05.md:1021—1033`：首次及重生成都返回170汉字时，回国私聊最终门允许220字，原始库接受，读库150字门隐藏；下一次正常短回复又被旧稳定键阻止。空可见收据被真正消费者拒绝，待发送成员不消费。这不是“首稿一长就直接写入”，也不能外推整条回国主线必然停住。

已核当前完整调用者、完整增量写口及其所有既有混合差异、读库／镜像投影、单气泡验收、稳定键与撤回合并、生成及重生成、回国收据与成员状态提交，以及真实事件消费者的接线。所有手机 `微信短文本` 调用点中，只有回国手机仍用220，其余同族调用使用共享硬上限；没有批量改写其他入口。

## 2. 修前五问与边界

**设计初衷：**后续私聊需要让玩家看到完整消息，再由真实可见消息凭据消费一个待发送成员；失败仍可重试。

**正常运行：**生产者至多按原规则重生成一次，最终验收收口；持久写回调重读当前库并去重；调用者使用当前读库中的可见消息签发收据，下一成员受下一世界时段限制。

**真实影响：**旧消息原始记录存在，却既不可见又占键，导致可选后续私聊不能正常补送。没有证据表明整条主线本体因此永久停住。

**根因：**最终接受220与显示150的契约不一致；原始库稳定键去重不分辨这种旧不可见长度错误；调用者把变量回调成功当成消息送达，允许空凭据或同键错会话记录进入事件。

**兼容策略：**不截句、不清空微信、不伪造收据；只给当前请求显式指定的同会话同键旧超长私聊提供原子替换。其他入口不启用此选项时保持原去重及快速返回。已见消息、非长度格式错误、照片／通话／撤回／玩家／系统记录、其他会话以及未来／其他分支不允许借此删除。

## 3. 实际补丁与所有权

| 文件 | 本轮精确增量 | 内容 |
|---|---:|---|
| `脚本/游戏逻辑/手机/回国手机.ts` | 22增7删 | 最终接受改用共享150汉字上限；显式给出恢复范围；统一读取正确会话、发送方和文本类型的实存消息；写入后及保存等待后均拒绝空／错归属送达凭据。 |
| `脚本/游戏逻辑/手机/数据层.ts` | 21增1删 | 给增量增加可选 `替换不可见超长私聊` 范围；在原同步提交回调中核对并替换指定旧记录，仍按实际插入分配序号与计数。 |
| 合计 | **43增8删** | 不包括原有混合差异。 |

数据层开工已有75增32删；当前相对HEAD为96增33删，不能把这些全算成本轮。只读 `verify-play033-s2p5-ownership.mjs` 在内存中精确扣除本轮补丁后，两份源码都逐字节回到开工SHA，没有实际恢复或回退文件。生成引擎、本轮之前的SNAP-15源码和测试、摘要／节拍、旧helper／测试、a4e6／c72d原复审报告等12份采样文件保持开工指纹。输出为 `play033-s2p5-ownership.json`。

数据层开工SHA：`4356b152a035db9aead063a20df2c873a0d5b7f37130eada94035029e23df60a`；修后：`b7300ab324963f916ba6d855482b0aab535e6740fc3987c62d6fb0f7064eafc9`。

调用者开工SHA：`5b66654f7c71a365b41a6b017f44634aba426b4c8fcbaa1a47e5216306744b68`；修后：`d32369a3d3e3a766c80bcd9613bf638a0b7b16faedea5ff06b2d97281654ed79`。

没有更改生成内容提示、请求预算、路由、重生成次数、原世界时段暂缓机制、存档Schema或收据校验器。原始库仅移除当前投影中确属超长的精确对象；替换与插入同成同败，其他分支／未来记录仍由原投影合并保留。仅格式不合格而非长度不合格的旧记录仍占键，不冒称此补丁能修复所有任意损坏输入。

## 4. 实际测试与检查

新增 `tests/人妻公寓/回国私聊可见长度与占键恢复_PLAY033.test.mjs`，复用原helper，不修改旧测试断言。最终测试文件SHA：`803d08513c31c006e7812e048c685fc2541a0bc3f92f12cf2ffefcbf6800f716`。

所有日志位于 `.codex-tmp/unified-repair-20260907/`：

| 证据 | 实际结果 |
|---|---|
| `play033-s2p5-before.tap` | 修前49项：31通过、18失败；为同一原编号的不同边界，不是18个BUG。 |
| `play033-s2p5-after.tap` | 首版补丁后同一49项全部通过。 |
| `play033-s2p5-extended.tap` | 扩为70项，全部通过。 |
| `play033-s2p5-related.tap` | 初次18文件联合：340项338通过、2失败。 |
| `play033-s2p5-final-related.tap` | 普通入口提前去重的微调后，重新执行同一18文件：**340项338通过、2失败，0取消／跳过**；包含本项70项和SNAP-15的62项，两专项均全通过。 |
| `play033-s2p5-eslint-final.json` | 两份源码及新测试各0错误、0警告。 |
| `tsconfig.play033-s2p5.json` | tsc通过；末次重跑使用此次新构建 `build-1788808655208-bfea4b` 的隔离声明。 |
| `build-1788808655208-bfea4b/summary.json` | 隔离生产构建成功：游戏逻辑0错误3警告，客户端0错误2警告。Node模块类型提示保留。 |
| `play033-s2p5-ownership.json` | 精确增量反向核对及12份开工采样保护通过。 |

不得将49、70、340或重复执行相加为独立测试覆盖数。联合文件包含新专项、SNAP-15、回国及原回国手机边界、手机生成完整性、P3／P4／P6、真实时间并发／跨状态／原子提交、原始投影、撤回、刷新镜像／持久化、预约／余波同事务恢复和受控生成等待。

新增行为覆盖150／151／170／220／221汉字、ASCII／emoji不计汉字、首标签剥离、真实首次与单次重生成、两稿仍长或第二稿合格、两名成员、同拍暂缓与下一时段恢复、旧多个坏记录、同键好坏混合、错误范围、错误会话／发送方／消息类型、其他事件及玩家原文、标识撞撤回墓碑、并发只插入一次／重复不增序、写入许可拒绝／提交前异常、回调执行前或已提交后切聊／世代／swipe失效、未来时段／未来楼／其他分支保留、真实恢复镜像重开，以及保存等待中消息删除／改长／改会话的最终收据复核。

**自动化边界：**使用完整当前回国手机模块、真实小生成重试函数／长度规则／最终验收、真实读写库／镜像／序号及去重、真实时间线租约／回国收据和真实成员状态函数；只读核过主入口事件消费者，但没有跑完整MVU主入口写锁。提供方返回中性、预解析响应，宿主变量／浏览器存储为隔离适配，不调用真实模型、不宣称验证所有封套提供方或实际玩家环境。超时等待属于联合中的既有受控等待专项，不把它冒称回国调用者四分钟端到端实测。

宿主保存抛错用例仍沿用产品原有“本地可见消息＋恢复镜像”的送达语义：断言真实宿主适配没有落盘、镜像重开仍可读，并非假报物理存盘成功。此补丁没有把函数返回true升级成宿主已硬保存，也未重做保存事务设计。

## 5. 两项联合失败保留

**P6旧布局断言。** `手机系统拆分P6.test.mjs:138` 用例中的节拍源码固定正则（原断言154行）仍失败，和SNAP-15前轮及本窗口补验是同一项。节拍源码SHA在本轮一直为 `7a912e678a07fde425c7eca6f4e245ef18033cd2a93beae019099b396dbd6751`，P6旧测试Git diff为空。

**玩家硬保存旧定位字符串。** `微信刷新持久化.test.mjs:340` 的353行仍查找 `已成功落库 = true;`。当前发送函数早已采用读库中的稳定身份核对接受事实，故此字符串位置为-1；失败断言的直接输入 `邀约与发消息.ts` 与本轮开工SHA相同。该旧测试确有此前另一段288行关联修订断言的既有修改，不能声称整个文件Git diff为空；本轮没有修改它，失败所在340—353行不属于那段既有改动。并行独立复审s2v6也另行复现并解释了同一失败。

两项均未因PLAY-033新增代码产生，不据此把保存或节拍功能全部判坏；同样不删断言、回退既有改进或把340项说成全绿。首轮与最终联合失败日志都保留。

## 6. 构建隔离、并行复审和当前计数

实际沿用已审阅的隔离构建脚本，先剔除schema dump／监听／同步三个副作用钩子，再运行两个真实生产入口。声明和产物只写 `.codex-tmp`。主目录与候选正式dist各72文件，构建期前后都不变；受构建脚本监控的源码也保持构建前后不变。此为构建期保护，加上前述12份开工采样保护，不冒称全仓整个任务期间无人并行修改。

准备收尾时成功读取并行新报告 `修复代码独立复审_2026-09-08_s2v6.md` 和其总账增量：**PLAY-023新增否定副词边界，原号重新待复修**；013／030／031仍是原待处置项，不重复加数。保留s2v6报告、16项独立诊断及此前专项成功历史，不将其16项或2370项测试算作本窗口执行。

本窗口推进了SNAP-15补验完成和PLAY-033新修复两个原编号；同时纳入并行复审重开023一个原编号。以第86节的48项为基准，`48＋重开023的1－完成033的1＝48`。当前口径为 **43项未闭环代码＋4项条件待证＋1项素材＝48个主要原编号**；PLAY本地验收完成为 `23－023的1＋033的1＝23`。这不是回退本窗口已完成的两个修复，而是新证据使一个旧完成项需要继续复修。

SNAP-03／04、25／26及其他待补验项不借用本组通过自动关闭。后续按最新统一记录及s2v6的原编号断点续接；本轮不重做全游戏审查，也不先改写023旧报告使之重新变绿。所有修复仍未提交、未组卡、未正式构建dist或发布。
