# PLAY-039修复与PLAY-046取证记录

日期：2026-09-07。继续用户指定原工作树 `ws_0a48808756`，不新建工作树、不切分支、不回退；主目录 `ws_cc29b2ba45`仅用于只读原号证据。

## 1. 本轮开工证据限制与046跳过

首次组合调用查询Git状态／分支／HEAD／暂存区、统一记录最新章节及目录规则清单，被工具安全检查整体拦截：`因 OpenAI 无法确定请求的安全状态，已拦截此工具调用。` 没有取得任何子命令结果；没有拆分、换读法或脚本重取。本轮不把上轮第68节的HEAD、56项总数和19项完成数冒充已经再次核实的当前全局事实，也不覆盖可能存在的并行增量。

按上轮已交接的046入口，成功只读主目录最新索引、原报告PLAY-046全文及独立复核第9节对应条目。设计是可读见闻署名与当前Persona引用依据一致，保留内部`发:'我'`、稳定消息键、接收人和历史原文；不能全局替换“玩家”。随后针对046的候选文件定位、真实见闻／引用消费者搜索及目标diff-stat组合调用同样被拦截，未改源码、未新增或运行046测试。**046保持必要源码取证受限，待复审；本轮没有闭环或扣数。** 不重取该被拒目标，转独立PLAY-039。

## 2. PLAY-039修前五问与所有权

1. **设计初衷：**两张由脚本按真实事务维护的流水表不应进入插件通用手动填表；其他记忆和自定义表仍可操作。保护层不应占用游戏主生命周期。
2. **正常运行：**识别真实表名和兼容表头，收敛公开API选择和可见组件选择；未就绪不能取消时执行前仍失败关闭，等外部有效变化后再尝试。旧版input路径和安全全选须保留。
3. **影响范围：**V2选择器DOM、MutationObserver扫描调度、公开选择API、全选／执行捕获、热重载、切聊天及pagehide清理；不改SQL表结构、数据库写事务、快照、剧情或消息。
4. **真实根因：**`取消新版手动面板受保护选择`看到aria-checked=true时先disabled=false、点击、再disabled=true；组件runtimeReady=false会拒绝取消。观察器无条件响应disabled／aria-checked变更，再排零延迟扫描。自己的“true→false→true”属性往返再次触发自己，形成稳定输入下无效重试。原执行安全门仍有效，不能把空转写成已误填数据库。
5. **兼容边界：**只处理扫描的自触发边界，不删观察器、不去掉执行前核验、不放过未知组件状态、不更改公开API保护或旧版input规则。必须保留外部disabled／aria-checked真实变化、节点重绘、未就绪转就绪、全选、API替换和清理。

已读原玩家报告1175—1189行、独立复核第9节对应证据；原报告旧浏览器测量是历史证据，本轮不复述为新测量。当前实际读取候选`数据库桥.ts:1585—2208`完整手动选择／API／DOM／捕获／观察器／清理链，以及原专项前310行；当前目标git diff为空，开工产品SHA256为`5ae78beff24e34a5bfd6768c0741c41b1b3bf1b9e3bfdcacf31cb53968f54d4f`。原`数据库手动填表保护与朋友圈轨迹.test.mjs`SHA256为`47041bf93b7b8cb452075d4cb15727316cd2be91f61ffb78d7f9ba1f9ac094a8`，不改其断言。

## 3. 验证准备

本机有独立Chromium headless shell，Node v24.12.0提供WebSocket；根依赖没有直接可解析的Playwright／Puppeteer。计划用现有浏览器原生DOM／MutationObserver执行真实保护模块片段，CDP只负责隔离页通信；外部插件API、组件props与计时器采用明确适配。不连接SillyTavern、不访问玩家数据库或模型、不下载安装新依赖。测试时浏览器使用本轮独立临时profile并在结束时关闭，不写正式dist。

下方仅追加实际取得的修前／修后结果；未执行内容不算通过。

## 4. PLAY-039实际补丁

产品修改仅`数据库桥.ts`手动保护安装函数的MutationObserver回调及`attributeOldValue:true`：在一次投递批次内，按实际Element及属性名记录首次旧值，与最终属性值比较；只有真正改变的属性或节点增删才排扫描。自己的disabled往返以及同值写入不再制造下一轮扫描，同时同批其他控件的真实改变仍触发核对。

没有取消观察器、全局断开监听、扩大超时或永久记住“此按钮失败所以再不处理”；原五次有界启动扫描保持，外部就绪、勾选、重绘和用户执行仍能重新校验。API包装、执行前失败关闭、全选逐项等待、旧input、清理及其他数据库机制未改。当前实际diff只有这一处29增1删，无整文件格式化。

## 5. 修前／修后与最终验收

新增`tests/人妻公寓/数据库选择器自扫描_PLAY039.test.mjs`。浏览器使用已经安装的Chromium headless shell，每个用例独立about:blank页；真实DOM、按钮click及原生MutationObserver均未替换。测试提取并运行当前完整手动保护函数集合、实际表头兼容判断、API包装、安装／事件捕获／观察器和清理函数；外部插件API查找、组件props和受控计时是明示适配，不宣称加载了已安装数据库插件或玩家宿主。

### 修前

初始15个独立浏览器案例：**10通过／5失败**，0取消／跳过。`play039-h8d4-before.tap`保留原失败结果。永不就绪案例在80步扫描上限内已经160次受保护checkbox handler调用，继续推进仍增长，实际选择不变、执行0次；这是受控步数诊断，不是80毫秒或玩家电脑固定速率。未就绪执行、外部覆盖disabled、同值disabled写入和普通DOM变化分别导致自扫描不收敛。正常就绪、全选、旧input、清理与API恢复等对照原本通过。

### 修后及扩展

仅上述观察器补丁后，新15＋原专项9＝**24／24**。之后保留全部旧断言，补4项：同批两个控件的属性往返、aria-checked同值写入、净零变化与另一个控件真实变化混合、关闭面板后迟到重放与重开恢复。新浏览器19＋原专项9最终**28／28**；`play039-h8d4-focused-final.tap`。没有变更产品第二版或以删除断言取绿灯。

### 最终相关回归与静态

- 最终全部`数据库*.test.mjs`相关组合：**193／193**，0失败／取消／跳过。最末验证日志`play039-h8d4-database-verified.tap`；包含上述28，不重复累计。此前同补丁193／193日志`play039-h8d4-database-final.tap`亦保留。
- 定向类型配置`tsconfig.play039-h8d4.json`：tsc退出0。
- 产品ESLint0错误／0警告；新测试最初1条prefer-const警告，仅将测试数据绑定改const后，以`--max-warnings 0`重新执行，最终两文件0错误／0警告。`play039-h8d4-eslint-final.json`保留最终结果，原警告日志不删除。
- 最终193项是在该测试const调整后重新运行，不能把旧日志的文件版本与最终测试混用。数据库相关回归保留Node SQLite实验性警告，不因此宣称没有任何环境提示。
- 定向产品`git diff --check`通过；当前实际产品diff已完整读回复核。

## 6. 隔离生产构建与保护范围

本轮实际执行既有隔离构建器，结果：

`.codex-tmp/unified-repair-20260907/build-1788786173228-0e381f/summary.json`

两个真实production入口均成功，游戏逻辑0错误／3警告、客户端0错误／2警告；Node对webpack.config.ts的模块类型提示保留。schema dump、监听及同步写回hook禁用，自动导入与组件声明仍按生产插件生成但写入隔离目录。正式dist各72文件前后未变，构建期间受监控源码也未变：

- 主目录dist聚合：`417d5fc4e7a8c45de92c1ba00f990586cde4b8e261df0950e2832dd16120d752`。
- 候选dist聚合：`9121deb567bf03a22eaee484bd54abfc4b208ce3a1f3a25f23817eb036c293e6`。

这是构建期间保护，不冒充本轮开工至收尾全树逐字节双端证明。没有正式构建dist、组卡、版本修改、Git暂存／提交／推送，未触碰用户真实数据库或模型。

末次实际指纹：

- 039产品：`ed3c26d6fce5701f815db75c4226e93c147eee5fa79e8b1bc8933842cd0ee198`。
- 原数据库手动保护测试：`47041bf93b7b8cb452075d4cb15727316cd2be91f61ffb78d7f9ba1f9ac094a8`，与本轮开工一致。
- 原a4e6报告：`40c8e53036ff116edd32d4e536c04e774c9aed65540a4c17da8595005987ac5e`；原c72d报告：`c4588d97f9d1114ea9a2e86a2bac43479bbd61d7a4bd566873c0587cb12bba01`，同前轮已记录值，未编辑它们。
- 022源码：`6050c26e9feefc7d218aab1d35ef21f809bcf149feb5db509b75b3fc6d758a49`；025源码：`981306c2554f30528c4c528de5aa717b835647f9d2ef92ba7c3f4240da6d6b8c`，同前轮最终值。本轮没有重修或重新签发它们的行为结论。

## 7. 状态与记账

**PLAY-039本轮恢复／取得本地代码验收完成，未提交／整合／发布。** 结论限定为当前实现的保护层空转修复及上述真实浏览器／隔离适配范围，不代表已安装插件各版本的真实宿主联调。此次新增完成原编号仅039，046仍取证受限；022、025、038、033继承各自旧状态，不因本次数据库回归或构建自动关闭。

本轮开工的最新总账／Git整体查询被拦截，未取得全局最新章节，故**不机械改写剩余总数、不虚称现在恰为55项／20项**。统一记录采用上轮第68节已提供的唯一交接句作为追加锚点，使用h8d4唯一标识补记，不占用可能已被并行窗口使用的递增节号、不覆盖其后内容。后续统一核算时仅按039原编号核对一次，若尚未扣除再扣，不能重复减数。

下一独立候选按原计划手机机制组为PLAY-042：两种普通语音挂断后的本请求取消／生成槽释放。已定位原报告1309行和最新索引条目，但本轮尚未读取其当前产品或修改，不能写成已经实施。继续时先核最新总账，若该项已有新进展则以实际记录为准；不能用039的DOM取消测试代替042的生成租约验收。
